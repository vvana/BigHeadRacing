# Прогресс: Big Head Racing — гонки в стиле Rock'n'Roll Racing

## Цель
Аркадная изометрическая гонка с оружием в духе Rock'n'Roll Racing (SNES).
Выбранный стек: **Godot 4.3+ / GDScript**, подход — **3D с фиксированной
изометрической ортокамерой** (низкополи-машины, физика прыжков «из коробки»).

## Сделано (2026-08-12)
Создан рабочий стартовый скелет в `D:\h\Big Head Racing`:

- `project.godot` — конфиг, главная сцена `scenes/Main.tscn`, autoload InputSetup.
- `scripts/InputSetup.gd` — регистрация управления кодом в InputMap
  (accelerate/brake/steer/handbrake + заделы fire/jump). Раскладку держим
  в коде, а не в project.godot — нет проблем с сериализацией InputEventKey.
- `scripts/Car.gd` — аркадная машина: RigidBody3D + 4 луча-«колеса» с пружинной
  подвеской (raycast suspension), аркадное сцепление с гашением бокового сноса,
  дрифт на ручнике (Space), прыжок (Shift, фишка RnRR), выравнивание в воздухе,
  зеркалирование руля на заднем ходу. Вперёд = -Z (конвенция Godot).
- `scripts/TrackBuilder.gd` — процедурная кольцевая трасса из Curve3D
  (замкнутый контур ~76×56 м): земля, тёмное полотно, красные стены по обеим
  сторонам (StaticBody3D), 2 жёлтых трамплина, стартовая линия;
  `start_transform()` отдаёт точку спавна.
- `scripts/IsoCamera.gd` — ортографическая камера, pitch -32° / yaw 45°,
  НЕ вращается с машиной (как в оригинале), плавное слежение lerp.
- `scripts/Main.gd` — сборка мира целиком из кода: свет+среда (красноватая
  «чужая планета»), трасса, машина из BoxMesh (корпус/кабина/жёлтый нос-маркер
  направления), камера, HUD (скорость км/ч + подсказка управления).
- `scenes/Main.tscn` — минимальная сцена, только ссылка на Main.gd.

Всё процедурное, внешних ассетов нет — проект должен запускаться сразу.

## Осталось
- ~~Установить Godot~~ ГОТОВО: Godot 4.3-stable в `D:\Soft\Godot\Godot_v4.3-stable_win64.exe`
  (+ _console.exe для отладки). Запуск игры:
  `D:\Soft\Godot\Godot_v4.3-stable_win64.exe --path "D:\h\Big Head Racing"`
- НОВАЯ МАШИНА (2026-08-13): проект теперь в `E:\UnityProjects\BigHeadRacing`,
  Godot 4.3-stable скачан в `E:\Soft\Godot\Godot_v4.3-stable_win64.exe`
  (+ `_console.exe`; на D: не было места). Прогнан `--headless --import`
  (exit 0) и тесты: test_curve PASS, TestLap PASS (худший ИИ 636 м).
  Запуск игры:
  `E:\Soft\Godot\Godot_v4.3-stable_win64.exe --path "E:\UnityProjects\BigHeadRacing"`
- ~~Проверить запуск~~ ГОТОВО (2026-08-12): headless-прогон 300 кадров чистый
  (exit 0, script-ошибок нет). Исправлен `Curve3D.closed` (нет в 4.3) →
  замыкание дублем первой точки. Шум «Parameter m is null» в headless —
  безвредный dummy-рендерер.
- Пользователь пробует ездить; подтюнить физику (grip/engine_power/steer_speed)
  по ощущениям.

## Модели машин (2026-08-12)
- Скачан пак «hot-wheels-turbo-driver-cars» (Sketchfab) →
  `assets/models/hotwheels/source/Turbo Driver Cars.glb` (+ textures/).
- В GLB 8 машин × 2 ряда (z=+1 обычные, z=-1 «TSH»-вариант), все детали
  машины стоят в одной точке-слоте, геометрия в вершинах. Модели смотрят в +Z.
- `scripts/CarModelLibrary.gd`: static `build(car_id, target_length, base_y,
  variant)` — выдёргивает машину из GLB, центрует, разворачивает на PI,
  масштабирует. ID: stock, rd, sharky, invader, arachno, dk, dakar, jt.
- Main.gd: игрок ездит на "sharky" (Sharkruiser); боксы остались фолбэком
  (`_build_placeholder_visual`). `tools/dump_glb.gd` — отладочный дамп GLB
  (путь к GLB можно передать после `--`).
- После добавления нового class_name нужен `--headless --import` (кэш классов).
- Второй пак «planet-hot-wheels-highway-35-cars» (Sketchfab) →
  `assets/models/highway35/source/HW35 cars.glb`: +35 машин Highway 35 /
  World Race (deora, twinmill, krazy8s, backdraft, vulture, …).
  Особенность: узлы Z-up (повёрнуты), «перёд» уже в -Z.
- CarModelLibrary обобщён: список GLB_PATHS, AABB с учётом поворота узла,
  автоопределение «переда» по колесу wheel_front (z>0 → разворот на PI).
  Все ID — в CarModelLibrary.CAR_IDS (41 шт.).
- `tools/test_car_build.gd` — тест сборки всех машин: 41/41 ok.

## Экран выбора машины (2026-08-12)
- `scripts/GameState.gd` — autoload, хранит `selected_car_id` между сценами.
- `scripts/CarSelect.gd` + `scenes/CarSelect.tscn` — подиум, вращающаяся
  машина, названия (DISPLAY_NAMES), листание ←→/AD, Enter/Space — в гонку.
  Это теперь стартовая сцена (run/main_scene).
- Main.gd: машина игрока из GameState; Esc в гонке — назад к выбору.
- Сетка всех машин (правая половина экрана): миниатюры рендерятся в
  SubViewport (own_world_3d, transparent_bg, UPDATE_ONCE + await
  frame_post_draw) один раз за запуск, кэш в GameState.car_thumbs.
  Навигация ←→↑↓/AD, клик мышью выбирает, повторный клик/Enter — старт.
- Ускорение миниатюр: рендер пачками по 8 вьюпортов за кадр + дисковый кэш
  PNG в user://thumbs (%APPDATA%\Godot\app_userdata\Big Head Racing\thumbs).
  Первый запуск ~5-6 кадров, дальше — мгновенно с диска. При замене паков
  моделей кэш надо удалить вручную.

## Вращение колёс (2026-08-12)
- CarModelLibrary: каждое колесо оборачивается в пивот "WheelPivot_*"
  в центре ступицы (геометрия деталей запечена от центра машины, поэтому
  без пивота крутить не вокруг чего). Меты пивота: wheel_radius (мировой),
  is_front, spin_sign (-1 при развороте контейнера на PI).
- Car.gd: collect_wheels(model) + _animate_wheels() — spin по скорости
  качения (rotation.x += speed/radius), передние поворачиваются рулём
  (rotation.y = _steer_visual, сглаженный lerp).

## Гладкая трасса (2026-08-12)
- TrackBuilder переделан с коробок на непрерывные меши (SurfaceTool):
  полотно — одна лента-стрип по кривой; ограждение — два цельных бортика
  (грань к трассе + наружная + верх), нормали заданы вручную, cull off.
- Коллизия стен — ConcavePolygonShape3D по тем же треугольникам
  (backface_collision = true). SAMPLES = 256 на круг.
- `_sample_frames()` — общий предрасчёт точек/перпендикуляров контура.
- Фикс «застрял в стене»: у Car включён continuous_cd (тонкая concave-стена
  на скорости тоннелировалась), + действие "respawn" (R) — телепорт на
  ближайшую точку оси трассы (TrackBuilder.respawn_transform по
  get_closest_offset), скорости обнуляются.
- Фикс «проваливается под асфальт при прыжке»: пружина стала прогрессивной
  (×(1+2c²) при сжатии c), демпфер 8→11, коллизионный бокс продлён вниз
  до -0.3 (посадка модели -0.35) — жёсткий упор при пробое подвески.
- Фикс «Shift-прыжок не работает»: прыжок срабатывал, но импульс был
  jump_impulse*mass*0.1 → взлётная 0.7 м/с (2 см, невидимо). Убран 0.1,
  jump_impulse теперь = взлётная скорость в м/с (6.0 → ~1.8 м высоты).
  Клавиши-модификаторы в InputMap матчатся нормально (проверено скриптом).
- Фикс «застрял на трамплине»: продлённый вниз бокс втыкался углом в наклон.
  Коллизия теперь «санки» (ConvexPolygonShape3D): днище -0.28 как упор,
  нос/корма скошены (кромка y=0.05 на z=±1.5) — заезжает на наклон.
  Коллизию строит сам Car._build_collision() (Main больше не делает).
- Регрессионный автотест: `tools/TestRamp.tscn` + test_ramp.gd — машина
  газует через трамплин 6 с, PASS/STUCK + телеметрия по секундам.
  Запуск: `godot --headless --path . res://tools/TestRamp.tscn`
- Фикс «R не работает»: сам механизм исправен (автотест
  tools/TestRespawn.tscn — PASS, инжект клавиши в загруженный Main), но
  respawn возвращал в БЛИЖАЙШУЮ точку оси — т.е. обратно в ту же ловушку
  (трамплин стоит на оси). Теперь respawn_transform выбрасывает на +6 м
  вперёд по ходу трассы.
- R всё равно не срабатывал у пользователя в окне (в headless-тесте — да).
  Main переведён с _unhandled_input на прямой опрос Input в _process
  (как езда). Плюс автовозврат: >11 м от оси трассы → жёлтое предупреждение
  и через 3 с _respawn() сам. TrackBuilder.distance_from_axis() — хелпер.

## Механика Rock'n'Roll Racing (2026-08-12) — БОЛЬШОЙ ЭТАП
- Физика перенастроена под RnRR: engine 110 (сотня ~2.5 c), brake 80,
  steer 3.4/2.2 (руль почти не слабеет на скорости), grip 14 (едет куда
  смотрит, дрифт только с ручником). Упругие столкновения: bounce 0.4 у
  машин + bounce 0.5 у стен (рикошет).
- Car.gd реструктурирован: общий _drive() для игрока и ИИ; _ai_control —
  едет к точке оси трассы (lookahead 6+0.45*v), стреляет по цели в конусе
  22° / 22 м, кидает мину под соперника ближе 10 м сзади; ai_rubber.
- Бой: shoot() — Projectile.gd (Area3D, 45 м/с, 30 dmg, живёт 1.4 с);
  drop_mine() — Mine.gd (взвод 0.7 c, 35 dmg, живёт 25 c); take_damage с
  толчком; _explode() — вспышка FlashFx.gd, невидим+freeze 2 c, респавн
  с полным HP. Боезапас: 6 снарядов + 3 мины, рефилл каждый круг.
- Main.gd = менеджер гонки: решётка 2×2 (игрок впереди слева), 3 ИИ на
  случайных машинах (не как у игрока, ±разброс характеристик), отсчёт
  3-2-1-GO (controls_enabled), прогресс по накопленному пути вдоль кривой
  (защита от среза — прогресс по ближайшей точке оси), круги (4), позиция,
  «резинка» ИИ ±(0.85..1.3) от разницы прогресса, финиш → место + Enter в
  гараж. HUD: скорость, круг, позиция, HP-бар с цветом, боезапас.
- InputSetup: + "drop" [L, C]. Управление: Ctrl/J огонь, L/C мина.
- Off-track: игрок 2 с с предупреждением, ИИ тихо через 1 с.
- Тесты PASS (test_ramp обновлён: controls_enabled = true).
- Фикс «машины стоят за бортом и не возвращаются» (2026-08-12): порог
  вылета был TRACK_HALF_WIDTH+4 = 11 м, а машина у наружной грани стены —
  всего 8.1 м от оси (замерено тестом) → детект не срабатывал НИКОГДА.
  Теперь OFFTRACK_DIST = TRACK_HALF_WIDTH+0.5 = 7.5 (на полотне максимум
  ~6.2). Плюс WALL_HEIGHT 1.2 → 1.7 (высота прыжка ~1.8) — реже улетают.
  Новый тест `tools/TestOfftrack.tscn` ставит машину за борт и проверяет
  авто-возврат.
- Фикс «перевернулась и так лежит» (2026-08-12): _check_offtrack →
  _check_recovery, три причины возврата — вылет (2 с игрок / 1 с ИИ),
  переворот (up_dot < 0.35, 1.5 с), застревание (v < 1.2 м/с, 5 с; только
  при controls_enabled, иначе решётка на старте считается застреванием).
  Игроку в HUD пишется причина. Выравнивание в воздухе усилено 4→14 +
  демпфер крена/тангажа 2.5 — реже приземляется на крышу.
  Тест `tools/TestFlip.tscn` переворачивает машину и ждёт восстановления.
- Уточнение (2026-08-12): отсчёт «Перевернулся!» стартовал ещё в полёте
  (кувырок на прыжке). Теперь нужны три условия: up_dot < 0.35 И машина
  у земли (Car.is_near_ground() — луч строго вниз по МИРУ, а не по оси
  кузова: у машины на крыше лучи подвески смотрят в небо) И |v.y| < 2.5.
  TestFlip расширен: фаза 1 — кувырок в воздухе (ложного отсчёта быть не
  должно), фаза 2 — лежит на крыше на земле (должна восстановиться).

- Стартовая черта → финишный створ (2026-08-12): узел "FinishGate" на
  нулевой отметке кривой — шахматная лента 14×2 клетки на асфальте,
  две металлические опоры (снаружи ограждения, h=5) и клетчатый баннер
  16×2 над дорогой. Только визуал, коллизий нет.

## Трасса с перепадами высот (2026-08-12)
- Контрольные точки кривой получили Y (-2..+7 м): подъёмы, спуски, гребни.
- ВАЖНО: дорога теперь StaticBody3D с СОБСТВЕННОЙ trimesh-коллизией —
  раньше машины ездили по плоскому боксу земли, при рельефе это ломалось.
- Земля — сетка 56×56 на 190 м с trimesh-коллизией. Высота узла:
  `_ground_height()` = вровень с полотном в радиусе TRACK_HALF_WIDTH+5
  (ровная обочина), дальше склон вниз 0.28/м + синусные холмы с
  нарастающим весом. Высоты считаются по узлам сетки (не по 4 на ячейку).
- `_rights` теперь строго горизонтальны (полотно без бокового крена).
- Тест `tools/TestLap.tscn`: гонка едет сама 60 с, проверяет что худший ИИ
  проехал > полкруга. Факт: ИИ делают ~3 круга (круг = 221 м).
- Фикс «пятна грунта на асфальте» (z-fighting): обочина была вровень с
  полотном, а сетка земли (3.4 м/ячейка) грубее дороги → на выпуклых
  местах треугольники земли пробивались наверх. Теперь GROUND_DROP = 1.2:
  обочина ниже полотна, ограждение продлено вниз юбкой (GROUND_DROP+0.4),
  так что ступенька не видна. GROUND_RES 56 → 72.
- test_ramp: критерий сменён на МАКСИМУМ пройденного (после трамплина
  машина на полном газу без руля бьётся в стену поворота и откатывается —
  это не «застряла на трамплине»).

## Взрыв без исчезновения (2026-08-12)
- Было: visible=false + freeze на 2 с → «машина пропала», потом появлялась
  с высоты 1.0 м и подпрыгивала.
- Стало: машина всегда видима. При подрыве — вспышка + подброс
  (5.5*mass вверх) и случайная закрутка, управление снято (alive=false),
  через 1.6 с сама встаёт на трассу с полным HP.
- Высота респавна/старта 1.0 → 0.62 м (примерно длина покоя подвески),
  чтобы не падать и не подскакивать.
- Тест `tools/TestExplosion.tscn`: следит, что visible ни разу не стал
  false, машина умерла и возродилась с HP 100 (HP проверяется в момент
  возрождения — иначе ИИ успевает подстрелить и тест врёт).

## Гладкая петля без изломов (2026-08-12)
- Причина «уголков» на трассе: касательные были фиксированной длины
  (normalized * 6.0) независимо от расстояния между опорными точками —
  на длинных участках кривая почти прямая, а в точке резко ломается.
- Теперь: касательная = (next - prev) / 6 (Катмулл-Ром, пропорционально
  шагу), а точки задаются по углу процедурно (14 шт., радиус
  33 + 6·sin2a + 2.5·sin(3a+1.2), высота 3.2·sin(2a+0.5) + 1.8·sin3a + 1)
  — широкие дуги, лёгкие шиканы, подъёмы/спуски. Длина круга 219 м.
- Тест `tools/test_curve.gd` (SceneTree-скрипт): максимальный поворот
  между сэмплами; PASS < 6° на шаге 0.55 м. Факт: 2.76°.

## Ступенчатый рельеф как в RnRR (2026-08-12)
- Вместо синусоидальных холмов — профиль плато: `HEIGHT_KEYS` (доля круга →
  высота) + `_profile_height()` со smoothstep на переходах. Сейчас:
  0 м старт → подъём → плато 7 м → РЕЗКИЙ сход до 1 м → плато 1 м →
  подъём → плато 4.5 м → сход до 0. Точек кривой 14 → 40 (иначе плато
  размазывается).
- Трамплины перенесены на кромки обрывов (t = 0.44 и 0.875) — слетаешь
  с плато вниз, как в оригинале.
- test_curve разделён: излом В ПЛАНЕ (дефект, лимит 6°, факт 1.71°) и
  перелом ПРОФИЛЯ (задумка — кромки плато, лимит 20°, факт 8.49°).

## Живость + руление в полёте + лимит разворота (2026-08-13)
- Физика «поживее»: mass 120 → 250, max_speed 26 → 34, engine 110 → 150,
  brake 80 → 100, steer_speed_min 2.2 → 2.6 (радиус на скорости ~13 м).
  ИИ на TestLap проезжают 808 м/мин против 636 раньше.
- Руление в полёте: в воздушной ветке _drive() рысканье от руля
  (air_steer_speed 2.2, мягче земли, lerp 6.0), выравнивание к горизонту
  как было — оно рысканье не трогает.
- Лимит разворота: Car._clamp_heading(delta) — угол «нос машины vs
  касательная трассы в ближайшей точке» не может превышать
  max_track_angle_deg (80°). Лишний угол снимается rotate() сразу, а
  рысканье урезается ПРЕДИКТИВНО ((sign*limit - ang)/delta): кламп идёт до
  интеграции физики, без предикции машина проскакивала бы предел на кадр
  и дребезжала на границе. Работает и для ИИ; пропускается при alive=false
  (закрутка взрыва) и без track.
- Тест `tools/TestHeading.tscn`: фаза 1 — разворот на 180° (должно
  довернуть до 80°), фаза 2 — 2 с насильного рысканья 6 рад/с (макс. угол
  за все кадры ≤ 80°+1). Факт: ровно 80.0°.

## Скольжение вдоль ограждений вместо отскока (2026-08-13)
- Было: упругие стены (bounce 0.5) — машина рикошетила к оси трассы.
- Стало: стены bounce 0.0 + группа "walls"; Car._wall_slide(delta) при
  контакте (contact_monitor + get_colliding_bodies) проецирует скорость
  на касательную трассы (вертикаль не трогает — прыжок жив) и доворачивает
  корпус вдоль стены (wall_align_speed 7.0, экспоненциально).
- ВАЖНО: доворот держится ещё 0.35 с после отрыва (_wall_align_time) с
  полным гашением angular_velocity.y — удар УГЛОМ кузова о стену
  закручивает машину, и без окна остаточное вращение разворачивало её
  обратно поперёк (в тесте угол был 67° вместо <30°).
- Упругость машина-машина осталась (bounce 0.4 у Car).
- Тест `tools/TestWallSlide.tscn`: пуск в стену под 55° на 20 м/с;
  критерии — отскок к оси < 3 м/с на всех кадрах после касания
  (факт 2.67 — медленный дрейф, не рикошет) и финальный угол к трассе
  < 30° (факт 19.2°). Касание детектится через car._touching_wall(),
  НЕ по расстоянию до оси: нос касается стены, когда центр ещё в ~5.2 м.

## Руль у стены + не заезжать на ограждение (2026-08-13)
Жалобы: «вдоль стенки невозможно рулить», «можно наехать на ограждение
и перевернуться». Причины: _wall_slide глушил ЛЮБОЕ рысканье и проецировал
всю скорость на касательную (руление от стены умирало), а rotate() у стены
вклинивал угол кузова в стену — депенетрация подбрасывала машину вверх
(заезд на стену → переворот). Переделано направленно:
- Скорость: гасится только компонента В стену (v_out = v·n, n — от оси к
  стене); вдоль и ОТ стены — не трогаем.
- Клапан подскока: при контакте v.y обрезается до 1.0 — на стену больше
  не заезжает. После прыжка (_jump_time 0.6 с) клапан отключён, чтобы
  прыжок у стены работал.
- Антипереворот: при контакте — та же выравнивающая пара сил, что в
  полёте (up→горизонт 14.0 + демпфер крена/тангажа 2.5).
- Руление: Car._yaw_cmd_sign (знак рысканья, которое просит руль,
  ставится в _drive). Если руль просит рысканье ПРОЧЬ от стены — доворот
  и гашение полностью пропускаются. Иначе доворот к касательной +
  angular_velocity.y = 0 (без руля вращение у стены — это закрутка от
  удара, её гасим; заодно закрыт регресс «закрутило от удара углом →
  раскрутило до лимита 80°»).
- TestWallSlide теперь двухфазный: фаза 1 — скольжение без отскока
  (отскок < 3 м/с, угол < 30°; факт 2.67 и 8.8°); фаза 2 — руль от стены
  через Input.action_press("accelerate"/"steer_*"), критерий — хоть на
  одном кадре нос ушёл от стены (fwd·n < -0.1) или машина покинула
  пристенок (dist < 4). Проверять только последний кадр нельзя: машина
  успевает пересечь трассу и «уткнуться» в противоположный борт.

## Самовольный разворот на взлёте/падении/прямой — исправлен (2026-08-13)
- Жалоба: «при подлёте, падении и иногда при прямой езде машина
  разворачивается, хотя я не рулю». Причина: рысканье гасилось ТОЛЬКО
  на земле при |speed| > 0.5 (lerp к нулю в ветке руля). В воздухе руль
  работал лишь при |steer| > 0.01, а без руля закрутка от кромки
  трамплина/угла кузова не гасилась вовсе — машина летела вращаясь.
- Теперь в _drive рысканье без руля затухает везде: в полёте lerp к
  steer*air_steer_speed идёт всегда (при steer=0 цель — ноль, 6.0/с);
  на земле при |speed| <= 0.5 — lerp к нулю (10.0/с).
- Тест `tools/TestAirSpin.tscn`: машине в полёте дают ω.y = 1.5 рад/с
  без руля; через 0.65 с должно быть < 0.3 (факт 0.021).
- Замечание: TestLap изредка печатает PASS, но процесс падает на выходе
  (exit -1073741819, access violation при завершении headless). Плавающий
  сбой Godot при выгрузке, на игру не влияет; смотреть на печать PASS,
  а не только на код выхода.

## Стены/приземления не замедляют + запрет езды по стене (2026-08-13)
Жалобы: «о стену и при приземлении машина замедляется», «иногда едет по
ограждению вертикально». Сделано:
- Слои коллизий: стены на layer 2, у Car collision_mask = 0b11, а ЛУЧИ
  ПОДВЕСКИ и is_near_ground — mask 1. Кузов со стеной сталкивается, но
  колёса стену «дорогой» не считают → вертикальная езда по стене убита.
- _wall_slide переделан в «ведение»: в динамической зоне у стены (вылет
  кузова в сторону стены: нос 1.5 + борт 0.85, проекции на нормаль, +
  упреждение v_out*3*delta) вся горизонтальная скорость перенаправляется
  вдоль касательной С СОХРАНЕНИЕМ МОДУЛЯ (не ниже _recent_hspeed).
  Ключевое: перехват ДО контакта — решатель съедает нормальную
  составляющую в тот же шаг, задним числом скорость уже не вернуть
  (первая версия с узкой полосой 1.4 м это и ловила: нос доставал до
  стены раньше входа центра в полосу → отскок 6.7 и просадка до 8.5).
- _recent_hspeed — «недавняя» горизонтальная скорость: максимум с
  затуханием 30 м/с² (в окне защиты приземления — 5 м/с²: серия
  кадров-укусов при скрежете иначе сползает и память). Мгновенная память
  затиралась кадром, где скорость уже съедена.
- _protect_landing_speed: 0.25 с после касания (контакт = 2 колеса ИЛИ
  корпус трётся о StaticBody не-стену — при жёсткой посадке корпус
  чиркает раньше колёс) модуль горизонтальной скорости не ниже
  _recent_hspeed. Направление не трогаем — руль работает.
- Трение корпуса 0.5 → 0.15: сцепление и так аркадной силой в _drive,
  трение материала только ворует скорость при скрежете.
- Прыжок через стену жив: ведение отключено 0.6 с после прыжка.
- Тесты: TestLanding (подброс на 3 м на 20 м/с; после посадки, пропуская
  4 переходных кадра, скорость ≥ 17; факт 17.7). TestWallSlide фаза 1
  ужесточена: мин. скорость у стены ≥ 16 (факт 18.8), отскок 0.00,
  угол 2.2°; «пристенок» детектится по _wall_align_time > 0 (машина
  теперь может вообще не коснуться стены). Эффект на игру: ИИ на
  TestLap проезжают 1011 м/мин (до правок дня — 636).

## Подбрасывание носа на ровном/подъёме — сглажено (2026-08-13)
- Жалоба: «машину иногда подбрасывает передом вверх на ровных местах или
  подъёме». Причины: кромка кузова цепляет стыки тримеша дороги на
  переходах профиля (трение уже снижено 0.5 → 0.15, это часть лечения),
  а резкий пик силы прогрессивной пружины на кадре-стыке даёт рывок
  тангажа, который нечему гасить.
- В _physics_process при on_ground добавлено гашение крена/тангажа:
  apply_torque(-spin_xz * 3.0 * mass * 0.1), рысканье не трогается.
  Следование рельефу сохраняется (подвеска сильнее демпфера), гаснут
  только резкие рывки. Все 10 тестов PASS (TestRamp — трамплины
  по-прежнему подбрасывают как надо: speed 20.5 на вылете).

## Подскоки и «после нырка носом едет назад» — исправлены (2026-08-13)
- Жалоба: «всё равно есть подскоки; при нырянии носом машину отталкивает
  назад и она ЕДЕТ назад». Две причины:
  1) Материальный bounce 0.4 у кузова действовал не только между
     машинами, но и о ДОРОГУ — каждый чирк корпуса о полотно давал
     рикошет-подскок, а удар носом о склон — отброс назад.
  2) _protect_landing_speed запоминала только МОДУЛЬ скорости: когда удар
     носом разворачивал вектор, защита «восстанавливала» задний ход и
     машина уезжала назад с прежней скоростью.
- Сделано: bounce 0.0; рикошет машина-машина теперь вручную —
  _bounce_off_cars(): разовый импульс от соперника в момент НОВОГО
  контакта (по словарю _touch_cars), пропорционален скорости сближения
  (×0.4), только по горизонтали.
- Память скорости хранит и направление: _recent_hdir снимается на пике
  (ДО удара). Восстановление: если текущее направление живо (dot > 0) —
  масштабируем его (руль работает); если развёрнуто/убито — возвращаем
  запомненный курс. Порог памяти 2 м/с.
- reset_speed_memory() при респавне (Main._respawn_car и _explode) —
  иначе защита «вернула» бы скорость, которой уже нет.
- Тест `tools/TestDive.tscn`: пикирование носом 35° на 18 м/с с 4 м;
  через ~1.5 с скорость ВДОЛЬ исходного курса должна быть > 5 м/с
  (факт 11.3 — раньше уезжала назад). ИИ на TestLap: 1044 м/мин.

## Стена слегка тормозит + быстрая посадка (чувство массы) (2026-08-13)
- По просьбе пользователя: ограждение должно «слегка притормаживать»,
  а после подлёта машина должна быстрее возвращаться на землю.
- Стена: при перехвате ведением теряется 25% скорости СБЛИЖЕНИЯ
  (0.25*v_out, разово — после перенаправления v_out≈0) + скрежет
  2.5 м/с² пока есть фактический контакт. ВАЖНО: память _recent_hspeed
  срезается вслед (minf), иначе она возвращала штраф обратно.
- Прижим в полёте: в _physics_process при не-on_ground доп. сила вниз —
  14g-доп при падении (vy<0), 5 при взлёте (прыжок не душим;
  jump_impulse 6.0 → 7.5, высота ~1.9 м — стену 1.7 всё ещё можно
  перепрыгнуть). Сила НАРАСТАЕТ за 0.3 с полёта (clampf(_air_time/0.3)):
  без этого микро-подлёты на стыках полотна получали полный прижим,
  машину вбивало в асфальт серией ударов и скорость съедалась
  (TestLanding падал с 17.6 до 15.8, TestRamp с 74 до 46 м).
- Отладка TestLanding по телеметрии: посадка стоит ~1.4 м/с (норм), а
  «просадку до 15.8» давал штраф СТЕНЫ — трасса в плане кольцевая, после
  посадки машина по хорде сближается с внешним бортом, срабатывает
  ведение. Тест теперь обрывает замер при _wall_align_time > 0 и меряет
  кадры 5..13 после касания (факт 18.3). TestWallSlide фаза 1: норма
  скорости у стены теперь ДИАПАЗОН 12..18.5 (тормозит слегка, не в ноль;
  факт 15.1).

## Широкая трасса + маркер игрока (2026-08-13)
- TRACK_HALF_WIDTH 7 → 9 (полотно 18 м). Всё остальное подстроилось
  само: пороги вылета, стартовая клетка, обочина, тесты — везде
  константа. Тесты все PASS (TestRamp: машина после трамплина теперь
  упирается в стену поворота на 39 м — критерий «максимум пройденного»
  это учитывает).
- Маркер игрока: ЗЕЛЁНЫЙ конус остриём вниз над машиной (y=2.4),
  unshaded + emission, cast_shadow OFF (тень от стрелки на машине
  мешала), лёгкое покачивание по высоте (0.12 м, 3 Гц). Вращение убрано
  по просьбе пользователя — отвлекало. Строится в
  Main._build_player_marker(), живёт ребёнком машины игрока.

## Прилипание к дороге — не подпрыгивает, нос не закидывает (2026-08-13)
- Жалоба: «при ударе об землю снова подпрыгивает, иногда забрасывая нос
  вверх; нужно более аркадное управление». Источники: отбой подвески и
  депенетрация кузова дают скорость вдоль нормали полотна НАРУЖУ;
  неравномерное сжатие пружин перед/зад на посадке закидывает нос.
- Сделано (в _physics_process при alive и on_ground):
  1) _ground_normal — средняя нормаль из лучей подвески (копится в
     _apply_suspension).
  2) Прилипание: v·n > 0.4 → лишнее срезается. Подъёмы/трамплины целы:
     на поверхности скорость идёт ВДОЛЬ неё (v·n≈0), на кромке колёса
     теряют контакт (кламп только при on_ground), после прыжка — клапан
     _jump_time.
  3) Выравнивание кузова к ПЛОСКОСТИ ДОРОГИ (не к горизонту!):
     torque = up×n*10 + гашение качки spin_xz*4 (было просто -spin*3).
- Все тесты PASS. TestRamp 39 м с остановкой у стены — это эффект
  ШИРОКОЙ трассы (без руля машина после трамплина утыкается в поворот),
  не прилипания: значение совпадает с прогоном до правки.

## Подскок разрешён, но параллельно земле (2026-08-13)
- Уточнение пользователя: отскок от земли — это ок (аркадный подскок),
  главное чтобы кузов оставался ПАРАЛЛЕЛЬНЫМ земле (не задирал нос).
- Клапан прилипания ослаблен: v·n срезается не свыше 0.4, а свыше
  3.0 м/с (подскок виден, дикие выбросы депенетрации по-прежнему
  отрезаны).
- Полётная ветка _drive: первые 0.5 с полёта корпус выравнивается к
  НОРМАЛИ ПОСЛЕДНЕЙ ОПОРЫ (_ground_normal, память из подвески) с
  плавным переходом к горизонту дальше (lerp по _air_time/0.5) —
  подскок над склоном идёт параллельно склону, дальний полёт как
  раньше к горизонту. Выравнивание к плоскости дороги на земле
  (up×n*10, качка *4) осталось. Все 11 тестов PASS.

## Ведение у стены — только при фактическом касании (2026-08-13)
- Жалоба: «едешь вдоль ограждения — машина, НЕ КАСАЯСЬ его,
  поворачивает вдоль, будто коллайдеры кривые». Причина: зона ведения
  имела запас 0.15 м + упреждение 3 кадра сближения — при плавном
  подруливании перехват срабатывал в десятках см от стены.
- Теперь запас 0, упреждение ровно 1.2 кадра: перехват всё ещё ДО
  решателя (скорость не съедается), но визуально машина реально
  касается ограждения (при мягком сближении зазор — миллиметры,
  дальше корпус дожимается до фактического контакта). Все 11 PASS.

## «Невидимая стена» при подлёте вдоль ограждения — исправлена (2026-08-13)
- Жалоба: «при подлёте вдоль стенки машина останавливается, будто
  невидимая стена поверх ограждений тормозит». Причина: после прыжка
  ведение у стены отключалось на 0.6 с (клапан _jump_time — чтобы можно
  было перепрыгнуть ограждение), и если в полёте машина задевала стену,
  контакт шёл БЕЗ перехвата — решатель мгновенно съедал скорость.
- Теперь ведение отключается не по таймеру прыжка, а ПО ВЫСОТЕ: если
  днище (y-0.3) выше кромки ограждения (axis_pos.y + WALL_HEIGHT) —
  ведения нет, стену можно перелетать; ниже кромки ведение работает
  всегда, в том числе в полёте и сразу после прыжка. Клапан _jump_time
  остался только на зажиме вертикали при контакте (прыжок у стены жив).
- ГРАБЛИ: первая правка сослалась на _wall_guiding, которого в текущей
  версии Car.gd нет (тест смотрит _wall_align_time) → скрипт не
  компилировался, ВСЕ машины ломались, тесты падали/висли. Симптом в
  тестах: «угол 0.0, пристенок=false» (машина так и стояла на решётке).
  Прогонять хотя бы один тест БЕЗ 2>$null — parse-ошибки видны только
  в stderr. Все 11 тестов PASS.

## «Тяжело вывернуть от ограждения» — исправлено (2026-08-17)
- Жалоба: «еду вдоль ограждения слева — тяжело вывернуть вправо, будто
  что-то мешает». Диагностический тест `tools/TestWallTurn.tscn`
  (test_wallturn.gd) воспроизвёл: три одинаковых манёвра (газ + руль от
  стены 1.5 с на 20 м/с) — правый борт, левый борт, середина трассы.
  До фикса у левого борта машина за 0.75 с уходила от стены на 0.16 м
  (в контроле — 6.4 м): «прилипала» на ~1.2 с.
- Две причины, обе в _wall_slide:
  1) Ведение при v_out > 0 ставило ВСЮ скорость строго на касательную.
     Трасса кривая — на кадре, где стена «подворачивает» к машине,
     появляется новое сближение, ведение снова ставит скорость на рельс
     и стирает боковую скорость, которую руль успел наработать. Итог:
     против кривизны не вырваться.
  2) Чтобы отвернуть нос от стены, корма должна вдавиться в стену —
     контактные импульсы решателя гасят рысканье (руль набирает его
     медленным lerp, контакт съедает быстрее).
- Фикс: если руль просит рысканье ПРОЧЬ от стены (steering_away — тот же
  признак, что и раньше пропускал доворот), ведение выпускает машину
  ПОД УГЛОМ 22° от ограждения, а не вдоль. Пары кадров хватает, чтобы
  отойти от стены, контакт рвётся, дальше рулится свободно. Заодно
  fwd/into_sign считаются один раз в начале _wall_slide (раньше — в
  хвосте функции повторно).
- После фикса: левый борт 3.66 м за 0.75 с, правый 9.09 м. Критерий
  теста: за 0.75 с руления от стены уйти ≥ 1.5 м у обоих бортов.
  Все 14 тестов PASS (TestWallSlide не задет: фаза 1 без руля —
  ведение вдоль как было; фаза 2 «руль от стены» стала только легче).

## Закрутка от столкновений машин (2026-08-18)
- Запрос: «врезаешься в другую машину — она должна сместиться или
  повернуть, например поддеваешь зад — и она поворачивает».
- Было: рикошет машина-машина (_bounce_off_cars) — только ЦЕНТРАЛЬНЫЙ
  импульс (центр-в-центр), момента вращения он не даёт; а руление в
  _drive каждый кадр жёстко переписывает angular_velocity.y (lerp 10/с)
  и съедает любую закрутку от удара за пару кадров.
- Сделано (Car.gd):
  1) В _bounce_off_cars при новом контакте, кроме толчка, — закрутка:
     момент = плечо × отн. скорость соперника (плечо — от центра к
     сопернику, не длиннее полукорпуса 1.5 м; rel обрезана 15 м/с);
     spin = момент.y * bump_spin (экспорт, 0.25), порог |spin| > 0.15,
     ИТОГОВАЯ angular_velocity.y клампится ±3.0 — серия контактов
     (удар-отскок-удар) не раскручивает волчком.
  2) Окно _bump_spin_time = 0.6 с: руление в _drive ослаблено
     (lerp 10 → 2.5 на земле, 6 → 2 в воздухе), иначе руль съедал бы
     закрутку мгновенно. Работает для игрока и ИИ одинаково.
- Центральный таран momenta не имеет (плечо ∥ скорости) — только толкает.
- Тест `tools/TestCarBounce.tscn` (test_car_bounce.gd): фаза 1 — таран
  15 м/с в УГОЛ кормы стоящей машины (смещение 0.7 м) — развернуло на
  67° и толкнуло до 12 м/с; фаза 2 — таран строго в центр — 7.5°
  (почти не крутит), толчок 16 м/с. Критерии: угол ≥ 20°, центр < 8°,
  толчок ≥ 1.5 м/с. Лишние ИИ паркуются на другом конце трассы с
  ammo=0 (иначе подстрелят жертву и тест соврёт). 2026-08-18: в тест
  добавлен seed(1234) — атакующий случаен, порог «центр < 8°» без
  seed изредка плавал (9.9°) и тест мигал.

## Кап боковых пинков на ровной езде (2026-08-19)
- Уточнение пользователя: не разворот задом наперёд, а «просто угол
  направления менял» — резкий сдвиг курса на ровном месте. Это боковая
  составляющая того же фантомного удара о ребро тримеша: капы стояли
  на вертикаль (vn), тангаж/крен и пристенок, а боковой пинок по
  курсу — нет.
- Фикс (Car.gd): на установившейся ровной езде (та же ветка, где
  allowed = 0.6) БОКОВОЕ изменение горизонтальной скорости за кадр
  ограничено 0.6 м/с (честная физика даёт максимум ~0.3: грип 14 →
  a = снос×1.4, за кадр 0.23 м/с; кап с двойным запасом). Продольная
  (газ/тормоз) не трогается. Исключения: пристенок (своё ведение),
  окно 0.3 с после честного толчка — новый таймер _ext_push_time
  ставится в _bounce_off_cars (контакт машин) и take_damage (снаряд/
  мина/взрыв). _prev_hvel обновляется В КОНЦЕ _physics_process.
  После телепорта/респавна кап молчит сам: активен только при обеих
  скоростях (прошлой и текущей) > 5 м/с и _ground_time > 0.35.
- TestBumps считает «kicks» (смена угла курса > 2.5°/кадр на плато вне
  пристенка): ИИ-сценарий 2 → 1 (пинки редки и потому «внезапны» в
  игре; метрика главное подтверждает — кап не даёт ложных срабатываний
  на честном рулении). Все 17 тестов + WallGhost без изменений PASS.

## Ведение у стены не «выстреливает» машину (2026-08-19)
- Жалоба: «всё равно отталкивается странно от ограждений, иногда
  внезапно вдоль дороги может поменять направление».
- Проверено и ОТПАЛО: перескок get_closest_offset на соседний виток
  трассы у стены (tools/check_axis_jump.gd — 0 конфликтных зон);
  удары в стену под 55-88° (новый tools/TestWallHit.tscn — разворотов
  нет). Осталась логика самого ведения (_wall_slide):
  1) «свободная» ветка (контакт есть, но v_out <= 0): направление
     бралось из МГНОВЕННОЙ скорости, которую решатель мог только что
     развернуть ударом о ребро, а величина НАКАЧИВАЛАСЬ из памяти
     _recent_hspeed до полной — машину «выстреливало» в случайную
     сторону на полной скорости. Теперь при движении от стены скорость
     НЕ переписывается вовсе (фантомные броски гасят капы: внутрь
     ≤ 1.2 м/с, vy ≤ 1.0, крен/тангаж ≤ 1.5 рад/с);
  2) знак «вдоль» на рельсе: при |продольной| < 2 м/с сигнал ненадёжен
     (мог быть искажён ударом) — берём память направления _recent_hdir,
     затем нос машины (он ограничен ±80° к оси, назад не смотрит).
     Раньше слабый/искажённый сигнал мог развернуть машину задом
     наперёд вдоль трассы на запомненной скорости.
- Все 17 тестов PASS + WallGhost (far_frames 157, без изменений) +
  WallHit (0 кадров «назад»). TestWallGhost научился считать развороты
  продольной скорости (rev=0).

## Кузов больше не трогает полотно — урок из WaterSlides (2026-08-19)
- Жалоба (повторная): «и отскакивает от ограды, и подбрасывает перед
  на ровном месте, и прыгать иногда начинает с потерей скорости».
  Просьба пользователя: посмотреть, как строились горки в WaterSlides.
- Уроки из E:\UnityProjects\WaterSlides\ПРОГРЕСС.md: (1) главный —
  НЕ ДАВАТЬ телу касаться фасеточного меша (там тело на горке вообще
  переведено на путевую модель, а «виртуальный пол» правит только
  скорость вдоль нормали); (2) скорость «в поверхность» срезать ДО
  солвера (у нас это уже делает _wall_slide перехватом за кадр).
- Сделано (Car.gd):
  1) днище «санок» поднято -0.28 → -0.12 (клиренс ~29 см): при обычной
     езде кузов НЕ касается дороги ВООБЩЕ — рёбра тримеша полотна ему
     безразличны; упор от провала срабатывает раньше пробоя пружин.
     Замер (TestBumps, счётчик кадров контакта кузов-дорога за 90 с):
     251 (-0.28) → 156 (-0.18) → 82 (-0.12, в осн. посадки).
     Подскоки на плато: 15 шт (vy до 7.6) → 4 шт (vy ≤ 1.15, все в
     зоне приземления после сходов).
  2) капы УГЛОВОЙ скорости («подбрасывает перед» — это тангаж, линейные
     капы его не трогали): на установившейся ровной езде крен/тангаж
     ≤ 1.2 рад/с (трамплины не задеты: там нормаль опоры наклонена и
     ветка выключена, порог 0.995 > cos 9°); в пристенке ≤ 1.5 рад/с.
- TestBumps научился считать контакты кузова с дорогой (body_contacts).
- Все 17 регрессионных тестов PASS (включая Landing/Dive/Ramp/Flip —
  упор днища и посадки не пострадали); WallGhost: far_frames 157
  (стабильно), вылеты за 2 м от стены не выросли.
- Если жалобы останутся: следующий шаг — радикальный, по образцу
  WaterSlides: дорога на отдельный слой (4), кузов с ней не
  сталкивается вовсе (лучи подвески видят 1|4), а «виртуальный пол»
  в коде держит кузов над полотном; минус — перевёрнутая машина
  ляжет «в дорогу» на 1.2 м до автопереворота.

## Колёса проваливались сквозь дорогу и притормаживали (2026-08-20)
- Жалоба: «в некоторых местах колёса проваливаются сквозь текстуру
  дороги, вроде бы даже притормаживают». Диагностика новым тестом
  `tools/TestWheelSink.tscn` (test_wheelsink.gd: ИИ едет 90 с, по 100
  корзинам круга меряется утопание НИЗА визуального колеса под полотно,
  прожатие подвески, контакты кузова, мин. скорость): утопание до
  0.39 м в ложбинах профиля и зонах приземления, строго коррелирует
  с прожатием подвески 0.8-0.99 и контактами днища (машина скребла
  полотном → «притормаживает»: v_min 8-17 при обычных 30+).
- Причина: кузов — жёсткое тело, колёса запечены в модель (низ на
  -0.35), а подвеска в ложбинах прожималась почти до упора — кузов
  проседал до асфальта, колёса визуально уходили под него, днище
  (-0.12) тёрлось о дорогу.
- Фиксы (Car.gd):
  1) Пружина: к прогрессии добавлен член 12·c⁶ — жёсткий «упор» у дна
     хода. Прожатие покоя (~0.25) не задето (добавка там ничтожна),
     обычная езда не изменилась, но в ложбинах кузов больше не доседает
     до касания днищем. Скорость в ложбинах теперь НЕ проседает
     (t=0.62-0.70: v_min 27-31 против прежних 12-26).
  2) Кламп визуальных колёс: _animate_wheels перенесена из
     _physics_process в _process (кадр рисуется ПОСЛЕ решателя — кламп
     должен видеть конечное положение кузова). Каждый кадр луч вниз от
     ступицы: если низ колеса ушёл бы под полотно, пивот приподнимается
     на глубину утопания (вверх мгновенно, вниз плавно 12/с). Упреждение
     на пару шагов решателя по скорости ТОЧКИ колеса (vy кузова +
     тангаж: при посадке носом колесо ныряет быстрее центра) — без него
     на жёсткой посадке колесо на кадр-два всё же ныряло. Меты пивота:
     rest_pos, lift (ставит collect_wheels).
  3) Из лучей клампа исключены ВСЕ машины (группа cars): кузов соперника
     рядом — не дорога, колесо «вспрыгивало» на него (в тесте ловилось
     как pen 2 м — луч бил в крышу AiCar3).
- Итог: worst pen 0.394 → 0.110 (единичные кадры самой жёсткой посадки),
  на плато ≤ 5 см (глазом не видно); контакты кузова за 90 с: 82 → 71;
  kicks 0. TestWheelSink стал регрессионным: PASS при worst pen < 0.2.
- ГРАБЛИ теста: луч «где полотно» должен исключать все машины, иначе
  ловит кузов соперника, сползшего на трассу с места парковки.
- Все 17 регрессионных тестов PASS (Lap 1292 м, Landing 18.5,
  AICorners 1 инцидент — местами даже лучше базы), WallGhost стабилен
  (far_frames 158, rev=0).

## Фантомные отбросы от ограждений укрощены (2026-08-19)
- Жалоба: «всё так же иногда подскакивает на ровном; очень сильно
  периодически отскакивает от ограждений; еду вдоль ограждения и будто
  на что-то наезжаю — отбрасывает от стены».
- Причина та же, что у подскоков на дороге: стена — тримеш из сотен
  сегментов, кузов-коробка при скольжении цепляет ВНУТРЕННИЕ рёбра
  стыков, решатель даёт разовый импульс до 14-17 м/с к оси (или вверх).
  Старое ведение при v_out < 0 сохраняло направление НЕИЗМЕННЫМ —
  отброс жил много кадров, машину реально уносило от стены.
- Телеметрия tools/TestWallGhost.tscn: 60 с газ + руль в стену, метрики
  «отбросов» (скорость к оси > 2.5), подскоков и — ГЛАВНАЯ — кадров,
  когда машину отнесло от стены > 2 м (far_frames), при среднем
  прижатии avg_dist. Скоростные всплески в тесте видны ДО капа (тест
  читает скорость раньше обработки машины) — судить надо по far_frames.
- Фикс (Car.gd, _wall_slide): в пристенке (контакт или окно доворота
  0.35 с) и БЕЗ руления прочь скорость ОТ стены ограничена 1.2 м/с
  (кривизна выпускает плавно, руль от стены — свободно); клапан
  подскока vy ≤ 1.0 расширен с «только контакт» на всё окно.
  Заодно клапан ровной езды ужесточён 1.0 → 0.6 м/с (жалоба «всё так
  же подпрыгивает»; 0.6 м/с ≈ 2 см — глазом не видно).
- Итог: far_frames 651 → 157 (в 4 раза; остаток в основном легитимные
  переходы между бортами на шиканах), avg_dist 7.15 → 7.56 (плотнее
  к стене при руле в неё). Все 17 регрессионных тестов PASS.

## Пятна травы убраны + фикс «подпрыгивает на ровном» (2026-08-19)
- Жалоба: «у полей пятна одинаково расположенные, большие, некрасиво;
  иногда на ровном месте машина подпрыгивает».
- Трава: (1) при конвертации grass_1 пятна смягчены — текстура
  смешана к своему среднему цвету (blend 0.45 к среднему (76,106,33));
  (2) тайл 10 → 14 м; (3) в _build_ground вершинам задаётся
  низкочастотная вариация яркости (два синуса разных масштабов,
  vertex_color_use_as_albedo) — повторяемость тайла глазом не видна.
- Подпрыгивание: диагностика тестом tools/TestBumps.tscn (одиночная
  машина на ИИ 90 с, соперники заглушены в углу карты; печатает
  всплески vy на плато). Причина — «фантомные» удары днища о
  внутренние рёбра треугольников trimesh-коллизии дороги (в момент
  подскока y машины низкий — днище чиркает по полотну).
- Фикс (Car.gd): клапан vn у земли стал двухступенчатым — 3 м/с
  разрешено только 0.35 с после посадки (аркадный отскок), при
  устоявшейся езде (_ground_time > 0.35) И ровной опоре
  (_ground_normal.y > 0.995) — максимум 1 м/с (~5 см подскока).
  Склоны/трамплины не задеты: там нормаль наклонена и клапан мягкий.
  Новый счётчик _ground_time (время на земле без отрыва).
- Результат: жёсткие подбросы 2-7 м/с на ровном исчезли; остаточные
  ≤ 1 м/с незаметны. Все 17 регрессионных тестов PASS.

## Зелёные поля + трибуны лицом к трассе (2026-08-19)
- Запрос: «трибуны должны быть развернуты к трассе и не залазить на
  неё, поля за трассой сделай зелёные, возьми материалы из ассетов».
- Земля: _build_ground получил планарные UV по миру (тайл 10 м) и
  текстуру травы grass_1 из Cartoon Tracks Pack (сконвертирована PIL
  из psd вместе с grass_2/gravel — лежат в cartoon/textures про
  запас). Низ скайбокса перекрашен в тон травы. ВАЖНО: после
  добавления новых PNG обязательно `godot --headless --import`,
  иначе load() вернёт null и земля будет белой.
- Трибуны: на стенде ScreenshotTribunes.tscn выяснено — у BEDRILL
  Tribune «лицо» +Z, у мультяшных prop_seats_* — -Z (задом). В
  _build_tribunes мультяшным добавлен разворот (флаг flip); новая
  страховка _push_outside: после установки берётся мировой AABB всех
  мешей пропса, и если ближайший угол ближе 1.5 м к ограждению —
  пропс отодвигается наружу (лечит и «залезание» на полотно из-за
  несимметричных пивотов у мультяшных моделей).
- В screenshot_track добавлен кадр tribune75.png (панорама с
  мультяшной трибуной).
- Тесты: Lap/Ramp/Offtrack/Finish/AICorners/WallSlide/MineDrop PASS.

## Голубое небо + разметка на дороге (2026-08-19)
- Вопрос пользователя: «почему у нас красное небо?» — это был фон
  BG_COLOR (0.35, 0.18, 0.12) с комментарием «небо чужой планеты».
  С зелёными деревьями и шарами смотрелось чужеродно.
- Main._setup_environment: вместо заливки — ProceduralSkyMaterial
  (голубой верх, светлый горизонт, коричневый низ в тон рельефу: в
  изометрии сверху за краем земли видна именно нижняя полусфера
  скайбокса, она сливается с пустыней). Ambient стал нейтральным.
- Разметка (TrackDecor._build_road_marks, из ассетов + процедурная):
  1) скобы стартовой решётки prop_gridline под 4 слотами машин
     (позиции повторяют Main._spawn_cars: ряды -2/-7 м, колонны ±2.2);
  2) белые стрелки Arrow_02_free (BEDRILL) за 18 м до вершин крутых
     поворотов, нацелены на вершину («куда ехать»); пики кривизны
     вынесены в общий _turn_peaks (их же используют щиты);
  3) пунктирная осевая: штрих 2.4 м каждые 8 м по всему кругу
     (BoxMesh, наклон по профилю через look_at), возле старта пропуск.
- Стенд tools/ScreenshotMarks.tscn: сравнение стрелок/линий из обоих
  паков на тёмной «дороге» (мультяшные стрелки оказались нечитаемыми
  серыми плашками — взяты BEDRILL).
- Тесты: Ramp/Lap/Finish/Position/AICorners/MineDrop/WallSlide PASS.

## Трасса длиннее + декор из готовых ассетов (2026-08-19)
- Запрос: «бери всё что нужно и сделай красивую трассу подлиннее».
- Трасса: радиус контура 33 → 52 м (+третья гармоника «дыхания»),
  круг 221 → 346 м; точек кривой 40 → 56, SAMPLES 256 → 384,
  земля 190 → 280 м (GROUND_RES 104).
- Ассеты в `assets/models/track_env/`: BEDRILL Track Environment Free
  (арка, баннер, флаги, конусы, шины, знаки, фонари, трибуна — FBX с
  текстурой, работают из коробки) и Cartoon Tracks Pack 1
  (`cartoon/`: стартовые огни, шары, деревья, трибуны, башня, тент,
  реклама, таблички команд). Мультяшные FBX ссылаются на .psd,
  которые Godot не читает: PSD сконвертированы PIL в PNG 1024
  (`cartoon/textures/`), назначаются кодом по имени материала FBX.
- Новый `scripts/TrackDecor.gd` (вызов из TrackBuilder._build_decor):
  финишная арка с клетчатым баннером (вместо самодельной из
  цилиндров — из _build_start_line убрана, шахматная лента осталась),
  ферма стартовых огней с красными LED (консоль нависает над полотном),
  башня, тент, таблички команд у старта; 3 трибуны, 5 шаров в небе,
  мелочь вдоль обочин с шагом 21 м (флаги/конусы/шины/реклама/фонари,
  чередуя стороны), знаки перед 5 самыми крутыми поворотами (поиск
  пиков кривизны, знак на внешней стороне за 12 м), 90 деревьев
  случайной россыпью (seed фикс.). Весь декор БЕЗ коллизий и за
  ограждением — геймплей не задет. Модели BEDRILL несут запечённое
  смещение из их демо-сцены — нормализация по AABB (_recenter).
- Грабли: в списках TREES/BALOONS забыл расширение «.FBX» — load()
  молча падал, деревья и шары не появлялись. Скриншоты обязательны.
- Служебные сцены: `tools/ScreenshotTrack.tscn` (кадры трассы в PNG:
  обзор, старт, середина, игровой ракурс — гонять после правок декора
  и СМОТРЕТЬ картинки), `tools/ScreenshotProps.tscn` (стенд BEDRILL-
  пропсов в ряд).
- Все 17 тестов PASS на новой трассе (Lap: круг 346 м, ИИ 1278 м;
  AICorners: 2 инцидента по 0.3 с — в норме).

## Ограждения выше + мины не висят в воздухе (2026-08-19)
- Запрос: «сделай ограждения повыше; мины не должны оставаться в
  воздухе, если их положили в воздухе».
- TrackBuilder.gd: WALL_HEIGHT 1.7 → 2.6 (прыжок с места ~1.9 м —
  стену больше не перепрыгнуть, только с трамплина). Кромка ведения
  у стены (Car._wall_slide) берёт ту же константу — поднялась сама.
  Обновлён комментарий у jump_impulse в Car.gd.
- Mine.gd: мина, сброшенная в полёте (с трамплина, в прыжке), больше
  не парит — падает с ускорением 9.8 м/с², луч вниз на шаг кадра ищет
  опору (слой 1: дорога/земля; стены — слой 2 — не опора), нашла —
  ложится на поверхность (+0.08 м). Улетевшая ниже y=-60 удаляется.
- Тест `tools/TestMineDrop.tscn` (test_mine_drop.gd): одна мина
  поднята на 4 м — за 2 с должна упасть до полотна (< 0.3 м), вторая
  сброшена как обычно. PASS (упала на 4.32 м, зазор 0.08 м у обеих).
- Регрессия: все 17 тестов PASS (WallSlide/WallTurn/Ramp/Offtrack/
  Lap/AICorners/Flip/Landing/Dive/AirSpin/Respawn/Finish/Position/
  Heading/CarBounce/Explosion + MineDrop).

## Машины «прилипали» друг к другу — разлеплены (2026-08-18)
- Жалоба: «при столкновении машины словно прилипают друг к другу».
  Две причины: трение материала корпуса (0.15) действует и МЕЖДУ
  машинами — корпуса в контакте цеплялись и ехали вместе; а после
  разового рикошета ничто не разводило прижатые кузова.
- Сделано (Car.gd):
  1) physics_material_override.friction 0.15 → 0.05 (сцепление с дорогой
     и так отдельной аркадной силой в _drive, штраф у стены — вручную).
  2) В _bounce_off_cars, пока корпуса СОПРИКАСАЮТСЯ (каждый кадр
     контакта, не только новый), — расталкивание 7 м/с² по горизонтали
     от соперника. Активный таран продавливает (движок 15 м/с²).
- TestCarBounce: фаза 3 — две машины борт к борту внахлёст (1.65 м межд
  центрами, без управления) должны разлепиться до ≥ 2.2 м за 0.75 с
  (факт 2.35; корпуса перестают перекрываться с 1.7). Заодно центральный
  таран стал чище: 5.2° (было 7.5).

## ИИ больше не вылетает на поворотах + руль зависит от скорости (2026-08-18)
- Жалоба: «противники на поворотах сами вылетают за трассу всегда».
  Базовый замер (новый тест TestAICorners, 60 с): 9 вылетов на троих.
- Диагностика (телеметрия в тесте, DEBUG_ZONE): главный механизм — ИИ
  ВЗЛЕТАЕТ с гребня подъёма на плато на 26 м/с (dy до +3.7 м, vy +4.7,
  0 колёс на земле), летит над кромкой ограждения (выше кромки ведение
  у стены отключено by design), трасса поворачивает — а он по прямой,
  падает снаружи (отметки 0.30-0.36). Раньше ИИ вообще не тормозил:
  газ всегда 1.0 (0.45 лишь при развороте > 50° — уже поздно).
- Сделано (Car.gd, _ai_control + _ai_allowed_speed + _axis_dir):
  умное планирование скорости — сэмплы оси трассы на 60 м вперёд
  (шаг 4 м), два предела:
  1) поворот В ПЛАНЕ: v = 0.8 * steer_speed_min / κ (нос крутится с
     конечной скоростью);
  2) ГРЕБЕНЬ (выпуклый перелом профиля): быстрее v²·κ_верт = g машина
     отрывается — предел 1.35*sqrt(g/κ_верт) (подскок можно, дальний
     полёт нельзя);
  запас тормозного пути: у точки на дистанции d можно v² = v_цели²+2ad
  (a = 8 м/с²). Если скорость выше предела +1.5 — тормоз, выше — накат.
- Main.gd _check_recovery: таймер вылета ЗАТУХАЕТ (−2*delta), а не
  сбрасывается в ноль — машина, дребезжащая у порога OFFTRACK_DIST
  (кадр внутри/кадр снаружи), раньше НИКОГДА не набирала таймер и
  каталась за ограждением бесконечно (в тесте: 24 «фронта» за 2 с).
- По ходу (просьба пользователя): скорость поворота коррелирует со
  скоростью машины — turn_rate *= clamp(|v|/steer_full_speed, 0, 1)
  (экспорт, 10 м/с). Стоя развернуться нельзя, полная сила руля с 10 м/с.
- Итог: 1 вылет за 60 с (толкучка на прямой, возврат за 0.6 с), темп ИИ
  948 м/мин (было ~1030 — цена торможения перед гребнями/поворотами).
- Тест `tools/TestAICorners.tscn` (test_ai_corners.gd): seed(1234) для
  воспроизводимости (машины/разброс ИИ случайны), инциденты = фронты
  «вне трассы» с окном слияния 3 с; критерии: ≤ 3 инцидентов, каждый
  эпизод снаружи < 2.5 с (автовозврат работает), худший ИИ > полкруга.
  DEBUG_ZONE := true — телеметрия зоны 0.25-0.40 для отладки.

## После финиша все машины останавливаются (2026-08-14)
- Запрос пользователя: «после финиша все машинки останавливаются».
  До правки останавливался (и то накатом, без тормоза) только игрок
  (controls_enabled = false), а ИИ бесконечно наматывали круги —
  проверено headless-тестом (за 15 c после финиша худший ИИ проезжал
  266 м).
- Car.gd: новый флаг `race_over`; в _physics_process при race_over и
  on_ground — тормозная сила против хода (brake_power), остаток
  горизонтальной скорости < 0.5 м/с обнуляется (иначе на уклоне ползла
  бы вечно), рысканье гасится. В воздухе не тормозим — сначала посадка.
- Main._finish_race(): у ВСЕХ машин controls_enabled = false +
  race_over = true. Застревание (stall) их не респавнит — проверка
  идёт только при controls_enabled.
- Тест `tools/TestFinish.tscn` (test_finish.gd): машина игрока
  переводится на ИИ-управление, гонка финиширует естественно (~47 c);
  через 8 c после финиша никто не должен двигаться. PASS (все встали
  за ~3 c, макс. скорость после остановки 0.00). TestRamp и TestLap —
  PASS (ветка активна только при race_over, физика гонки не задета).

## Позиция в гонке показывалась неверно — исправлена (2026-08-14)
- Жалоба: «позиция игрока в гонке неправильно отображается». Причина:
  прогресс каждой машины копился ОТ ЕЁ МЕСТА НА РЕШЁТКЕ (у всех старт 0),
  а решётка 2×2 — задний ряд стоит на ~5-7 м позади. ИИ из заднего ряда
  весь заезд имел фору прогресса: догнав игрока «колесо в колесо», по
  прогрессу он был впереди, и HUD показывал игроку худшее место.
- Фикс (Main._spawn_cars): стартовый прогресс = фактическая отметка
  машины относительно СТАРТОВОЙ ЛИНИИ (offset близко к концу круга →
  прогресс отрицательный, у игрока ≈ -2, у заднего ряда ≈ -7). Дальше
  всё сравнимо честно; финиш = 4 полных круга именно от линии.
- Тест `tools/TestPosition.tscn` (test_position.gd): ИИ заднего ряда
  телепортируется на 1 м позади игрока — место должно остаться 1
  (до фикса было 2). PASS (прогресс P=-1.7, ИИ2=-3.5).
  TestLap (1101 м/мин) и TestFinish (финиш 47 c, все встали) — PASS.

## Запуск из SiteKeeper (2026-08-14)
- Плитка «▶ Запустить игру» на вкладке Big Head Racing в SiteKeeper.
- Реальный лаунчер — `play.bat` (латиница!): `shell.openExternal` Electron
  молча игнорирует file://-ссылки с кириллицей в имени («Играть.bat» из
  плитки не запускался). «Играть.bat» оставлен и просто вызывает play.bat.

## Трасса сделана плоской (2026-08-20)
- Просьба пользователя: «давай сделаем трассу просто плоской без
  перепадов высот пока».
- TrackBuilder.gd: новый флаг `FLAT_TRACK := true` — _profile_height()
  возвращает 0. HEIGHT_KEYS и вся механика рельефа СОХРАНЕНЫ: вернуть
  перепады = выставить false. Контур в плане, земля, декор, трамплины
  (остались как прыжки на ровном) подстроились сами. Круг 343 м.
- Правки тестов под плоскую трассу и попутные фиксы:
  1) TestCarBounce: порог «гэп разлепления» 2.2 → 2.0 (факт на плоской
     2.18 — на рельефе фазе 3 помогал уклон; перекрытие корпусов
     заканчивается на 1.7, запас остался).
  2) TestWheelSink/TestBumps: заглушенным соперникам ставится
     alive=false — иначе автовозврат Main._check_recovery (вылет за
     трассу) через секунду ВОЗВРАЩАЛ «запаркованных в углу» соперников
     на трассу, и тест-машина их таранила (ловилось как утопание/пинки).
  3) Car.gd, кламп колёс: упреждение считается по НОРМАЛИ опоры
     (сближение точки колеса с плоскостью), а не только по vy — при
     заезде на трамплин поверхность «набегает» под колесо горизонтально
     (v·sin 9° ≈ 4.7 м/с), и колесо на кадр-два ныряло в кромку.
- Все 18 тестов PASS (Lap 1415 м — на плоской ИИ быстрее; AICorners
  0 инцидентов; WheelSink worst pen 0.147 — кромка трамплина, 1-2
  кадра). TestBumps: body_contacts 71 → 17, kicks 0. Скриншоты
  ScreenshotTrack смотрены глазами — трасса плоская, декор на местах.
  ВАЖНО: ScreenshotTrack НЕ работает в --headless (dummy-рендерер,
  вечное ожидание frame_post_draw) — запускать с окном.
- ПОДТВЕРЖДЕНИЕ пользователя (2026-08-20): «вот когда трасса плоская
  всё работает отлично». Итого вся серия жалоб (подскоки, утопание
  колёс, притормаживания, фантомные пинки) была завязана на РЕЛЬЕФ:
  переломы профиля + грубые фасеты тримеша полотна на склонах/ложбинах.
  Когда будем возвращать перепады (FLAT_TRACK=false), сначала: мельче
  сэмплы полотна на переходах профиля (SAMPLES локально/адаптивно) или
  более пологие S-переходы в HEIGHT_KEYS, и прогнать TestWheelSink/
  TestBumps на рельефе — теперь они это ловят числами.

## Система оружия с боксами — как в картинг-аркадах (2026-08-20)
- Запрос пользователя: одно оружие в руках; одно случайное даётся на
  старте; дальше — боксы на трассе (случайный вид); 8 видов оружия;
  уничтожение = мгновенный респавн с миганием и «призраком».
- НОВОЕ УСТРОЙСТВО БОЯ (HP/боезапас УДАЛЕНЫ: hp, ammo, mines,
  take_damage, shoot, drop_mine, refill_ammo, _explode больше нет):
  - `scripts/Weapons.gd` — enum 8 видов + имена: MINE, ROCKET, OIL,
    MAGNET, LASER, FREEZE, AIRSTRIKE, BOOST.
  - Car: `weapon` (одно, -1 = пусто), `use_weapon()` (тратит),
    `destroy()`, `apply_freeze/apply_oil_slip/push_from_blast`,
    `is_ghost()`. Кнопки fire И drop обе используют оружие.
  - СЛОИ КОЛЛИЗИЙ: машины переехали со слоя 1 на СЛОЙ 4
    (layer 0b100, mask 0b111). «Призрак» = layer 0 + mask 0b011
    (дорога/стены остаются, машины навылет). Area3D снарядов/мин/
    боксов ловят машины маской 0b100. Лучи подвески как были (mask 1).
- Виды оружия:
  - Мина (Mine.gd переделана): взрыв при наезде РАСТАЛКИВАЕТ все машины
    в радиусе 6 м (не уничтожает), сила тает с расстоянием.
  - Ракета (Projectile.gd переделан, flag freeze=false): 55 м/с вперёд,
    уничтожает машину при попадании.
  - Масло (OilSlick.gd): пятно сзади, наехавшего заносит (закрутка
    2.2-3.2 рад/с + грип 0.8 на 1.1 с; окна _bump_spin_time и
    _ext_push_time не дают рулю/капам съесть занос).
  - Магнит: все машины разово получают импульс 6 м/с К использовавшему.
  - Лазер (LaserFx.gd — визуал): луч 70 м, полуширина 1.6 м,
    уничтожает ВСЕХ на пути.
  - Заморозка (Projectile freeze=true): попавший синеет (IceShell —
    голубая полускорлупа на кузове) и едет ×0.55 на 3 с; дебаф ЗАРАЗЕН
    при контакте машин (передаётся остаток, _bounce_off_cars).
  - Авиаудар (Airstrike.gd): цель — ЛИДЕР гонки (Main.leader_car());
    4 тени на оси перед ним (10-28 м, случайный сдвиг вбок), через 1 с
    падение — все машины в радиусе 3.2 м от тени уничтожаются.
  - Ускорение: ×1.45 к пределу и тяге на 2.5 с.
- Уничтожение (destroy): вспышка → машина СРАЗУ на оси трассы с нулевой
  скоростью → «призрак» 2 с: мигает 3 раза (только визуал), НЕ
  сталкивается с машинами, но может ехать/разгоняться; потом всё как
  раньше. Дебафы при смерти сбрасываются, оружие сохраняется.
- Боксы (WeaponBox.gd): золотой вращающийся куб, 5 линий × 3 шт по
  кругу (t = 0.12/0.3/0.52/0.7/0.92), респавн 5 с. Подбор заменяет
  текущее оружие. Main._spawn_weapon_boxes().
- ИИ: применяет по типу — ракета/лазер/лёд по врагу в конусе впереди,
  мина/масло по врагу сзади, магнит при враге ближе 12 м, буст на газу,
  авиаудар сразу.
- HUD: вместо HP-бара и боезапаса — «Оружие: <имя>» (жёлтым) или
  «возьми бокс»; подсказка внизу обновлена.
- Тест `tools/TestWeapons.tscn` (test_weapons.gd): 12 сценарных
  проверок (ракета→призрак, лёд+заражение, мина толкает, лазер бьёт
  двоих, буст, магнит, масло легло/заносит, авиаудар создан/отработал,
  бокс даёт оружие). ГРАБЛИ: (1) жертв парковать только с alive=false —
  иначе автовозврат притащит их на трассу под снаряды теста; (2) позиции
  дальше ~15 м ставить ПО КРИВОЙ, а не по прямой от точки оси (прямая
  на 25 м уводит на ~5 м вбок, к стене).
- TestExplosion переписан под destroy/призрак (мигание, снятые слои,
  возврат на трассу). Все 19 тестов PASS; гоночные тесты (Lap 1443,
  AICorners 0 инцидентов) идут с ЖИВЫМ оружием у ИИ — цикл гонки
  устойчив к хаосу уничтожений.

## Помощь развороту у стены + дым/искры + ракеты авиаудара (2026-08-20)
Запрос: (1) после перпендикулярного удара в отбойник машина долго
разворачивается вдоль трассы («пока раза 4 не ударишься — не
развернёшься»); (2) тень от ракет авиаудара поменьше и видимые падающие
ракеты; (3) дым из-под колёс на заносе; (4) искры при столкновении машин.
- Помощь развороту (Car.gd). Причина застревания: сила руля ~ скорости
  (turn_rate *= |v|/steer_full_speed) — у стены машина почти стоит, руль
  бессилен; а нажатый руль отключает штатный доворот вдоль ограждения
  (steering_away в _wall_slide). Два механизма:
  1) автодоворот в _clamp_heading: нос поперёк оси > 35° И скорость
     < 8 м/с И колёса на земле → корпус сам крутится к касательной
     2.4 рад/с (до 35°, дальше руль);
  2) нижний порог силы руля: при |угол к оси| > 35° speed_factor не
     ниже 0.6 — рулить можно и почти стоя. Новый var _track_ang_abs
     (ставит _clamp_heading).
  Тест tools/TestWallRecover.tscn: машина СТОИТ вплотную к стене носом
  под 80°, «игрок» жмёт газ + руль от стены; критерий — угол < 30° за
  ≤ 2 с (факт 0.30; до фикса 0.68 — фикс главное помогает, когда игрок
  без газа в пол или сдаёт назад: раньше руль стоя был мёртв).
- Авиаудар (Airstrike.gd): тень-диск 2.56 → 1.1 м (SHADOW_RADIUS);
  видимые ракеты — серый цилиндр, красный нос-конус остриём вниз,
  4 стабилизатора, оранжевое «пламя» сверху; падают с 34 м квадратично
  по остатку таймера (у земли ~68 м/с), нос касается точки в момент
  взрыва. Начало координат ракеты — остриё.
- Дым на заносе (Car.gd): 2 CPUParticles3D у задних колёс
  (_build_smoke), серые тающие клубы, local_coords=false (остаются
  позади). Включение: на земле И (|боковой снос| > 3 м/с при скорости
  > 5 ИЛИ занос от масла). Новый var _side_speed (пишет _drive).
- Искры (новый scripts/SparksFx.gd, CPUParticles3D one-shot):
  жёлто-оранжевый сноп в точке контакта машин, сила от скорости
  сближения; спавн в _bounce_off_cars при closing > 2, только у одной
  машины из пары (меньший instance id) — иначе дублировалось бы.
  Сам удаляется по сигналу finished. После нового class_name — не
  забывать `--headless --import`.
- Все 20 регрессионных тестов PASS (Lap 1414, AICorners 0 инцидентов,
  Heading ровно 80.0° — помощь развороту лимит не ломает).
- Доработка дыма (2026-08-20, «дым слишком квадратный, на небольших
  заносах не нужен»): клубы были резкими квадратами — у QuadMesh не
  было текстуры. Теперь мягкое круглое пятно (GradientTexture2D
  FILL_RADIAL 64×64, белый центр → прозрачный край) + случайный поворот
  билборда (angle 0-360) + рост клуба по кривой (0.4 → 1.5 за жизнь).
  Порог включения поднят: |боковой снос| > 5 м/с при скорости > 8
  (было 3 и 5) — лёгкое подруливание не дымит; масло как было.

## Стена гасит удар без отброса + ракеты из-за экрана + магнит (2026-08-20)
Запрос: «машина отлетает при ударе об ограждения — стена должна гасить
удар, но не отталкивать; тени от ракет меньше и ракеты летят сверху
из-за экрана; магнит сильнее».
- Стена (Car.gd, _wall_slide). Источник «отлетания»: при НАЖАТОМ руле
  прочь от стены кап скорости «от стены» (1.2 м/с) не действовал вовсе,
  а ветка выпуска восстанавливала скорость из памяти _recent_hspeed и
  выстреливала под 22° от ограждения (sin22°·20 м/с ≈ 7.5 м/с наружу).
  Фиксы: (1) кап скорости от стены теперь ВСЕГДА в пристенке — без руля
  прочь 1.2 м/с, с рулём прочь 3.0 м/с (отойти можно, отлететь нельзя;
  выпуск под 22° жив, но его наружная компонента режется тем же капом
  в этом же кадре); (2) гашение удара усилено: штраф скорости сближения
  при перехвате 25% → 40% (скользящий удар почти не теряет — v_out мал,
  перпендикулярный гасится ощутимо).
- Авиаудар (Airstrike.gd): тень 1.1 → 0.75 м; ракеты летят НАКЛОННО
  «из-за верхнего края экрана»: старт = точка удара + UP·40 +
  26 м по горизонтали вдоль взгляда камеры (изокамера не вращается,
  дальше по взгляду = выше на экране; headless-фолбэк — вектор изокамеры
  (-0.707, 0, -0.707)). Корпус наклонён по траектории:
  quaternion = Quaternion(DOWN, вектор падения).
- Магнит: импульс 6 → 11 м/с.
- Тесты: WallSlide/WallTurn/WallHit/WallRecover/Weapons/Landing/Lap/
  AICorners PASS; WallGhost far_frames 162 (база 157-158), rev=0 —
  отбросы не выросли, ведение не сломано.

## Красивый UI из GUI Pack Cartoon + эффекты из Epic Toon FX (2026-08-20)
Запрос: «ассеты для UI из E:\UnityProjects\cars\Assets\GUIPackCartoon,
спецэффекты из E:\UnityProjects\Fish\Assets\Epic Toon FX, сделай
красивый UI».
- Ассеты скопированы в проект: `assets/ui/` (панели-пилюли, круги,
  кнопка, баннер-лента, стрелки, 8 иконок оружия, шрифт LilitaOne.ttf)
  и `assets/fx/` (smoke_cloud_2x2, sparkle, explosion_3x3). Источники:
  GUIPackCartoon\Demo\Sprites (готовые PNG; Sources — psd/svg) и
  Epic Toon FX\Textures. После добавления — `--headless --import`.
- Weapons.gd: словарь ICONS + static icon(kind) — иконки для HUD
  (мина=бомба с шипами, ракета=ракета, масло=фиол. зелье, магнит,
  лазер=синяя молния, заморозка=голубое зелье, авиаудар=петарды,
  буст=жёлтая молния).
- HUD гонки (Main.gd, _setup_hud переписан, хелперы _make_panel/
  _make_label): скорость и круг/место — глянцевые панели-пилюли
  (btn_rect_gray + self_modulate серо-синий), слот оружия — тёмный
  круг с иконкой и подписью («?» при пустом), предупреждение — красная
  плашка btn_rect_red, отсчёт 3-2-1-GO — огромные цифры LilitaOne с
  «выпрыгиванием» (tween scale 1.6→1, TRANS_BACK), финиш — розовый
  баннер-лента с текстом. _ammo_label/_center_label удалены
  (_weapon_icon/_weapon_name/_count_label/_finish_root).
- Экран выбора (CarSelect.gd): заголовок на розовом баннере, имя машины
  шрифтом LilitaOne на панели (имена латиницей — у LilitaOne НЕТ
  кириллицы, русский — дефолтный шрифт с обводкой), голубые
  стрелки-кнопки листания по бокам подиума, зелёная глянцевая СТАРТ
  (StyleBoxTexture + modulate_color на hover/pressed).
- Эффекты: дым колёс — мультяшные облачка из атласа 2×2 (материал
  particles_anim 2×2 + anim_offset random = случайный кадр каждой
  частице); искры — звёздочка sparkle.png; FlashFx ПЕРЕПИСАН:
  AnimatedSprite3D, 9 кадров explosion_3x3 (AtlasTexture), билборд,
  красится modulate в цвет эффекта, сигнатура spawn прежняя.
- ГРАБЛИ Godot UI (все пойманы скриншотами):
  1) set_anchors_preset СОХРАНЯЕТ текущий размер (подгоняет offsets) —
     «full rect» лейбл остаётся крошечным у левого верха; нужен
     set_anchors_and_offsets_preset.
  2) TextureRect: expand_mode ставить ДО size — при дефолтном
     KEEP_SIZE присвоение size клампится к размеру текстуры (512).
  3) NinePatchRect.modulate красит и детей (текст темнел) — красить
     через self_modulate.
  4) panel_round (Rounded Rectangle 256px) не годится для 9-slice:
     радиус в полтекстуры, панель выходит «ромбом» — панели собраны
     из кнопки-пилюли btn_rect_gray (margin 30).
- Доработка по замечаниям (2026-08-20, «текст в флагах по центру,
  чёрные облака с выпуклостями, оружие на E»):
  1) Панели HUD переведены с 9-slice кнопок пака на ПЛОСКИЕ
     StyleBoxFlat-панели (скруглённый прямоугольник, тонкая рамка):
     у ВСЕХ «прямоугольников» GUI Pack Cartoon края пузатые (замерено
     PIL: гуляют до 12-19 px) — растянутая 9-slice панель выглядит
     «облаком с выпуклостями». Кнопки/баннеры/иконки пака остались.
  2) Тексты на лентах-баннерах отцентрированы (убраны смещения вверх).
  3) Оружие перенесено на E (InputSetup: fire=[KEY_E], было Ctrl/J),
     подсказка внизу обновлена.
  4) Шрифт заменён: пользователь дал «Softie Cyr.ttf» (из
     E:\UnityProjects\Fish\Assets\TextMesh Pro) — МУЛЬТЯШНЫЙ И С
     КИРИЛЛИЦЕЙ (у LilitaOne кириллицы не было) → assets/ui/Softie.ttf,
     применён ко ВСЕМУ UI (HUD, отсчёт, баннеры, кнопка, подсказки),
     параметр cartoon у _make_label убран.
- ГРАБЛИ PowerShell (повторные!): Get-Content -Raw без -Encoding UTF8
  читает файл в ANSI(1251) → после Set-Content кириллица в Main.gd
  превратилась в мохибейк. ВОССТАНОВЛЕНО обратной перекодировкой .NET
  (UTF8-байты → GetString → GetBytes(1251) → байты; BOM-символ убрать).
  Больше НЕ править файлы с кириллицей через PowerShell-Replace —
  только Edit-инструменты.
- Скриншот-стенды (С ОКНОМ, headless не рендерит):
  tools/ScreenshotHud.tscn (кадры: отсчёт, GO, езда, предупреждение —
  подделывается в _process с process_priority=100, иначе Main гасит, —
  и финиш) и tools/ScreenshotSelect.tscn (экран выбора). PNG в
  tools/shots/, смотреть глазами после правок UI.
- Все 20 регрессионных тестов PASS.

## Метки авиаудара ярче + дым к колёсам + новая трасса 442 м (2026-08-20)
Запрос: «тень от ракет посильнее выделяться должна; дым не такой
длинный и из-под задних колёс (сейчас по бокам); трассу подлиннее и
другой формы».
- Авиаудар (Airstrike.gd): SHADOW_RADIUS 0.75 → 0.9, тень плотнее
  (alpha 0.8, unshaded) + ЯРКО-КРАСНОЕ КОЛЬЦО-ПРИЦЕЛ (TorusMesh,
  emission 2.0) ребёнком тени — пульсирует вместе с ней, видно издалека.
- Дым (Car.gd): эмиттеры сдвинуты с краёв корпуса (x ±0.85 — клубы
  торчали по бокам) внутрь колеи за задние колёса (±0.55, z 1.5);
  шлейф короче: lifetime 0.75 → 0.5, разлёт 0.5-1.2 (был 0.8-2.0),
  клубы мельче (quad 0.7, scale 0.55-0.95, рост до 1.2), amount 16.
- Трасса (TrackBuilder.gd): новый контур — базовый радиус 52 → 68,
  ПЕРВАЯ гармоника (10·sin(a+0.7)) даёт асимметрию «яйцом» + шиканы
  8·sin2a/4·sin3a/2·sin6a; точек 56 → 64, SAMPLES 384 → 512, земля
  280 → 340 м (GROUND_RES 126). Круг 343 → 442 м. test_curve: излом
  в плане 2.42° (лимит 6°). Боксы/трамплины/декор привязаны к доле
  круга — подстроились сами.
- В ScreenshotHud добавлен кадр hud_airstrike.png (авиаудар целится
  в ИГРОКА напрямую, не через use_weapon: лидер может быть вне кадра).
- Все 20 тестов PASS на новой трассе (Lap 1474 м/мин, AICorners 0
  инцидентов, WheelSink pen 0.000 — на плоской трассе с новым контуром
  колёса вообще не тонут).

## Настоящая гоночная конфигурация трассы + переменная ширина (2026-08-21)
Запрос: «трассу посложней, покруче повороты, прямые участки и изгибы,
сужения и расширения тоже».
- ПРИНЦИП ИЗМЕНЁН: контур больше не «дышащая окружность» (гармоники
  радиуса давали только плавные дуги — ни прямых, ни шпилек). Теперь
  трасса задаётся ЯВНОЙ последовательностью участков `SEGMENTS`
  («черепахой»): `["S", длина, полуширина]` — прямая,
  `["A", радиус, угол°, полуширина]` — дуга. Круг 442 → 726 м.
  Конфигурация: стартовая прямая ~46 м → быстрый правый R45 →
  S-шикана R28 → ШПИЛЬКА R19 (150°) → длинный левый R40 → длинная
  прямая ~77 м → R32 → крутой правый R26 (105°) → пологий левый R50.
- ЗАМЫКАНИЕ КОЛЬЦА ТОЧНОЕ, без подгонки руками: сумма углов дуг = 360°
  (обязательное условие, иначе излом на стыке), а длины ДВУХ «свободных»
  прямых (в SEGMENTS длина -1.0) считает `_solve_free_lengths()` —
  смещение линейно по этим длинам, поэтому хватает системы 2×2 по трём
  пробным проходам. Конфигурация спроектирована и проверена сначала
  скриптом на Python (замыкание, самопересечение, отрисовка в PNG),
  потом перенесена в GDScript.
- ГРАБЛИ (важные): в первой версии «черепахи» касательная записывалась
  по курсу ДО поворота, а шаг делался по курсу ПОСЛЕ — точка и её
  касательная расходились на полшага, безье «водило», эффективный
  радиус выходил меньше заданного (test_curve: 7.87° при лимите 6°).
  Правильно: касательная в точке — по текущему курсу, а шаг — ПО ХОРДЕ
  под ПОЛОВИНОЙ угла шага (chord = 2R·sin(δ/2)). После фикса излом
  5.55° = ровно кривизна шпильки, дефектов нет.
- ПЕРЕМЕННАЯ ШИРИНА полотна: 6.0 м (шпилька) … 11.0 м (прямые), плавные
  переходы (smoothstep) по ключам из SEGMENTS. `half_width_at_ratio/
  _at_offset/_at_pos()`. TRACK_HALF_WIDTH теперь = МАКСИМУМ (11) и
  используется только там, где нужен консервативный запас (обочина,
  деревья). Протащено во всё, что зависело от ширины:
  полотно и стены (_widths по сэмплам), стартовая решётка,
  Car._wall_slide (грань стены в ТЕКУЩЕЙ точке — иначе в шпильке
  ведение включалось бы внутри стены), Main (порог вылета
  OFFTRACK_MARGIN + ширина в точке; разброс боксов), Airstrike
  (разброс теней), TrackDecor (трибуны/мелочь/знаки жмутся к кромке).
- Трамплины больше не на фиксированных долях круга (они попадали бы на
  дуги нового контура), а в серединах двух самых длинных ПРЯМЫХ —
  `_ramp_ratios()` по собранным в _walk участкам.
- ПОБОЧНЫЙ ФИКС: TrackDecor._outward() считал «наружу» как направление
  от начала координат — верно только для круглой трассы; на контуре с
  шиканой декор уезжал бы ВНУТРЬ кольца. Теперь пробуются обе стороны:
  наружу та, где дальше до полотна (с другой стороны соседний виток).
- test_curve: лимит излома больше не константа 6°, а считается от
  САМОГО КРУТОГО радиуса конфигурации (step/R_min ×1.4) — крутой
  поворот это задумка, а не дефект; настоящий излом даст угол в разы
  больше. Печатает min_radius.
- ГРАБЛИ теста оружия: фазы должны быть ИЗОЛИРОВАНЫ. Жертва мины стоит
  в 25 м по кривой, и на трассе с прямыми это совпало с лучом лазера
  (70 м по прямой) — жертва уходила в «призраки», а призрака не берут
  ни магнит, ни масло (снят слой машин, Area3D его не видит): падали
  две последние проверки. Теперь перед лазером жертва паркуется.
- Земля 340 → 400 м (GROUND_RES 148), SAMPLES 512 → 768: контур
  габаритом 240×169 м, сэмплы должны оставаться ~1 м.
- ПОБОЧНЫЙ ФИКС (ошибка в логе): WeaponBox выключал `monitoring` прямо
  в обработчике body_entered — физика в этот момент внутри своего
  колбэка и блокирует смену («Function blocked during in/out signal»).
  Теперь `set_deferred("monitoring", false)`.
- Тесты, которые пришлось чинить под трассу с прямыми и дугами (все
  правки — про МЕСТО замера, физику не трогали):
  1) TestWallTurn ставил машину на 6% круга — теперь это вход в поворот
     R=45, и на 28 м/с центробежная сила (17 м/с², больше сцепления)
     прижимала её к внешней стене: тест мерил снос, а не «прилипание»
     (левый борт 1.26 м при норме 1.5). Место пуска теперь ВЫЧИСЛЯЕТСЯ:
     начало самой длинной ПРЯМОЙ без трамплина (_pick_start_t), а
     «вплотную к борту» — от фактической кромки, а не фиксированные
     7.8 м. Итог: борта симметричны (2.06 / 2.11 м), контроль 3.42.
  2) TestBumps: порог «пинка» был константой 2.5°/кадр — на шпильке
     машина законно доворачивает ~3°/кадр. Теперь порог считается от
     КРИВИЗНЫ ОСИ в этой точке (_axis_curvature, ×1.6): ловим доворот
     СВЕРХ поворота трассы. Зона трамплина стала несимметричной
     (−0.01…+0.10 круга): приземление после прыжка — не «подброс на
     ровном». Трамплины тест берёт у трассы (_ramp_ratios), а не из
     зашитых [0.44, 0.875]. Итог: kicks=0, подбросов нет.
  3) TestWeapons — см. грабли выше (изоляция фаз).
  4) TestOfftrack / TestWallGhost / TestWallRecover / TestAICorners —
     переведены на фактическую полуширину в точке.
- ГРАБЛИ (повтор известных): `var x := _main._track.метод()` не
  компилируется — `_main` объявлен как Node3D, тип не выводится
  («Cannot infer the type»), и тест молча ВИСНЕТ (скрипт не грузится,
  фаза не наступает). Писать тип явно. Проверять stderr, а не только
  stdout: parse-ошибки видны только там.

## Мина и магнит бьют сильнее (2026-08-21)
**Проблема:** оба оружия отрабатывали вяло. Мина: радиус 6 м, импульс
9 м/с в эпицентре, ЛИНЕЙНЫЙ спад — уже в 3 м от центра оставалось 4.5 м/с,
которые сцепление (grip 14, постоянная времени ~0.7 с) съедало за пару
кадров. Подброса не было ВООБЩЕ: на ровном полотне клапан отскока режет
вертикальную скорость до 0.6 м/с (`allowed` в `_physics_process`), а
взрыв этот клапан не открывал. Закрутки не было тоже. Магнит: 11 м/с
без учёта дистанции.

**Что сделано:**
- `Mine.gd`: BLAST_RADIUS 6 → 10 м, BLAST_SPEED 9 → 18 м/с, спад стал
  КВАДРАТИЧНЫМ (`1 - t²` вместо `1 - t`) — рядом с эпицентром взрыв
  держит почти полную мощь и сходит на нет только к кромке. Добавлены
  BLAST_SPIN 3.2 рад/с и BLAST_LIFT 0.45 (доля подброса), обе тают тем
  же falloff. Вспышка FlashFx 2.0 → 3.2 под новый радиус.
- `Car._use_magnet()`: 22 м/с вблизи → 11 м/с на 40 м и дальше (lerp по
  дистанции: магнит достаёт всю трассу, но рядом сдёргивает жёстко),
  закрутка до 2.4 рад/с (только вблизи), подброс 0.12 — магнит ВОЛОЧИТ
  по земле, а не подкидывает. Вспышка 1.8 → 2.6.
- `Car.push_from_blast(dir, power, spin := 0.0, lift := 0.35)` — два
  новых необязательных параметра, старые вызовы совместимы. Главное:
  на окно толчка снимаются ВСЕ страховки, которые иначе съедали удар:
  1) `_ext_push_time` 0.4 → 0.7 с (кап боковых пинков — толчок честный);
  2) `_jump_time = max(_jump_time, 0.4)` при lift > 0.05 — ИНАЧЕ ПОДБРОСА
     НЕ ВИДНО: клапан отскока от земли режет вертикаль до 0.6 м/с на
     ровном. Заодно открывается клапан вертикали у стены (строка 825) —
     прижатую к отбойнику машину мина тоже подкидывает;
  3) `_bump_spin_time = max(_bump_spin_time, 0.8)` при spin ≠ 0 — иначе
     жёсткий lerp рысканья (yaw_rate 10) гасит закрутку в тот же кадр
     и разворота от взрыва не будет. Закрутка клампится ±4 рад/с.

**Замеры** (временный стенд, машина стоит, снят после):
| случай | пик скорости | смещение за ~1.5 с |
|---|---|---|
| мина в упор | 17.9 м/с (было ~9) | 11.4 м + подброс ~2.3 м + вращение |
| мина в 5 м (полрадиуса) | 13.4 м/с (было ~1.5) | 9.4 м |
| магнит, жертва в 8 м | 19.7 м/с (было 11) | 12.4 м к магу |

**Грабли:** мина взрывается ТОЛЬКО от наезда машины (`body_entered` →
`_try_trigger`), сама по себе — никогда. В замерочном стенде мина,
положенная в 5 м сбоку от жертвы, просто лежала до конца фазы, и
«сила» читалась как 0.2 м — пришлось ставить на неё вторую машину.
Это же ограничение надо помнить при любых будущих тестах взрыва.

**Файлы:** `scripts/Mine.gd`, `scripts/Car.gd`.
**Тесты:** все 20 сцен + test_curve — PASS (пороги TestWeapons «мина
толкает > 2 м/с» и «магнит > 1 м/с» пройдены с большим запасом).

## Магнит ещё сильнее + масло заносит всерьёз (2026-08-21)
**Магнит** (`Car._use_magnet`): 22 → 32 м/с вблизи (это почти max_speed
34), 11 → 18 м/с на дальней дистанции, радиус спада 40 → 45 м, закрутка
2.4 → 3.6 рад/с. Вспышка 2.6 → 3.2. Замер: жертва в 8 м — пик скорости
19.7 → 29.4 м/с, сдвиг к магу 11.3 → 20.8 м, пик рысканья 1.92 → 2.96
рад/с. Тоннелирования сквозь отбойник нет: у Car включён `continuous_cd`.

**Масло** (`Car.apply_oil_slip` + `OilSlick`): раньше занос был вялый —
1.1 с, сцепление 0.8, тяга при этом ПОЛНАЯ (газ вытаскивал из заноса).
Теперь:
- `slip_duration` 1.1 → 2.0 с;
- новый экспорт `slip_grip` 0.3 (было зашито 0.8, обычное сцепление 14);
- новый экспорт `slip_thrust` 0.35 — ТЯГА И ТОРМОЗ режутся на масле
  (колёса буксуют). Это главное по ощущениям: из заноса больше нельзя
  выехать газом, машину просто несёт юзом;
- закрутка 2.2…3.2 → 3.2…4.4 рад/с, кламп ±3.5 → ±4.5;
- радиус пятна 1.7 → 2.4 м (и коллизия, и меш) — мимо уже не проскочишь.

**Замер масла** (влетел на 20 м/с, окно 1.2 с): пик рысканья 2.63 → 3.79
рад/с. Максимальный угол разворота в обоих случаях ~111° — он упирается
в `_clamp_heading` (max_track_angle_deg = 80° к оси трассы, плюс сама ось
поворачивает под скользящей машиной). То есть «сильнее» у масла
проявляется НЕ в большем угле, а в том, как быстро машину туда кидает,
сколько она там держится (1.1 → 2.0 с) и в потере газа.

**Грабли замера:** боковой снос от масла = 0.0 м — и это ВЕРНО, а не
баг стенда: сцепления почти нет, машина крутится на месте вокруг
вертикали, продолжая скользить по прежнему курсу. Мерить масло надо по
углу/рысканью, а не по сносу. И второе: длинное окно замера (2.5 с)
давало одинаковые цифры до и после — на 20 м/с машина за это время
доезжала до отбойника, и стенд мерил УДАР О СТЕНУ, а не масло. Окно
пришлось урезать до 1.2 с и печатать контроль «занос сработал».

**Риск, принятый осознанно:** `apply_oil_slip` ставит
`_ext_push_time = slip_duration`, то есть кап боковых пинков (защита от
фантомных рывков на ровном, см. раздел от 2026-08-19) теперь отключён на
2 с вместо 1.1. Окно длинное, но это ровно тот случай, когда дикое
боковое движение — задумка, а не глюк.

**Файлы:** `scripts/Car.gd`, `scripts/OilSlick.gd`.
**Тесты:** все 20 сцен + test_curve — PASS.

## Занос от масла крутит в разрешённую сторону (2026-08-21)
**Проблема:** сторона закрутки была чисто случайной. У ограждения в
половине случаев занос втыкал нос В СТЕНУ — а там его тут же и съедали
`_clamp_heading` (предел 80° к оси) и ведение у стены: вместо эффектного
вращения выходил тычок в отбойник.

**Решение:** новый `Car._spin_away_from_wall() -> float`. На свободном
полотне возвращает 0.0 (сторона остаётся случайной), у ограждения —
знак рысканья, уводящий нос ОТ стены. `apply_oil_slip` берёт его, если
он ненулевой.
- «Близко к стене» = `dist + 3.0 м >= half_width_at_offset(off) −
  WALL_THICKNESS/2`. Грань берём в ТЕКУЩЕЙ точке трассы, а не константой:
  полотно переменной ширины (см. раздел про гоночную конфигурацию).
- Нормаль «наружу» считается ОТ ОСИ трассы (`pos − axis_pos`), поэтому
  работает для обоих бортов, внутреннего и внешнего, без отдельных веток.
- Знак «в стену» — ровно тот же, что `into_sign` в `_wall_slide`:
  `signf(fwd.signed_angle_to(n, UP))`. Разрешённая сторона — минус он.
  ВАЖНО держать эти две формулы согласованными: если знак разъедется,
  занос будет крутить нос туда, откуда ведение у стены его выталкивает.

**Тест:** в TestWeapons добавлена фаза «масло у стены» — жертва ставится
в 1 м от грани отбойника носом вдоль трассы, занос роняется напрямую
(`apply_oil_slip`, без пятна: проверяется ВЫБОР СТОРОНЫ, а не
срабатывание Area3D). Две проверки: «занос у стены не в стену» (знак
рысканья) и «нос ушёл от стены» (через 0.5 с проекция носа на нормаль к
стене < −0.15). Тест ПРОВЕРЕН НА ЛОЖНОЕ СРАБАТЫВАНИЕ: с инвертированным
знаком в `_spin_away_from_wall` обе проверки падают.

**Файлы:** `scripts/Car.gd`, `tools/test_weapons.gd`.
**Тесты:** все 20 сцен + test_curve — PASS.

## Значки действующих эффектов над машиной + новый значок буста (2026-08-21)
Запрос: «когда на тебя действуют магнитом — показать над машиной магнит,
когда ускорение — значок ускорения, но значок ускорения нужно другой».

**Новый значок ускорения.** Старый `assets/ui/w_boost.png` был
`Bolt - Yellow (Border).png` из GUI Pack Cartoon (совпадает побайтно —
проверено по md5, так что откатить можно из пака) — пузатая жёлтая
клякса, молния в ней не читается. Нарисован свой: ДВОЙНОЙ ШЕВРОН вверх,
256×256, в стиле пака (толстая тёплая обводка #C4551A, глянцевый
градиент #FFE855 → #FF930E, белый блик по левому плечу). Генератор — PIL,
суперсэмплинг ×4.
- ГРАБЛИ рисования: сначала обводка делалась штрихом по контуру
  (`draw.line(..., joint="curve")`) — она съедала половину заливки
  ВНУТРЬ, а на остром носу шеврона `joint="curve"` срезал кончик
  «галочкой». Правильно — рисовать обводку ТОЛЬКО НАРУЖУ: дилатация
  маски формы (union 64 сдвигов по окружности радиуса обводки), под неё
  подкладывается цвет обводки, сверху — заливка по исходной маске.
- После замены PNG обязателен `godot --headless --path . --import`.

**Значок над крышей** (`Car`): `Sprite3D`-билборд, собирается в
`_build_status_icon`, живёт в `_tick_status_icon` (зовётся из `_process`).
- `top_level = true` ОБЯЗАТЕЛЕН: иначе значок наследует поворот кузова и
  при закрутке от масла/кувырке ездит вокруг машины и уходит под землю.
  Позиция ставится вручную каждый кадр.
- Два источника, и они устроены ПО-РАЗНОМУ. Разовый (магнит) —
  `show_effect_icon(kind, duration)`, зовёт применивший
  (`_use_magnet` → жертве, 1.5 с). Длящийся (буст) читается прямо из
  `_boost_time`, СВОЙ таймер ему не заводится: `destroy()` обнуляет буст,
  и отдельный таймер оставил бы значок висеть над уже отброшенной
  машиной. Магнит приоритетнее буста.
- Высота: 2.35 м, а у игрока 3.25 м — над его крышей уже висит зелёная
  стрелка-маркер (Main, 2.4 + конус 0.6). Ширина значка 1.7 м (1.3 в
  кадре читалось мелко).
- Оживление: «выпрыгивание» (scale 1.6 → 1 за 0.16 с), покачивание по
  высоте, затухание alpha в последние 0.3 с.

**ГРАБЛИ стенда (повтор известных, снова наступил):** в
`screenshot_hud.gd` написал `var right := _main._car.global_transform...`
— `_main` объявлен как Node3D, тип `_main._car` НЕ выводится, скрипт не
парсится, и стенд ВИСИТ с пустым окном без единого кадра и без сообщений
в stdout (ошибка ушла в stderr). Пишите тип явно: `var player: Car =
_main._car`. И ещё: первый кадр значков попал на оранжевые тени
авиаудара (фаза 330) — снимок сдвинут на 430/465.

**Файлы:** `scripts/Car.gd`, `assets/ui/w_boost.png` (перерисован),
`tools/screenshot_hud.gd`, `tools/test_weapons.gd`.
**Снимки:** `tools/shots/hud_status_magnet.png`, `hud_status_boost.png`.
**Тесты:** все 20 сцен + test_curve — PASS. В TestWeapons добавлены
проверки «значок магнита у жертвы» и «значок буста у атакующего».

## Вернули перепад высот — одна горка (2026-08-21)
Запрос: «давай вернём в одном месте высоту в виде горки». Трасса была
сделана плоской 2026-08-20 (`FLAT_TRACK := true`), но вся механика
рельефа сохранялась — полотно, ограждения, обочина и декор и так следуют
`HEIGHT_KEYS`.

**Что сделано:** `FLAT_TRACK := false`, а прежний многоплатовый профиль
RnRR заменён на ОДНУ горку:
```
[0.000, 0.0] [0.140, 0.0] [0.178, 4.0] [0.190, 4.0] [0.228, 0.0] [1.000, 0.0]
```
Высота гребня 4 м, подъём и спуск по ~28 м, между ними ~9 м ровного
гребня. Круг из-за рельефа 726 → 727 м.

**Почему именно доля 0.14…0.23.** Горка симметрична относительно
середины прямой 0.156…0.211 (40 м). Места выбирались не на глаз:
- трамплины трасса ставит САМА на серединах двух самых длинных прямых
  (`_ramp_ratios()` → 0.564 и 0.722) — прыжок с трамплина, стоящего на
  склоне, непредсказуем, пересекаться нельзя;
- стартовая прямая 0.000…0.064 занята решёткой и створом;
- гребень должен быть на ПРЯМОЙ (крен полотна не делается — `_sample_frames`
  держит «вправо» горизонтальным, так что на горке есть тангаж, но нет
  поперечного наклона; на дуге это ощущалось бы как проседание).
Подъём начинается ещё на выходе из первого правого, спуск заканчивается
на входе в шикану — это осознанно, так интереснее.

**Тест TestBumps переписан на самопроверку.** В нём лежал массив
`FLAT_ZONES` — «ровные» доли круга, СКОПИРОВАННЫЕ из старых HEIGHT_KEYS.
При любой правке профиля он молча разъезжается с реальностью: зоны
съезжают на склоны, и тест начинает считать законный подъём «подбросом»
(или наоборот — перестаёт смотреть туда, где ровно). Заменён на
`_is_level(t)`: ровно там, где `TrackBuilder._profile_height` не меняется
в окрестности ±0.02 круга. Теперь правка профиля тест не ломает.

**Замеры до/после (тот же seed):**
| | плоская | с горкой |
|---|---|---|
| TestBumps: подбросы / пинки | 0 / 0 | 0 / 0 |
| TestBumps: касания кузовом дороги за 90 с | 29 | **23** |
| TestAICorners: вылеты ИИ за 60 с | 0 | **1** (эпизод 1.0 с) |
| TestRamp: скорость на замере | 25.7 м/с | 18.9 м/с |

- Касаний кузовом стало МЕНЬШЕ, не больше — горка ничего не разбила.
- Один вылет ИИ появился (порог теста — до 3 вылетов и эпизод < 2.5 с,
  автовозврат его подобрал за 1.0 с). Случился он на доле 0.46, ДАЛЕКО
  от горки: это эффект бабочки — рельеф сдвинул тайминги всей пачки, и к
  16-й секунде машины оказались в другой толкучке. Саму горку ИИ проходит
  без замечаний.
- TestRamp мерит скорость на доле 0.12, а это теперь подножие подъёма —
  машина честно тормозит в горку. ПОБОЧНО ВЫЯСНИЛОСЬ: этот тест давно
  устарел — он считает, что трамплин на доле 0.12, а трамплины стоят на
  0.564 и 0.722. Фактически он проверяет «машина едет и не застревает».
  Не переделывал (вне задачи), но знать об этом надо.

**Снимки:** `tools/shots/hill_side.png` (силуэт сбоку),
`hill_drive.png` (вид гонщика — дорога уходит вверх, за гребнем ничего
не видно). Грабли стенда: камеру «за 26 м до подножия» сначала считал
экстраполяцией по касательной — подъём начинается на выходе из дуги, и
камера уехала ЗА ограждение, в кадре был один красный борт. Правильно
брать точку ПО КРИВОЙ: `sample_baked(length * 0.140 - 26.0)`.

**Файлы:** `scripts/TrackBuilder.gd`, `tools/test_bumps.gd`,
`tools/screenshot_track.gd`.
**Тесты:** все 20 сцен + test_curve — PASS.

## Сетевая игра на двоих через VDS + одиночные боксы (2026-08-21)
Запрос: «при запуске двух экземпляров игры играть по сети через мой VDS,
реального игрока показывать маркером, плюс два бота; боксы лежат по
одному, не исчезают, каждый проехавший забирает свой случайный бонус».

### Боксы с оружием
Было: тройки поперёк трассы, бокс исчезал на 5 с после подбора — кто
успел, тот и съел. Стало: ПО ОДНОМУ на отметку посреди полотна, не
исчезает никогда, каждый проехавший получает СВОЙ случайный бонус.
От повторной выдачи защищает личный для машины откат 2.5 с
(`WeaponBox._next_pickup`, ключ — instance_id): `body_entered` бьёт
только на входе, но машина, стоящая на боксе, дёргается на подвеске и
входит-выходит каждые пару кадров — без отката она фармила бы оружие.

### Сеть: выделенный сервер
Пользователь просил «как лучше и быстрее» — выбран ВЫДЕЛЕННЫЙ СЕРВЕР на
VDS (а не «хостит один из игроков»): у хоста был бы нулевой пинг, а у
второго весь — условия неравные. Транспорт ENet/UDP, порт 9977.

- `scripts/Net.gd` (новый autoload): режим OFFLINE/SERVER/CLIENT, подъём
  сервера, подключение, раздача слотов, адрес в `user://net.cfg`.
- Сервер — ТОТ ЖЕ `Main.tscn`, запущенный ключом `-- --server`: без
  камеры, HUD и своей машины. Отдельная сцена не нужна, и это важно:
  адресация RPC в Godot идёт ПО ПУТИ УЗЛА, у сервера и клиента гонка
  обязана лежать в одном и том же `/root/Main`.
- Машин по сети всегда 4: слоты 0 и 1 — живые игроки, 2 и 3 — боты.
  НЕзанятый слот ведёт бот; игрок подключился — машина переходит под его
  ввод, вышел — снова бот. Гонка не встаёт из-за отсутствия второго.
- Ростер моделей: клиент присылает свой выбор в `_rx_hello`, сервер
  раздаёт всем — иначе игрок видел бы у соперника не ту машину.

### Что где считается
Сервер считает ВСЮ физику и 20 раз в секунду шлёт снимок: на машину
10 float (позиция, кватернион, скорость) + 3 байта (оружие, живость с
«призраком», значок эффекта). Кватернион, а не базис: 4 числа вместо 9 и
корректная интерполяция поворота.
- Чужие машины на клиенте — МАРИОНЕТКИ: физика выключена (`freeze`
  kinematic), позиция тянется к снимку, причём цель ЭКСТРАПОЛИРУЕТСЯ по
  скорости из снимка — 20 снимков против 60 кадров иначе дают рывки.
- СВОЯ машина на клиенте считается ЛОКАЛЬНО (`NetRole.OWNED`), иначе руль
  отвечал бы через пинг. Снимок её мягко подтягивает (lerp 0.12), а при
  невязке > 4 м переставляет.
- Ввод: оси — ненадёжным пакетом каждый кадр (потерять кадр газа не
  страшно, ждать переотправки — страшно), прыжок и выстрел — надёжным
  (потерянный выстрел не восстановить).

### Оружие по сети
Стреляет сервер. Клиент выстрел только ВИДИТ: `net_broadcast_weapon` →
`_rx_weapon_fx` → клиент поднимает копию с флагом `inert` (добавлен в
Mine, OilSlick, Projectile, Airstrike) — она летит и рисуется, но никого
не бьёт. Иначе машину било бы дважды и по-разному на каждом экране.
Боксы на клиенте тоже инертны: оружие выдаёт сервер и присылает в снимке.
`Car._player_control` больше не зовёт `use_weapon()`, если роль не LOCAL —
локальный выстрел породил бы вторую ракету и списал бокс дважды.

### Маркеры
Своя машина — зелёная стрелка, живой соперник — ОРАНЖЕВАЯ, боты без
стрелки: так «реальный игрок» виден с первого взгляда.
ПОБОЧНЫЙ ФИКС: высота значка эффектов над крышей считалась по
`is_player`, а по сети маркеры у обоих живых игроков — теперь по факту
маркера (`Car.has_marker`).

### НАСТОЯЩИЙ БАГ, пойманный на стенде
`Main._on_peer_left` брал слот из `Net.slot_of_peer` — но Net
подписывается на `peer_disconnected` РАНЬШЕ (в `start_server`), к моменту
вызова Main слот уже стёрт, обработчик молча выходил, и машина ушедшего
игрока оставалась на его последнем вводе — газ в пол навсегда. Лечение:
Net сам эмитит `player_joined(id, slot)` / `player_left(id, slot)`, слот
едет В СИГНАЛЕ, порядок подписки больше ни на что не влияет.

### Стенд `tools/TestNet.tscn` (сквозной, с настоящим сервером)
Корень сцены зовётся **Main** и несёт тот же `Main.gd` (см. выше про пути
RPC), проверяльщик — ДОЧЕРНИЙ узел: дочерний `_ready` идёт раньше
родительского, там и подключаемся, чтобы `Main._ready` увидел уже
выставленный режим клиента.
Проверяет: выдачу слота, ростер, что своя машина поехала (значит сервер
принял ввод и вернул позицию), что марионетки едут (снимки идут), что
маркер соперника есть и что своя машина не марионетка.
Результаты: одиночный клиент PASS (проехал 180 м); ДВА клиента разом —
оба PASS, слоты 0 и 1, каждый видит другого; после ухода обоих сервер сам
перезапустил трассу.

### Грабли UI (поймано скриншот-стендом)
Панель подключения сперва поставил на анкер 0.75 по горизонтали — и она
уехала ПОД сетку машин (та начинается с ~685 px): в кадре её не было
вовсе. Теперь адрес и «ПО СЕТИ» стоят в левой половине, рядом со «СТАРТ»,
на фиксированных offset'ах от левого края. Проверять такое надо глазами:
`godot --path . res://tools/ScreenshotSelect.tscn -- tools/shots`.

### Развёртывание
`server/README.md` (установка Godot 4.3 headless, rsync проекта,
`ufw allow 9977/udp`, ручная проверка, systemd) и
`server/bighead-server.service`. Самая частая причина «сервер не ответил»
— открыт TCP вместо UDP.

### Что осталось приблизительным (честно)
- «Призрак» после уничтожения у марионеток не мигает: таймер приходит
  прямым присвоением, а `_tick_effects` у них не крутится.
- Клиент, подключившийся В СЕРЕДИНЕ заезда, считает свой прогресс с нуля
  (позиции-то приходят, а накопленный путь — нет), поэтому круг и место в
  его HUD будут врать до конца заезда.
- Взрывы и попадания на клиенте видны по РЕЗУЛЬТАТУ (машину отбросило,
  она стала призраком), а не покадрово: копии оружия инертны.

**Файлы:** новые `scripts/Net.gd`, `server/README.md`,
`server/bighead-server.service`, `tools/test_net.gd`, `tools/TestNet.tscn`;
правлены `scripts/Main.gd`, `Car.gd`, `WeaponBox.gd`, `Mine.gd`,
`OilSlick.gd`, `Projectile.gd`, `Airstrike.gd`, `CarSelect.gd`,
`project.godot`, `README.md`.
**Тесты:** все 20 сцен + test_curve — PASS (оффлайн не задет), плюс
сетевой стенд на одном и на двух клиентах.

## Лобби 5 секунд + адрес VDS по умолчанию (2026-08-21)
Запрос: «вначале 5 сек для подключения игрока по сети, если его нет — по
старинке с ботами». Плюс пользователь дал адрес VDS: 139.100.234.166.

- Сервер (`Main`): первый подключившийся игрок запускает таймер
  `LOBBY_WAIT = 5.0` (`_tick_lobby` в `_server_tick`). Пришёл второй —
  старт сразу; не пришёл — старт с ботом в пустом слоте («по старинке»).
  Пробел по-прежнему стартует немедленно. Клиенту в лобби идёт обратный
  отсчёт: `_rx_lobby(players, secs)` шлётся при каждой смене целой
  секунды, подпись «Ждём второго: N…».
- `Net.host` по умолчанию = 139.100.234.166 — поле на экране выбора сразу
  готово к «ПО СЕТИ»; своё значение по-прежнему запоминается в net.cfg.
- Заодно: таймаут подключения на клиенте (CONNECT_TIMEOUT = 5 с) — ENet
  молчит очень долго, и без него экран «Подключение…» висел бы, пока
  игрок сам не догадается выйти. Не ответил — рвём и пишем «Сервер не
  ответил за 5 с».
- TestNet ПЕРЕВЁРНУТ: раньше он ПРОСИЛ сервер стартовать
  (_rx_start_request), теперь наоборот проверяет, что заезд начался САМ
  (новая проверка «старт без второго игрока», начало на ~6.8 с ≈ 5 с
  лобби + отсчёт 3-2-1). Прогоны: одиночный клиент PASS, два клиента
  разом — оба PASS (при двоих старт мгновенный).

**Файлы:** `scripts/Main.gd`, `scripts/Net.gd`, `scripts/CarSelect.gd`,
`tools/test_net.gd`, `README.md`, `server/README.md`.

## Сетевая игра на двоих + сервер на VDS (2026-08-21)
Запрос: «при запуске двух экземпляров игры можно было играть по сетке, в игре
показывается реальный игрок с маркером и два бота, подключение через мой VDS;
боксы лежат по одному, не исчезают, каждый проехавший берёт свой бонус».
Затем: «вначале 5 сек на подключение, если игрока нет — по старинке с ботами»
и «разворачивай сервер сам».

### Боксы с оружием
Было: тройка боксов поперёк трассы на каждой из 5 отметок, подобравший
забирал бокс, остальным 5 секунд пусто. Стало: **один** бокс по центру
полотна, **не исчезает**, и каждая проехавшая машина получает СВОЙ случайный
бонус. От повторной выдачи — личный откат на машину (`PER_CAR_COOLDOWN` 2.5 с)
в словаре `_next_pickup` по `get_instance_id()`. Откат нужен именно личный, а
не общий: `body_entered` бьёт только на входе, но машина, стоящая на боксе,
дёргается на подвеске и входит-выходит каждые пару кадров — без отката она
фармила бы оружие.

### Модель сети — ВЫДЕЛЕННЫЙ СЕРВЕР
`scripts/Net.gd` (autoload `Net`) + сетевая часть в `Main.gd`. Сервер — тот же
`Main.tscn`, запущенный с ключом: `godot --headless --path . res://scenes/Main.tscn -- --server`.
Транспорт ENet/UDP, порт 9977. Никто из игроков не хостит — пинг у обоих
одинаковый.
- Машин всегда 4: слоты **0 и 1** держатся за живыми игроками, **2 и 3** боты.
  Слот без игрока ведёт бот (`net_role = LOCAL`), игрок подключился — сервер
  переводит машину на присланный ввод (`SERVER_INPUT`), вышел — снова бот.
  Одна и та же машина, никаких спавнов на ходу.
- `Car.NetRole`: LOCAL / SERVER_INPUT / **PUPPET** (клиент физику не считает,
  тянется к снимкам) / **OWNED** (своя машина на клиенте).
- **OWNED — главное решение по отзывчивости.** Своя машина на клиенте
  считается ЛОКАЛЬНО (иначе руль отвечал бы через пинг), а снимок сервера
  подтягивает её мягко (`lerp` 0.12); при расхождении больше 4 м —
  переставляет. Стрелять локально при этом ЗАПРЕЩЕНО (`net_role != LOCAL` →
  выход из `_player_control`): оружие тратит сервер, локальный выстрел породил
  бы вторую ракету и списал бокс дважды.
- Снимок: на машину 10 float (позиция, КВАТЕРНИОН, скорость) + 3 байта
  (оружие+1, живость с призраком, значок эффекта+2), 20 раз в секунду,
  `unreliable_ordered`. Кватернион вместо базиса — 4 числа вместо 9 и
  корректная интерполяция поворота.
- Ввод: оси — `unreliable_ordered` каждый кадр (потерять кадр газа не страшно,
  ждать переотправки — страшно), прыжок/выстрел — `reliable` (потерянный
  выстрел не восстановить).
- Марионетки экстраполируются по скорости из снимка: снимков 20/с, кадров 60 —
  без экстраполяции машины едут рывками.
- **Оружие у клиентов только КАРТИНКА.** Сервер шлёт событие выстрела,
  клиент порождает копию с `inert = true` (Mine/OilSlick/Projectile/Airstrike):
  monitoring выключен, никого не бьёт. Иначе машину било бы дважды и по-разному
  на каждом экране. Боксы на клиенте тоже инертны — оружие приходит в снимке.

### Маркеры
`_build_player_marker(color)`: **своя** машина — зелёная стрелка, **живой
соперник** — оранжевая, боты без стрелки. Поэтому же в `Car` появился
`has_marker`: высоту значка эффекта над крышей теперь выбирает он, а не
`is_player` — по сети маркеры есть у обоих живых игроков, а `is_player` только
у своей машины.

### Лобби: 5 секунд и по старинке
Первый подключившийся запускает отсчёт `LOBBY_WAIT = 5.0` (`_tick_lobby`).
Второй успел — старт сразу, не успел — стартуем с ботом в пустом слоте.
Клиенту каждую секунду уходит `_rx_lobby(игроков, секунд)`. Плюс на клиенте
`CONNECT_TIMEOUT = 5 c`: ENet сам может молчать очень долго, поэтому ждём
ответа сервера вручную и честно пишем «не ответил», а не висим на
«Подключение…».

### Развёрнуто на VDS 139.100.234.166 (Selectel, Ubuntu 24.04, 1 CPU/960 МБ)
Godot 4.3 headless в `/usr/local/bin/godot`, проект в `/opt/bighead`,
служба `bighead-server.service` под пользователем `bighead`, автозапуск.
Расход ~113 МБ RAM и ~22% ядра. Детали и команды — `server/README.md`.
Ключ доступа — `vps_root_key` из проекта `Desktop\shift-journal` (адрес уже
был в `known_hosts`, а `vps_key` от мессенджера не годится: он для юзера
`tunnel`, у которого «This account is currently not available»).

### ГРАБЛИ (все настоящие, поймано на живом сервере)
1. **RPC адресуются ПО ПУТИ УЗЛА.** На сервере гонка в `/root/Main`, значит и у
   клиента обязана быть `/root/Main`. Поэтому тестовый стенд `TestNet.tscn`
   имеет корень с именем **Main** и тем же `Main.gd`, а проверяльщик висит
   ДОЧЕРНИМ узлом (его `_ready` вызывается раньше родительского — там и
   подключаемся, чтобы `Main._ready` увидел уже выставленный режим клиента).
   Загрузить Main как ребёнка своей сцены нельзя — путь разъедется.
2. **Порядок обработчиков `peer_disconnected`.** `Net` подписывается первым и
   к моменту вызова `Main` уже СТИРАЕТ слот из `slot_of_peer` — `Main` не
   находил машину и оставлял её на замершем вводе ушедшего игрока (газ в пол
   навсегда). Лечится своими сигналами `Net.player_joined/player_left(id, slot)`,
   слот едет прямо в сигнале.
3. **Подсевший к идущему заезду навсегда оставался в лобби**: отсчёт он
   пропустил, `controls_enabled` никто не включал. Добавлен `_rx_race_running`.
4. **journald глотал ВСЁ.** Headless-рендер сыпал в stderr тысячи
   `Parameter "m" is null` (dummy-рендер на каждый меш), journald включал rate
   limit и выбрасывал в том числе `[net]`-строки — лог сервера был бесполезен,
   и первый неудачный прогон на VDS я диагностировал вслепую. Лечение:
   **вся косметика на сервере отключена** — декор трассы и створ (у них ноль
   коллизий), свет/небо, модели машин, кубы боксов, дым, скорлупа льда, значок
   эффекта, а меши полотна/земли/стен/трамплинов не цепляются к телам
   (`TrackBuilder._add_visual`, коллизия — отдельный узел). Осталось 4 строки
   на перезапуск трассы: предупреждение возникает уже на `mesh.mesh = ...`,
   до всякой проверки. Дальше чистить — переписывать геометрию, от которой
   зависят все физические тесты; не стал. Побочно: память 148 → 113 МБ.
5. Минимальная Ubuntu: нет `unzip`, и Godot падает без `libfontconfig.so.1`
   даже с `--headless`. Полный список библиотек — в `server/README.md`.
6. В systemd-юните обязателен `Environment=HOME=/home/bighead`: без HOME Godot
   не может писать в `~/.local/share/godot` и падает при старте.
7. **Отключая косметику, я сам сломал `test_curve`.** `TrackBuilder` стал
   обращаться к autoload `Net`, а `tools/test_curve.gd` запускается через
   `--script` — в этом режиме autoload'ы НЕ ЗАГРУЖАЮТСЯ. Компиляция скрипта
   падала, стенд молча ВИС (ошибка только в stderr). Лечение: в TrackBuilder
   своя статическая проверка `_headless_server()` по командной строке, без
   всякого autoload. Правило на будущее: **код, который трогают
   `--script`-стенды, не должен зависеть от autoload'ов.**

### Тест
`tools/TestNet.tscn` — сквозной: подключается к работающему серверу, НЕ просит
старт (проверяет как раз 5-секундное ожидание), даёт газ и сверяет, что слот и
ростер выданы, заезд начался сам, своя машина проехала, снимки ботов идут,
маркер соперника есть, своя машина не марионетка. Адрес — аргументом после
`--` (по умолчанию 127.0.0.1).

Прогнано: локально (1 клиент и 2 одновременно) и **на живом VDS** (1 и 2
клиента). Все PASS. В логе сервера видно, как оба заняли слоты 0 и 1 и заезд
стартовал сразу, а после выхода обоих трасса перезапустилась.
Оффлайн-регрессия: все 20 сцен + test_curve — PASS.

### Осталось на потом
- Экрана «результаты сетевого заезда» нет: финиш показывает своё место, дальше
  Enter → гараж.
- Круги клиент считает сам по присланным позициям (совпадает, потому что
  позиции те же), но подсевший посреди заезда начнёт счёт с нуля.
- «Призрак» после уничтожения у марионеток не мигает: `_ghost_time` ставится
  присвоением из снимка, а собственный таймер у них не крутится.

## Сетевые фиксы: пропавший пол и дёрганое движение (2026-08-24)
Жалоба: «при игре по сети нет пола (нижней поверхности), движение машины
очень прерывистое, дёрганное».

### Пол
Причина — МОЯ ЖЕ ОШИБКА при отключении косметики на сервере (21.08):
автозамена `body.add_child(mesh)` → `_add_visual(body, mesh)` зацепила
строку ВНУТРИ САМОГО помощника `_add_visual` — он стал вызывать сам себя
вместо `add_child`. Рекурсия до лимита стека глоталась молча, и меши
полотна/земли/стен/трамплинов не добавлялись ВООБЩЕ НИ У КОГО (клиент,
одиночная игра — у всех чёрная пустота под колёсами; физика работала —
коллизии отдельными узлами). Урок: после массовой автозамены перечитать
diff самого хелпера. Диагноз поставлен стендом `tools/ShotNet.tscn`
(сетевой клиент с окном, снимки + печать числа детей у Ground/Road:
было по 1, стало по 2).

### Дёрганое движение — три настоящих причины
1. **Подтяжка СВОЕЙ машины.** `net_correct` делал `lerp(0.12)` от
   расхождения с сервером КАЖДЫЙ снимок — рывки до половины кадрового
   шага. Теперь: мёртвая зона 0.5 м (меньше — не трогаем вовсе; локальная
   и серверная физика при одном коде на разных кадрах расходятся
   постоянно), шаг подтяжки не больше 8 см за снимок (NET_FIX_STEP,
   против ~40 см обычного шага — глазу не видно), скорость в мягком
   режиме не трогаем совсем. Замер: макс. сдвиг за снимок ровно 0.080 м.
   ГРАБЛИ по пути: попытка размазать подтяжку по кадрам двиганьем
   global_position КАЖДЫЙ кадр физики уронила скорость машины втрое —
   телепорт динамического тела каждый кадр ломает решатель. Не делать.
2. **Экстраполяция марионеток при неровном приходе пакетов.** Возраст
   снимка не ограничивался: запоздавший пакет уводил цель далеко вперёд,
   следующий возвращал назад. На локалхосте невидимо, на двух клиентах —
   4 рывка назад за 419 кадров. Теперь `MAX_EXTRAP` + запрет шага НАЗАД
   против присланной скорости (лучше замереть на кадр, чем откатиться).
   Итог: 0 рывков назад во всех прогонах, включая живой VDS.
3. **Частота снимков** 30 → 60 Гц (= частоте физики). Замер: 5.0% → 1.2%
   расхождения на локалхосте. Трафик ~10 КБ/с на клиента — ничто.

### MAX_EXTRAP подобран замерами НА ЖИВОМ VDS (двумя клиентами)
0.05 с → 42% | **0.12 с → 25%** | 0.22 с → 39%. На локалхосте 2-5% при
любом значении — по нему подобрать НЕЛЬЗЯ, пакеты идут ровно.

### Что НЕ помогло (чтобы не переделывать в третий раз)
- «Едем от текущего места к последнему снимку за фактический интервал» —
  ~50%: машина доезжает за кадр-другой и ЖДЁТ следующего пакета.
- Учебный буфер снимков с отставанием (interpolation delay 0.12-0.15 с,
  рисуем между двумя полученными снимками) — 60% на интернете, 21% на
  локалхосте. Хуже простой экстраполяции ВЕЗДЕ. Вернул экстраполяцию.

### Метрика плавности (tools/test_net.gd) — сама дважды врала
1. Мерил в `_process` — headless крутит его в разы чаще физики, почти все
   шаги нулевые. Мерить надо в `_physics_process`, где марионетка и
   двигается.
2. Брал скорость из `car.linear_velocity` — у ЗАМОРОЖЕННОГО
   кинематического тела Godot пересчитывает её сам по сдвигу трансформа
   и выдаёт вдвое больше реальной (62 м/с при максимуме 34). Полчаса
   чинил интерполяцию, которая была не виновата. Брать `_snap_vel`.
Итоговая метрика: средняя относительная разница «путь за кадр» против
«присланная скорость × dt». Эталоны: одиночная игра 0.3%, локалхост
2-5%, живой VDS 21-31%. Порог теста 35% — учитывает реальный канал и
ловит все неудачные варианты (50-60%).

### Ещё два сетевых фикса по пути
- **hello с повтором.** Клиент, подключившийся, пока сервер перезапускал
  сцену после чужого финиша (8 с), слал `_rx_hello` в несуществующий
  `/root/Main` — RPC молча пропадал, клиент навсегда оставался без слота
  (все машины марионетки). Теперь `_say_hello` повторяет запрос раз в
  секунду до 12 раз, пока не выдан слот. Поймано вживую стендом ShotNet.
- **Оранжевая метка — только над ЖИВЫМ игроком.** Раньше вешалась на
  вторую машину безусловно, вися над ботом. Теперь `_rx_welcome` везёт
  маску занятых слотов, `_rx_slot_taken` показывает/прячет метку при
  подключении/выходе. ГРАБЛИ: рассылку `_rx_slot_taken` сначала поставил
  в КОНЕЦ `_on_peer_joined` — когда второй игрок запускал заезд своим
  подключением, функция выходила раньше отправки (у первого метка не
  загоралась). Поймано двухклиентским прогоном: проверка «метка скрыта»
  обязана падать при двух клиентах — и не падала у первого.

**Файлы:** `scripts/Car.gd`, `scripts/Main.gd`, `scripts/TrackBuilder.gd`,
`tools/test_net.gd`, `tools/shot_net.gd` + `tools/ShotNet.tscn` (новый
стенд: сетевой клиент с окном и снимками экрана).
**Тесты:** все 20 оффлайн-сцен PASS; TestNet PASS на локалхосте (1 и 2
клиента) и на живом VDS (1 и 2 клиента). Код на VDS обновлён.

## Рывки по сети добиты: своя машина стала КЛИЕНТ-АВТОРИТЕТНОЙ (2026-08-24)
Жалоба (повторная, после фиксов подтяжки): «машина по-прежнему двигается
рывками, играть невозможно». Причина АРХИТЕКТУРНАЯ, и точечными фиксами
(мёртвая зона, шаг 8 см, 60 Гц) она не лечилась: сервер считал свою копию
машины игрока по вводу, пришедшему с опозданием на пинг, — его состояние
ВСЕГДА отстаёт и расходится с локальным на каждом повороте. Подтяжка к
нему тянула машину назад каждый снимок, а невязка больше 5 м (обычное
дело на повороте при реальном пинге) давала жёсткий телепорт.

### Что сделано
Своя машина клиента теперь полностью клиент-авторитетна:
- Клиент считает её физику сам и 60 раз/с шлёт серверу состояние
  (`_rx_pstate`: позиция, кватернион, скорость — те же 10 float).
  Подтяжка/телепорты своей машины УДАЛЕНЫ ВООВСЕ (`net_correct`,
  NET_DEADZONE/NET_SNAP/NET_FIX_STEP — выпилены). Сеть свою машину не
  трогает совсем — рывкам взяться неоткуда.
- Сервер держит машину живого игрока МАРИОНЕТКОЙ (тот же
  `_follow_snapshot`, что у марионеток клиента) и ретранслирует её
  второму игроку. Роль SERVER_INPUT и весь ввод осей (`_rx_input`,
  net_th/st/hb/jump) удалены; `_rx_press` шлёт только выстрел.
- ГРАБЛИ: в снимке для марионетки сервер обязан слать скорость ИЗ
  ПРИСЛАННОГО СНИМКА (`_snap_vel`), а не `linear_velocity` — у
  замороженного тела Godot пересчитывает её по сдвигу трансформа и
  завышает вдвое (те же грабли, что в метрике test_net 24.08).
- Эффекты оружия по машине игрока сервер ПЕРЕСЫЛАЕТ владельцу
  (`Car._forward_fx` → `Main.net_forward_fx` → RPC `_rx_fx`):
  DESTROY / BLAST(толчок мины и магнита) / FREEZE / OIL / BOOST — физику
  (толчок, закрутку, телепорт «призрака», тягу буста) применяет клиент,
  иначе игрок удара бы не почувствовал. BOOST ради этого вынесен из
  use_weapon в `apply_boost()`. Сервер параллельно ведёт свои таймеры
  (призрак/заморозка) для снимков — марионетки теперь тикают
  `_tick_effects` (побочно починилось «призрак у марионеток не мигает»).
- Выстрел игрока по-прежнему применяет ТОЛЬКО сервер (net_fire у
  марионетки обрабатывается в ветке PUPPET `_physics_process`).
- Возврат на трассу (R и автовозврат) для своей машины делает КЛИЕНТ:
  `_check_recovery` пропускает марионеток на сервере, а на клиенте правит
  только свою машину.
- В `_rx_state` для OWNED не трогаем ни позицию, ни alive/призрака —
  от сервера своей машине нужны только оружие из бокса и значок эффекта.

### Стенды и тесты
- test_net/shot_net газуют через `Input.action_press("accelerate")`
  (раньше слали `_rx_input`, его больше нет); метрика «подтяжки» удалена.
- TestNet: одиночный PASS (марионетка 4.3%, рывков назад 0), два клиента
  разом — оба прошли (FAIL «метка скрыта» у первого закономерен: второй
  игрок подключился и метка загорелась). Все 20 оффлайн-сцен +
  test_curve — PASS.

### Осталось честно приблизительным
- Столкновение игрок-игрок каждый видит по-своему (каждый толкает
  марионетку соперника у себя); заморозка «заразой» от машины игрока
  к боту по сети не передаётся (у марионетки не крутится
  _bounce_off_cars). На игру на двоих не влияет.

**Файлы:** `scripts/Car.gd`, `scripts/Main.gd`, `tools/test_net.gd`,
`tools/shot_net.gd`, `README.md`, `server/README.md`.

### Сервер на VDS обновлён (та же сессия)
Протокол RPC изменился (`_rx_input` удалён, `_rx_press` без аргументов,
новые `_rx_pstate`/`_rx_fx`) — старый сервер с новым клиентом
несовместим, поэтому код на VDS залит сразу (scripts/scenes/project.godot
→ /opt/bighead, restart bighead-server). Проверка НА ЖИВОМ VDS: TestNet
одним клиентом — PASS (марионетка 15.9% против прежних 21-31%, рывков
назад 0, своя машина проехала 206 м); двумя клиентами разом — у обоих
все проверки ok, кроме закономерной «метка скрыта» (второй игрок
подключился — метка обязана загореться, тест одиночный по устройству).

## Дёрганый соперник, дикий импульс при ударе, «пропала трасса» (2026-08-24)
Жалобы после первой игры вдвоём: «противник движется дёргано; врезается в
момент рывка — я получаю огромный импульс ускорения; у игрока по сети
пропадала трасса». Три причины, все настоящие:

1. **Двойное сглаживание при ретрансляции.** Сервер слал второму клиенту
   позицию СВОЕЙ марионетки (которая уже сглаживает и экстраполирует
   присланный владельцем поток), а клиент сглаживал поверх — рывки.
   Теперь `_pack_state` для машин живых игроков ретранслирует СЫРОЕ
   последнее состояние владельца (`_snap_pos/_snap_rot/_snap_vel`).
   Замер на живом VDS двумя клиентами: соперник-игрок 12.8-15.3%,
   рывков назад 0 (норма ботов 21-31%). В test_net добавлен отдельный
   замер машины соперника-игрока (осмыслен только при двух клиентах).
2. **Огромный импульс — два источника, оба закрыты.**
   а) `_bounce_off_cars` брал `other.linear_velocity` — у замороженной
   марионетки Godot пересчитывает её по сдвигу трансформа и на рывке
   канала выдаёт десятки м/с; рикошет 0.4×сближение швырял машину.
   Теперь скорость соперника — `contact_velocity()` (у марионетки — из
   снимка), а сближение ограничено 20 м/с (толчок ≤ 8 м/с).
   б) Депенетрация решателя: марионетка ТЕЛЕПОРТИРУЕТСЯ к снимкам и,
   шагнув в наш кузов, решатель выдавливает нас диким разовым импульсом.
   Кап: при касании марионетки всё, что физика добавила к скорости сверх
   конца нашего прошлого кадра (`_post_vel`), ограничено 5 м/с. Честный
   рикошет не задет (наш импульс идёт позже в том же кадре), толчок
   взрыва защищён окном `_blast_time` (ставит push_from_blast), а после
   телепорта память чистит reset_speed_memory — иначе кап «возвращал» бы
   только что обнулённую скорость.
3. **«Пропала трасса»** — камера уезжала вслед за машиной, отброшенной
   этим импульсом в пустоту (а при NaN в трансформе — навсегда: lerp с
   NaN отравляет камеру). Страховки: `_check_recovery` мгновенно (без
   таймера) возвращает машину при нечисловой позиции или |pos| > 600 м
   (NaN сперва обнуляется — respawn_transform от NaN вернул бы NaN);
   `_client_tick` не шлёт битое состояние; `_rx_pstate` выбрасывает
   битые пакеты (иначе NaN отравил бы снимки ВСЕМ); IsoCamera
   пропускает кадр при нечисловой цели.

ГРАБЛИ деплоя: после появления НОВОГО class_name (Minimap из
параллельной сессии) сервер на VDS упал в Parse Error «Could not find
type Minimap» — кэш глобальных классов строит только `--import`, одного
копирования файлов мало. Лечение: `systemctl stop` → `sudo -u bighead
HOME=/home/bighead godot --headless --path /opt/bighead --import` →
`start`. Правило: **новый class_name → --import и на VDS тоже.**

Прогнано: 20 оффлайн + curve + TestMinimap PASS; TestNet локально (1 и
2 клиента) и на живом VDS (2 клиента) — PASS (FAIL «метка скрыта» при
двух клиентах закономерен). Код на VDS обновлён.

**Файлы:** `scripts/Car.gd`, `scripts/Main.gd`, `scripts/IsoCamera.gd`,
`tools/test_net.gd`. (В коммит попал и WIP мини-карты параллельной
сессии: `scripts/Minimap.gd`, правки `Main.gd`/`TrackBuilder.gd`,
`tools/TestMinimap.tscn` — Main.gd общий, разделить нельзя; её секцию
в журнал допишет она.)

## Мини-карта: видно, где кто едет (2026-08-24)
Просьба: «сделай мини карту, чтобы было видно где кто находится».

### Что сделано
- `scripts/Minimap.gd` (новый) — Control, который сам рисует карту:
  полотно трассы одним вызовом `canvas_item_add_triangle_array` (квады
  между сэмплами оси), красные линии ограждений, белая чёрточка старта
  и точки машин. Своя машина — зелёный ТРЕУГОЛЬНИК носом по ходу,
  живой соперник — оранжевая точка, боты — серо-голубые (те же цвета,
  что у стрелок над машинами, чтобы не заводить второй язык меток).
  Выбывшая машина гаснет до alpha 0.35.
- `TrackBuilder.plan_samples()` — публичная выдача оси трассы в плане
  (X,Z) и полуширины полотна; раньше `_pts`/`_widths` были внутренними.
- `Main._setup_hud()` — панель 228×158 в правом верхнем углу, привязана
  к правому краю (anchor 1.0), чтобы не уезжать при другом разрешении.
  По сети `_rx_welcome`/`_rx_slot_taken` красят точки: свой слот
  зелёный, второй ЖИВОЙ игрок оранжевый (пока слот ведёт бот — серый).

### Две грабли
- **Сжатие карты = SIN наклона камеры, а не косинус.** Взял сгоряча
  cos(32°)=0.85 — направления на карте разошлись с экранными на 12°.
  Горизонтальный сдвиг на экране изокамеры поднимается ровно на
  sin(наклона): 0.53. С ним расхождение 0.0°. Ловится тестом.
- **Свою машину рисовать ПОСЛЕДНЕЙ.** На старте все стоят кучей, и точка
  бота накрывала зелёную стрелку — себя на карте было не найти.
- Перпендикуляр полотна берётся в МИРОВОМ плане до поворота: сжатие по
  вертикали неравномерно тянет углы, и «перпендикуляр» повёрнутой оси
  перестал бы быть перпендикуляром дороги.

### Тест
`tools/TestMinimap.tscn` + `tools/test_minimap.gd`: все точки внутри
панели, противоположные стороны круга далеко друг от друга, соседние —
рядом, у каждой машины своя точка, и главное — угол сдвига вдоль трассы
на КАРТЕ сходится с проекцией камеры (порог 3°, факт 0.0°).
PASS; все 20 оффлайн-сцен прогнаны заново — PASS.
Служебно: `tools/crop_shot.gd` — вырезать и увеличить кусок снимка
(смотреть карту на скриншотах HUD).

**Файлы:** `scripts/Minimap.gd` (новый), `scripts/Main.gd`,
`scripts/TrackBuilder.gd`, `tools/test_minimap.gd`,
`tools/TestMinimap.tscn`, `tools/crop_shot.gd`, `README.md`.

## Аудит сетевой части по практикам сетевых игр (2026-08-24)
Запрос: «доработать сетевую часть лучшими наработками сетевых игр,
проверить на известные сетевые баги». Пройден чек-лист типовых проблем.

### Что уже было в порядке (проверено, не трогал)
- Третий игрок отвергается осмысленно (Net._on_peer_connected).
- Слот берётся по sender id, а не из пакета — подделать чужой слот нельзя.
- Hello с ретраями; таймаут подключения 5 с; надёжные RPC для разовых
  событий, ненадёжные упорядоченные — для потоков.
- Битые данные (NaN) отбрасываются на входе (_rx_pstate) и не шлются.
- Экстраполяция марионеток с MAX_EXTRAP и запретом шага назад (замеры
  на живом VDS — см. секции выше; interpolation delay пробован и хуже).

### Найдено и исправлено (5 пунктов)
1. **Гибель сервера посреди заезда никак не обрабатывалась** — клиент
   навсегда оставался с замершими марионетками (классика). Теперь Main
   слушает Net.left: «Связь с сервером потеряна», управление снято,
   через 3 с — в гараж. Проверено убийством живого сервера: клиент
   печатает [net]-строку и уходит в гараж. После финиша не дёргаемся.
2. **Watchdog снимков**: соединение может числиться живым, а сервер
   завис — ENet заметит через десятки секунд. Клиент считает время от
   последнего _rx_state: в гонке > 5 с тишины = связь потеряна (тот же
   путь, что п.1).
3. **Версия протокола** (`Net.PROTOCOL`, сейчас 3) едет в _rx_hello.
   Не совпала — сервер шлёт _rx_kick с текстом «обнови игру: git pull»
   и рвёт соединение (с задержкой 0.5 с, чтобы сообщение дошло); клиент
   глушит ретраи hello слотом −2 и не даёт «связь потеряна» перетереть
   причину (_kicked). СТАРЫЙ клиент (hello с 1 аргументом) до проверки
   не доходит — Godot сам отбрасывает RPC с другой сигнатурой («Method
   expected 2 arguments»), клиент после 12 попыток видит подсказку про
   версии в _say_hello. Проверено git worktree с прошлым коммитом:
   старый клиент против нового сервера получает внятный отказ, а не
   тихий рассинхрон.
4. **Санитария клиент-авторитетных данных на сервере**: кроме NaN,
   отбрасываются позиции за пределами мира (>600 м) и скорости >60 м/с —
   сервер не транслирует мусор второму игроку.
5. **Поздний вход: круги и место больше не врут.** Сервер при подсадке
   к идущему заезду шлёт _rx_progress (прогресс + круги всех машин);
   клиент садит _last_offset по фактическим позициям, чтобы первый кадр
   не прибавил «ноль → текущая точка». Закрыт давний пункт из «осталось
   приблизительным».
Плюс: **потоки состояния переведены на ENet-канал 1** (reliable-события
на 0) — потерянный надёжный пакет больше не задерживает снимки
(head-of-line blocking). ВАЖНО: каналы надо выделить явно —
create_server/create_client(..., 8), иначе отправка с transfer_channel>0
молча не уходит.

### Отвергнуто осознанно
- Interpolation delay вместо экстраполяции — уже мерили, хуже везде.
- Полный server-authoritative с предсказанием и перемоткой (rollback) —
  правильная «большая» архитектура, но для кооператива на двоих
  клиент-авторитетность проще и уже даёт ровную картинку.
- Шифрование/античит сверх санитарии — игра для друзей.

### Прогоны
21 оффлайн + curve PASS; TestNet локально (1 и 2 клиента) и на живом
VDS (2 клиента: соперник-игрок 17.6-26.2%, рывков назад 0) — PASS;
тест несовместимых версий (worktree) и тест убийства сервера — вручную,
оба ведут себя как задумано. VDS обновлён (протокол 3).

**Файлы:** `scripts/Net.gd`, `scripts/Main.gd`, `server/README.md`.

## «Сервер не ответил за 5 с» — тесты затирали адрес VDS (2026-08-24)
Жалоба: игра пишет «сервер не ответил за 5 сек». Сервер на VDS при этом
был жив (в логе виден чужой заезд). Причина: `Net.join_server()`
СОХРАНЯЕТ адрес в `user://net.cfg`, а тестовые стенды (TestNet, ShotNet)
бьют в 127.0.0.1 под тем же пользователем Windows — каждый прогон тестов
затирал игроку адрес VDS локальным. В поле «ПО СЕТИ» оказывался
127.0.0.1, где сервера нет.
- Фикс: `join_server(address, port, remember := true)`; стенды зовут с
  `remember=false` — конфиг игрока не трогается (проверено: после
  прогона net.cfg не создаётся).
- Пострадавший net.cfg с 127.0.0.1 удалён — игра вернулась к адресу VDS
  по умолчанию (правится в поле на экране выбора машины).
- Урок: **тестовые стенды не должны писать в user:// игрока** — любое
  «запомнить» в общем профиле аукается на живом пользователе.

**Файлы:** `scripts/Net.gd`, `tools/test_net.gd`, `tools/shot_net.gd`.

## «У игрока по сети чёрный экран» — диагностика (2026-08-24)
Жалоба: у второго игрока при старте чёрный экран, при этом первый видит
его машину на трассе. По логу сервера: пир занял слот 1, прошёл
рукопожатие с проверкой версии (отказа в логе нет), 26 с посидел и
вышел. Значит, скрипты у него работают ПОЛНОСТЬЮ (сеть, слот, поток
состояния) — чёрный экран это рендер на его машине, не логика игры.
- Снимками ShotNet проверено: сетевой клиент рендерится нормально и на
  Forward+ (Vulkan), и на gl_compatibility — сама игра исправна на обоих
  рендерерах.
- Наиболее вероятные причины на стороне игрока: (а) первый вход в
  гонку = компиляция шейдеров всего мира — на слабом GPU окно «висит
  чёрным» десятки секунд (26 с ожидания очень похожи); (б) Vulkan-драйвер
  / старая видеокарта.
- В README добавлен раздел «Если чёрный экран»: подождать первый вход,
  `--rendering-method mobile`, `--rendering-method gl_compatibility`,
  обновить драйверы, прислать вывод консольной версии.
Дальше — по обратной связи от игрока (нужен вывод его консоли, если
обходные пути не помогут).

## Экран лобби + лента событий оружия (2026-08-24)
Просьба: «лобби отдельным экраном, где появляются машины подключённых
игроков; когда оружие действует на другого игрока — писать кто, чем и по
кому (имён нет — Player 1 и т.д.)».

### Экран лобби (`scripts/Lobby.gd` — новый, class_name Lobby)
- Полноэкранный Control ПОВЕРХ HUD внутри сцены Main (добавляется
  последним ребёнком CanvasLayer, только у клиента). Отдельной сценой
  лобби быть НЕ может: RPC адресуются по пути узла, клиент обязан сидеть
  в /root/Main с самого рукопожатия (грабли №1 сетевой части).
- Тёмный «гараж», лента-баннер «ЛОББИ», два подиума Player 1 / Player 2
  со вращающимися машинами (SubViewport с own_world_3d — как миниатюры
  CarSelect; названия машин из CarSelect.DISPLAY_NAMES через preload
  скрипта). Свой слот зелёный + «— ты», живой соперник оранжевый (те же
  цвета, что стрелки над машинами), пустой слот — «Ждём игрока…».
  Статус: «Игроков N/2, ждём второго: M…»; подсказки про пробел и Esc.
- На скрытом экране вьюпорты подиумов переводятся в UPDATE_DISABLED —
  не рендерить их под гонкой впустую.
- `_lobby_label` из Main выпилен, все его роли (подключение, «связь
  потеряна», отказ сервера по версии, «слот не выдан») показывает экран
  лобби (set_status + show_screen). Клиент ведёт `_slot_taken[2]`:
  маска из `_rx_welcome` + обновления `_rx_slot_taken` (теперь слот
  запоминается и ДО welcome, раньше обработчик выходил раньше времени) →
  `_update_lobby_slots()`; смена ростера (`_apply_roster`) обновляет
  модели на подиумах.

### Лента событий оружия («кто кого чем»)
- HUD: `_feed_box` (VBox) справа под мини-картой; запись —
  «Player 1 [иконка оружия] → Player 2», своё имя зелёное, чужие жёлтые.
  Висит 3.4 с, угасает твином; больше 5 разом — старейшая вытесняется.
  Имён пока нет: `car_label(i)` = "Player i+1" (боты тоже).
- Источник событий — ТОЛЬКО там, где оружие настоящее: сервер или
  оффлайн (у клиентов копии инертны). `Car.notify_hit_by(attacker,kind)`
  → `Main.report_weapon_hit` (фильтрует самопопадание и чужие узлы) →
  на сервере RPC `_rx_weapon_event` всем, оффлайн — сразу в ленту.
- Обвязка по видам: Projectile (shooter, ROCKET/FREEZE), Mine (dropper,
  MINE — взрыв бьёт и хозяина, самопопадание отсеивается), OilSlick
  (dropper, OIL — только если занос реально начнётся: повторный наезд во
  время заноса apply_oil_slip игнорирует), магнит и лазер (self, в
  _use_magnet/_use_laser), Airstrike — новое поле `attacker` (ставит
  _use_airstrike).
- **PROTOCOL 4 → 5**: новый @rpc-метод сдвигает таблицу RPC-идентификаторов,
  старые клиенты несовместимы (согласовано с параллельной сессией:
  5 покрывает и её `_rx_car_finished`, второй раз НЕ поднимать).

### Грабли
- Правка Main.gd через bash-heredoc с python: `\\\n` в строке доехала до
  файла литеральным «\n» — Parse Error. После скриптовых правок прогонять
  `--import` и читать stderr (правило из журнала работает).

### Проверено
- `--headless --import` чистый; TestWeapons / TestExplosion / TestLap /
  TestMineDrop — PASS.
- Живой прогон с локальным сервером: ShotNet — скрин лобби (подиум с
  машиной Player 1, «Ждём игрока…» у Player 2, отсчёт ожидания), после
  GO лобби спряталось, гонка рисуется. Стенд-одноразка снял ленту
  событий: три записи под мини-картой, свой ник зелёный.
- TestNet: рукопожатие/слот/ростер/старт/своя машина — ok; замеры
  плавности марионетки FAIL — это WIP параллельной сессии (перенос
  `_follow_snapshot` в `_process` + адаптация метрики), не эта правка.
  Полную регрессию, сборку dist и деплой VDS делает она (протокол 5!).

**Файлы:** `scripts/Lobby.gd` (новый), `scripts/Main.gd`,
`scripts/Car.gd`, `scripts/Net.gd`, `scripts/Projectile.gd`,
`scripts/Mine.gd`, `scripts/OilSlick.gd`, `scripts/Airstrike.gd`,
`README.md`.

## Пофинишный финиш, лобби 20 с, exe-сборка в dist/ (2026-08-24)
Запросы: «если 3 и 4 не доехали — игра останавливается, нужно дать
финишировать», «игрок по сети всё так же рывками», «я стартанул, а у
него ещё не запустилось», «нужен exe в папке — иерархия папок у второго
разработчика другая, из исходников не запускается».

### Финиш стал пофинишным
Раньше ПЕРВАЯ машина, прошедшая 4 круга (в сети — хоть бот!), обрывала
заезд всем. Теперь доехавшая машина останавливается и получает место ПО
ПОРЯДКУ ПЕРЕСЕЧЕНИЯ (`_car_finished`, `_finish_order`), остальные
доезжают. Свой финиш — баннер сразу (`_show_finish`), гонка вокруг
продолжается. Заезд закрывается целиком, когда доехали ВСЕ или через
FINISH_TIMEOUT = 40 с после первого финишёра (недоехавшим — место по
прогрессу). По сети порядок пересечения решает СЕРВЕР
(`_rx_car_finished(i, place)`), иначе места у двоих разошлись бы.
test_finish: лимит 150 → 220 с (ждём всех).

### Рывки соперника — что дало эффект, а что откачено
- Дубликат снимка больше НЕ сбрасывает возраст экстраполяции
  (net_apply_snapshot): сервер ретранслирует последнее состояние
  владельца своим тиком, и когда пакет владельца не успевал между
  тиками, уходила копия — сброс возраста на ней замирал марионетку на
  кадр («стоп-скачок»). Это и был остаточный источник рывков.
- Перенос движения марионетки в _process (ради мониторов >60 Гц) —
  ПРОБОВАН И ОТКАЧЕН: метрика стенда в headless рухнула 2.6% → 57% с
  40 рывками назад. Путь, который стенд не может проверить, не выпускаем;
  на будущее — встроенная интерполяция физики Godot 4.4+.
- Коэффициент подтяжки в _follow_snapshot сделан независимым от частоты
  кадров (1-0.65^(delta·60)) — безвредно и пригодится.
Живой VDS, два клиента: соперник-игрок 15-23%, рывков назад 0.

### Лобби 20 секунд (было 5)
В жизни второй игрок не успевал: у него первый вход = компиляция
шейдеров. Пробел по-прежнему стартует сразу. TestNet перестроен на
АДАПТИВНЫЕ тайминги (от момента фактического старта, а не фиксированные
окна — LOBBY_WAIT менялся уже дважды и каждый раз ломал стенд);
shot_net подвинут под 20 с.

### Готовая сборка в dist/
`export_presets.cfg` (Windows Desktop, x86_64, pck отдельно — вместе
с exe вышло бы >100 МБ, лимит файла GitHub) + `dist/BigHeadRacing.exe`
(80 МБ) и `BigHeadRacing.pck` (32 МБ). После pull второй разработчик
просто запускает exe — Godot и пути не нужны. Проверка сборки:
`dist\BigHeadRacing.exe --headless --log-file <лог> res://tools/TestX.tscn`
(обычный exe без консоли, вывод только через --log-file). Сборку
ПЕРЕСОБИРАТЬ при каждом изменении кода игры перед коммитом — версии
защищены PROTOCOL, устаревший exe получит «обнови игру».

### Параллельная работа трёх сессий (урок координации)
Одновременно со мной третья сессия делала экран лобби + ленту событий
оружия (секция «Экран лобби + лента событий оружия», PROTOCOL = 5 общий
на оба несовместимых изменения). Грабли: фоновая регрессия, запущенная
во время ЧУЖИХ правок, ловит файлы посреди записи — тест может
зависнуть; в раннер добавлен пер-тестовый таймаут 300 с. Договорённость:
коммит/деплой/VDS — через одну сессию (эту), остальные сообщают
готовность и пишут свои секции в PROGRESS сами.

Прогнано: 21 оффлайн + curve PASS (чистая машина), TestNet локально
(1 и 2 клиента) и живой VDS (2 клиента) PASS, TestWeapons внутри
собранного exe PASS. VDS обновлён (не забыт --import: class_name Lobby).

**Файлы (моя часть):** `scripts/Main.gd`, `scripts/Car.gd`,
`scripts/Net.gd`, `tools/test_net.gd`, `tools/test_finish.gd`,
`tools/shot_net.gd`, `export_presets.cfg`, `dist/*`, `README.md`.

## Шум «Attempt to disconnect a nonexistent connection» в exe — не наш (2026-08-24)
В логе TestWeapons из СОБРАННОГО exe (dist/) сыпятся ошибки
«Attempt to disconnect a nonexistent connection from 'CarN'. Signal:
'tree_entered'/'tree_exiting', callable: ''». Проверено сборкой pck из
коммита 6f202f8 (ДО лобби/ленты/финиша) и прогоном тем же exe
(`BigHeadRacing.exe --main-pack old.pck --headless res://tools/...`):
шум ТОТ ЖЕ — к правкам 24.08 отношения не имеет. Это движок: Area3D
(снаряд/мина/бокс) при удалении, пока машина ещё в зоне пересечения
(queue_free при попадании, teardown при выходе), дважды чистит свои
связи с tree_entered/tree_exiting тела — известная безвредная
особенность Godot 4.x. В редакторском бинарнике не печатается, в
экспортном debug-шаблоне — печатается. Тесты PASS, на игру не влияет.
Приём для будущих проверок «виноват ли новый код»: exe умеет
`--main-pack <старый.pck>`, а pck собирается без шаблонов экспорта
(`--export-pack "Windows Desktop" out.pck` в worktree старого коммита).

## 4 игрока, синхронный старт, фикс «еду после финиша», пульки (2026-08-24)
Запросы: «после финиша можно продолжить ехать с табличкой ФИНИШ», «я могу
стартануть, пока у другого лобби ещё не загрузилось — синхронизировать
вход из лобби», «масштабировать на 4 игроков по сети», «пульки побольше».

### Фикс «еду с табличкой ФИНИШ» — перезапуск клиентов (_rx_reset)
Причина: после заезда сервер перезапускал СВОЮ сцену, а клиентские
оставались старыми. Сервер начинал новый заезд (второй игрок мог
переподключиться или лобби истекало), и приехавший `_rx_count("GO!")`
включал `controls_enabled` финишировавшему игроку в старую сцену — он
ехал дальше прямо с баннером «ФИНИШ». Теперь `_reset_server_after_race`
перед перезагрузкой шлёт новый RPC `_rx_reset`: клиент сбрасывает
`Net.my_slot = -1` и перезагружает сцену — чистое лобби следующего
заезда, hello уходит заново (ветка «соединение уже установлено» в
`_ready` — та же, что при обычном входе из гаража). Страховки: `_rx_count`
и `_rx_race_running` игнорируются при `_my_finished`/`_finished`.

### Синхронный вход из лобби в гонку
Раньше сервер стартовал заезд в момент ENet-подключения второго игрока
(`_on_peer_joined`), хотя его сцена могла ещё грузиться (первый вход =
компиляция шейдеров) — первый уезжал, пока у второго чёрный экран/лобби
не открылось. Теперь старт проходит через одну точку `_maybe_start()`:
- «загрузился» = слот прислал hello (`_hello_done`; hello клиент шлёт из
  ГОТОВОЙ сцены Main) — до этого слот блокирует старт;
- старт случается, когда (все слоты заняты ИЛИ старт запрошен) И все
  подключённые загрузились. Запрос старта (`_want_start`) ставят таймаут
  лобби и пробел (`_rx_start_request` теперь только просит);
- молчун (старый клиент, зависшая загрузка) отпускает старт через
  HELLO_GRACE = 25 с от подключения — иначе держал бы всех вечно;
- пока ждём чью-то загрузку, клиентам едет `_rx_lobby(n, -1)` → «Ждём,
  пока у всех загрузится…» (один раз, не каждый тик — `_loading_told`).

### 4 игрока по сети
- `Net.PLAYER_SLOTS` 2 → 4 (машин в заезде по-прежнему 4, теперь ВСЕ
  слоты игроковые; пустые ведут боты). `PROTOCOL` 5 → 6 — бамп ОБЩИЙ с
  параллельной сессией случайных трасс (`_rx_track`), как в прошлый раз
  с 5: оба новых @rpc-метода сдвигают таблицу RPC разом.
- Маркеры: `_rival_marker` → словарь `_rival_markers` (слот → стрелка),
  оранжевая стрелка заводится над КАЖДЫМ чужим слотом и включается по
  маске занятых (`_rx_welcome`/`_rx_slot_taken`). Мини-карта:
  `rival_index` → словарь `rivals` (несколько оранжевых точек).
- Лобби: 4 подиума В РЯД (288 px + зазор 14 = 1194 в окно 1280),
  подписи Player 1..4; `_slot_taken` на 4.
- test_net.gd подстроен: «метки соперников есть/скрыты» проверяют все
  3 чужих маркера; бот для замера плавности ищется как чужой слот БЕЗ
  видимой метки (раньше жёстко слот 2, а это теперь слот игрока).
- ВАЖНО для UX: два игрока из четырёх больше не «полный дом» — между
  заездами лобби ждёт LOBBY_WAIT (20 с) или пробел, мгновенного старта
  по второму подключению нет.

### Пульки побольше
Projectile: видимый шар 0.26 → 0.42 м, коллизия 0.4 → 0.5 (попадать
стало чуть легче — аркаде на пользу). TestWeapons PASS.

### Прогнано (на общем дереве, уже с правками трасс параллельной сессии)
TestWeapons / TestMinimap / TestFinish / TestLap / TestExplosion — PASS;
TestNet: 1 клиент PASS, 2 клиента разом — оба PASS кроме закономерного
FAIL «метки скрыты» (второй игрок подключён — метка обязана гореть);
сервер печатает «слотов игроков: 4». Новый стенд полного цикла
`tools/TestReset.tscn` (test_reset.gd): клиент стоит, боты доезжают,
FINISH_TIMEOUT закрывает заезд, сервер перезапускается — клиент обязан
перезагрузиться и снова получить слот. PASS на 181 с (гонять отдельно,
идёт 3-6 минут; `static var` в пробнике переживает reload сцены).
Первый прогон TestNet дал ложный FAIL плавности 40.8% — разогрев
параллельной нагрузки; повтор 14.8%, два клиента 9%. Мерить плавность
на машине без лишней нагрузки.

### Координация и деплой
Рабочее дерево ОБЩЕЕ со «трассовой» сессией (протокол 6 один на двоих).
Коммит + пересборка dist + деплой VDS — одним разом, когда готовы обе
части: у клиента и сервера таблица RPC должна совпадать, деплоить
пол-работы нельзя (проверка PROTOCOL этот случай НЕ ловит — версии-то
равны). README и server/README обновлены под 4 игроков и синхронный
старт.

**Файлы (моя часть):** `scripts/Net.gd`, `scripts/Main.gd`,
`scripts/Lobby.gd`, `scripts/Minimap.gd`, `scripts/Projectile.gd`,
`tools/test_net.gd`, `tools/test_reset.gd` + `tools/TestReset.tscn`
(новые), `README.md`, `server/README.md`.

## Песчаная трасса + случайный выбор трассы + ускорители + фикс колёс (2026-08-25)
Запросы: «новая трасса по песчаной местности, выбирается случайно, длина
как у существующей, отбойников нет, на песке скорость ниже и всегда
песчаный дым из-под колёс»; «ускорители на трассе, но только перед
прямыми участками»; «исправь глюк — при приземлении колёса вылазили за
текстуру машины вверх».

### Виды трасс (TrackBuilder.kind: "grass" | "sand")
- TrackBuilder параметризован видом: `kind` ставится ДО add_child;
  `has_walls`, `offtrack_margin`, `_segments`, `_ground_drop` настраиваются
  в _ready. Статические SEGMENTS/HEIGHT_KEYS/_profile_height НЕ тронуты —
  их читают test_curve/test_bumps, и классика осталась байт-в-байт.
- Песчаная: SEGMENTS_SAND (685 м против 727 у классики), ПЛОСКАЯ, БЕЗ
  ограждений (_build_walls пропускается). Конфигурация подобрана
  скриптовым перебором: замыкание (обе свободные прямые ПОЛОЖИТЕЛЬНЫЕ:
  первая ручная раскладка давала −57 м и излом 177°), длина 600-760,
  сближение витков не ближе 26 м. ГРАБЛИ: интеграл дуги для ОТРИЦАТЕЛЬНОЙ
  развёртки — со знаком (sg*R*(sin…)), без него решатель врёт.
- Земля: gravel.png с тонировкой в песок (средний цвет текстуры 135,118,89
  → тинт 1.45/1.32/1.05), полотно — «укатанный песок» (0.64,0.53,0.36).
  Без стен прятать GROUND_DROP=1.2 нечем → на песке просвет 0.05 (полотно
  и так приподнято на 0.05 — ступенька 0.10 м, z-fighting'а нет). За
  обочиной склон 0.05/м и дюны ×0.35 — по песку МОЖНО ехать.
- Съезд на песок легален: порог автовозврата = полуширина + 12 м
  (offtrack_margin трассы; Main._check_recovery берёт его у трассы, конст
  OFFTRACK_MARGIN остался — его читает test_offtrack).
- Песок наказывает сам: Car._on_sand (дальше полуширины от оси на песчаной
  трассе) → fx_mult ×0.55 (тяга и потолок), пыль из-под колёс ВСЕГДА
  (порог скорости 3 м/с), цвет пыли песочный (градиент в _build_smoke;
  track известен в _ready — Main ставит его до add_child). У марионеток
  пыль не идёт (как и дым дрифта) — известное упрощение.
- Car: _wall_slide и _spin_away_from_wall выходят сразу при
  not track.has_walls. Minimap: кромки полотна на трассе без стен
  песочные, не красные (edge_color в setup).
- Деревья на песке не строятся (TrackDecor); низ скайбокса — в тон песка.

### Случайный выбор трассы
- Оффлайн: CarSelect._start_race пишет GameState.track_kind =
  pick_random_kind(). Тесты грузят Main напрямую, GameState пуст →
  классика: регрессия детерминирована.
- По сети вид диктует СЕРВЕР: выбирает случайно на каждый заезд (сцена
  перезапускается между заездами), клиенту шлёт НОВЫЙ RPC `_rx_track`
  ПЕРЕД welcome. Не совпало — клиент пишет GameState.track_kind и
  reload_current_scene (приём _rx_reset), новая сцена строит нужную
  трассу и заново шлёт hello. `--track=grass|sand` в user args фиксирует
  выбор сервера — ОБЯЗАТЕЛЬНО для сетевых стендов (TestNet перезагрузку
  сцены переживает плохо: чекер стартует заново).
- PROTOCOL остался 6 (общий бамп с параллельной сессией «4 игрока», как
  договаривались для 5): _rx_track дописан в комментарий истории.

### Ускорители (scripts/BoostPad.gd — новый class_name!)
- Area3D-плита по образцу WeaponBox: ловит машины (mask 0b100), личный
  откат 3 с на машину, на клиенте monitoring выключен (по сети срабатывает
  сервер, эффект своей машине едет путём бонуса BOOST: apply_boost →
  _forward_fx → _rx_fx). Эффект = Car.apply_boost() (×1.45 на 2.5 с).
- Ставятся TrackBuilder._build_boost_pads: В НАЧАЛЕ прямых длиннее 25 м
  (+6 м от начала), СТАРТОВАЯ прямая пропускается (решётка и створ).
  Отметки — в track.boost_pad_offsets (для тестов). Есть на обеих трассах
  и на сервере (косметика в headless не строится).
- Визуал: бирюзовая плита с эмиссией + белые шевроны по ходу (-Z).

### Фикс «колёса вылазили за текстуру машины вверх при приземлении»
- Причина: кламп колёс к полотну (_animate_wheels) поднимал пивот на
  глубину утопания до 0.6 м, а упреждение по скорости сближения при
  жёсткой посадке добавляло ещё до ~0.26 м — колесо выскакивало НАД
  кузовом. Подъём теперь ограничен 60% радиуса колеса: выше — колесо
  гарантированно торчит из арки; обычное утопание ≤ 11 см меньше капа,
  так что защита от «колёс под асфальтом» не пострадала (TestWheelSink
  PASS, worst pen прежний).

### Тесты/стенды (новые)
- tools/TestSandLap.tscn — ИИ 60 с по песчаной трассе (без стен, круг
  685 м, худший проехал 1598 м) + фаза «игрок на песке»: максимум
  18.8 м/с при лимите 25.5, пыль в 144 кадрах из 167. PASS.
- tools/TestBoostPad.tscn — машина ходом (25 м/с) через первую плиту:
  буст получен, максимум 49.6 м/с при max_speed 34. PASS. ГРАБЛИ: с
  места буст истекает раньше выхода за обычный потолок — старт ходом.
- tools/ShotSand.tscn + shot_sand.gd — скриншот-стенд песчаной трассы.
- test_curve.gd проверяет теперь ОБЕ трассы (излом в плане: grass 5.55°,
  sand 5.00° — PASS).
- Прогнана вся оффлайн-регрессия (см. ниже). TestLap: ИИ с ускорителями
  проезжают 1637 м/мин (было ~1290) — это фича, не регрессия.

**Файлы:** `scripts/TrackBuilder.gd`, `scripts/Car.gd`, `scripts/Main.gd`,
`scripts/BoostPad.gd` (новый), `scripts/GameState.gd`,
`scripts/CarSelect.gd`, `scripts/TrackDecor.gd`, `scripts/Minimap.gd`,
`scripts/Net.gd` (комментарий протокола), `tools/test_curve.gd`,
`tools/TestSandLap.tscn`+`test_sand_lap.gd`,
`tools/TestBoostPad.tscn`+`test_boost_pad.gd`,
`tools/ShotSand.tscn`+`shot_sand.gd`, `README.md`.
ВАЖНО: новый class_name BoostPad → на VDS перед стартом `--import`
(правило из журнала). Коммит/deploy — координирует параллельная сессия
(протокол 6 общий).

## Магнит осаживает передних, шансы боксов от позиции, мина крупнее, огонь буста (2026-08-25)
Запросы: «магнит не только притягивает, но и режет скорость впередиедущим
вдвое, задних только притягивает», «мину побольше», «последнему вдвое реже
мина и масло», «сильно отставшему чаще ускорение», «при ускорении — огонь
из зада машины».

### Магнит различает перед/зад (Car._use_magnet)
- «Впереди» — по ПРОГРЕССУ ГОНКИ (новый Main.progress_of(car), тот же
  накопленный путь, которым считаются места), не по геометрии: на петлях
  машина «перед носом» может по заезду быть позади. Фолбэк без Main
  (стенды) — геометрически перед носом.
- Впередиедущему ДО рывка притяжения режется скорость вдвое — новый
  Car.apply_speed_cut(factor): горизонтальная linear_velocity ×factor,
  память _recent_hspeed срезается вслед (иначе защита приземления/ведение
  у стены вернули бы срезанное). Срез до рывка, не после — иначе
  порезался бы сам импульс притяжения. Задних — только тянет, как раньше.
- Сеть: машина игрока клиент-авторитетна → эффект пересылается владельцу
  новым NetFx.SLOW (Car._forward_fx → Main._rx_fx). НОВЫХ @rpc-методов
  нет, таблица RPC не менялась — PROTOCOL остался 6; но деплоить
  клиент/сервер, как всегда, одним разом.

### Шансы боксов зависят от положения в гонке
- Weapons.random_weapon(is_last, behind_gap) — взвешенный выбор: у
  ПОСЛЕДНЕГО вес мины и масла 0.5 (вдвое реже — ставить назад некому);
  вес ускорения растёт с отставанием от лидера: с 30 м, к 110 м ×3.
  Без аргументов — равновероятно (стартовое оружие, стенды).
- Контекст знает менеджер гонки: новый Main.pickup_weapon_for(car)
  (место через _place_of, отставание от лидера по _progress); WeaponBox
  зовёт его через car.race, без гонки — старый random_weapon(). По сети
  оружие и так выдаёт сервер — у него прогресс всех есть.

### Мина крупнее (Mine.gd)
- Видимый корпус ×~1.6: цилиндр 0.5/0.6×0.26 (был 0.32/0.38×0.16),
  колпачок 0.11 (0.07); посадка на землю +0.13 (низ корпуса -0.13).
- ГРАБЛИ: радиус срабатывания 0.7→0.9 ОТКАЧЕН — доставал до кормы
  сбросившей машины (мина ложится в 2.4 м от центра, край «санок» ~1.5 м,
  зазор 0.9) и рвался под хозяином: TestMineDrop падал (воздушная мина
  взрывалась об машину, тест зависал на freed-объекте). Побольше — только
  визуал, триггер прежний 0.7.

### Огонь из выхлопа при ускорении (Car._build_boost_flame)
- CPUParticles3D из кормы (0, 0.42, 1.6): та же текстура облачка, что у
  дыма заноса, но клубы живут 0.28 с, летят струёй назад (+Z, разброс 7°),
  перекрашены в огонь (жёлтое ядро → оранжевый → прозрачный красный) и
  съёживаются к концу жизни. Включается в _tick_effects, пока идёт буст.
- Марионетки по сети: свой _boost_time у них не тикает — признак буста
  берётся из значка эффекта снимка (_status_time > 0 и _status_kind ==
  BOOST). У net_make_puppet огонь гасится вместе с дымом.

Прогнано: TestMineDrop, TestWeapons, TestBoostPad, TestExplosion,
TestLap — PASS (см. вывод прогона в сессии).
**Файлы:** `scripts/Car.gd`, `scripts/Main.gd`, `scripts/Mine.gd`,
`scripts/Weapons.gd`, `scripts/WeaponBox.gd`.

## «Гаражный» интерфейс + всплывающие анонсы (2026-08-25)
Запрос: «красивый, удобный, стильный интерфейс, ориентироваться на
картинки в корне проекта (НЕ мультяшный как сейчас); побольше
всплывающих надписей — double kill от лазера/авиаудара, последний круг
и т.п.».

### Стиль «гаражная эмаль» (по AI-референсам в корне проекта)
- Палитра/типографика в новом `scripts/UiKit.gd` (class_name!):
  чернильный INK, сталь STEEL/RIM, эмаль WHITE/YELLOW/ORANGE/RED/TEAL;
  шрифт **Russo One** (OFL, скачан с github google/fonts в
  `assets/ui/RussoOne.ttf` + лицензия рядом; кириллица есть) —
  мультяшный Softie из UI убран ПОЛНОСТЬЮ.
- Ассеты генерирует `tools/gen_ui_assets.py` (py -3.10, PIL+numpy) в
  `assets/ui/garage/`:
  - 8 восьмиугольных значков оружия wg_*.png — НАРЕЗАНЫ из референса
    `_STYLE_CORE_A_sheet_of_12_weap_2.jpg` (сетка 5×3, фон убирается
    заливкой от краёв ячейки — замкнутые тёмные области внутри значка
    заливка не съедает); Weapons.ICONS переключён на них (иконки над
    машинами в 3D — те же);
  - пустой гекс-слот slot_empty.png из `_STYLE_CORE_A_HUD_weapon_slot_`
    (окно y 0.28–0.665 ячейки, иначе в кадр лезет подпись «EMPTY»);
  - эмалевые таблички plate_{white,yellow,orange,red,teal,steel}[_s].png
    (запечены: глянец-градиент, шум, кант, заклёпки; _s = 256×96 с
    9-slice полями 20 px для панелей HUD, большие 384×192 / поля 40);
  - тайлы hazard.png (жёлто-чёрные полосы 45°) и checker.png (шахматка);
  - кнопки-стрелки arrow_l/arrow_r.png; фон лобби backdrop_rust.jpg
    (ржавая панель из референса, затемнена до 0.30).
- HUD гонки (Main._setup_hud): скорость — белая эмаль с чернильными
  цифрами и хазард-полоской (как SPEED-табличка референса); круг —
  жёлтая; место — оранжевая; мини-карта и лента событий — сталь;
  слот оружия: пустой = гекс EMPTY, с оружием = восьмиугольный значок
  на весь слот (q_mark больше не нужен); предупреждение — красная
  табличка; отсчёт 3-2-1 жёлтый / GO! бирюзовый; финиш — стальная
  плита с шахматными лентами. `_warn_panel` теперь Control
  (NinePatchRect), тесты/стенды совместимы.
- CarSelect: белая эмалевая табличка-заголовок, стальная табличка имени,
  оранжевый СТАРТ / бирюзовая ПО СЕТИ (UiKit.style_button), стальное
  поле адреса, стрелки-таблички, сетка на стали с жёлтой рамкой выбора.
- Lobby: ржавый фон, стальная плита «ЛОББИ» с хазардом, стальные слоты.

### Всплывающие анонсы (`scripts/Announcer.gd`, class_name!)
- Табличка-«штамп» влетает по центру (scale 1.7→1.0 TRANS_BACK, лёгкий
  случайный наклон), висит, уходит вверх растворяясь. Два яруса:
  big (очередь, по одному) и small (строкой ниже); у big снизу
  аварийная лента.
- События (Main, работает оффлайн и на клиенте — события оружия и так
  едут через _rx_weapon_event, прогресс в снимках):
  - серии убийств: летальные виды (ROCKET/LASER/AIRSTRIKE) в окне 1.2 с
    на атакующего → «ДВОЙНОЕ/ТРОЙНОЕ УБИЙСТВО!» (лазер и авиаудар
    кладут нескольких за кадр — окно это ловит);
  - «ПЕРВАЯ КРОВЬ!» — первое летальное попадание заезда (чьё угодно);
  - «ПОСЛЕДНИЙ КРУГ!» — мой выезд на круг 4/4 (жёлтая);
  - «ВЫ ЛИДЕР!» / «Лидерство потеряно» — смена 1-го места с дебаунсом
    0.8 с и молчанием первые 5 с гонки (старая раздача мест — не событие);
  - личные: «Вас уничтожил Player N» (красная) / «Player N уничтожен!»
    (бирюзовая) — малым ярусом.
- Счётчик серий в `Main._register_kill` зовётся из `_show_weapon_event`,
  т.е. на каждом клиенте локально — RPC не добавлялись, PROTOCOL не тронут.
- Мина НЕ летальна (только толчок) — в сериях не участвует.

### Стенды/тесты
- screenshot_hud.gd: + кадр hud_announce.png (принудительный big+small
  анонс на 470-м кадре). ВАЖНО про кадр: организменные анонсы гонки
  (ИИ реально убивают друг друга) занимают очередь — форс-анонс может
  быть снят в середине влёта (полупрозрачным), это очередь, не баг.
- Новый стенд `tools/ScreenshotLobby.tscn` (+screenshot_lobby.gd):
  лобби без сети, слоты заполняются вручную.
- Прогнано headless: TestWeapons, TestMinimap, TestExplosion, TestLap,
  TestFinish — PASS. Скриншоты HUD/CarSelect/Lobby сняты и отсмотрены.
- ВАЖНО: новые class_name (UiKit, Announcer) → на чистой машине/VDS
  перед запуском обязателен `--headless --import`.
- dist/ НЕ пересобран (коммита в этой сессии нет) — пересобрать exe+pck
  перед следующим коммитом/деплоем, как требует правило журнала.

**Файлы:** `scripts/UiKit.gd`, `scripts/Announcer.gd` (новые),
`scripts/Main.gd`, `scripts/Weapons.gd`, `scripts/Lobby.gd`,
`scripts/CarSelect.gd`, `tools/gen_ui_assets.py` (новый),
`tools/screenshot_hud.gd`, `tools/screenshot_lobby.gd` +
`tools/ScreenshotLobby.tscn` (новые), `assets/ui/RussoOne.ttf` (+OFL),
`assets/ui/garage/*` (генерируемые PNG/JPG).

## Опыт, ИИ попроще, огонь буста, анонсы дольше, бонусы не по центру (2026-08-25)
Запросы: «всплывающие сообщения должны висеть подольше», «игру попроще —
противники едут очень хорошо», «набор опыта, за который дальше будем
открывать штуки; просчитать сколько очков за какое место», «огонь похож
на оранжевый дым — сделать огонь», «бонусы/ускорители/трамплины не должны
стоять строго по центру, бонусов побольше».

### Анонсы висят дольше (Announcer.gd)
BIG_HOLD 2.1 → 3.8 с, SMALL_HOLD 1.7 → 3.0 с. Очередь/анимации не тронуты.

### ИИ проще (Car.gd + Main.gd)
- Новый `Car.ai_skill` (постоянный «класс» бота): Main._spawn_cars даёт
  ботам randf_range(0.86, 0.94) — темп ниже игрока на 6-14%.
- «Резинка» переделана: ОФФЛАЙН меряется от ИГРОКА (машина 0), не от
  лидера — убежавший вперёд бот сбрасывает (кламп снизу 0.8: его можно
  догнать), отставший поджимает слабее прежнего (сверху 1.15, было 1.3:
  от него можно уехать). По сети — по-прежнему от лидера (машина 0 там
  просто слот). ai_skill вкладывается множителем в ai_rubber в Main.
- Тестам хватает запаса: критерии TestLap/SandLap — «полкруга за 60 с»,
  боты и с 0.72 темпа едут ~1400 м/мин.

### Опыт (GameState.gd — профиль в user://profile.cfg)
- За место на финише: 1-е — 100, 2-е — 60, 3-е — 40, 4-е — 25 очков
  (GameState.PLACE_XP) + 10 за каждого уничтоженного соперника (KILL_XP;
  счётчик _my_kills в Main._register_kill, ветка «ai == me»).
- Уровень: на следующий нужно 100·N (1→2: 100, 2→3: 200…; до 5-го
  уровня суммарно 1000). GameState.level_info() → [уровень, опыт внутри
  уровня, цена следующего]. Прикидка темпа: середняк (~55 очков/заезд)
  берёт 2-й уровень за 2 заезда, 5-й — за ~18; победитель — вдвое быстрее.
- Начисление в Main._show_finish — ОДИН раз (guard _my_finished), у себя:
  оффлайн и на клиенте; сервер HUD не строит. Профиль пишется сразу
  (ConfigFile). На баннере финиша — жёлтая строка «+N ОПЫТА · УРОВЕНЬ K
  (x/y)»; при переходе уровня — анонс «НОВЫЙ УРОВЕНЬ K!». В гараже
  (CarSelect) под заголовком — «УРОВЕНЬ N · ОПЫТ x/y».
- За уровни дальше открывать машины/трассы/оружие — см. «Дальше».

### Огонь буста стал огнём (Car._build_boost_flame)
Было: текстура ДЫМА, перекрашенная в оранжевый → «оранжевый дым».
Стало: радиальное пятно (GradientTexture2D) + АДДИТИВНОЕ смешивание
(BLEND_MODE_ADD) — перекрывающиеся клубы высветляются и «светятся»;
рампа бело-жёлтое ядро → оранжевый → гаснущий тёмно-красный; жизнь
клуба 0.28 → 0.22 с (длинный хвост читался дымом), частиц 26 → 36.

### Бонусы/ускорители/трамплины не по центру, бонусов больше
- Боксы оружия (Main._spawn_weapon_boxes): 5 → 9 отметок, смещение к
  бортам ±2.4/3.6 м, стороны чередуются. Смещения ДЕТЕРМИНИРОВАННЫЕ
  (без randf — не сдвигать поток случайных чисел у стендов).
- Ускорители (TrackBuilder._build_boost_pads): сдвиг ±3.2 м (стороны
  чередуются), прямые > 55 м получают ВТОРУЮ плиту на 60% длины у
  другого борта. Новый массив boost_pad_laterals (тесты целятся по нему).
- Трамплины (_build_ramps): сдвиг ±4.6 м — КРУПНЫЙ, ≥ полтрамплина +
  полкорпуса: едущий по оси минует трамплин целиком (частичный наезд на
  боковую кромку кувыркал бы машину), прыжок теперь по желанию.
- Новый хелпер TrackBuilder.right_at_offset(off) — «вправо» полотна из
  предрассчитанных кадров. Все смещения ограничены полушириной
  (half_width_at_offset, maxf(0,…) на случай узких мест).
- ИИ едет по оси → ускорители/боксы он теперь чаще минует (тоже в
  сторону «игра попроще»); test_boost_pad.gd целится в плиту с учётом
  boost_pad_laterals.

Регрессия: см. прогон в сессии (Ramp/BoostPad/Lap/SandLap/Weapons/
Explosion/Finish). dist/ НЕ пересобран — пересобрать перед коммитом.
**Файлы:** `scripts/Announcer.gd`, `scripts/Car.gd`, `scripts/Main.gd`,
`scripts/GameState.gd`, `scripts/CarSelect.gd`, `scripts/TrackBuilder.gd`,
`tools/test_boost_pad.gd`.

## Спецэффекты из Epic Toon FX по всей игре (2026-08-25)
Запрос: «из E:\UnityProjects\Fish\Assets\Epic Toon FX можно взять
спецэффекты и использовать их в игре — сделай это». Ранее оттуда были
взяты 3 текстуры (explosion_3x3, sparkle, smoke_cloud_2x2); теперь
эффекты расширены по всей игре.

### Новые текстуры в assets/fx/ (белые силуэты с альфой — красятся)
Подготовлены PIL-скриптом (ужаты/нарезаны): `fire_6x3.png` (атлас огня
6×3, 2048→1024), `ring_shockwave.png` (кольцо), `muzzleflash01.png`
(вспышка дула, 1024→512), `snowflake.png`, `glow1.png`,
`oil_splat.png` (клякса — ячейка (0,0) атласа splat01_flat_3x3, самая
плотная по альфе, на квадратном холсте). После добавления PNG —
`--headless --import` (правило журнала).

### Новый scripts/FxKit.gd (class_name! на VDS перед стартом --import)
Static-хелперы разовых эффектов: `ring` (кольцо ударной волны — лежит
на земле, разлетается и тает, аддитивное), `smoke_burst` (клубы дыма,
атлас 2×2, случайный кадр), `snow_burst` (снежинки во все стороны),
`muzzle_flash` (билборд-вспышка у дула, 0.12 с). Все сами удаляются
(tween/finished).

### Куда встроено
- Огонь буста (Car._build_boost_flame): вместо радиального пятна —
  АНИМИРОВАННЫЕ кадры огня fire_6x3 (particles_anim 6×3, случайный
  стартовый кадр + прокрутка по жизни, loop). Аддитив остался.
- Взрыв машины (Car.destroy): + кольцо, клубы дыма, искры SparksFx.
- Мина (Mine._try_trigger): + кольцо 5.5 м, дым, искры.
- Авиаудар (Airstrike): у каждого взрыва + кольцо и дым.
- Ракета/ледышка (Car.use_weapon): вспышка у дула (оранжевая/голубая);
  у снаряда ШЛЕЙФ (Projectile._build_trail): ракета — glow1 аддитивно
  (огненный след), ледышка — снежинки. Попадание: ракета — искры,
  ледышка — снежный разлёт.
- Заморозка (Car.apply_freeze + заражение в _bounce_off_cars):
  снежный разлёт над машиной.
- Магнит (Car._use_magnet): + фиолетовое кольцо 5 м.
- Бонус БУСТ (use_weapon): + бирюзовое кольцо. Оба кольца
  продублированы в Main._spawn_weapon_visual (клиентская картинка).
- Масло (OilSlick): вместо идеального круга-цилиндра — мультяшная
  КЛЯКСА oil_splat (квад 6.4 м, чёрная с масляным бликом, поворот
  псевдослучайный БЕЗ randf — не сдвигать поток случайных чисел у
  стендов с seed; из get_instance_id).
- Бокс оружия (WeaponBox): при подборе + сноп искр.

### Проверка
- Тесты: TestWeapons, TestMineDrop, TestExplosion, TestBoostPad,
  TestLap, TestCarBounce (seed-чувствительный) — все PASS.
- Новый скриншот-стенд `tools/ShotFx.tscn`+`shot_fx.gd` (С ОКНОМ):
  по кадрам форсит буст/ракету/масло/заморозку/взрыв возле игрока,
  кадры в tools/shots_fx/. Отсмотрено: огненный шлейф буста, кольцо
  магнита, клякса, ледяная машина, кольцо+вспышка взрыва — на месте.
- Сеть не менялась (RPC не трогались, PROTOCOL прежний): эффекты
  встроены ровно туда, где уже были FlashFx — паритет клиента прежний.
- dist/ НЕ пересобран — пересобрать exe+pck перед коммитом/деплоем.

**Файлы:** `scripts/FxKit.gd` (новый), `scripts/Car.gd`,
`scripts/Projectile.gd`, `scripts/Mine.gd`, `scripts/OilSlick.gd`,
`scripts/WeaponBox.gd`, `scripts/Airstrike.gd`, `scripts/Main.gd`,
`tools/ShotFx.tscn`+`shot_fx.gd` (новые), `assets/fx/*` (6 новых PNG).

## Песчаная трасса стала шире (2026-08-25)
- Запрос: «на песчаной трассе сделай трассу шире».
- TrackBuilder.SEGMENTS_SAND: полуширины участков подняты с 7.0…10.0 до
  9.0…11.0 м (максимум = TRACK_HALF_WIDTH, его хватило — константу не
  трогали; классика SEGMENTS не задета). Полотно шире на 3-4 м.
- Геометрия оси не менялась (ширина на кривую не влияет): замыкание,
  свободные прямые, сумма углов — как были. Витки сближаются не ближе
  26 м по осям → даже при 11+11 полотна не слипаются (зазор ≥ 4 м).
- Проверка: CURVE TEST [sand] PASS (685 м, мин. радиус 20), SAND LAP
  TEST PASS, скриншоты стенда ShotSand отсмотрены (полотно шире,
  наложений витков нет).
- **Файлы:** `scripts/TrackBuilder.gd` (только SEGMENTS_SAND + коммент).

## Короче огонь, дольше анонсы, ещё эффекты + фикс «залп в (0,0,0)» (2026-08-25)
Запросы: «след от огня надо сделать покороче», «всплывающие сообщения
подольше держать на экране», «добавь больше спецэффектов из Epic Toon FX».

### След от огня короче
- Огонь буста (Car._build_boost_flame): lifetime 0.22 → 0.13, скорость
  струи 7-11 → 5-8, amount 36 → 30. Хвост на бусте был ~13 м (частицы
  в мире, машина 49 м/с), стал ~6 м — компактный факел у сопла.
- Шлейф ракеты (Projectile._build_trail): lifetime 0.3 → 0.18
  (при 45 м/с снаряда след тянулся на ~13 м).

### Анонсы висят дольше (Announcer.gd, вторая просьба подряд)
BIG_HOLD 3.8 → 6.0 с, SMALL_HOLD 3.0 → 4.8 с.

### ВАЖНЫЙ ФИКС: разовые CPUParticles3D стреляли в (0,0,0)
У нового CPUParticles3D `emitting = true` ПО УМОЛЧАНИЮ: при add_child
one-shot залп с explosiveness=1.0 уходит СРАЗУ, в точке (0,0,0) — ДО
`fx.global_position = ...`. Финальное `emitting = true` — no-op (уже
true). Из-за этого smoke_burst/snow_burst/lightning_burst и новые
эффекты вылетали в начале координат (у старта трассы — потому на
отсмотренных скриншотах у стартовой зоны выглядело «на месте»).
Диагностировано стендом с вариантами: позиция до add_child — работает,
emitting=false → позиция → true — работает, игровой паттерн — залп в
нуле. Фикс: во всех 6 разовых функциях FxKit первым делом
`fx.emitting = false`. Правило на будущее: разовому CPUParticles3D
глушить emitting ДО add_child.

### Новые текстуры (tools/gen_fx_assets.py — теперь скриптом, не ад-хок)
`star_4x4.png` (звёзды-контуры, 2048→512), `lightning_3x3.png`
(электрические осколки, из lightning_v1_3x3, 2048→1024),
`confetti_3x3.png` (ЦВЕТНОЙ атлас 3×3, не красится),
`scorch.png` (выжженное пятно, белое — красится). После PNG — `--import`.

### Новые эффекты (FxKit) и куда встроены
- `stars_burst` — мультяшные звёзды из точки удара: таран машин
  (в _bounce_off_cars при closing > 4, спавнит меньший instance id),
  подбор бокса оружия. Кадры атласа только 0..0.45 — у нижних рядов
  контур слишком тонкий, с игровой камеры не читается.
- `lightning_burst` — электрические осколки: магнит (фиолетовые — у
  применившего и НАД КАЖДОЙ жертвой; клиентская копия в
  Main._spawn_weapon_visual), лазер (красноватые на каждой жертве).
- `confetti_burst` — залп конфетти над машиной игрока на финише
  (Main._show_finish; победителю второй залп повыше).
- `scorch` — тёмное выжженное пятно на полотне после взрыва машины,
  мины, каждой ракеты авиаудара; луч вниз (mask 1) кладёт по рельефу,
  висит ~6.5 с и тает. Поворот псевдослучайный из instance_id (не randf
  — не сдвигать поток случайных чисел у стендов с seed).
- `fire_burst` — сноп огня (кадры fire_6x3): взрыв машины, мина,
  авиаудар.

### Проверка
- ShotFx-стенд расширен кадрами fx_magnet/fx_stars/fx_confetti;
  отсмотрено: короткий факел буста, конфетти, звёзды, разряды на
  жертвах магнита, кольцо+дым+пятна взрыва — на месте.
- Тесты: Weapons, MineDrop, Explosion, BoostPad, CarBounce (seed),
  Lap, Finish — все PASS. Сеть/RPC не тронуты, PROTOCOL прежний.
- dist/ НЕ пересобран — пересобрать exe+pck перед коммитом/деплоем.

**Файлы:** `scripts/FxKit.gd`, `scripts/Car.gd`, `scripts/Projectile.gd`,
`scripts/Mine.gd`, `scripts/Airstrike.gd`, `scripts/WeaponBox.gd`,
`scripts/Main.gd`, `scripts/Announcer.gd`, `tools/shot_fx.gd`,
`tools/gen_fx_assets.py` (новый), `assets/fx/*` (4 новых PNG).

## Следы шин на асфальте при сильных заносах (2026-08-25)
Запрос: «следы от шин на асфальте при сильных заносах».

### Новый scripts/SkidTrail.gd (class_name! на VDS перед стартом --import)
Лента-стрип (MeshInstance3D + ArrayMesh TRIANGLE_STRIP), которую машина
тянет за колесом: add_point(точка касания, нормаль) наращивает пары
вершин (ширина 0.22 м, LIFT 0.05 над полотном — не мерцает о дорогу),
finish() — полоса лежит 7 с, растворяется 3 с и удаляется сама.
Лимиты: 150 точек на ленту (дальше Car заводит новую), шаг точек
0.3 м, скачок > 3 м = телепорт/респавн — лента рвётся.

### Интеграция (Car.gd)
- `_skid_active` считается в _physics_process рядом с дымом: тот же
  порог сильного заноса (|_side_speed| > 5 при скорости > 8, или масло),
  НО без песчаной ветки: на песке след резины не рисуем (там пыль),
  и только на полотне (track.distance_from_axis < полуширина + 0.3 —
  на траве следов нет). Вне трассы/стендов (track == null) — рисуем.
- Ленты тянет _animate_wheels: у него УЖЕ есть лучи к дороге под каждым
  колесом (кламп колёс) — точка и нормаль берутся из того же
  intersect_ray, новых лучей ноль. Чертят только ЗАДНИЕ колёса
  (is_front false), как и дым. Словарь _skid_trails: пивот -> лента.
- Занос кончился / колесо без контакта / alive=false — _end_skid()
  закрывает ленту. Марионетки по сети не чертят (у них _side_speed
  не считается — то же упрощение, что у дыма).

### Проверка
- Новый стенд `tools/ShotSkid.tscn`+shot_skid.gd (С ОКНОМ): разгон
  инжектом ввода, занос ручник+руль, кадры в tools/shots_fx/skid_*.png.
  Отсмотрено: парные тёмные дуги за задними колёсами по траектории
  заноса, ложатся и у ботов (ИИ дрифтует сам).
- Тесты: Weapons, Lap, SandLap, WallSlide, Explosion, Ramp — PASS.
- Физика не тронута (чистый визуал), сеть/RPC не тронуты.
- dist/ НЕ пересобран — пересобрать exe+pck перед коммитом/деплоем.

**Файлы:** `scripts/SkidTrail.gd` (новый), `scripts/Car.gd`,
`tools/ShotSkid.tscn`+`tools/shot_skid.gd` (новые).

## Ночная городская трасса с неоном (2026-08-25)
Запрос: «соберем новую трассу в стиле ночного города и неоновых огней».
Третий вид трассы `TrackBuilder.KIND_NEON` ("neon") — в общем котле
случайного выбора (KINDS), по сети раздаёт сервер как обычно
(`--track=neon` фиксирует). PROTOCOL не тронут (RPC не менялись), но
клиент/сервер деплоить одним разом: старый клиент незнакомый вид
проигнорирует и построит классику.

### Контур (SEGMENTS_NEON, 703 м)
- «Городские улицы»: почти все повороты прямоугольные (90°), два длинных
  проспекта (свободные прямые ~103 и ~107 м), шикана, срезанный квартал
  90°+60°. ПЛОСКАЯ (профиль с горкой — только у классики: _height_at
  теперь `_profile_height if kind == KIND_GRASS else 0`).
- Конфигурация подобрана численным перебором (скрипт в scratchpad, как
  для песчаной): замыкание точное, обе свободные прямые положительные,
  витки не ближе 30 м, габарит 247×189 м (земля 400). Сумма углов:
  правые 495 − левые 135 = 360 ✓. CURVE TEST [neon] PASS (излом 5.21°
  при лимите 7.05°).
- Новый static `TrackBuilder.segments_for(kind)` — общая точка правды
  для _ready и test_curve (раньше тест выбирал сегменты сам тернаркой).

### Вид ночного города
- Стены ЕСТЬ (в отличие от песка), тёмный бетон (0.10,0.10,0.16);
  поверху — неоновые «трубки» (_build_neon_strips): лента 0.18 м
  (верх + бока), UNSHADED + эмиссия 2.2; внутренний борт ГОЛУБОЙ,
  внешний МАДЖЕНТА — стороны различимы боковым зрением. Косметика,
  на сервере не строится (_headless_server).
- Полотно почти чёрное (0.09,0.09,0.12), земля — гравий в тёмно-синем
  тинте (0.30,0.33,0.44), пустыри почти плоские (склон 0.03, холмы
  ×0.15) — на них стоят здания. Пунктирная осевая светится холодным
  белым (TrackDecor), трамплины — эмиссивно-жёлтые (ночью иначе чёрные).
- Окружение (Main._setup_environment, ветка neon): «луна» 0.25
  (холодная), небо почти чёрное с багровым заревом у горизонта
  (0.17,0.07,0.22), ambient синий 0.55, **glow включён** (intensity
  0.7) — все эмиссивные материалы реально светятся. Рендерер проекта
  Forward Plus — glow поддержан.
- Город (TrackDecor._build_city): до 70 коробок-небоскрёбов вокруг
  трассы (seed фиксирован), тёмный бокс + эмиссивная текстура ОКОН,
  сгенерированная кодом (128×128, 8×8 окон, часть горит тёплым/
  холодным), ТРИПЛАНАРНАЯ проекция ПО МИРУ (uv1_world_triplanar) —
  окна ~2.2 м на фасадах коробки любого размера без UV. Дома стоят по
  «городской сетке» (повороты кратны 90° ± чуть). ВЫСОТА ОГРАНИЧЕНА
  расстоянием до трассы (h ≤ 0.5·d): камера смотрит под −32°
  (tan ≈ 0.62), ближний высокий дом закрыл бы машину. На части домов
  неоновые вывески (плашки 5 цветов, эмиссия 2.4) фасадом к трассе.
- ГРАБЛИ: у StandardMaterial3D оператор эмиссии по умолчанию ADD
  (цвет + текстура) — с белым emission-цветом светился ВЕСЬ дом
  сплошным белым (так и было на первом скриншоте). С текстурой окон
  emission-цвет должен быть ЧЁРНЫМ.
- Деревья теперь только на классике (в городе вместо них здания),
  воздушных шаров ночью нет. Трибуны/арка/обочина остались.
- Фары (Car._build_headlights, только на neon-трассе; track известен в
  _ready — Main ставит его до add_child): два SpotLight3D вперёд, чуть
  вниз, БЕЗ теней (8 бестеневых спотов на заезд — дёшево) + светящиеся
  «лампы»-кубики на носу. Есть и у марионеток по сети (строятся в
  _ready по track.kind).
- Мини-карта: кромки в цвет неона (голубые), не красные.

### Тесты/стенды
- tools/TestNeonLap.tscn + test_neon_lap.gd — ИИ 60 с по городу
  (худший проехал 1191 м при круге 703), стены на месте, у всех машин
  ≥ 2 SpotLight3D. PASS.
- tools/ShotNeon.tscn + shot_neon.gd — скриншот-стенд (5 ракурсов в
  tools/shots_neon/), отсмотрено: трубки светятся, город в окнах,
  зарево, фары дают пятно света на стартовой клетке.
- test_curve.gd теперь гоняет ВСЕ KINDS через segments_for. Прогнано:
  CURVE (3 трассы), TestNeonLap, TestLap (1271 м), TestSandLap,
  TestMinimap — все PASS.
- ГРАБЛИ: `var x := a and b` с нетипизированными полями Main не
  компилится в 4.3 («Cannot infer the type») — в тестах явный `: bool`.

**Файлы:** `scripts/TrackBuilder.gd`, `scripts/TrackDecor.gd`,
`scripts/Main.gd`, `scripts/Car.gd`, `scripts/Minimap.gd`,
`tools/test_curve.gd`, `tools/TestNeonLap.tscn`+`test_neon_lap.gd`
(новые), `tools/ShotNeon.tscn`+`shot_neon.gd` (новые), `README.md`.
dist/ НЕ пересобран — пересобрать exe+pck перед коммитом/деплоем.

## Надписи держатся дольше + фары не светят сквозь ограждения (2026-08-25)
**Проблема 1:** надписи в игре пропадали слишком быстро.
- Лента событий оружия (Main.gd): FEED_LIFETIME 3.4 -> 7.0 с.
- Анонсы по центру (Announcer.gd): BIG_HOLD 6.0 -> 9.0, SMALL_HOLD
  4.8 -> 7.0 с.

**Проблема 2:** свет фар на ночной трассе проходил сквозь ограждения.
Причина — у спотов фар было `shadow_enabled = false` (ставили ради
дешевизны): без тени свет в Godot не заслоняется геометрией вообще.
Включены тени (`shadow_enabled = true`) — споты узкие (30°) и короткие
(22 м), тени для них недороги; ограждения (MeshInstance3D в
TrackBuilder._build_walls) тень отбрасывают по умолчанию. Если на
слабых машинах просядет FPS на ночной трассе — включать тени только
фарам своей машины.

**Файлы:** `scripts/Main.gd`, `scripts/Announcer.gd`, `scripts/Car.gd`.
dist/ НЕ пересобран.

## Лента не «мигает», лазер виден ночью, крыши без окон (2026-08-25)
**«Всё равно сообщения меньше секунды держатся».** Констант мало:
замер (новый тест TestFeedTiming) показал, что в бою события идут чаще
1/с, и при переполнении ленты (FEED_MAX=5) новая запись МГНОВЕННО
вытесняла старейшую — записи жили <1 с. Плюс игра запускалась из
dist/ (exe от 9:11, до правок). Сделано:
- Main.gd: очередь `_feed_pending` + `FEED_MIN_SHOW := 2.5` — запись
  нельзя вытеснить, пока не провисела 2.5 с; лишние события ждут в
  очереди (качает `_pump_feed` из `_process`). `_show_weapon_event`
  разбит: регистрация убийства сразу, показ — через очередь
  (`_add_feed_entry`).
- Новый тест `tools/TestFeedTiming.tscn` + `test_feed_timing.gd`:
  запись прожила 13 с — PASS.

**Лазер не видно в темноте (ночной город).** У LaserFx был
`SHADING_MODE_UNSHADED` + emission — а в unshaded Godot ИГНОРИРУЕТ
emission, луч рисовался тусклым альбедо. Убран unshaded, эмиссия
усилена (energy 6.0) — луч светится сам и зажигает glow ночью.

**Окна на крышах домов.** Трипланарная проекция окон красила и верхнюю
грань коробки здания, а камера смотрит сверху. TrackDecor._build_city:
на каждый дом — тёмная плита-«карниз» чуть шире дома (w+0.6, h=0.5)
поверх крыши, прикрывает окна.

Тесты: TestFeedTiming, TestNeonLap, TestWeapons — все PASS.
**dist/ ПЕРЕСОБРАН** (14:51): exe+pck со всеми правками дня.
**Файлы:** `scripts/Main.gd`, `scripts/LaserFx.gd`,
`scripts/TrackDecor.gd`, `tools/test_feed_timing.gd` (новый),
`tools/TestFeedTiming.tscn` (новый).

## Буст с плиты без таблички и с узким огнём; лента ещё спокойнее (2026-08-25)
Пользователь: «всплывающие сообщения не починил» (вероятно, играл в
exe от 9:11 — сборка ДО правок; см. запись выше) + просьбы по плитам.
- `Car.apply_boost(from_pad := false)`: буст с плиты-ускорителя теперь
  БЕЗ значка над машиной (плиты срабатывают каждые несколько секунд,
  табличка мельтешила) и с УЗКИМ языком огня (scale 0.26–0.4, spread 3
  против 0.45–0.7/5 у турбины-бонуса). Флаг уезжает и по сети
  (NetFx.BOOST args), BoostPad зовёт `apply_boost(true)`.
  Отдельной «узкой» текстуры в Epic Toon FX нет (там белые
  альфа-атласы, наш fire_6x3 оттуда же) — сужено параметрами частиц.
- Лента: FEED_MIN_SHOW 2.5 -> 4.0 с (запись гарантированно видна 4 с).
- Тесты: TestBoostPad, TestWeapons, TestFeedTiming — PASS.
- **dist/ ПЕРЕСОБРАН (15:00)** — игроку запускать `dist/BigHeadRacing.exe`
  от 15:00, старые exe правок не содержат!
**Файлы:** `scripts/Car.gd`, `scripts/BoostPad.gd`, `scripts/Main.gd`.

ВАЖНО (выяснилось позже): пользователь запускает игру через SiteKeeper →
`play.bat` → Godot с `--path` по ИСХОДНИКАМ, а не из dist/. Значит правки
кода подтягиваются при каждом новом запуске игры, а dist/ нужен только
для раздачи другим игрокам. Вкладка Big Head Racing в SiteKeeper
обновлена (итоги дня + пункт «проверить сообщения после перезапуска»).

## НАСТОЯЩИЙ баг анонсов: set_parallel склеивал растворение с паузой (2026-08-25)
Пользователь снова: «все сообщения быстро пропадают». Прошлые тесты
считали УЗЛЫ и врали: узел анонса жил 9 с, но его alpha падала в 0 уже
к 0.7 с — игрок видел табличку меньше секунды. Диагностика: рендер-стенд
`tools/ShotMsgTimeline.tscn` (скриншоты HUD через 0.5/2/4/6/8/10 с
после события) показал полупрозрачную уходящую табличку уже на 0.5 с.

ПРИЧИНА (ГРАБЛИ Godot, запомнить!): в Announcer._show твин строился как
`set_parallel(true) → появление → set_parallel(false) → tween_interval →
set_parallel(true) → растворение`. НО set_parallel(true) присоединяет
следующие твинеры к ТЕКУЩЕМУ шагу — растворение приклеилось к самому
интервалу и шло ОДНОВРЕМЕННО с паузой, а не после неё. Правильно —
точечный `tw.parallel().tween_property(...)` (кладёт один твинер
параллельно предыдущему), как уже сделано в FxKit через chain().

Сделано:
- Announcer._show переписан на parallel()-цепочку: появление (0.26 с) →
  пауза 9/7 с → растворение 0.28 с. Замер: анонс ВИДЕН 9.4 с.
- Новый тест `tools/TestAnnouncer.tscn` + `test_announcer.gd`: меряет
  ВИДИМОСТЬ (alpha > 0.5), а не жизнь узла — регресс больше не пройдёт.
- Рендер-стенд `tools/ShotMsgTimeline.tscn` + `shot_msg_timeline.gd`
  оставлен: скриншоты подтвердили — плашка плотная и на 2 с, и на 8 с.
- Тесты: TestAnnouncer, TestFeedTiming — PASS. dist/ пересобран (15:37).
**Файлы:** `scripts/Announcer.gd`, `tools/test_announcer.gd` (новый),
`tools/TestAnnouncer.tscn` (новый), `tools/shot_msg_timeline.gd` (новый),
`tools/ShotMsgTimeline.tscn` (новый).

## Значок мины был полупрозрачным — перевырезан из исходника (2026-08-25)
У `assets/ui/garage/wg_mine.png` была убита альфа (верх ~142, низ ~30
при норме ~210-245 у соседей): при первом вырезании из листа фон,
видимо, выедался по ЯРКОСТИ и съел светлые части самой картинки.
RGB в прозрачных местах потерян (чёрный) — простое восстановление альфы
не помогло. Сделано: разовый инструмент `tools/recut_mine_icon.gd`
(godot --headless -s) перевырезал плитку мины из
`_STYLE_CORE_A_sheet_of_12_weap_2.jpg` (Rect 28,498,242,242): фон
выеден ЗАЛИВКОЙ ОТ КРАЁВ по близости к серому фона (чёрная мина внутри
не задета), Lanczos до 256x256. Бэкап: `wg_mine.png.bak_20260825`.
После замены PNG прогнан `--import` и пересобран dist (15:57).
Скриншот подтверждает: значки мины в ленте плотные.
ГРАБЛИ PowerShell 5.1: .ps1 без BOM читается как ANSI (кириллица
ломает парсер), а пиксельные циклы с Queue капризны — картинки проще
резать через Godot Image API (get_region/get_pixelv/save_png).
**Файлы:** `assets/ui/garage/wg_mine.png`, `tools/recut_mine_icon.gd`
(новый), `tools/shot_msg_timeline.gd` (мина в слот для скрина).

## Анонсы короче (3/2.5 с) и выше от центра (2026-08-25)
После починки бага пользователь: 9/7 с — слишком долго, нужно 2-3 с,
и по центру таблички перекрывали обзор. Announcer: BIG_HOLD 9 -> 3.0,
SMALL_HOLD 7 -> 2.5; ярусы подняты к верхней кромке BIG_Y 0.24 -> 0.14,
SMALL_Y 0.42 -> 0.26. Порог TestAnnouncer привязан к Announcer.BIG_HOLD
(не хардкод). Замер: виден 3.4 с — PASS; скриншот ShotMsgTimeline:
таблички сверху, машина и трасса не перекрыты. dist/ пересобран (15:41).
**Файлы:** `scripts/Announcer.gd`, `tools/test_announcer.gd`.

## Плавные отбойники и перепады высот — машины меньше цепляют рёбра (2026-08-25)
**Проблема:** «машинки постоянно задевают об рёбра» — у ограждений и на
горке. Причина в дискретности геометрии: стены и полотно — тримеши из
фасеток по ~0.95 м (SAMPLES=768), кузов ловил изломы на стыках; плюс у
профиля горки (smoothstep) скачок кривизны на границах плато — ступенька
вертикального ускорения, подвеска пробивалась, днище било по фасеткам.

**Сделано (`scripts/TrackBuilder.gd`):**
- SAMPLES 768 → 1536: фасетки стен/полотна по ~0.5 м, излом на каждом
  стыке вдвое меньше. Меши статические — стройка и физика переносят легко.
- Профиль горки: подъём/спуск удлинены ~28 → ~42 м (ключи 0.140/0.178/
  0.190/0.228 → 0.122/0.180/0.188/0.246, симметрия вокруг 0.184
  сохранена, трамплины 0.564/0.722 и стартовая прямая не задеты), а
  smoothstep заменён квинтикой `_ease_quintic` (smootherstep): у неё
  нулевая ВТОРАЯ производная на концах — кривизна входит/выходит плавно.
  Пиковая кривизна профиля упала в ~2.4 раза (0.031 → 0.013 1/м).
- Переходы ширины полотна (`half_width_at_ratio`) — тоже на квинтике:
  линия ограждения без изломов на границах ключей ширины.
- ГРАБЛИ (`scripts/TrackDecor.gd`): окно оценки кривизны в `_turn_peaks`
  было В СЭМПЛАХ (w=6) — при удвоении плотности оно сжалось бы вдвое в
  метрах, углы пиков упали бы ниже порога 0.30 и щиты/стрелки перед
  поворотами МОЛЧА пропали бы. Теперь окно в метрах (~5.7 м).
- `tools/screenshot_track.gd`: подножие горки 0.140 → 0.122.

**Замер (TestBumps, детерминированный — два прогона байт-в-байт):**
до: 3 подброса (vy до 1.82), kick 11.8°, body_contacts=9;
после: 2 подброса (vy до 1.42), kick 3.1° (вчетверо мягче), contacts=13.
Остаточные события — у шпильки (t≈0.36), не на горке.

Тесты: test_curve (grass/sand/neon), Lap, Ramp, WallSlide, WallTurn,
WallRecover, CarBounce, SandLap, NeonLap, BoostPad, Minimap, Offtrack,
Respawn, Landing, Dive — все PASS. **dist/BigHeadRacing.pck ПЕРЕСОБРАН
(15:00)** — в нём же правки параллельной сессии (лента/буст); exe не
менялся (скрипты живут в pck). TestLap прогнан из собранного pck — PASS.
**Файлы:** `scripts/TrackBuilder.gd`, `scripts/TrackDecor.gd`,
`tools/screenshot_track.gd`, `dist/BigHeadRacing.pck`.

## Регрессионные тесты (все PASS)
`godot --headless --path . res://tools/TestX.tscn`, X = Ramp | Respawn |
Offtrack | Flip | Lap | Explosion | Heading | WallSlide | AirSpin |
Landing | Dive | Finish | Position | WallTurn | CarBounce | AICorners |
MineDrop | WheelSink | Weapons | WallRecover | Minimap | SandLap |
NeonLap | BoostPad.
Плюс `--script tools/test_curve.gd` (все трассы: grass/sand/neon).
СЕТЕВОЙ стенд отдельно — ему нужен ЖИВОЙ сервер в фоне, причём с
ФИКСИРОВАННОЙ трассой (случайная песчаная перезагрузит сцену стенда):
```
godot --headless --path . res://scenes/Main.tscn -- --server --track=grass
godot --headless --path . res://tools/TestNet.tscn
```
Два клиента разом (проверка слотов) — запустить TestNet дважды.
Полный цикл заезда с перезапуском клиентов — `res://tools/TestReset.tscn`
(тоже нужен живой сервер; идёт 3-6 минут, гонять при правках финиша/лобби).
Гонять после правок физики/трассы.

## Дальше
- Тратить ОПЫТ/уровни: открывать машины, трассы, оружие (опыт уже
  копится — GameState.level_info()).
- Деньги за место в заезде + магазин апгрейдов (двигатель/броня/шины/
  боезапас) между гонками — сердце прогрессии RnRR.
- Подбираемые бонусы на трассе (деньги, ремонт, ускорители-стрелки).
- Несколько трасс (пресеты точек в TrackBuilder), выбор дивизиона.
- Звук: двигатель (pitch от оборотов), выстрел, взрыв, музыка.
- Урон от столкновений машин, имена соперников над машинами.
- Дальше по роадмапу: ИИ-соперники (путь по той же Curve3D + резинка),
  круги/чекпоинты/позиции, оружие (пушка вперёд + мины назад, как в RnRR),
  деньги/апгрейды между заездами, несколько трасс, сплитскрин, музыка.

## Открытые вопросы
- Годовая версия Godot: писал под 4.3 (features в project.godot), 4.4/4.5
  тоже подойдут — при открытии Godot может предложить конвертацию, это ок.
- `Curve3D.closed = true` появился в Godot 4.3 — на более старых версиях
  заменить на дублирование первой точки в конец.
- Арт: пока BoxMesh-заглушки; потом низкополи-модели (Kenney.nl racing kit
  бесплатный — хороший старт) или свои.

## СЕТЬ НЕ СТАРТОВАЛА: на VDS остался протокол 5; фон лобби выкидывался из exe (2026-08-25)
Жалобы: «сетевая игра не начинается», «у игрока по сети нет фона в лобби»,
«обратный отсчёт по сети не идёт».

**Диагностика:**
- Локальный стенд (headless-сервер + TestNet) — PASS: старт сам через
  21.8 с, боты заняли пустые слоты. Значит код сети исправен.
- `ssh root@139.100.234.166 "grep PROTOCOL /opt/bighead/scripts/Net.gd"`
  → **PROTOCOL := 5**. На VDS не заливали код с коммита aa3dc52 (протокол 6:
  4 игрока, синхронный старт, _rx_reset/_rx_track). Из-за сдвига таблицы
  RPC часть вызовов доходит (лобби тикает), часть молча пропадает (hello,
  отсчёт, снимки) — выглядит как «лобби есть, а игра не начинается».
- Фон лобби: в `export_presets.cfg` стоял `exclude_filter` c `*.jpg` —
  он выкидывал из сборки ВСЕ jpg, включая фон лобби
  `assets/ui/garage/backdrop_rust.jpg` (фильтр задумывался против
  «листов»-исходников в корне проекта). Игрок с exe видел лобби без фона,
  из исходников фон был.

**Сделано:**
- `export_presets.cfg`: exclude теперь точечный —
  `*tools/shots*.png` (скриншоты стендов) + маски листов в корне
  (`_STYLE_CORE*.jpg, Vivid_*.jpg, Create_*.jpg, A_seamless_*.jpg,
  Stylised_*.jpg`). Проверено по строкам в pck: backdrop_rust внутри,
  ctex листов и скриншотов не попадают. ГРАБЛИ: пути в фильтре надо
  писать с `*` в начале (`*tools/...`) — matched по полному res://-пути;
  записи-пустышки путей в pck остаются, но данных (.ctex) нет.
- **dist/ пересобран (16:21)** — pck 40.2 → 36.5 МБ. Второму игроку
  ОБЯЗАТЕЛЬНО раздать новый exe+pck (у него старая сборка: и без фона,
  и протокол может быть старый).
- Заливка на VDS из этой сессии заблокирована политикой (scp) — создан
  **`Обновить сервер.bat`** в корне проекта: пакует scripts+scenes+
  project.godot+assets, заливает на VDS, прогоняет `--import`,
  перезапускает службу и печатает `active` + `PROTOCOL := 6`.
  ЗАПУСТИТЬ РУКАМИ. Ключ берёт из `%USERPROFILE%\Desktop\shift-journal`.
- Заодно по просьбам: огонь буста меньше/уже и ниже, «как из трубы»
  (Car.gd: scale 0.22-0.34 у турбины / 0.14-0.22 у плиты, spread 3/2,
  скорость струи 7-10, сопло Vector3(0, 0.3, 1.85) — ЗА бампером, кузов
  3.2 м; раньше половина струи в изометрии ложилась на багажник).
  Следы шин реже: пороги ВЫШЕ дымовых (снос >6.5 и скорость >11, либо
  масло и >6; у дыма 5/8 и 3) и лежат меньше (SkidTrail LIFETIME 7→4,
  FADE 3→2.5).
- Тесты: TestNet (с локальным сервером), TestBoostPad, TestWeapons,
  TestWallSlide, TestLap — PASS. Скриншот ShotFx: пламя узкое, за кормой.
  ГРАБЛИ стенда ShotFx: без аргумента пишет в user://shots — для
  tools/shots_fx запускать с `-- E:/.../tools/shots_fx`, иначе смотришь
  СТАРЫЕ картинки (наступил, потерял три пересъёмки).
**Файлы:** `scripts/Car.gd`, `scripts/SkidTrail.gd`, `export_presets.cfg`,
`Обновить сервер.bat` (новый), `dist/BigHeadRacing.pck`.

## VDS обновлён до протокола 6, сеть проверена на бою (2026-08-26)
Продолжение вчерашнего: scp на VDS прошёл (пользователь явно разрешил).
При деплое наступили на ГРАБЛИ: `godot --import` 40 МБ ассетов на VDS
(1 CPU, 960 МБ, БЕЗ свопа) рядом с работающей службой увёл машину в
OOM-своп — SSH не отвечал ~20 минут (даже с ConnectTimeout=120). Машина
отвисла САМА (перезагрузка через панель не понадобилась), импорт доехал.

Сделано:
- Служба перезапущена с новым кодом: в логе `[net] слотов игроков: 4`,
  на диске `PROTOCOL := 6`.
- Добавлен своп 1 ГБ `/swapfile` + запись в fstab (защита от повторения).
- `Обновить сервер.bat` теперь ОСТАНАВЛИВАЕТ службу перед `--import` и
  запускает после; правило задокументировано в server/README.md.
- Проверка боем: TestNet против 139.100.234.166 — **PASS** (старт сам на
  18.8 с, сервер раздал трассу neon, слот/ростер ок, снимки идут,
  марионетка ровно 23% — норма для живого канала).
Осталось: раздать игрокам новый dist (exe+pck от 25.08 16:21) — у старых
сборок нет фона лобби и старый протокол («не та версия»).
**Файлы:** `server/README.md`, `Обновить сервер.bat`.

## Респавн «через пол-трассы», боты из ниоткуда и боты в лобби (2026-08-26)
**Жалобы:** «при съезде с трассы телепортирует иногда вперёд через половину
трассы»; «начали играть по сетке — появились в разных местах, боты вообще
догнали нас откуда-то сзади»; «в лобби нужно показывать, что боты
подключились, если сетевых игроков не нашлось».

### 1. ГЛАВНОЕ: Curve3D.get_closest_offset прыгал на чужой виток кольца
Отметка машины на оси считалась КАЖДЫЙ РАЗ заново поиском ближайшей точки
по ВСЕЙ кривой. Пока машина на полотне — верно. Но кольцо подходит само к
себе: новый замер `tools/LoopGap.tscn` — участки, разнесённые по ходу гонки
на 150+ м, сближаются до **53 м на классике**, 43 на неоне, 55 на песке.
Значит машине хватает уехать от оси на ~27 м, чтобы «ближайшей» стала точка
на другом конце трассы. А уехать легко: до автовозврата игроку дают 2 с, на
25 м/с это 50 м.
Замер (новый стенд `tools/TestAxisJump.tscn`): машину отвели на 34 м вбок от
отметки 169 м — **глобальный поиск даёт 317 м, прыжок 148 м вперёд**; после
правки возврат ставит машину на 178.7 м, то есть на честные +9 м. От
этой отметки считались:
- респавн (`respawn_transform` = +6 м от неё) — отсюда «телепорт через
  пол-трассы»;
- прогресс/круги/места (`Main._physics_process` берёт разницу отметок за
  кадр) — прыжок сразу давал полкруга прогресса;
- цель ИИ (`Car._ai_control`) — бот правил на чужой участок и приезжал
  «откуда-то сзади»;
- `Car._clamp_heading` — разворот кузова по ЧУЖОЙ касательной (это же и
  старая жалоба «внезапно меняет направление вдоль дороги»);
- `_wall_slide`, `_spin_away_from_wall`, детект вылета, авиаудар.
Почему проверка 2026-08-19 (`tools/check_axis_jump.gd`) прыжков НЕ нашла:
она брала точки у стены, на полотне. Ломается всё именно СНАРУЖИ.

**Сделано.** Отметка стала СОСТОЯНИЕМ машины: `Car.track_offset`, обновляется
раз за кадр физики (`sync_track_offset`, кадровый замок `_offset_frame` —
Main и сама машина зовут её вдвоём), после телепортов сбрасывается
(`reset_track_offset`: спавн, респавн, `destroy`, первый снимок марионетки).
`TrackBuilder.closest_offset_near(pos, prev_off)` устроен как СТРАЖ, а не
замена: пока ответ движка правдоподобен (сдвиг ≤ `OFFSET_MAX_STEP` = 12 м —
за кадр машина проезжает меньше метра), возвращается он, и на полотне не
меняется НИЧЕГО. Прыгнул — ищем сами в окне ±45 м вокруг прошлой отметки
(грубый проход 1.5 м + 6 уточнений делением, точность ~2 см).
Так же добавлены `respawn_transform_at(off)` и `distance_from_axis_at(pos, off)`.
Респавн теперь НЕ дарит 6 м прогресса (`_last_offset` двигается вместе с
машиной).

### 2. Сеть: подсевший к идущему заезду вставал на стартовую решётку
Сцену клиент строит с нуля, его машина стоит на решётке — а по трассе в это
время едет её серверная копия (её вёл бот). Игрок появлялся у старта посреди
заезда («оказались в разных местах»), а у остальных его машина в тот же миг
телепортировалась с трассы назад к решётке («бот приехал откуда-то сзади»).
- `_rx_race_running` получил аргумент: сервер отдаёт позицию и поворот
  машины этого слота, клиент пересаживает свою машину туда.
- Лобби продлевает ожидание до `JOIN_GRACE` = 8 с на каждого зашедшего:
  друзья жмут «играть» не по секундомеру, и зашедший на 20-й секунде чужого
  ожидания больше не подсаживается к уже идущей гонке.

### 3. Лобби показывает ботов
Перед стартом без полного комплекта людей сервер шлёт `_rx_bots(маска)`,
лобби на `BOTS_SHOW` = 2.2 с показывает в пустых слотах МАШИНУ БОТА и подпись
«БОТ» (серо-стальная, метки живых — зелёная/оранжевая — не размываются),
статус: «Больше игроков не нашлось. Пустые слоты заняли боты (N) — поехали!».
Если за эти 2.2 с успевает зайти живой игрок, попытка старта ОТМЕНЯЕТСЯ
(`_start_gen`), `_rx_bots(0)` возвращает слоты в «ждём игрока…».

### ПРОТОКОЛ 6 → 7
Новый @rpc-метод (`_rx_bots`) и новая сигнатура (`_rx_race_running`).
**dist/ ПЕРЕСОБРАН (26.08 10:10)** — exe+pck с протоколом 7.
**НАДО РУКАМИ: запустить `Обновить сервер.bat` (VDS) и раздать игрокам новый
dist — иначе «версии игры не совпадают».**

**Тесты:** новый `TestAxisJump` — PASS. Прогнаны Offtrack, Respawn, Lap,
Landing, Dive, Flip, Ramp, Explosion, Heading, WallSlide, AirSpin, Finish,
Position, WallTurn, CarBounce, AICorners, MineDrop, WheelSink, Weapons,
WallRecover, Minimap, SandLap, NeonLap, BoostPad — все PASS; test_curve
(grass/sand/neon) — PASS. Сетевой стенд TestNet против локального сервера —
PASS (старт сам на 22.6 с). TestBumps — БАЙТ-В-БАЙТ как до правок
(contacts=13, kicks=1, тот же BUMP vy=1.42 в t=0.359), то есть езда по
полотну не изменилась вовсе.
**Грабли:** первая версия `closest_offset_near` ЗАМЕНЯЛА ответ движка своим
поиском — на полотне отметка сдвигалась на сантиметры, линия ИИ уезжала, и
TestBumps показал contacts 13 -> 20, kicks 1 -> 10 (пинок 3.1° -> 9.3°).
Отсюда и правило «сторож, а не замена»: свой поиск включается ТОЛЬКО когда
ответ движка прыгнул.
**Файлы:** `scripts/TrackBuilder.gd`, `scripts/Car.gd`, `scripts/Main.gd`,
`scripts/Net.gd`, `scripts/Lobby.gd`, `scripts/Airstrike.gd`,
`tools/screenshot_lobby.gd`, `tools/test_axis_jump.gd` (новый),
`tools/TestAxisJump.tscn` (новый), `tools/loop_gap.gd` (новый),
`tools/LoopGap.tscn` (новый).

## VDS обновлён до протокола 7; замер джиттера на живом канале (2026-08-26)
Деплой сделан по `Обновить сервер.bat` (руками разобран на шаги: pack → scp →
stop службы → tar → chown → `--import` → start). Результат: служба `active`,
на диске `PROTOCOL := 7`. Коммит `cd3babf` запушен в `origin/Andrei` — второй
разработчик получает новый код и `dist/` (exe+pck от 26.08 10:10).

**ВНИМАНИЕ на будущее: TestNet против ЖИВОГО VDS сегодня шатается.** Девять
прогонов подряд одним и тем же билдом:
| джиттер | рывков назад | итог |
|---|---|---|
| 14.5% | 2 | FAIL |
| 22.5% | 1 | FAIL |
| 13.3% | 0 | PASS |
| 35.9% | 0 | FAIL |
| 26.8% | 0 | PASS |
| 10.4% | 0 | PASS |
| 17.3% | 0 | PASS |
| 36.4% | 0 | FAIL |
| 26.4% | 0 | PASS |
Медиана ~22% — ровно в задокументированной полосе живого VDS (21-31%), но
два прогона вылезли за порог 35%, а «рывков назад» с НУЛЕВЫМ допуском ловил
одиночные пакеты дважды. Это качество канала, а НЕ регрессия: код движения
марионетки (`Car._follow_snapshot`, `net_apply_snapshot`) правками этой
сессии не тронут вовсе, а тот же билд против ЛОКАЛЬНОГО сервера даёт 5.7 и
6.0% при нуле рывков (стенд PASS оба раза).
Порог трогать НЕ СТАЛ намеренно: подкрутить критерий, чтобы стенд позеленел,
— это спрятать данные. Если шаткость надоест, честный ход — считать метрику
по МЕДИАНЕ нескольких прогонов, а не по одному, и допускать 1 рывок назад на
400 кадров. Решать пользователю.

## Дёрганые соперники = ФРИЗЫ КЛИЕНТА на постройке моделей; кап удара марионетки; провал под полотно (2026-08-26)
Жалобы: «все игроки кроме меня едут дёргано; если при рывке врезаются в
меня — огромный импульс, вылетаю за трассу»; «перевернулся, ехал под
асфальтом колёсами вверх, респавн непонятно где».

**Диагностика по шагам (все замеры — новые, оставлены в коде):**
1. Ping до VDS: 1 мс, разброс 0. Канал НЕ виноват — вчерашнее объяснение
   «дрожание канала» было ошибкой.
2. В tools/test_net.gd добавлен замер РОВНОСТИ ПОТОКА СНИМКОВ. ГРАБЛИ:
   считать приходы опросом из _physics_process НЕЛЬЗЯ — опрос 60 Гц и
   снимки 60 Гц сливаются (алиасинг), поток «занижался» до 43/с даже на
   локалхосте. Счёт перенесён в сам Main._rx_state (_state_seen/_gap_sum).
3. В Main._physics_process добавлен ВАЧДОГ фризов (печать при кадре >250 мс,
   строка `[freeze] ...`). Он и указал виновника: СЕРВЕР НЕ ФРИЗИТ ВООБЩЕ
   (поток 60.0/с, худшая пауза 23-70 мс), а КЛИЕНТ замирал на 0.3-2.2 с.
4. Таймер в CarModelLibrary.build (строка `[slow] ...`): КАЖДЫЙ вызов
   стоил 300-600 мс — ради одной машины инстанциировался ЦЕЛЫЙ GLB-пак
   (35+ машин, сотни узлов). Раздача ростера = до 8 вызовов подряд
   (4 машины заезда + 4 подиума лобби) → клиент вставал на 1-2 с, все
   марионетки на экране «прыгали» разом, а прыгнувшая В ИГРОКА марионетка
   выбивала его дичайшей депенетрацией.

**Сделано:**
- CarModelLibrary: кэш инстанциированных паков (_pack_cache, живёт до
  конца игры, src больше НЕ освобождается — только читается). Осталась
  одна медленная сборка за сессию — самая первая, при загрузке сцены.
  Итог замера: [freeze] исчезли, поток 60.0/с, джиттер марионетки
  3.5-7.4% (было 20-38%) — лучший результат за всю историю стенда.
- Car._physics_process: кап депенетрации от марионетки теперь смотрит
  ТЕКУЩИЙ список контактов, а не флаг с прошлого кадра — прежде самый
  первый (самый дикий) импульс от прыгнувшей марионетки проходил мимо
  капа. Ровно это игрок и ловил.
- Main._check_recovery: ПРОВАЛ ПОД ПОЛОТНО (в плане — на дороге, по
  высоте — на 1+ м ниже неё) возвращает машину сразу. Раньше не ловилось
  ничем: вылет меряет расстояние в плане, под дорогой оно ~0. Новый стенд
  tools/TestFallThrough.tscn — PASS.
**Файлы:** scripts/CarModelLibrary.gd, scripts/Car.gd, scripts/Main.gd,
tools/test_net.gd, tools/test_fallthrough.gd (новый),
tools/TestFallThrough.tscn (новый).

## Марионетки: не твёрдые для решателя + визуальная интерполяция модели (2026-08-26)
Повторная жалоба после кэша моделей: «всё равно все едут дёргано и
подталкивают меня». Два оставшихся источника, оба лечатся архитектурно:

**1. «Подталкивают» — решатель физики.** Марионетка — замороженное
кинематическое тело, ТЕЛЕПОРТИРУЮЩЕЕСЯ к снимкам. Шагнув в чужой кузов,
решатель выдавливал того депенетрацией; кап 5 м/с резал хвост, но не
корень. Теперь машина-марионетка вообще НЕ ТВЁРДАЯ для других машин
(net_make_puppet ставит add_collision_exception_with на все машины;
пара пропускается, если исключение есть хотя бы у одной стороны).
Аркадный толчок остался — его давала и даёт СВОЯ логика
_bounce_off_cars; для марионеток «касание» меряется капсулами вдоль
кузова (отрезок ±0.9 м по курсу, контакт при сближении осей < 1.7 м),
дальше — общий код рикошета (расталкивание, closing-импульс ≤ 8 м/с,
закрутка). Оружию исключения не мешают: снаряды ловят по слою 4.
Бонус: на СЕРВЕРЕ боты больше не получают депенетрацию от марионеток
игроков — их движение в снимках тоже стало чище.
Новый стенд tools/TestPuppetPush.tscn: марионетка телепортируется
вплотную и «едет» в жертву — растолкало (зазор 1.0 → 6.7 м), пик
скорости жертвы 7.3 м/с (кап дикости 12) — PASS.

**2. «Едут дёргано» — шаг физики против кадра рендера.** Тело марионетки
шагает 60 Гц; на реальном рендере (fps плавает) это дёргается даже при
идеальном потоке снимков (стенды headless это НЕ ловили — там нет
рендера). Классическое лечение фиксированного шага: модель (CarModel)
отвязана от тела (top_level) и каждый кадр рендера ставится МЕЖДУ двумя
последними положениями тела по Engine.get_physics_interpolation_fraction().
Физика/оружие/снимки видят тело, глаз — модель. Телепорты (>4 м/тик) не
интерполируются (мигнуть, а не проехать призраком). Выход из марионетки
(welcome/уход игрока) возвращает модель под тело: net_visual_reset.
ВАЖНО: сам _follow_snapshot НЕ трогался (переписывался дважды, оба раза
хуже — см. историю в Car.gd).

Тесты: PuppetPush (новый), FallThrough, CarBounce, Offtrack, Respawn,
Flip, AxisJump, Explosion, Lap, Weapons, TestNet — PASS; TestBumps байт
в байт. Протокол не менялся (7).
**Файлы:** scripts/Car.gd, scripts/Main.gd, tools/test_puppet_push.gd
(новый), tools/TestPuppetPush.tscn (новый).

## «Фризы клиента» оказались артефактом стендов; на сервере отключена косметика (2026-08-26)
Продолжение охоты за «все едут дёргано». После кэша моделей стенды всё
равно ловили фризы 0.3-2.2 с посреди гонки. Разбор по шагам:
- вачдог «между чем» (метки физика/машина/рендер в кадре): точка затыка
  ГУЛЯЛА между прогонами — почерк внешней блокировки, не своей функции;
- оружие, destroy, респавн — все с таймерами, все молчат (0-1 мс);
- фризит и ОФФЛАЙН (TestLap), сеть ни при чём; AICorners — нет; фоновые
  Godot убиты — фризит всё равно;
- решающий опыт: TestLap с `2>/dev/null` — НОЛЬ фризов в трёх прогонах
  подряд. Виновник: headless dummy-рендер на каждый созданный меш сыпет
  в stderr «Parameter m is null» с бэктрейсом; ТЫСЯЧИ строк, уходя в
  пайп/консоль, синхронно стопорят главный поток на 1-2 с.
**ГРАБЛИ на будущее: headless-стенды запускать с `2>NUL` (cmd) или
`2>/dev/null` — иначе замеры времени кадра ВРУТ.** В настоящей игре
(живой рендер) этих ошибок нет вовсе.

Боевое следствие: на VDS сервер создавал ту же косметику (кольца,
вспышки, искры, лазер, дым) — ненужные меши + тот же спам в journald
(rate limit уже ловили 2026-08-25) → реальные микро-затыки сервера.
Сделано: все static-точки входа эффектов (FxKit.ring/smoke_burst/
snow_burst/stars_burst/lightning_burst/confetti_burst/scorch/fire_burst/
muzzle_flash, FlashFx.spawn, SparksFx.spawn, LaserFx.spawn) выходят
сразу на выделенном сервере (FxKit._skip — по ключу `--server`, как
TrackBuilder._headless_server; косметика самой машины уже была
отключена в Car._ready). Замер: лог сервера за заезд — 30 строк вместо
тысяч, фризов 0; поток снимков у клиента 60.0/с, худшая пауза 26 мс.
**Файлы:** scripts/FxKit.gd, scripts/FlashFx.gd, scripts/SparksFx.gd,
scripts/LaserFx.gd (+ диагностика в Main.gd/Car.gd: вачдоги, таймеры).

## Места, дёрганье, чужая гонка; протокол 8 (2026-08-26/27)
Жалобы: «в HUD МЕСТО 2/4, а на баннере ФИНИШ! МЕСТО 3 ИЗ 4»; «подёргивания
у противников остаются»; «игра закрывается 6 сек»; «после нового запуска
началась не новая игра, а продолжилась другая».

### 1. Места расходились ПО ПОСТРОЕНИЮ
HUD сравнивал накопленный путь всех машин, баннер выдавал место по порядку
пересечения линии. Доехавший ОСТАНАВЛИВАЕТСЯ, его путь замирает, и едущий
сзади «обгонял» его в HUD — хотя первое место уже занято.
**Сделано:** `_place_of` теперь считает финишировавших занявшими свои места
навсегда (`_finish_order`), остальные распределяются ниже по прогрессу.
Клиент заполняет `_finish_order` из `_rx_car_finished` — HUD и баннер
считают ОДНО И ТО ЖЕ число. Новый стенд `tools/TestPlaceFinish.tscn`
(арифметика мест без четырёхкруговой гонки) — PASS.

### 2. Дёрганье: дрожал ВЕСЬ КАДР, а не соперники
Оставшаяся причина после кэша моделей и потока 60/с: тело шагает ровно
60 Гц, кадр рендера идёт своим темпом, и на близких частотах два ритма
плывут — то за кадр рендера проходит два шага физики, то ни одного. Своя
машина стоит в центре и дрожит ВМЕСТЕ с камерой (кажется ровной), а трасса
и соперники дёргаются. Отсюда и формулировка «все едут дёргано, кроме меня».
**Сделано:**
- визуальная интерполяция моделей распространена со ВСЕХ машин, а не только
  марионеток (`Car._track_visual`, `visual_origin()`); телепорт (>4 м/тик)
  по-прежнему не интерполируется;
- КАМЕРА переведена из `_physics_process` в `_process` и следует за
  `visual_origin()`; сглаживание кадронезависимое (`1 - exp(-k·dt)`);
- стрелка-указатель и значок эффекта стали top_level и ставятся по
  `visual_origin()` — иначе машина уползала бы из-под собственной стрелки.
`_follow_snapshot` и физика НЕ тронуты: TestBumps байт в байт (13/1).
Новый стенд `tools/TestMarkerFollow.tscn` (стрелка и значок над машиной на
ходу) — PASS; он же сразу поймал мигание стрелки в начале координат на
первом кадре (исправлено в `_attach_marker`).

### 3. Чужая гонка вместо новой — ДВЕ причины, обе на сервере
- `[net] старт заезда, игроков: 0`: после ухода последнего игрока ожидание
  лобби дотикивало в пустоту, и сервер начинал заезд, в котором нет ни
  одного человека; зашедший подсаживался к этой ничьей гонке. Теперь
  `_maybe_start` не стартует без людей, `_on_peer_left` гасит ожидание.
- Подсадка к идущему заезду (протокол 7) была без срока давности. Теперь
  позже `JOIN_LATE_MAX` = 25 с игрок в чужую гонку НЕ влезает, а ждёт в
  лобби следующей (`_rx_lobby_wait_next`, флаг `_wait_next_race`: баннер
  чужого финиша и опыт ему не начисляются, автовозврат не дёргает машину).
Новый стенд `tools/TestLateJoin.tscn` — PASS. Проверка на живом сервере:
после деплоя простоял 16 часов и НИ ОДНОГО пустого заезда не начал.

### ПРОТОКОЛ 7 → 8 (новый @rpc `_rx_lobby_wait_next` сдвигает таблицу)
dist пересобран 26.08 16:58, VDS обновлён пользователем (на диске
`PROTOCOL := 8`, служба active).

### 4. «Игра закрывается 6 сек» — НЕ ВОСПРОИЗВЕЛОСЬ, нужен лог
В логе клиента (`%APPDATA%\Godot\app_userdata\Big Head Racing\logs\`) выход
шёл сразу после `[net] сервер начал новый заезд — перезагружаем сцену`, но
это оказалось ложным следом: строки «Leaked/Pages in use at exit» — обычный
хвост ШТАТНОГО выхода этого проекта (кэш паков живёт вне дерева), они же
печатаются при нормальном quit стенда.
Проверено: TestReset с ЖИВЫМ рендером против VDS — три перезагрузки подряд,
память 76→97→84→85 МБ, вылета нет. Стенд заодно починен: он рапортовал PASS
за перезагрузку от `_rx_track` («сервер выбрал другую трассу») и за начало
нового заезда — теперь требует, чтобы гонка шла в ПРОШЛОМ запуске сцены.
Добавлена улика на будущее: `Main._mem_note()` печатает память/узлы/объекты
при готовности сцены и при каждом reset.
**НАДО:** лог с того ПК, где игра закрывается (папка выше, файл godot.log).

### Мелочь: стенд ускорителей врал
TestBoostPad падал через раз и на СТАРОМ коде: боты получают случайное
стартовое оружие и сбивали машину ракетой до плиты. Теперь стенд разоружает
ботов на время замера — три прогона подряд дают одинаковые 49.6 м/с.

**Тесты:** Bumps (13/1, байт в байт), Lap, Position, Finish, Respawn,
Offtrack, WheelSink, CarBounce, PuppetPush, FallThrough, Flip, AxisJump,
Explosion, Weapons, Minimap, Landing, Ramp, Dive, Heading, WallSlide,
AirSpin, WallTurn, MineDrop, WallRecover, SandLap, NeonLap, BoostPad,
PlaceFinish (новый), MarkerFollow (новый), AICorners — PASS. TestNet против
локального сервера — PASS (поток 60.1/с, джиттер марионетки 7.9%, рывков 0).
TestReset и TestLateJoin — PASS.
**ГРАБЛИ:** стенды под нагрузкой (рядом крутится локальный сервер) дают
ложные FAIL по таймингам — SandLap упал в общем прогоне и прошёл в одиночку.
**Файлы:** `scripts/Main.gd`, `scripts/Car.gd`, `scripts/IsoCamera.gd`,
`scripts/Net.gd`, `tools/test_place_finish.gd` + `TestPlaceFinish.tscn`
(новые), `tools/test_marker_follow.gd` + `TestMarkerFollow.tscn` (новые),
`tools/test_late_join.gd` + `TestLateJoin.tscn` (новые),
`tools/test_reset.gd`, `tools/test_boost_pad.gd`, `dist/` (exe+pck 16:58).

## Сквозной проезд, корень дёрганья (дробь = 0), лобби 5 с (2026-08-27)
Пользователь проверил в бою и вернул три пункта: «столкновений нет в сетевом
режиме, проезжаю сквозь машины», «боты по сети всё равно дёргаются», «у
сетевого игрока после перезагрузки пропадает uikit». Плюс просьба: ожидание
игроков — 5 с. Урок сессии: НЕ рапортовать «устранено», пока стенд не
воспроизвёл жалобу и не позеленел на исправлении.

### 1. Сквозь машины — прямое следствие вчерашних «нетвёрдых марионеток»
Новый стенд tools/TestPuppetSolid.tscn (таран стоящего соперника на
14 м/с) воспроизвёл сразу: машина проезжала на 12.5 м ЗА центр соперника,
зазор центров 0.04 м. Аркадный толчок _bounce_off_cars геометрию не
заменяет. **Сделано:** марионетки снова ТВЁРДЫЕ (net_make_puppet ставит
_set_solid_to_cars(true); исключения решателя убраны). От «дикого импульса
прыгнувшей марионетки» защищает кап депенетрации 5 м/с за кадр (смотрит
текущие контакты), а корень тех рывков — фризы постройки моделей — давно
вылечен кэшем. Капсульное «касание» в _bounce_off_cars ОСТАВЛЕНО как
дублёр (solver-контакт на рывке канала может мигнуть мимо кадра; дубль
отсеивает now[id]). TestPuppetSolid PASS, TestPuppetPush PASS (кап жив),
TestBumps байт в байт (13/1).

### 2. Дёрганье: ВСЯ вчерашняя интерполяция была холостой
Новый стенд tools/DbgFraction.tscn показал:
**Engine.get_physics_interpolation_fraction() в Godot 4.3 всегда возвращает
0**, пока в project.godot не включено physics/common/physics_interpolation.
То есть модель просто рисовалась на ПРОШЛОМ кадре физики — вчерашняя
«плавность» ничего не сглаживала (и стенд MarkerFollow этого не ловил:
он сравнивал стрелку с visual_origin, а врали ОБА одинаково).
**Сделано:**
- в project.godot включён physics/common/physics_interpolation (автодвижение
  3D-узлов в 4.3 он НЕ включает — 3D-интерполяция появилась в 4.4; меняется
  только дробь, физика не тронута — вся регрессия прогнана);
- новый замер КАРТИНКИ tools/DbgVisualJitter.tscn (живой рендер, шаг
  тела/модели против скорости из снимка, на 60 fps): тело 10.2%, модель
  10.5% — на ровном vsync 60/60 узкое место НЕ биение кадров, а неровность
  шагов самого тела от прихода снимков;
- подтяжка марионетки усилена: k 0.35 -> 0.2 на кадре (основание 0.65 ->
  0.80) + упреждение LEAD = 0.03 c по скорости снимка, компенсирующее
  половину хвоста сглаживания. Подбор по замерам: 0.80 без LEAD — 6.8%,
  LEAD 0.06 раскачивал обратно до 9.8%, LEAD 0.03 — 7.2% при вдвое меньшем
  запаздывании. _follow_snapshot структурно НЕ переписан (третьей переделки
  нет — только константы), защита «назад не едем» и кап возраста на месте.
**Замеры после:** TestNet локально 0.9-2.4% (было 7.9, эталон одиночки 0.3);
против ЖИВОГО VDS 2.4-7.7% при вчерашней медиане ~22% и выбросах до 36%.
Один прогон из пяти поймал 1 рывок назад из 410 кадров — нулевой допуск
стенда, известная шаткость (см. запись 26.08), порог не трогал.

### 3. «Пропадает uikit после перезагрузки» — НЕ ВОСПРОИЗВЕЛОСЬ (4 сценария)
tools/dbg_reload_hud.gd (живой рендер, скриншоты): исходники + локальный
сервер, exe из dist + локальный сервер; после _rx_reset И после смены
трассы (_rx_track); проверены и HUD гонки, и ЛОББИ между заездами — всё
на месте (снимки смотрел глазами). Возможно, жалоба со вчерашнего билда.
**НАДО от игрока: godot.log или скриншот** (%APPDATA%\Godot\app_userdata\
Big Head Racing\logs).

### 4. Лобби: ожидание игроков 20 -> 5 с (просьба пользователя)
JOIN_GRACE тоже 8 -> 5 (продление длиннее самого ожидания выглядело бы как
«зашёл второй — ждать стало дольше»). Синхронный старт по hello остаётся —
былую проблему «у второго грузится дольше» короткое лобби не вернёт.

Протокол НЕ менялся (8): новых @rpc нет, вчерашние клиенты совместимы.
**dist пересобран 27.08 09:48. НАДО РУКАМИ: «Обновить сервер.bat» (на VDS
до сих пор вчерашний код — там лобби 20 с, нетвёрдые марионетки на ботах)
и раздать игрокам dist 09:48 (иначе у них останется сквозной проезд).**

**Тесты:** вся регрессия — PASS (Bumps 13/1 байт в байт; Lap, Position,
Finish, Respawn, Offtrack, WheelSink, CarBounce, PuppetSolid (новый),
PuppetPush, FallThrough, Flip, AxisJump, Explosion, Weapons, Minimap,
Landing, Ramp, Dive, Heading, WallSlide, AirSpin, WallTurn, MineDrop,
WallRecover, SandLap, NeonLap, BoostPad, PlaceFinish, MarkerFollow,
AICorners). Сетевые: TestNet локально и против VDS (5 прогонов, 4 PASS /
1 FAIL по нулевому допуску рывков), TestLateJoin с коротким лобби PASS,
TestReset полный цикл (2 перезагрузки, заезд был) PASS, прогон цикла на
exe из dist — HUD и лобби целы.
**Файлы:** scripts/Car.gd, scripts/Main.gd, project.godot,
tools/test_puppet_solid.gd + TestPuppetSolid.tscn (новые),
tools/dbg_fraction.gd + DbgFraction.tscn (новые),
tools/dbg_visual_jitter.gd + DbgVisualJitter.tscn (новые),
tools/dbg_reload_hud.gd + DbgReloadHud.tscn (новые), dist/ (09:48).

## Деплой 27.08 и замер после него (2026-08-27, продолжение)
Пользователь: «сам всё делай». Сделано:
- VDS обновлён по шагам «Обновить сервер.bat» (scp → stop → tar → chown →
  --import → start). Проверено на диске: PROTOCOL := 8, LOBBY_WAIT := 5.0,
  physics_interpolation в project.godot. Служба active, память 336/960 МБ,
  load 0.11, вачдог фризов сервера молчит.
- Дымовой TestNet против боевого сервера: заезд стартует на 10-й секунде
  (лобби 5 с + показ ботов 2.2 + отсчёт) — короткое ожидание работает.
- Джиттер против VDS после деплоя: 6.6-37.8% по прогонам, при этом «худшая
  пауза» ПОТОКА СНИМКОВ 133-408 мс. Разобрано: ICMP-пинг 0 мс / 0 потерь,
  клиентский вачдог фризов молчит, серверный тоже — паузы живут в самом
  UDP-потоке (пачки потерь/переупорядочения на unreliable_ordered). Это та
  же полоса канала, что задокументирована 26.08 (10-36%); утренняя серия
  2.4-7.7% была тихим окном. Клиентское сглаживание проверяемо улучшило
  контролируемый замер (локально 7.9 -> 0.9-2.4%), но дыру канала в 0.4 с
  никакая подтяжка не спрячет — машина честно замирает и догоняет.

## «Теперь ещё БОЛЬШЕ дёргаются»: твёрдость вешала физику сервера (2026-08-27)
Жалоба пользователя после утреннего деплоя — и она была верной: я сделал
хуже. Разбор и почему это не поймали стенды:

### Причина: GodotPhysics зависает на «кинематика-телепорт + динамика»
Утренняя правка «марионетки снова твёрдые» дала на сервере паузы кадра
физики 300-430 мс (вачдог: `[freeze-где] машина -> рендер: 324 мс`), причём
в КАЖДОМ заезде. Для клиента это дыра в потоке снимков — все чужие машины
замирают разом. Прежние стенды этого не видели: TestPuppetSolid/PuppetPush
проверяют ОДИН контакт за прогон, а виснет решатель на длящемся контакте
четырёх машин в заезде. Поймано бисекцией: сервер с исключениями — паузы
34-51 мс, сервер без исключений — 300-430 мс.

### Решение: непроницаемость БЕЗ решателя
Исключения марионеток вернулись (решатель их пару не видит вовсе), а
сквозной проезд закрывает РУЧНОЕ выдавливание в `_bounce_off_cars`: при
перекрытии капсул наша машина отодвигается позиционно, шаг выдавливания
держит темп сближения (не меньше 0.25 м/кадр — иначе полный ход туннелирует
между кадрами). Скорость выдавливание НЕ трогает: первая версия гасила
сближение и сразу уронила TestPuppetPush (жертву разгоняло дичее 12 м/с) —
толчок и закрутку по-прежнему даёт общий рикошет.
TestPuppetSolid ужесточён до тарана 30 м/с — PASS (машина встаёт в 2.2 м
от центра соперника вместо проезда на 12.5 м за него).

### Заодно найдена НАСТОЯЩАЯ причина «боты дёргаются» на живом канале
Печать `[gap]` в Main._rx_state (новая) показала: до VDS пакеты НЕ теряются
(поток 59.8 из 60), но приходят ПАЧКАМИ — за 16 с замера 11 дыр по 100-400
мс. На капе экстраполяции 0.12 с марионетка в каждую дыру замирала и
догоняла. MAX_EXTRAP пересмотрен 0.12 -> 0.40 (обоснование и таблица
замеров — в комментарии у константы). Замер картинки на живом рендере
(доля кадров, где картинка проехала меньше пятой части положенного):
  было 13.4% дрожания и 8-10 замираний на 600 кадров,
  стало 5.2-10.6% дрожания и 1-2 замирания.
Старые замеры, на которых стоял 0.12, делались при фризах сервера — они
мерили догон после ЕГО зависаний, а не поведение канала.

### Урок (главное)
Стенд на ОДИН контакт не заменяет наблюдения за сервером в заезде. Теперь
после каждой правки физики марионеток смотреть `grep freeze` в логе сервера
и `[gap]` у клиента, а не только «PASS» стендов.

**Тесты:** вся регрессия PASS (Bumps 13/1 байт в байт, PuppetSolid на 30 м/с,
PuppetPush, CarBounce, FallThrough, Lap, Position, Finish, Respawn, Offtrack,
Flip, AxisJump, Explosion, Weapons, Minimap, WheelSink, Landing, Ramp, Dive,
Heading, WallSlide, AirSpin, WallTurn, MineDrop, WallRecover, SandLap,
NeonLap, BoostPad, PlaceFinish, MarkerFollow, AICorners). Локальный сервер:
ноль фризов, поток 60.0/с, худшая пауза 34-42 мс. VDS: обновлён, ноль
фризов, CPU 96% простаивает.
**Файлы:** scripts/Car.gd, scripts/Main.gd, tools/dbg_visual_jitter.gd
(замирания + печать дыр), tools/test_puppet_solid.gd (таран 30 м/с),
dist/ (exe+pck 27.08 11:28), VDS обновлён.

## Рывки ботов добиты: воспроизведение снимков с отставанием (2026-08-27)
Пользователь: «стало лучше, но боты всё равно движутся с рывками». Верно:
экстраполяция ГАДАЕТ в дырах канала — машина едет по прямой по старой
скорости, на повороте это враньё вбок, и приехавшая пачка дёргает её
коррекцией. Сколько ни тюнингуй кап — гадание останется гаданием.

### Сделано: клиентская марионетка едет ПО ЗАПИСИ
Car._follow_buffered (только клиент; сервер остаётся на экстраполяции —
ему отставание вредно, по позициям марионеток бьёт оружие):
- каждому снимку присваивается время на СИНТЕТИЧЕСКОЙ шкале (+1/60 к
  прошлому) — пачка, слипшаяся в канале, раскладывается обратно в ритм
  отправки сервера;
- воспроизведение идёт с отставанием BUF_DELAY = 0.35 с и адаптивной
  скоростью ±15% (буфер тает — плеер замедляется, растягивая настоящие
  данные на дыру; разбух — догоняет);
- перепривязка шкалы, если плеер обогнал запись (пауза дубликатов у
  стоящей машины, дыра аплинка владельца) — без неё буфер жил в режиме
  вечного недобора, гистограмма темпа расползалась на 0.6-1.4 (ловилось
  новой печатью «темп:» в DbgVisualJitter);
- недобор буфера — короткая экстраполяция (кап MAX_EXTRAP) и честное
  замирание; телепорт >8 м и правило «назад не едем» на месте;
- contact_velocity марионетки на клиенте — скорость воспроизводимого
  КУСКА записи (по нему едет картинка и от него честно считать толчки);
  та же правка в замерах test_net/DbgVisualJitter — сверка с последним
  пакетом штрафовала бы буфер за само его устройство (ошибка замера 24.08,
  из-за которой буфер тогда забраковали).

### Замеры (дрожание картинки / замираний на 600 кадров, живой рендер):
  вчерашний код:               13.4% / 8-10
  утренняя экстраполяция 0.40:  5-13% / 1-16 (нестабильно)
  буфер 0.20:                   7-14% / 3-11 (дыры длиннее отставания)
  буфер 0.35:                2.6-7.4% / 0-1  ← в бою
Финал на exe из dist против VDS: 3.4 / 3.5 / 6.9%, замираний 0/0/0.
TestNet против VDS: 2.5 / 2.8 / 3.7%, рывков назад 0 — лучшие числа за
историю проекта (вчера медиана ~22%). Вся регрессия PASS, TestBumps байт
в байт. Плата: соперник на экране позади себя настоящего на ~0.35 с.

**Файлы:** scripts/Car.gd (буфер), tools/test_net.gd,
tools/dbg_visual_jitter.gd (метрика по contact_velocity, гистограмма
темпа), dist/ (13:01), VDS обновлён (BUF_DELAY на диске проверен).

## Места на финише, значок эффекта, параллельные заезды (Rooms); протокол 10 (2026-08-27)
Жалобы: «места при финише слева и по центру разные», «пропала индикация
действующего оружия/эффекта над машиной». Задание: «сервер должен держать
столько заездов, сколько игроков подключится» (пятый и опоздавшие ждали).

### 1. Места: клиент НЕ заполнял _finish_order
Правка 26.08 обещала «клиент заполняет _finish_order из _rx_car_finished» —
но в коде этого НЕ БЫЛО (TestPlaceFinish гонял только оффлайн-арифметику).
По сети слева HUD продолжал считать место по прогрессу, а баннер брал
место сервера. Сделано: _rx_car_finished кладёт машину в _finish_order по
месту сервера (по индексу, не append — дубль не собьёт). PlaceFinish PASS.

### 2. Значок эффекта: сервер паковал _status_shown, который живёт только у спрайта
С 24.08 (косметика на сервере отключена) _build_status_icon на сервере не
зовётся, _tick_status_icon выходит по _status_icon == null — _status_shown
на сервере НАВСЕГДА -2, и в снимке (flags[f+2]) значок не ехал НИКОМУ:
ни магнит над своей машиной, ни буст соперника. Сделано:
- Car.status_icon_kind() — чистая функция «какой значок действует» (без
  Sprite3D); сервер пакует её (кодировка та же, <2 = пусто, протокол цел);
- таймер _status_time тикает в _tick_effects (физика, есть и на сервере),
  из _tick_status_icon декремент убран (раньше на сервере он не гас бы).
Стенды MarkerFollow, Weapons (значки магнита/буста) — PASS.

### 3. Параллельные заезды: «комнаты» (scripts/Rooms.gd — новый)
Сервер на 9977 — «ворота» и первый заезд. Игроку без места (4 слота заняты
ИЛИ опоздал позже JOIN_LATE_MAX — случай со скриншота «Заезд уже идёт — в
него не влезть») ворота шлют _rx_redirect(порт): клиент прозрачно
переподключается к комнате — такому же headless-процессу (--server --room
--port=N) на порту 9978..9977+ROOMS_MAX, который ворота поднимают сами.
- Реестр — визитки user://rooms/<порт>.json ({port, players, joinable,
  ts}), каждый сервер пишет свою раз в секунду; устаревшие (>5 с)
  игнорируются и прибираются. Файлы вместо второго транспорта — нарочно.
- ROOMS_MAX = 3: VDS 1 CPU/960 МБ, процесс ~113-145 МБ и ~22% ядра —
  итого до 4 одновременных гонок (16 игроков). Комнаты не плодят процессы
  сами; пустая комната без гонки гасится через 120 с (ROOM_IDLE_EXIT).
- Net: пир сверх слотов теперь «гость» (guests, не отвергается), ключи
  --port=N/--room, create_server до PLAYER_SLOTS+8, redirect_hops (кап 3
  прыжка — защита от пинг-понга; сброс в _rx_welcome и при ручном входе).
- Фолбэки как раньше: комнат нет — опоздавший со слотом ждёт следующего
  заезда (_rx_lobby_wait_next), гость без слота получает внятный отказ.
- Порт комнаты в net.cfg НЕ пишется (remember=false) — «дом» игрока
  остаётся воротами. Main ловит join_failed (комната умерла между
  визиткой и подключением) — раньше клиент завис бы в лобби молча.
### ПРОТОКОЛ 9 -> 10 (_rx_redirect — новый @rpc, сдвиг таблицы)
dist пересобран (27.08 13:45), VDS обновлён и проверен вживую: 6 клиентов
-> 4 в заезд ворот, 2 гостя уехали в комнату 9978, поднятую самим VDS
(журнал: [rooms] поднимаем комнату... pid 78865; память 395/960 МБ).
Комнаты живут в cgroup службы — systemctl stop гасит и их.

**Стенды:** tools/TestRooms.tscn + test_rooms.gd (НОВЫЕ; клиент печатает
«ROOMS CLIENT: port=... slot=...», ключ --stay — сидеть в заезде; адрес
аргументом). Прогоны: 6 клиентов локально -> 4x9977 + 2x9978; сценарий
опоздавшего (45 с после старта) -> уехал в 9978. TestLateJoin ПЕРЕПИСАН
под новое поведение (ждёт перенаправления, а не wait_next) — PASS.
TestNet против локальных ворот — PASS (джиттер 0.8%, поток 59.8/с).
PlaceFinish, MarkerFollow, Weapons, Bumps (13/1 байт в байт) — PASS.
ВНИМАНИЕ стендам: у test_rooms/TestLateJoin сцена перезагружается при
редиректе — «_ready» должен не переподключаться, если Net.is_online().
**Файлы:** scripts/Rooms.gd (новый), scripts/Net.gd, scripts/Main.gd,
scripts/Car.gd, tools/test_rooms.gd + TestRooms.tscn (новые),
tools/test_late_join.gd (переписан), server/README.md (раздел про
комнаты), «Обновить сервер.bat» (текст PROTOCOL := 10), dist/.

**Проверка гашения на VDS:** комната 9978 сама вышла ровно по таймеру —
в журнале «[rooms] комната на порту 9978 пуста 120 с — гасимся», визитка
9978.json удалена, остался только 9977.json ворот. Память вернулась.

## Соперник-игрок ровный (метки тиков, протокол 9) + комнаты (протокол 10)
## (2026-08-27, продолжение)
Жалоба: «боты едут ровно, а человек по сети дёргается». Верно по построению:
бот — чистый поток 60/с с сервера, машина игрока — два прыжка (владелец →
сервер → мы), и сервер ретранслирует «последний присланный» снимок СВОИМ
тиком. Часы владельца и сервера бьются: состояния в ретрансляции то
пропускаются (два пакета владельца между тиками сервера), то дублируются —
ровная шкала буфера «+1/60 на снимок» для такого потока ВРЁТ. Воспроизведено
на локалхосте двумя клиентами: соперник-игрок 13.5% дрожания при 3.5-6% у
ботов. Попытка ретранслировать сглаженное тело серверной марионетки дала
лишь 8.8% — полумера.

**Сделано (протокол 9): метка тика автора в каждом состоянии.**
- владелец шлёт 11-м числом свой Engine.get_physics_frames();
- сервер хранит метку (_snap_stamp) и ретранслирует её вместе с сырым
  состоянием; боту метка — тик самого сервера;
- клиентский буфер строит шкалу ПО МЕТКАМ (t = stamp/60): пропуски в
  ретрансляции становятся парами с большим спаном и доигрываются с
  настоящей средней скоростью; дубликаты отсеиваются по «метка не выросла»;
- метка прыгнула назад ИЛИ больше чем на 2 с вперёд — у слота сменился
  автор (бот <-> игрок, у каждого свои часы): буфер в печку, начинаем
  заново (без этого пара «старое время -> новое» растягивалась на минуты
  и машина ползла).
Замер (два клиента, живой рендер): соперник-игрок 3.5 / 4.3 / 3.5%,
замираний 0-3 — неотличим от бота. В DbgVisualJitter телепорты геймплея
(уничтожение, респавн) выведены из среднего отдельным счётчиком — один
респавн водителя раньше давал «69% дрожания» при идеальном темпе.

**Параллельно вторым разработчиком добавлены КОМНАТЫ (протокол 10,
Rooms.gd)** — пока я мерил соперника, в рабочей папке появились Rooms.gd,
TestRooms и протокол 10 (отсюда скриншот пользователя «у сервера 9, у тебя
10»: сервер был на моём 9, игра из папки — на 10). Устройство: ворота +
до 3 комнат-процессов на соседних портах, реестр визиток user://rooms,
гости и опоздавшие перенаправляются _rx_redirect, пустая комната гасится
через 120 с. Прогнал их сценарии:
- 4 клиента заполняют ворота, 5-й гостем уезжает в поднятую комнату 9978
  и получает слот — PASS;
- опоздавший к идущему заезду уезжает в комнату вместо ожидания
  (обновлённый TestLateJoin) — PASS;
- пустая комната самогасится, визитка прибрана — проверено;
- вся регрессия (31 стенд) на дереве с комнатами — PASS, TestBumps байт
  в байт; TestNet против VDS 3.0-4.2%, рывков 0.
Второй разработчик заодно починил мой недодел: клиентский _finish_order
теперь заполняется из _rx_car_finished (я исправил место только там, где
список вёл сервер), и значок эффекта в снимке берётся из
status_icon_kind() — _status_shown на выделенном сервере не обновлялся.

**Граабли моего стенда, чтобы не повторять:** убивать клиентов Stop-Process
и тут же подключать нового нельзя — ворота ещё держат фантомные слоты
(ENet замечает смерть пира секунды), новый уезжает «гостем» в комнату,
которую я тоже убил, и виснет на «Сервер не ответил». В бою так бывает
только при падении комнаты в 5-секундное окно свежести визитки; клиент
показывает причину и Esc (обработчик _on_join_failed_in_race уже есть).

VDS обновлён до протокола 10 (Rooms.gd на диске, служба active), dist
пересобран. **Порты комнат 9978-9980 — UDP; на VDS файрвол пуст, но при
переезде их надо открыть.**
**Файлы:** scripts/Car.gd, scripts/Main.gd, scripts/Net.gd (метки, п.9-10),
scripts/Rooms.gd + tools/test_rooms.gd + TestRooms.tscn (вторым
разработчиком), tools/test_late_join.gd (обновлён им же),
tools/dbg_visual_jitter.gd (телепорты отдельно), dist/ (13:56).

## «Друг у друга стоим на месте»: дедупликация меток глотала смену автора (2026-08-27)
Жалоба: «мы с игроком по сети друг у друга просто стоим на месте, хотя на
своих компах едем». Корень — в метках тиков протокола 9, и он был виден
только на ДОЛГО РАБОТАЮЩЕМ сервере:
- пока владелец машины не прислал ни одного состояния, сервер метит её
  снимки СВОИМИ часами (Main._pack_state: _snap_seen ещё false) — у VDS,
  работающего часами, метка огромная (Engine.get_physics_frames());
- поехал игрок — сервер ретранслирует метки ВЛАДЕЛЬЦА, а у свежезапущенной
  игры счётчик тиков крошечный;
- в Car.net_apply_snapshot проверка дубликата стояла как `stamp <=
  _snap_stamp` И РАНЬШЕ ветки «метка прыгнула назад — сменился автор,
  буфер в печку»: ветка смены автора была НЕДОСТИЖИМЫМ кодом, все
  состояния живого игрока навсегда отбрасывались как «старые» — его
  машина замирала у всех остальных.
Почему вчерашние замеры были зелёными: на локалхосте сервер стартует на
секунды раньше клиента, разрыв часов крошечный, и метки владельца
«догоняли» метку сервера за секунды — замирание самозалечивалось до
начала замера. На VDS разрыв — часы, замирание вечное.

**Сделано:** дубликат — только РАВНАЯ метка (`==`); метка меньше прошлой —
это смена автора (канал ordered, внутри одного автора метки не убывают),
пакет проходит вниз, там буфер сбрасывается и шкала начинается заново.
Новый стенд tools/TestStampSwap.tscn воспроизводит боевую последовательность
(1 с снимков с «часами сервера» 1e6, затем «часы владельца» с 600): на
старом коде машина стоит (уехала 0.00 м) — FAIL, на исправлении едет
36.5 м — PASS.

**Тесты:** StampSwap (новый) PASS; Bumps байт в байт (13/1, тот же BUMP
vy=1.42 t=0.359), PuppetPush, PuppetSolid, MarkerFollow, Weapons,
FallThrough — PASS; TestNet против локального сервера PASS (джиттер 5.2%,
поток 60.0/с, рывков 0); TestNet против ЖИВОГО VDS после деплоя PASS
(джиттер 1.2%, рывков 0; первый прогон упал «слот не выдан за 8 с» —
попал в окно рестарта службы, повтор чистый).

Протокол НЕ менялся (10). dist пересобран 27.08 14:09, VDS обновлён
(на диске проверен `stamp == _snap_stamp`, служба active).
**НАДО РУКАМИ: раздать игрокам dist 14:09 — баг КЛИЕНТСКИЙ, старые клиенты
против давно работающего сервера так и будут видеть замерших соперников
(версии совпадают, сервер их пустит).**
**Файлы:** scripts/Car.gd, tools/test_stamp_swap.gd + TestStampSwap.tscn
(новые), dist/ (14:09).

## «Сервер не ответил за 5 c» у друга: пустой заезд + тупик перенаправления (2026-08-28)
Жалоба: «у игрока пишет, что сервер не ответил за 5 сек, а у меня три
бота подключаются». Разбор по журналу VDS (время серверное, 27.08):
- 13:52:30 «старт заезда, игроков: 0» — последний игрок ушёл В ОКНЕ показа
  ботов (BOTS_SHOW 2.2 с), а хвост await в _start_with_bots проверял только
  «не зашёл ли новый» (_start_gen). Заезд стартовал ПУСТЫМ. Правка 26.08
  закрывала лишь тик лобби, это окно — нет.
- 13:53:01 друг подключился к воротам, получил слот — но пустая гонка
  «шла» уже 31 с > JOIN_LATE_MAX, и его выслали в комнату 9978.
- До комнаты он не добрался, а в клиенте у этого пути не было ни своего
  таймаута, ни отступления: тупиковый экран. Ворота при этом были живы.
- Комната погасла по таймеру 120 c и осталась ЗОМБИ-процессом
  (`[godot] <defunct>`): ворота не ждут детей.
Проверка «порт закрыт снаружи» НЕ подтвердилась: пробник (новый
tools/dbg_probe_port.gd, ENet-рукопожатие) с дев-машины открывает ЖИВУЮ
комнату 9978 за 0.1 с; мой первый FAIL стучался в уже погасшую комнату, а
tcpdump показал, что пакеты до сетевухи VDS доходят. Файрвол ОС пуст.

**Сделано:**
1. Пустой заезд закрыт с двух сторон: _on_peer_left при уходе последнего
   игрока отменяет и НАЧАТУЮ попытку старта (_starting=false, _start_gen+1 —
   как _on_peer_joined при входе), а _start_net_race сам не стартует без
   людей («последний рубеж», печать «старт отменён: игроков не осталось»).
2. Клиент после _rx_redirect больше не тупикует: свой таймаут
   CONNECT_TIMEOUT на устанавливающееся соединение (_watch_join_timeout) и
   ВОЗВРАТ ДОМОЙ — Net.home_port (порт из net.cfg / ручного подключения;
   redirect его не трогает). Комната молчит → «возвращаемся к воротам», там
   либо слот, либо новое перенаправление (кап redirect_hops бережёт от
   вечного круга). Тупиковый экран остался только для молчащего ДОМА.
3. Rooms.reap_children(): ворота раз в секунду (вместе с визиткой) хоронят
   завершившиеся процессы комнат — OS.is_process_running делает
   waitpid(WNOHANG). Зомби на VDS: было 1, после деплоя 0.

**Стенды (оба сперва воспроизвели жалобу на старом коде):**
- tools/TestEmptyStart.tscn (новый): игрок уходит через 0.5 c после показа
  ботов. Старый код — «старт заезда, игроков: 0», FAIL; новый — PASS.
- tools/TestHomeFallback.tscn (новый): клиент в состоянии «после redirect»
  бьёт в мёртвый порт 39999, дом — живые локальные ворота. Старый код —
  застрял (слот не получен за 25 с), FAIL; новый — вернулся, слот 0, PASS.
**Регрессия:** TestNet локально PASS (0.9%, рывков 0), TestLateJoin с
полной хореографией (ворота + сидящий клиент + опоздавший → уехал в 9978)
PASS, TestBumps байт в байт (13/1), TestStampSwap PASS. TestNet против
боевого VDS после деплоя PASS (1.6%, рывков 0).

Протокол НЕ менялся (10). dist пересобран (15:20), VDS обновлён (правки
проверены на диске, служба active, зомби прибраны рестартом).
**ГРАБЛИ ssh-деплоя:** `pkill -f 'godot.*--room'` по ssh УБИВАЕТ САМ
ssh-шелл (его командная строка содержит образец) — обрыв деплоя с кодом
255. pkill не нужен вовсе: комнаты живут в cgroup службы, systemctl stop
гасит их сам.
**НАДО РУКАМИ: раздать игрокам dist 15:20** (возврат к воротам — клиентский
код; старый клиент при молчащей комнате по-прежнему будет тупиковать).
**Файлы:** scripts/Main.gd, scripts/Net.gd, scripts/Rooms.gd,
tools/dbg_probe_port.gd (новый), tools/test_empty_start.gd +
TestEmptyStart.tscn (новые), tools/test_home_fallback.gd +
TestHomeFallback.tscn (новые), dist/.

## Поле адреса подставляло порт комнаты — вечное «не ответил за 5 с» (2026-08-28)
Скриншот игрока: в поле адреса гаража «139.100.234.166:99» и «Сервер не
ответил за 5 с». Это ОБРЕЗАННОЕ «:9978» (поле 200 px не вмещало хвост) —
порт МЁРТВОЙ КОМНАТЫ. Цепочка отравления:
- CarSelect подставлял в поле "%s:%d" % [Net.host, Net.port], а Net.port
  после _rx_redirect равен порту комнаты;
- игрок вернулся в гараж, нажал «ПО СЕТИ» — join_server(remember=true)
  СОХРАНИЛ порт комнаты в net.cfg как постоянный;
- комнаты смертны (гаснут за 120 c), и дальше каждый запуск бил в пустой
  порт: «Сервер не ответил за 5 с», хотя ворота 9977 живы.
**Сделано:**
- поле подставляет ДОМАШНИЙ порт (Net.home_port, перенаправление его не
  трогает); стандартный порт не пишется вовсе — только «адрес»;
- поле расширено 200 -> 240 px (обрезанный порт и был причиной, почему
  игрок не видел, куда стучится игра);
- амнистия уже отравленных net.cfg в Net._load_config: сохранённый порт из
  диапазона комнат (PORT+1..PORT+ROOMS_MAX) откатывается к воротам — туда
  он мог попасть только этой ошибкой. (Если кто-то НАРОЧНО держит сервер
  на 9978-9980 — его порт сбросится, надо дописать в поле руками.)
**Стенд tools/TestSelectPrefill.tscn (новый):** старый код — в поле
«...:9978» и cfg остаётся отравленным (FAIL), новый — «адрес» без порта и
порт в cfg откатился к 9977 (PASS). net.cfg стенд бережно восстанавливает.
**Регрессия:** SelectPrefill, EmptyStart, HomeFallback, TestNet локально —
PASS. Протокол не менялся (10). dist пересобран (28.08 9:06), VDS обновлён
для синхронности кода (правки клиентские; служба active).
Раздача игрокам — через git (dist в коммите), договорено с пользователем.
**Файлы:** scripts/CarSelect.gd, scripts/Net.gd,
tools/test_select_prefill.gd + TestSelectPrefill.tscn (новые), dist/.

## Толчки игрок-игроку, отмотка лазера, мгновенный луч; протокол 11 (2026-08-28)
Жалобы: «мы видим немного разную картину: машина друга у меня чуть позади»;
«при столкновении не могу его сдвинуть или поддеть»; «нажал E, а лазер
увидел позже, хотя мог бы подбить машину друга».

### Почему картины разные (объяснение, НЕ баг)
Машины клиент-авторитетны, соперник рисуется по записи снимков с
отставанием BUF_DELAY = 0.35 c (буфер 27.08 — он и убрал рывки). Значит
на моём экране друг ВСЕГДА чуть позади своего настоящего места, и наоборот.
Убрать нельзя: без буфера возвращается гадание в дырах канала (замер 27.08:
буфер 0.35 — 2.6-7.4% дрожания против 5-13% и до 16 замираний у чистой
экстраполяции). Лечится не «синхронизацией картинок», а тем, чтобы
ПОСЛЕДСТВИЯ считались по картине того, кто действует, — два пункта ниже.

### 1. «Не могу сдвинуть» — рикошет двигал только СВОЮ машину
_bounce_off_cars у каждого клиента считает толчок себе; машину соперника
он подвинуть не может (она клиент-авторитетна, её позицию диктует владелец),
а у владельца на экране агрессор отстаёт на буфер и до него «не дотягивается».
Итог: тараню — отлетаю сам, соперник едет как ни в чём не бывало.
**Сделано (протокол 11):** толчок ДОСТАВЛЯЕТСЯ владельцу.
- агрессор считает величины по СВОЕЙ картине (направление, темп сближения,
  закрутка — та же формула рикошета) и шлёт `Main.net_report_shove`;
- сервер-арбитр `_rx_shove` проверяет правдоподобие (жертва — марионетка,
  машины ближе 8 м ПО ЕГО картине, числа конечны), зажимает капы
  (сближение ≤ 20, закрутка ±3) и пересылает владельцу как NetFx.SHOVE;
- владелец применяет `Car.apply_net_shove` — те же капы, что у рикошета;
- ДЕДУП: свой рикошет о марионетку помечает контакт (`_touch_mute`, 0.4 c),
  и событие о том же касании отбрасывается — иначе тёрка бок-о-бок била бы
  машину дважды. Антидребезг 0.15 c на пару и на клиенте, и на сервере.

### 2. Лазер: попадал на экране, промахивался на сервере
Стрелявший целится в картину, отстающую на буфер + полёт пакета, а сервер
мерил коридор луча по ТЕКУЩИМ позициям. **Сделано:** машины ведут историю
позиций на сервере (`Car._pos_hist`, 44 кадра ≈ 0.7 c), и выстрел ЖИВОГО
ИГРОКА (на сервере его машина — марионетка) отматывает цели на 0.4 c
(`past_position`). Боты и оффлайн стреляют по текущему миру — их картина и
есть правда.

### 3. «Нажал E — луч увидел позже»
Просьба летела на сервер и картинка возвращалась: задержка в полный пинг.
**Сделано:** свой лазер рисуется СРАЗУ в `_client_tick` (луч — мгновенное
оружие, урон по-прежнему считает сервер), а эхо `_rx_weapon_fx` о своём же
выстреле гасится 1 c (`_laser_predicted`) — иначе луч двоился бы.

**Стенды (новые):** `tools/TestShove.tscn` — толчок применяется (4.0 м/с,
закрутка 1.5), кап держит (сближение 50 -> 8.0 м/с), дубль после своего
рикошета гасится (0.0 м/с); `tools/TestLaserRewind.tscn` — жертва уезжает
на 4 м вбок за 0.4 c: на СТАРОМ коде промах (FAIL, жалоба воспроизведена),
на новом попадание (PASS).
**Регрессия:** Bumps байт в байт (13/1), PuppetPush, PuppetSolid,
CarBounce, Weapons, StampSwap, EmptyStart, HomeFallback — PASS; TestNet
локально (0.8%, рывков 0) и против VDS после деплоя (4.5%, рывков 0) — PASS.

### ПРОТОКОЛ 10 -> 11 (`_rx_shove` — новый @rpc, сдвиг таблицы)
VDS обновлён и проверен на диске (PROTOCOL := 11, служба active). dist
пересобран 28.08 12:05. **ВНИМАНИЕ: старые клиенты не подключатся вовсе —
раздать git-коммит ОБОИМ игрокам.**
**ГРАБЛИ деплоя:** ssh к VDS отвалился по таймауту ПОСЛЕ успешного scp —
проверять `ls /tmp/code.tgz` и доигрывать только шаг развёртывания, а не
гонять 40 МБ заново.
**Файлы:** scripts/Car.gd, scripts/Main.gd, scripts/Net.gd,
tools/test_shove.gd + TestShove.tscn (новые), tools/test_laser_rewind.gd +
TestLaserRewind.tscn (новые), «Обновить сервер.bat» (текст PROTOCOL := 11),
dist/ (12:05).

## Фары болтались на машине и висели мимо кузова (2026-08-28)
**Жалоба:** «фары не жёстко прикреплены к машинам».

**Причина 1 — болтанка.** С 26.08 картинка машины отвязана от тела
(`CarModel.top_level`) и каждый кадр рендера ставится МЕЖДУ двумя
последними положениями тела (`Car._process`, интерполяция фикс-шага).
Стрелку и значок эффекта тогда перевели на `visual_origin()`, а ФАРЫ
остались детьми ТЕЛА — и шагали с частотой физики. На ходу лампы и
лучи отставали от собственного кузова на шаг тела: замерено 0.42 м при
25 м/с, туда-сюда каждый кадр. Это и есть «не жёстко прикреплены».

**Причина 2 — мимо кузова.** Место фар было ОДНО на все машины
(x=±0.55, y=0.42, z=-1.42), а в паках их 41 и они очень разные: высота
кузова от 0.57 м (wildthing) до 1.88 м (redbaron), ширина носа от 0.28
до 0.88 м. У пяти машин (wildthing, twinmill, sweet16, roadrocket,
sidedraft) лампа висела ВЫШЕ КРЫШИ, у узконосых — сбоку в воздухе.

**Сделано:**
1. Держатель `Headlights` с `top_level`: споты и лампы больше не дети
   тела, а `_process` ставит держатель ровно туда же, куда модель
   (тот же `xf`). Заодно `xf` вынесен из-под `if model != null` — он
   нужен обоим.
2. `Car.fit_headlights(model)` (зовёт `Main._set_car_model` сразу после
   `collect_wheels`) сажает фары по ВЕРШИНАМ модели, без колёс:
   высота — 0.55 высоты носа (передние 30 см), ширина носа меряется
   НА ЭТОЙ высоте, кромка — у ВНЕШНЕГО края лампы. Порядок важен: нос
   сужается и кверху, и к передку, и по «средней» кромке лампа
   зависала на 5-10 см впереди борта. Ширина лампы тоже от носа
   (0.55 полуширины, не больше 0.26 м) — у остроносой sharky лампа
   0.11 м, у грузовика 0.26 м. Наружу торчит 2 см, 6 утоплено.
3. Расчёт — в статической `Car.headlight_anchor(model, model_xf)`,
   чтобы отладочный дамп считал ровно то же, что игра.
   **ГРАБЛИ:** у `top_level`-узла в `transform` лежит МИРОВОЕ
   положение, поэтому позицию модели в осях машины берём через
   `global_transform.affine_inverse() * model.global_transform`
   (на `model.transform` первый вариант снимка увёл фары за трассу).

**Стенды:**
- `tools/TestHeadlights.tscn` + `test_headlights.gd` (новый): машина
  едет, каждый кадр меряется расстояние «лампа — видимый центр» (у
  жёстко связанных точек оно постоянно при любом повороте) и вылет
  ламп за габариты модели. Старый код — болтанка 0.423 м, FAIL
  (жалоба воспроизведена); новый — 0.000 м, PASS.
- `tools/DbgCarBox.tscn` + `dbg_car_box.gd` (новый): дамп по всем 41
  машине — куда села фара и не мимо ли кузова. Старое место: мимо у 5
  машин, новое: 0.
- `tools/ShotHeadlights.tscn` + `shot_headlights.gd` (новый): крупный
  план носа 8 машин на ночной трассе, снимки в `tools/shots_headlights/`.
  Отсмотрено: лампы сидят в носу, не висят и не тонут.
- `tools/test_neon_lap.gd`: счёт спотов теперь рекурсивный (они переехали
  в держатель) — иначе стенд бы упал на пустом месте.

**Регрессия:** Headlights, MarkerFollow, NeonLap (фар 8 — ok), Lap
(1223 м), StampSwap, PuppetSolid — PASS; Bumps байт в байт (13/1);
TestNet локально PASS (расхождение 19% против обычных ~1% — на машине
параллельно крутились два чужих headless-прогона, рывков назад 0).
Протокол НЕ менялся. Физика не тронута: правка целиком косметическая,
сервер фар не строит вовсе (`Net.is_server()` выходит раньше).

**dist НЕ пересобран:** сборка 28.08 12:05 сделана ДО этой правки —
пересобрать перед раздачей игрокам.
**Файлы:** `scripts/Car.gd`, `scripts/Main.gd`, `tools/test_neon_lap.gd`,
`tools/test_headlights.gd` + `TestHeadlights.tscn` (новые),
`tools/dbg_car_box.gd` + `DbgCarBox.tscn` (новые),
`tools/shot_headlights.gd` + `ShotHeadlights.tscn` (новые),
`tools/shots_headlights/`, `README.md`.

## Поиск утечек памяти: замер + разбор кода (2026-08-28)

Запрос: «найди утечки памяти». Проверено ЗАМЕРОМ, а не только чтением кода.

### Новые стенды
- `tools/TestMemLeak.tscn` (`tools/test_memleak.gd`) — офлайн-заезд под
  тройной нагрузкой оружием (всем машинам огонь каждые 0.5 с, подрыв раз в
  4 с), замер раз в 20 с: память, объекты, узлы, СИРОТЫ, ресурсы + перепись
  узлов ПО КЛАССАМ. Растущий класс и есть утечка.
- `tools/TestMemCycle.tscn` (`tools/test_memcycle.gd`) — цикл «собрать
  гонку из Main.tscn -> погонять 6 с -> освободить целиком», 12 раз. Это
  главный сценарий клиента: сцена перезагружается после каждого заезда
  (`_rx_reset`), при смене трассы (`_rx_track`) и при переезде в комнату
  (`_rx_redirect`).
Отчёты пишутся в `memleak_report.txt` / `memcycle_report.txt` (в
.gitignore). Пишутся через `FileAccess` со сбросом буфера: stdout
headless-Godot, уходя в файл, буферизуется блоками, и от оборванного
прогона не оставалось ничего.

### ГЛАВНЫЙ ВЫВОД: накопления НЕТ
12 циклов сборки/разбора гонки подряд: память 115.3 -> 114.4 МБ, узлов
после разбора 5, ресурсов 892, сирот 342 — **все числа стоят на месте**.
Разбор сцены ничего не удерживает. Заезд под тройной нагрузкой 200 с:
память 171 -> 196 МБ и дальше колеблется в 190-198 (не растёт), узлов
730 -> ~1050 с колебаниями вниз (1105 на 120 с, 985 на 140 с) — это живые
эффекты в полёте, а не накопление. Версия «игра за вечер съедает память»
НЕ подтвердилась.

### 342 узла-сироты — это кэш паков моделей (осознанный, НЕ растёт)
`CarModelLibrary._pack_cache` (`scripts/CarModelLibrary.gd:49-56`) держит
распакованные GLB-паки ВНЕ дерева; `free` для них нет нигде. Число сирот
342 одинаково в первую и в последнюю секунду обоих прогонов — кэш
выделяется один раз и не растёт. Именно он даёт «Leaked instances» в хвосте
штатного выхода (ПРОГРЕСС 2026-08-26 гадал об этом — теперь подтверждено
замером). Трогать не надо: кэш чинил заморозку клиента на 1-2 с.

### Что реально стоит починить (по убыванию)
1. **Сервер строит и держит меш ограждений.** `TrackBuilder._build_walls`
   (`scripts/TrackBuilder.gd:675`) цепляет меш через `body.add_child(mesh)`
   в обход `_add_visual` (строки 109-114), который на headless-сервере меш
   гасит. Земля (562), полотно (610), трамплины (776), неон (83), створ
   (836) и декор (875) отсечены правильно — ограждения единственная дыра.
   Цена: ~18 тыс. треугольников в КАЖДОМ серверном процессе (ворота + до 3
   комнат на VDS 960 МБ) плюс тот самый headless-спам «Parameter m is
   null», ради борьбы с которым `_add_visual` и написан.
2. **Мина взрывается несколько раз.** `Mine._try_trigger`
   (`scripts/Mine.gd:111-142`): `queue_free()` срабатывает лишь в конце
   кадра, а `_armed` остаётся true, поэтому цикл взведения
   `for b in get_overlapping_bodies()` и два `body_entered` в одном кадре
   дают полный взрыв на каждое тело — расталкивание всех машин заново и по
   6 новых FX-узлов за повтор. Не утечка (узлы самоудаляются), но
   геймплейный баг: тройной толчок на одной мине.
3. **Дубль стрелок-указателей.** `Main._attach_marker`
   (`scripts/Main.gd:338-352`) безусловно создаёт новый маркер, а
   `_rx_welcome` (1801, 1810) зовёт его на свой слот и на всех соперников.
   Поле `has_marker` (`Car.gd:92`) ставится, но НЕ проверяется. Сервер
   отвечает welcome на КАЖДЫЙ hello, а `_say_hello` повторяет hello раз в
   секунду — при RTT > 1 c (или подвисании сервера) приходит второй welcome
   и рождается 4 лишних маркера. Старые ссылки затираются, но узлы остаются
   в сцене: над трассой навсегда зависает лишний конус на месте старта.
4. **`SkidTrail._rebuild`** (`scripts/SkidTrail.gd:66-80`) создаёт НОВЫЙ
   `ArrayMesh` на каждую точку следа — до ~1000 созданий/освобождений RID в
   секунду при заносе 4 машин, работа квадратична по длине ленты (150
   точек). Не утечка (старый меш освобождается по refcount), но самая
   тяжёлая аллокационная нагрузка в проекте.
5. **Косметика оружия строится на сервере.** `Mine`, `OilSlick`,
   `Projectile`, `Airstrike` создают меши без проверки `Net.is_server()`
   (в отличие от `WeaponBox.gd:34` и `BoostPad.gd:34`); авиаудар — 36
   `MeshInstance3D` за кадр. Память не течёт, но это тот же headless-спам.
6. **Мелочи:** след колеса остаётся в сцене навсегда, если машину
   освободили прямо в заносе (`Car.gd` не зовёт `_end_skid` при выходе из
   дерева, `SkidTrail` без своего таймера жизни) — до 2 узлов на событие;
   `CarSelect._watch_connect_timeout` (`scripts/CarSelect.gd:176-191`) не
   отменяет прошлый таймер — два нажатия «ПО СЕТИ» подряд, и таймер первой
   попытки рвёт вторую, ещё живую (проверка «уже подключены» на 187 спасает
   только от совсем позднего срабатывания); `TrackDecor.gd:535` — при
   некорневом `Node3D` в ассете `as` вернёт null и экземпляр останется
   орфаном навсегда.

**Ничего из перечисленного не правил** — это отчёт по запросу «найди».
**Файлы:** tools/test_memleak.gd + TestMemLeak.tscn (новые),
tools/test_memcycle.gd + TestMemCycle.tscn (новые), .gitignore.

## Заморозка не заражала по сети: дебафа не было в снимке (2026-08-28)

Жалоба: «я был замороженный, а мой друг по сети ехал впритык ко мне и не
заморозился тоже».

### Причина
Заразность дебафа считается в `Car._bounce_off_cars` по `other._freeze_time`
(«коснулся синей машины — перенял остаток»). Но `_freeze_time` НИКУДА по
сети не ездил: снимок (`Main._pack_state` → `_rx_state`) вёз только
позицию, поворот, скорость, метку тика и 3 байта (оружие, живость с
«призраком», значок эффекта). На чужом экране моя машина всегда была
«тёплой»: ни ледяной шубы (`_ice_shell` тоже висит на `_freeze_time`), ни
заражения. Локально всё работало — потому баг и жил только в сетевой игре.

Вторая половина дыры — обратный ход. Заморозку МОЖНО подхватить у себя
на клиенте (то самое заражение от касания), а машина клиент-авторитетна,
и сервер о такой «синеве» не узнавал бы вовсе: дальше по цепочке (на
третьего игрока) она не пошла бы даже с полем в снимке.

### Что сделано (протокол 11 → 12)
1. `Main._pack_state` — 4-й байт на машину: остаток заморозки в десятых
   секунды (потолок 25.5 с при дебафе 3 с). `_rx_state` читает его с шагом
   4 и зовёт `Car.net_set_freeze` — но НЕ для своей машины: она
   клиент-авторитетна, ей заморозку шлёт отдельный `_rx_fx`.
2. `Main._client_tick` — 12-е число в состоянии владельца: своя заморозка.
   `_rx_pstate` его принимает с той же санитарией, что и остальное (NaN
   не пропускаем, потолок 10 с), и берёт МАКСИМУМ со своим значением:
   попав ледышкой, сервер морозит машину у себя и лишь потом пересылает
   эффект владельцу — целый пинг тот докладывает «не заморожен», и на
   голой замене шуба у всех остальных мигала бы.
3. `Car.freeze_left()` и `Car.net_set_freeze()` — чтение и установка
   дебафа снаружи; на переходе «не был заморожен → стал» `net_set_freeze`
   выдаёт снежный залп, как и `apply_freeze` (`FxKit._skip` гасит его на
   сервере).
4. `Net.PROTOCOL` 11 → 12: раскладка снимка изменилась, старый клиент
   читал бы её со сдвигом. Несовпадающие версии не играют — рукопожатие
   их разводит с понятным сообщением.

### Стенды (оба падают на старом коде — проверено)
- `tools/TestFreezeSpread.tscn` (`test_freeze_spread.gd`, новый) —
  офлайн, две части: сервер пакует снимок замороженной машины, клиент его
  разбирает (заморозка обязана доехать до марионетки), и своя машина
  таранит замороженную марионетку — обязана перенять дебаф. Прогон: снимок
  довозит 3.00 с из 3.00, зазор кузовов 1.59 м, дебаф перешёл на 34-м
  кадре сближения. PASS. С отключённой строкой в `_rx_state` — FAIL
  («снимок довёз 0.00 с»), то есть стенд ловит именно ту дыру.
- `tools/TestNetFreeze.tscn` (`test_net_freeze.gd`, новый) — ПАРНЫЙ,
  живой сервер и два клиента: `freezer` держит свою машину замороженной,
  `watcher` смотрит на слот живого соперника. Прогон на локальном сервере:
  соперник был заморожен в 99% кадров (356 из 359), PASS. С отключённым
  чтением заморозки в `_rx_pstate` — 0% из 359, FAIL: обратный ход
  «клиент → сервер» действительно нужен, а не «на всякий случай».

### ВАЖНО: чужая правка в рабочей копии
В `Main._rx_state` во время этой работы появился НЕ мой кусок —
разбор природы дыр `[gap]` (тиков сервера / наших кадров). Он не
компилируется: `_srv_stamp_prev` и `_gap_frames_prev` нигде не объявлены,
и из-за этого `Main.gd` сейчас не грузится вовсе. Это подтверждённая работа
параллельного контекстного окна (другая задача) — я его НЕ трогал; после
объявления двух полей всё должно собраться. Мои стенды прогонялись
ДО появления этого куска.

**Файлы:** `scripts/Main.gd`, `scripts/Car.gd`, `scripts/Net.gd`,
`tools/test_freeze_spread.gd` + `TestFreezeSpread.tscn` (новые),
`tools/test_net_freeze.gd` + `TestNetFreeze.tscn` (новые), `README.md`.
**dist НЕ пересобран.** Протокол изменился — сервер и клиенты обязаны
обновиться ВМЕСТЕ, иначе рукопожатие разведёт их по версиям.

## Отставание картинки: где на самом деле рвётся канал; адаптивный буфер (2026-08-28)
Задание: «нужно убрать это отставание и без дрожания, поищи решение».
Прежний ответ «0.35 c — плата за плавность, убрать нельзя» оказался
ПОЛОВИНЧАТЫМ: 0.35 — это ХУДШИЙ случай канала, который платили ВСЕГДА.

### Что говорит индустрия
Стандарт — АДАПТИВНОЕ отставание (Unity Netcode for Entities, адаптивный
буфер Overwatch); у Valve фиксированные 100 мс (cl_interp 0.1), и те
считаются наследием эпохи модемов. При наших 60 снимках/с «учебный» буфер —
2-3 снимка, то есть 33-50 мс. Наши 0.35 c были ВДЕСЯТЕРО больше нормы.

### Где рвётся — измерено, а не предположено
Прежние сессии дважды ошибались с диагнозом («сервер фризит», «канал
приносит пачками»). Разобрано тремя новыми замерами:
1. Клиент печатает ПРИРОДУ ДЫР: сколько тиков сервер ПОРОДИЛ за окно
   против того, сколько снимков ДОШЛО (метка бота = часы сервера).
   Итог: сервер стабильно 60.0 тиков/с, доходит 54-59.
2. Зонд главного цикла НА СЕРВЕРЕ с порогом 50 мс (прежний вачдог ловил
   от 250 мс — потому и «молчал»): за все прогоны НИ ОДНОЙ строки, шагов
   физики строго по одному на кадр. Сервер не фризит и не шлёт пачками.
3. tcpdump на eth0 VDS: снимки УХОДЯТ ровно (интервалы 13.7/20.7 мс,
   ~60/с), дыр по 100-330 мс на выходе НЕТ ВОВСЕ.
**Вывод: рвёт ПОСЛЕДНЯЯ МИЛЯ (путь до клиента), а не игра и не сервер.**
Контрольный опыт заодно подтвердил механику клумпинга: сервер с
`--max-fps 20` (idle-цикл медленнее физики) сразу даёт худшую паузу 58 мс
вместо 32 — отправка привязана к кадру цикла. На VDS цикл ровный, поэтому
здесь это не виновник, но знать полезно.

### Сделано: отставание ходит за каналом
Car: BUF_DELAY (0.35) заменён на static net_buf_delay + net_note_gap(gap),
который зовёт Main._rx_state на каждый снимок. Держим худший разрыв за
недавнее окно: РАСТЁМ мгновенно (дыра — сразу закладываемся), СПАДАЕМ
медленно (BUF_DECAY 0.06 c/с, ~5 c с потолка до пола). Пол 60 мс, потолок
прежние 350 мс, старт 120 мс; net_reset_buf_delay в _rx_welcome (переезд
в комнату/переподключение — канал меряем заново).

### Замеры (живой рендер, боевой VDS, A/B в одних условиях)
| вариант | дрожание модели | замирания | отставание |
|---|---|---|---|
| фиксированные 350 мс (как было) | 5.1% | 0 из 600 | 350 мс всегда |
| адаптивное, прогон 1 | 4.1% | 0 из 600 | среднее 156 мс |
| адаптивное, прогон 2 | 4.9% | 0 из 600 | среднее 181 мс |
На локальном сервере отставание схлопывается до 60 мс (соперник вместо 7 м
позади себя настоящего идёт в метре). TestNet против VDS: рывков назад 0.
То есть плавность НЕ ухудшилась, а отставание вдвое-вшестеро меньше.

### Честный остаток
Совсем убрать нельзя: нельзя нарисовать данные раньше, чем они дошли.
Пол 60 мс — это 3-4 снимка, минимум для интерполяции. Дальше отставание
задаёт КАЧЕСТВО КАНАЛА игрока: на этой линии дыры до 330 мс, и в такие
минуты буфер честно вырастет. Что ещё можно сделать, если захочется:
избыточность снимков (в каждом пакете 2-3 прошлых состояния — потеря
одного пакета перестаёт быть дырой; пакет 218 байт, вырастет до ~650).

**Стенд tools/TestBufAdapt.tscn (новый):** чистый поток -> пол 60 мс; дыра
300 мс -> мгновенно 350; спад за 2 с -> 250 мс, за 10 с -> обратно 60.
**Регрессия:** Bumps 13/1 байт в байт, BufAdapt, Shove, LaserRewind,
StampSwap, PuppetSolid, PuppetPush, CarBounce, Weapons, EmptyStart,
PlaceFinish, MarkerFollow, FreezeSpread — PASS; TestNet локально и против
VDS — PASS.
**ГРАБЛИ:** TestBumps, запущенный сразу за другим экземпляром Godot, даёт
ложные 12/3 вместо 13/1 (та же чувствительность к нагрузке, что отмечена
27.08). Три чистых прогона подряд — 13/1. Не пугаться и перепроверять.
**Файлы:** scripts/Car.gd, scripts/Main.gd, tools/test_buf_adapt.gd +
TestBufAdapt.tscn (новые), tools/dbg_probe_port.gd.

## Почему отставание не опускается ниже: виноват Wi-Fi и VPN игрока (2026-08-28)
Задание: «ещё уменьши отставание и не допускай дрожания». Сделано две
вещи: подобран предел, ниже которого дрожание появляется, и НАЙДЕНА
настоящая причина, по которой этот предел так высок.

### 1. Растяжение времени вместо замирания (Car._follow_buffered)
Когда запись кончается, плеер раньше упирался и машина ВСТАВАЛА, а
экстраполяция догадывала по скорости (рывок коррекции, когда приезжала
правда). Теперь при иссякающем запасе (< 0.05 c) темп воспроизведения
плавно падает до 0.35 — приём из джиттер-буферов телефонии (time-scale
modification): соперник доли секунды едет медленнее, но по НАСТОЯЩИМ
данным, без гадания. `_play_rate` учтён в contact_velocity и
linear_velocity — толчки считаются от того, что видно на экране, и замеры
не врут (иначе растянутый кусок выглядел бы «замиранием»).
Стенд tools/TestStretch.tscn (новый): молчание канала 0.3 c. Новый код —
темп 0.35, проехала 2.08 м по записи; старый — темп 0.86 и 3.53 м
ГАДАНИЕМ (стенд на нём FAIL).

### 2. Подбор предела (живой VDS, замираний на 600 кадров, 3 прогона)
| настройка | без растяжения | с растяжением |
|---|---|---|
| x1.25+0.025, спад 0.06, пол 60 мс (~150 мс на этой линии) | 0/0/0 | 0/0/0 |
| x1.0+0.03, спад 0.12, пол 50 мс (~90 мс) | 2/0/0 | 0/1/0 |
| x0.8+0.03, спад 0.20, пол 40 мс (~70 мс) | 0/10/1 | 0/2/1 |
Требование «не допускать дрожания» держит ТОЛЬКО первый набор — он и
оставлен. Финальная проверка: 5.0% и 5.6% дрожания, замираний 0/600 оба
прогона. Локально отставание 60 мс при дрожании 1.0% и нуле рывков.

### 3. НАСТОЯЩАЯ причина: канал самого игрока
Почему предел так высок — выяснено измерениями, и это меняет прежние
выводы журнала:
- два НЕЗАВИСИМЫХ клиента с одной машины провалились в одну и ту же
  секунду (14:10:52) — затык общий для машины/линии, не для нашего сокета;
- потери почти все ОДИНОЧНЫЕ (серии 1:28, 1:29, 1:36 2:2) — значит паузы
  100-450 мс это не пропажа пачки, а ОПОЗДАНИЕ (пакеты приходят кучей);
- машина сидит на Wi-Fi (Realtek RTL8822CE), и пинг ДО СВОЕГО РОУТЕРА
  теряет 10-16% пакетов (4 из 25 и 4 из 40) — на локальном звене это
  очень плохо, и ретрансмиты Wi-Fi дают ровно такие пачки;
- весь трафик идёт через VPN-туннель happ-tun (маршрут по умолчанию
  0.0.0.0/0 через 172.18.0.2, метрика 0) — userspace-туннель добавляет
  своё пакетирование.
**ГРАБЛИ, объясняющие прежние ошибочные диагнозы:** записанный в журнале
«ping до VDS: 1 мс, разброс 0» (26.08) — ФИКЦИЯ: на ICMP отвечает сам
туннель, до сервера пакет не идёт (проверено: 25 из 25 по 0 мс, при этом
до роутера 16% потерь). Из-за этого «идеального» пинга канал считали
здоровым и три сессии искали причину в игре и сервере.
**Что реально уберёт отставание у игрока (кода не касается):** кабель
вместо Wi-Fi или починка Wi-Fi (ближе к точке, другой канал, выключить
энергосбережение адаптера) и вывод игры/адреса сервера из VPN. Буфер
адаптивный — на чистом канале он САМ схлопнется к 60 мс.

**Тесты:** Stretch (новый), BufAdapt, Bumps 13/1 байт в байт, Shove,
LaserRewind, StampSwap, PuppetSolid, PuppetPush, CarBounce, Weapons,
FreezeSpread, PlaceFinish — PASS; TestNet локально (отставание 61 мс,
дрожание 1.0%, рывков 0) — PASS.
**ГРАБЛИ:** скрипт подбора констант оставил в Car.gd значения последнего
варианта — поймал стенд TestBufAdapt (ждал пол 60, увидел 40). Всегда
прогонять BufAdapt после подборов.
**Файлы:** scripts/Car.gd, scripts/Main.gd (замер потерь по меткам),
tools/test_stretch.gd + TestStretch.tscn (новые), tools/test_net.gd,
dist/ (14:03).

## Отмотка для ВСЕХ прицельных оружий, места с сервера, подбор кубов (2026-08-29)
Жалобы: «оружие пролетает сквозь даже ботов»; «я на своём экране еду
первым, а на его экране вторым»; «иногда проезжаю не собирая кубы, хотя
кажется что задеваю». Все три — одна и та же болезнь: игрок действует по
СВОЕЙ картине, а сервер считал по «сейчас».

### 1. Снаряды и магнит получили компенсацию (было только у лазера)
Лазер отматывал цели с 27.08, ракета/ледышка — нет вовсе, оттого и
«пролетает сквозь». Теперь Projectile несёт `lag` и проверяет попадание по
ОТМОТАННЫМ положениям (`Car.past_position`), причём не точкой, а ОТРЕЗКОМ
за кадр (на 55 м/с снаряд проходит 0.92 м, проверка «где он сейчас»
пропускала задетых вскользь). Area3D по машинам при `lag > 0` отключена —
иначе снаряд срабатывал бы дважды; о мир и ограждения гаснет как прежде.
Магнит тоже целится по отмотанным положениям.
Мина и масло компенсации НЕ требуют (кладутся под себя), авиаудар — тоже
(цель выбирается по прогрессу, а не прицелом).

### 2. Отставание теперь МЕРЯЕТСЯ, а не угадывается (протокол 13)
Прежние 0.4 c у лазера были догадкой, а буфер стал адаптивным (60-350 мс).
Владелец шлёт своё `Car.net_buf_delay` 13-м числом в `_rx_pstate`, сервер
кладёт его в `Car.net_client_lag` (потолок 0.5 c — больше это уже подарок
стреляющему) и отматывает ровно на него. `Car.net_shot_lag()` — одно место
принятия решения: не сервер или не марионетка — ноль.

### 3. Места раздаёт СЕРВЕР (пятый байт в снимке)
Клиент считать место сам не может честно: свою машину он видит «сейчас»,
чужие — с отставанием буфера, и в плотной борьбе КАЖДЫЙ видит себя
впереди. Ровно это игрок и описал. Теперь место каждой машины едет 5-м
байтом флагов, а `_place_of` на клиенте возвращает присланное.

### 4. Кубы с бонусом: подбор по СЫРЫМ данным владельца
На сервере тело машины игрока ведётся сглаженно (`_follow_snapshot` —
подтяжка с упреждением), и на затыках канала (а они, как выяснено 28.08,
частые) сглаженный путь уходит от настоящего на метры. Куб всего 1.3 м —
Area3D его промахивала. Теперь `WeaponBox._physics_process` на сервере сам
проверяет машины-марионетки по `Car.true_position()/true_forward()` (сырое
состояние владельца) зазором до отрезка кузова < 1.5 м; для ботов и
оффлайна остался прежний Area3D. Выдача вынесена в `_give` (общий откат
PER_CAR_COOLDOWN не даёт задвоения).

**Новые стенды (оба воспроизводят жалобу на старом коде):**
- `tools/TestRocketRewind.tscn`: цель долго стоит, трогается вбок в миг
  выстрела, полёт короче отставания. Без компенсации — FAIL (промах),
  с ней — PASS.
- `tools/TestBoxPickup.tscn`: сырые данные ведут точно через куб, тело
  отведено на 2.5 м. Старый код — FAIL, новый — PASS.
**ГРАБЛИ стендов (обе поймали ложное «PASS»):**
- бокс, созданный ПРЯМО НА машине, засчитывает вход до того, как стенд
  разведёт тело и сырые данные — ставить куб впереди;
- судить о подборе по полю `weapon` нельзя: игра выдаёт стартовое оружие;
  признак — отметка выдачи в самом боксе;
- машина сама подтягивает тело к снимку, поэтому стенду нужен
  `set_physics_process(false)` на ней;
- сценарий отмотки надо строить от момента ПОПАДАНИЯ, а не выстрела:
  первая версия стенда ракеты «зеленела» и без компенсации.

**Тесты:** Bumps 13/1 байт в байт, BoxPickup (новый), RocketRewind
(новый), LaserRewind (обновлён под измеренное отставание), Weapons,
Stretch, BufAdapt, Shove, StampSwap, PuppetSolid, PuppetPush, CarBounce,
FreezeSpread (обновлён: байтов на машину стало 5), PlaceFinish, Minimap —
PASS. TestNet локально: отставание 61 мс, дрожание 2.0%, рывков 0 — PASS.
**ПРОТОКОЛ 12 -> 13** (13-е число в `_rx_pstate`, 5-й байт флагов).
**Файлы:** scripts/Projectile.gd, scripts/Car.gd, scripts/Main.gd,
scripts/Net.gd, scripts/WeaponBox.gd, tools/test_rocket_rewind.gd +
TestRocketRewind.tscn (новые), tools/test_box_pickup.gd +
TestBoxPickup.tscn (новые), tools/test_laser_rewind.gd,
tools/test_freeze_spread.gd, «Обновить сервер.bat», dist/.

## Подсадка в хвост, видимая гибель соперника, мина убивает (2026-08-31)
Жалобы: «второй игрок несколько раз появлялся не у старта, а далеко
впереди — я его даже не видел на своём экране»; «при уничтожении другого
игрока не вижу, что он взрывается, а потом мигает при появлении, пока
неуязвим — он будто продолжает ехать»; «пусть мина взрывает авто».

### 1. Подсевший к идущему заезду садится В ХВОСТ ПОЛЯ
Пока слот пустует, машину в нём ведёт бот, а подсесть разрешено первые
`JOIN_LATE_MAX = 25 c` — за это время бот уезжает на сотни метров.
`_rx_hello` отдавал подсевшему РОВНО место бота (`_rx_race_running`), и
игрок появлялся впереди всех. Теперь перед этим зовётся
`Main._seat_joiner_at_tail(slot)`: машина переезжает на `JOIN_TAIL_GAP = 8`
метров позади самой отставшей, прогресс и круги пересчитываются от неё
(иначе место в HUD врало бы до конца заезда). Двигаем ТОЛЬКО НАЗАД: кто и
так позади всех, остаётся где был — подсадка не отбирает честно
проеханное. Идемпотентно: повторный hello второй раз уже не сдвинет.

### 2. Гибель соперника теперь ВИДНА
Две отдельные дыры, обе чинились здесь:
- **взрыва не видел никто, кроме владельца.** `Car.destroy()` слал эффект
  только владельцу машины (`NetFx.DESTROY`), остальные видели лишь переезд
  снимками — «как будто едет дальше». Добавлены `Main.net_broadcast_destroy`
  и `_rx_destroy_fx(idx)`: клиент рисует взрыв ТАМ, ГДЕ МАШИНА ВИДНА ЕМУ
  (`Car.visual_origin()`) — картинка марионетки отстаёт на буфер, и взрыв
  по «серверному сейчас» вспыхнул бы в стороне. Своя машина событие
  пропускает: её взрывает `_rx_fx DESTROY`.
- **марионетка НЕ МИГАЛА ВООБЩЕ.** Фаза мигания считалась по ОСТАТКУ
  призрака (`elapsed = ghost_time - _ghost_time`), а марионетке остаток
  подливают по 0.3 c каждым снимком — «прошло» вечно равнялось 1.7 из 2.0,
  то есть последней фазе, которая всегда видна. Заведён `Car._ghost_age`
  (прожитое время призрака), по нему и мигаем. Признак из снимка теперь
  идёт через `Car.net_set_ghost()` (`_start_ghost`/`_end_ghost`), а не
  прямой записью в `_ghost_time`: только там обнуляется фаза, снимаются и
  возвращаются контакты с машинами.
- Мина у клиентов ИНЕРТНА и о срабатывании не знала: лежала на дороге до
  конца жизни, а машины рядом разлетались «сами по себе». Сервер шлёт
  `_rx_mine_fx(pos)` — взрыв на экране и уборка инертной копии.

### 3. Мина уничтожает, а не только расталкивает
`Mine.LETHAL_RADIUS = 4.0` (чуть больше корпуса): в эпицентре — `destroy()`
со всем, что к нему прилагается (взрыв, появление на трассе, мигание
неуязвимости); дальше и до `BLAST_RADIUS = 10` — прежнее расталкивание.
В ленту событий (и в счёт убийств — `MINE` добавлен в `LETHAL_KINDS`) идут
только погибшие: раньше запись получал каждый, кого качнуло за десять
метров.

### ПРОТОКОЛ 13 → 14
Раскладка снимка НЕ менялась, но два новых RPC сдвигают нумерацию всех
остальных (Godot раздаёт номера по именам методов) — старый клиент с новым
сервером понимал бы вызовы неверно. Сервер и клиенты обязаны обновиться
ВМЕСТЕ; «Обновить сервер.bat» теперь ждёт в ответе `PROTOCOL := 14`.

### Стенды (все три падают на старом коде — проверено)
- `tools/TestMineKill.tscn` (новый): наехавший гибнет и переезжает (6.6 м),
  машина в 7 м получает толчок 8.4 м/с и НЕ гибнет, инертная копия убрана
  событием. Со старым правилом: «жертва призрак=false» — FAIL.
- `tools/TestGhostBlink.tscn` (новый): событие взрыва даёт призрака,
  при непрерывно приезжающем признаке видимость переключилась 5 раз, после
  снятия признака машина снова видима и твёрдая. На старой формуле фазы —
  0 миганий, FAIL.
- `tools/TestJoinTail.tscn` (новый): слот, уехавший на 406 м, посажен на
  38 м (8.0 м до хвоста); отставший слот не тронут. 
- `tools/test_weapons.gd` ОБНОВЛЁН под новое правило мины: наехавшая жертва
  теперь обязана стать призраком, толчок проверяется на машине в 6.5 м
  сбоку. Призрак с жертвы снимается вручную — дальше она нужна живой
  (магнит и масло призрака не берут).

**Регрессия:** Bumps 13/1 байт в байт, Weapons, MineDrop, Explosion,
Respawn, CarBounce, Shove, PuppetSolid, PuppetPush, FreezeSpread,
PlaceFinish, BufAdapt, Stretch, StampSwap, LaserRewind, RocketRewind,
BoxPickup, Minimap — PASS; TestNet против локального сервера — PASS
(марионетка идёт ровно, назад не дёргает).
**Файлы:** `scripts/Mine.gd`, `scripts/Car.gd`, `scripts/Main.gd`,
`scripts/Net.gd`, `scripts/Weapons.gd`, `tools/test_weapons.gd`,
`tools/test_mine_kill.gd` + `TestMineKill.tscn` (новые),
`tools/test_ghost_blink.gd` + `TestGhostBlink.tscn` (новые),
`tools/test_join_tail.gd` + `TestJoinTail.tscn` (новые), `README.md`,
«Обновить сервер.bat», `dist/` пересобран.

**Заодно (из списка известных багов):** `Mine._blown` — мина рвётся ровно
один раз. `queue_free()` отложен до конца кадра, а за этот кадр
`body_entered` может прийти ещё от одной машины (и цикл взведения
перебирает все тела разом) — одна мина отрабатывала взрыв по нескольку
раз. С уничтожением в эпицентре это дало бы двойные записи в ленте и
двойной счёт убийств.

### Правка того же дня: подсадки в идущий заезд НЕТ ВОВСЕ
Реакция пользователя на «сажаем в хвост»: «такого не должно быть, если
кто-то не успел в лобби, то для него создается другое лобби». Сделано:
- `JOIN_LATE_MAX` (25 с «подсадки вместо бота») удалён вместе с
  `_race_start_time`. `_on_peer_joined`: `late = _net_started` — всякий
  пришедший после старта считается опоздавшим, машину в его слоте
  продолжает вести бот.
- `_joinable_here()` → `not _net_started`: начавшийся заезд больше не
  объявляет себя свободным в реестре комнат, и чужие ворота его не выберут.
- Дальше работает прежний путь `_route_elsewhere`: опоздавшему поднимают
  ОТДЕЛЬНУЮ комнату (свой порт, своё лобби, свой отсчёт). Запасной путь
  «жди следующего заезда» остаётся на случай, когда комнат не осталось
  (ROOMS_MAX).
- `_seat_joiner_at_tail` ОСТАВЛЕН: остаётся один законный путь в идущую
  гонку — игрок БЫЛ в лобби, но догрузился после старта (заезд ушёл без
  него по HELLO_GRACE), и его машину всё это время вёл бот. Стенд
  TestJoinTail переписан под этот сценарий.

**Живая проверка:** ворота + клиент `TestRooms --stay` (держит заезд
идущим) + `TestLateJoin` через 20 с после старта — раньше это было «внутри
25 секунд» и игрока сажали в гонку, теперь в логе ворот
«[rooms] поднимаем комнату на порту 9978» и «пир отправлен в заезд на
порту 9978», стенд PASS. TestReset (полный цикл заезд → перезапуск →
новый слот, 164 с) — PASS: вернувшийся после `_rx_reset` игрок опоздавшим
НЕ считается. TestNet, Weapons, PlaceFinish, MineKill, GhostBlink,
JoinTail — PASS.

**ОТКРЫТЫЙ ВОПРОС (спросил, ответа пока нет — оставлено как было):**
окно «попасть в один заезд с другом» теперь равно ожиданию в лобби, а это
`LOBBY_WAIT = 5 c` (продление `JOIN_GRACE` тоже 5 с). Нажали «играть» с
разницей в 10 секунд — уже разные гонки. Если жалоба «мы хотели вместе, а
попали в разные заезды» появится, поднимать надо именно LOBBY_WAIT (было
20 с до 27.08). README заодно поправлен: там оставалось враньё про
«ждёт 20 секунд».
**Файлы:** `scripts/Main.gd`, `README.md`, `server/README.md`,
`tools/test_late_join.gd`, `tools/test_join_tail.gd`, `dist/` пересобран.

## Выбор числа участников заезда 4..8 (2026-08-31)
Задание: «давай добавим выбор кол-ва участников гонки от 4-8», уточнено —
до 8 ЖИВЫХ игроков.

### Как устроено
- `GameState.race_size` (4..8, кламп в сеттере, хранится в profile.cfg) —
  желание игрока; панель «УЧАСТНИКОВ −/+» в гараже (CarSelect, слева от
  «СТАРТ»). Оффлайн: машин в заезде ровно столько (игрок + N−1 ботов).
- `Net.race_size` — размер ТЕКУЩЕГО сетевого заезда. `Net.PLAYER_SLOTS`
  теперь ПОТОЛОК (8), а не размер: свободные слоты ищутся до race_size.
- Размер задаёт ПЕРВЫЙ игрок пустого лобби: желание едет 3-м аргументом
  hello; сервер (один игрок, гонка не идёт) принимает его и перестраивает
  сцену — соединение и слот живут в autoload и перезагрузку переживают,
  повторный hello (раз в секунду) застаёт новую сцену. Остальным размер
  диктуется 2-м аргументом `_rx_track` — не совпало, клиент
  перестраивается, как при чужой трассе.
- Массивы по машинам (`_slot_taken`, `_net_place`) больше не фиксированные
  четвёрки — наполняются в `_spawn_cars`. Все `Net.PLAYER_SLOTS` в Main
  заменены на `_cars.size()` / `Net.race_size` по смыслу.
- Лобби: подиумов по числу слотов; до 4 — прежний ряд, 5..8 — ДВА ряда
  панелей поменьше (250×250), статус переезжает вниз, подсказка ужимается
  (в один ряд 5+ панелей не лезут по ширине 1280, полноразмерные два ряда
  — по высоте 720). Скриншоты сняты: lobby.png (4, байт в байт прежний
  вид) и lobby8.png.
- Опыт за места 5..8 — как за 4-е (PLACE_XP клампится, как и было).

### ПОПУТНО НАЙДЕНА И ЗАКРЫТА ГОНКА в перестройке клиента (_rebuilding)
`_rx_track` с чужой трассой перезагружает сцену, но смена сцены происходит
в конце кадра, а welcome, приехавший С НЕЮ В ОДНОМ ПАКЕТЕ, исполняется ещё
в старой сцене: `Net.my_slot` записывался в умирающую сцену, новая видела
«слот уже есть», НЕ представлялась заново (`_say_hello` выходит сразу) и
оставалась без своей машины и стрелок. Гонка ЖИВАЯ и существовала ДО этой
задачи: успел пакет в тот же кадр — клиент завис, кадром позже — RPC не
нашёл узла («Failed to get path from RPC: Main» в логах стендов — это ОН,
безвредный путь той же развилки). Поймана стендом TestSizeNet (стрелок 0
при выданном слоте). Лечение: флаг `_rebuilding` ставится перед
reload_current_scene (в `_rx_track` и `_rx_reset`), `_rx_welcome` в
помеченной сцене игнорируется — новая сцена честно здоровается заново.

### Стенды
- `tools/TestRaceSize.tscn` (новый, офлайн): race_size 7 — сцена строит
  7 машин, счётчики той же длины, решётка без наложений (зазор 4.4 м),
  кламп 99→8 и 1→4.
- `tools/TestSizeNet.tscn` (новый, живой сервер): клиент хочет 6 — сервер
  (поднят под 4) принимает («первый игрок выбрал заезд на 6 машин»),
  клиент получает слот, 6 машин и 5 стрелок соперников. До флага
  _rebuilding стенд честно падал (слот есть, стрелок 0).
  ГРАБЛИ: серверу нужен СВЕЖИЙ процесс — прошлый прогон оставляет ему
  размер 6, и «сервер под 4» уже не воспроизводится.
- `tools/screenshot_lobby.gd`: ключ `--eight` — снимок двухрядного лобби.
**Регрессия:** TestNet (свежий сервер), TestReset (полный цикл 168 с,
перезапуск после заезда жив с флагом _rebuilding), Bumps 13/1 байт в
байт, Weapons, MineKill, GhostBlink, JoinTail, PlaceFinish, FreezeSpread,
Minimap, Shove, StampSwap, LaserRewind, RocketRewind, BoxPickup,
RaceSize, SizeNet — PASS.
**Протокол ОСТАЛСЯ 14** (он ещё не выкатывался): изменения сигнатур hello
и _rx_track вписаны в его описание в Net.gd. Сервер и клиенты обновлять
ВМЕСТЕ («Обновить сервер.bat» ждёт PROTOCOL := 14).
**ВНИМАНИЕ (не проверено под нагрузкой):** 8 живых игроков на VDS
(1 ядро, 960 МБ) не прогонялись — трафик снимков и физика вырастут вдвое;
если появятся жалобы на дёргание при полном заезде, мерить нагрузку VDS.
**Файлы:** scripts/GameState.gd, scripts/Net.gd, scripts/Main.gd,
scripts/Lobby.gd, scripts/CarSelect.gd, tools/test_race_size.gd +
TestRaceSize.tscn (новые), tools/test_size_net.gd + TestSizeNet.tscn
(новые), tools/test_net.gd, tools/screenshot_lobby.gd, README.md, dist/.

### Правка того же дня: цифра по центру; сервер VDS обновлён до 14
Жалобы: «кол-во игроков не по центру» и «с другом по сети не смогли
начать гонку» (на экране «Сервер не выдал слот. Возможно, версии игры
различаются»).
1. Цифра в панели «УЧАСТНИКОВ» — ровно в полосе между кнопками (50..100)
   и по их ФАКТИЧЕСКОЙ середине: кнопка перерастает заявленные 36 px
   (минимальный размер задаёт табличка-стайлбокс, поля 20+20 плюс текст),
   и цифра, отцентрованная по 36, висела выше. Проверено скриншотами
   (ScreenshotSelect + кроп панели).
2. «Не выдал слот» — это НЕ баг новой сетевой части: VDS ещё крутил
   протокол 13, а новый hello (3 аргумента) старый сервер молча
   отбрасывает — клиент видел штатную подсказку про версии. Выполнены
   шаги «Обновить сервер.bat» (tar → scp → stop → import → start):
   сервер active, `PROTOCOL := 14` на месте. TestNet против живого VDS —
   PASS (слот, ростер, снимки, метки).
ОСТАЛОСЬ ДЛЯ ИГРЫ ВДВОЁМ: другу нужна НОВАЯ сборка — изменения ещё не
закоммичены/не отправлены в git (жду отмашки пользователя), git pull у
друга пока не поможет.
**Файлы:** scripts/CarSelect.gd, dist/ пересобран, /opt/bighead на VDS.

### Правка того же дня, вторая: кнопки −/+ держат размер (жалоба «снова криво»)
Игрок перезапустил игру — цифра всё равно не по центру, хотя скриншот-стенд
у меня показывал ровно. ПРИЧИНА не в координатах цифры: кнопка от
UiKit.style_button ПЕРЕРАСТАЕТ заданный size — её минимальный размер это
«поля таблички-стайлбокса (20 px с каждой стороны) + метрики шрифта», а
метрики зависят от машины (масштаб окна меняет их через оверсэмплинг
шрифта). Поэтому у меня было ровно, у игрока — криво, и подгонка «на
глазок» не могла помочь В ПРИНЦИПЕ. Заодно на 38 px табличка с четырьмя
заклёпками — каша, знака не видно.
Лечение: CarSelect._mini_button — плоская заливка (StyleBoxFlat, поля
2 px) вместо таблички; кнопки держат ровно 38×38, цифра — тот же бокс
(y 24, высота 38). Проверено скриншотами на 1280×720 И 1920×1080 —
раскладка байт в байт одинаковая.
**ГРАБЛИ на будущее:** UiKit.style_button годится только кнопкам от
~200×70 — мелкой кнопке он навязывает и рост (поля 20+20 + шрифт,
машинозависимо!), и нечитаемые заклёпки.
**Файлы:** scripts/CarSelect.gd, dist/ пересобран.

## Магнит присмирел, лазер держится за нос, новое оружие «Глушилка» (2026-08-31)
Жалобы и задание: «магнит иногда магнитит куда-то за пределы трассы, где
никого нет»; «однажды лазер прошёл сквозь меня от другого игрока, а я всё
равно продолжал ехать»; «лазер пусть держится на экране чуть подольше, но
выходит из переда машины всегда, не остаётся на том месте, где применили»;
«силу магнита, когда его использовали несколько раз, надо поуменьшить — а
то несколько машин применили, и тебя выкидывает с трассы с огромной
силой»; «добавь оружие, которое при попадании на 5 секунд меняет у того, в
кого попало, кнопки лево и право; при выстреле — звуковая волна».

### 1. МАГНИТ: три отдельные болезни, все вылечены
- **«магнитит за пределы трассы»** — ДВЕ причины, обе устранены:
  1) магнит «доставал всю трассу» (спад силы шёл к MAGNET_FAR, но не к
     нулю). Соперника за 200 м не видно, а рывок к нему идёт ПО ХОРДЕ,
     через местность — ровно «тянет туда, где никого нет». Теперь
     `MAGNET_RANGE = 55 м` — дальше магнит не трогает ВООБЩЕ;
  2) направление рывка считалось из ОТМОТАННОЙ точки жертвы
     (`past_position`) к нынешнему магниту. После телепорта жертвы (взрыв,
     респавн) история держит точку в другом конце трассы, и вектор
     получался в никуда. Теперь отмотка нужна ТОЛЬКО для дальности (мы
     обязаны судить по картине стрелявшего, протокол 13), а направление —
     по нынешним положениям.
- **«несколько применили — выкидывает с огромной силой»**: заведена
  усталость `Car.magnet_wear()` — рывки по одной машине в окне 3 c слабеют
  (1.0, 0.55, 0.42…), базовая сила снижена (32→22 вблизи, 18→8 вдали),
  закрутка 3.6→2.6, срез скорости впередиедущим 0.5→0.65 и тоже слабеет.
  Замер стендом: было 28.6 + 28.6 + 28.6 м/с, стало 19.3 + 10.7 + 7.4.
- **не выдёргивать с полотна** (`Car._magnet_guard`): составляющая рывка
  НАРУЖУ от оси трассы гасится по мере приближения к краю (запас < 5 м до
  грани борта), у самого края наружу не тянет вовсе — рывок
  разворачивается ВДОЛЬ трассы; если и вдоль некуда (магнит ровно сбоку за
  ограждением) — машину не трогаем. На песчаной трассе, где ограждений
  нет, это и был полёт в дюны.

### 2. ЛАЗЕР ЕДЕТ С МАШИНОЙ и виден дольше
`LaserFx` получил `source` — стержень каждый кадр встаёт от ВИДИМОГО
положения стрелявшего (`visual_origin`) по его нынешнему курсу. Раньше луч
рисовался один раз в точке нажатия: на 40 м/с машина за треть секунды
уезжала от собственного луча на десяток метров — «луч остаётся там, где
применили». Жизнь 0.3 → 0.55 c, причём первые 45% времени яркость
держится и только потом тает. Источник передан из всех трёх точек: свой
выстрел (`Car._use_laser`), мгновенная отрисовка у клиента
(`Main._client_tick`) и чужой выстрел (`_rx_weapon_fx` → `_cars[idx]`).

### 3. НОВОЕ ОРУЖИЕ: ГЛУШИЛКА (Weapons.SCRAMBLE, 9-е)
`scripts/ScrambleWave.gd` — звуковая волна вперёд: три бирюзовых кольца
поперёк полёта, расходящиеся по фазе. Скорость 38 м/с (медленнее ракеты —
волну видно и от неё можно уйти), жизнь 1.8 c, радиус попадания 2.6 м
(это конус звука, а не пуля). Попал — у жертвы на 5 c МЕНЯЮТСЯ МЕСТАМИ
лево и право: инверсия сделана ОДНОЙ строкой в `Car._drive`, поэтому
достаётся и игроку, и боту. Уничтожения нет.
- Компенсация задержки — как у снарядов (`lag = net_shot_lag()`, проверка
  по отрезку за кадр), инертная копия у клиентов — как у ракеты.
- По сети: `Car.NetFx.SCRAMBLE` пересылается владельцу (машина
  клиент-авторитетна — рулит её клиент, без пересылки эффекта он бы не
  почувствовал). Значок над крышей едет в снимке прежним 3-м байтом.
- Игроку, которого сбили, кроме значка приходит анонс «Управление сбито:
  лево и право поменялись!» — иначе это читается как поломка игры.
- Значок `assets/ui/garage/wg_scramble.png` запечён с нуля
  (`tools/gen_scramble_icon.py`): в листе-референсе звуковой волны нет.

### 4. «Лазер прошёл сквозь меня, а я поехал дальше» — НЕ ВОСПРОИЗВЕЛОСЬ
Отдельного дефекта не нашлось: это либо промах по коридору (луч 1.6 м от
оси — визуально «сквозь» проходит и близкий промах), либо картина
31.08 «жертва погибла, но её взрыв и мигание видел только владелец» —
починено в тот же день (`net_broadcast_destroy`). Оставлено под
наблюдением: если повторится, следующий шаг — писать на сервере лог
попаданий лазера с дистанцией до оси луча.

### ПРОТОКОЛ 14 → 15
Список RPC и раскладка снимка НЕ менялись, но по сети поехали НОВЫЕ КОДЫ:
вид оружия 8 (`_rx_weapon_fx` / `_rx_weapon_event`) и `NetFx.SCRAMBLE` в
`_rx_fx`. Старый клиент их молча пропустит — выстрел был бы невидим, а
попавший игрок ничего бы не почувствовал. «Обновить сервер.bat» ждёт
`PROTOCOL := 15`.

### Стенды
- `tools/TestScramble.tscn` (новый): волна долетает и вешает эффект > 4 c,
  соседа в 7 м не задевает, стрелявший цел; при ОДИНАКОВОМ вводе «руль
  влево» жертва доворачивает в ПРОТИВОПОЛОЖНУЮ сторону от контрольной
  машины; через 5 c эффект истекает сам и руль возвращается.
- `tools/TestMagnet.tscn` (новый): сила трёх рывков подряд падает,
  машина за 70 м не трогается вовсе, жертва у внешнего борта получает
  рывок ВДОЛЬ трассы (наружу 0.00 м/с). На старом коде — 28.6/28.6/28.6,
  дальнюю дёргает, наружу 21.6 м/с: стенд падает по трём пунктам из
  четырёх (проверено откатом Car.gd+Main.gd).
- `tools/test_weapons.gd` дополнен: «луч нарисован», «луч едет с машиной»
  (после сдвига машины на 10 м луч сместился ровно на 10.0 м) и «глушилка
  сбила управление».
**ГРАБЛИ стендов (обе поймали ложный FAIL):**
- рулить дольше ~0.2 c нельзя: жертва с перевёрнутым рулём доезжает до
  ограждения, и доворот ей дальше задаёт ведение у стены (первая версия
  стенда видела «жертва 1.62, контроль 5.53» — обе в плюс);
- срок эффекта отсчитывать надо от ПОПАДАНИЯ, а не от выстрела: волна
  летит 15 м почти полсекунды.

### ПОПУТНО ПОЧИНЕН СТЕНД TestWeapons (падал в HEAD, к задаче отношения не имеет)
«мина толкает дальних» и «занос у стены не в стену» падали НА ЧИСТОМ
HEAD — с 31.08, когда размер заезда стал выбираемым: в профиле
`race_size = 8`, а стенд паркует за карту только машины 1..3. Лишние
четыре ездили по трассе, ловили чужие снаряды и толкали жертв. Теперь
стенд паркует ВСЕ машины с индексом ≥ 4 и от размера заезда не зависит.

**Регрессия:** Weapons, Scramble, Magnet, MineKill, GhostBlink,
FreezeSpread, PlaceFinish, LaserRewind, RocketRewind, BoxPickup,
Explosion, MineDrop, Shove, StampSwap, PuppetSolid, PuppetPush,
CarBounce, BufAdapt, Stretch, Minimap, RaceSize, Respawn — PASS;
Bumps 13/1 байт в байт; MemCycle — память ровная (115.6 МБ на всех 10
циклах); TestNet против ЛОКАЛЬНОГО сервера — PASS (отставание 60 мс,
рывков 0) и против ЖИВОГО VDS — PASS (отставание 160 мс).
**dist/ ПЕРЕСОБРАН** (12:52), тесты Scramble/Magnet/Weapons прогнаны
ИЗ СОБРАННОГО exe — PASS.
**VDS ОБНОВЛЁН**: «Обновить сервер.bat» отработал, сервер active,
`PROTOCOL := 15` на месте.
**Файлы:** `scripts/Weapons.gd`, `scripts/Car.gd`, `scripts/LaserFx.gd`,
`scripts/ScrambleWave.gd` (новый), `scripts/Main.gd`, `scripts/Net.gd`,
`tools/gen_scramble_icon.py` (новый),
`assets/ui/garage/wg_scramble.png` (новый), `tools/test_scramble.gd` +
`TestScramble.tscn` (новые), `tools/test_magnet.gd` + `TestMagnet.tscn`
(новые), `tools/test_weapons.gd`, `README.md`, «Обновить сервер.bat»,
`dist/`, /opt/bighead на VDS.
ОСТАЛОСЬ: изменения не закоммичены (жду отмашки) — другу для игры вдвоём
нужна новая сборка, у него протокол 14.

### Правка того же дня: магнит снова достаёт ВСЮ трассу
Реакция пользователя на ограничение дальности: «магнит должен работать
через всю трассу, не только на 55 м». Ограничение снято — вернулась
прежняя дальность (сила спадает до MAGNET_FAR к 55 м и дальше не меняется,
как было до правки). Но лечение самой жалобы («магнитит куда-то за пределы
трассы, где никого нет») ОСТАЛОСЬ, только теперь чинится не обрезкой
дальности, а НАПРАВЛЕНИЕМ рывка:
- `Car._magnet_pull_dir`: до 18 м тянет прямо к магниту (это и есть
  магнит), с 18 до 45 м направление плавно переходит в КАСАТЕЛЬНУЮ К
  ТРАССЕ, дальше — строго вдоль полотна. Сторону даёт кратчайший ход по
  кольцу (`wrapf(track_offset - victim.track_offset, ±длина/2)`), то есть
  жертву волочёт по трассе вперёд или назад — туда, где магнит.
- Прямая и трасса могут смотреть навстречу (шпилька) — смесь схлопывается
  в ноль, тогда берём направление трассы.
Замер стендом на дальней машине (68 м по прямой, 70 м по кривой): было
«вдоль 14.9, ПОПЕРЁК 9.8 м/с» — то есть жертву волокло вбок с полотна;
стало «вдоль 7.93, поперёк 0.06». Усталость от повторных рывков и защита
края полотна не тронуты.
**Стенд TestMagnet ПЕРЕПИСАН под новое правило:** вместо «дальнюю не
трогает» проверяется «дальнюю магнит достаёт» + «тянет вдоль трассы»
(вдоль > поперёк ×2) + «тянет слабее ближней». На старом коде падают
«вдоль трассы», обе проверки усталости и «у края наружу не тянет».
**Регрессия:** Weapons, Scramble, MineKill, PlaceFinish, Shove, Magnet —
PASS; TestNet против живого VDS — PASS. dist/ пересобран (13:03), магнит
прогнан ИЗ СБОРКИ — PASS. VDS обновлён повторно (active, PROTOCOL := 15).
**Файлы:** `scripts/Car.gd`, `tools/test_magnet.gd`, `README.md`, `dist/`,
/opt/bighead на VDS.

## Глушилка: горизонтальные волны радиуса действия; «пролетела сквозь бота» (2026-08-31)
Жалобы: «глушилка в момент выстрела должна пускать волны по горизонтали,
чтобы было видно её радиус действия»; «стрелял в бота, который был прямо
передо мной, — оружие пролетело сквозь него».

### Почему пролетала сквозь: ТРИ причины разом (ScrambleWave переписан)
1. **Сфера попадания 0.9 м при кольцах на ~2.5 м**: визуал обещал широкую
   волну, а Area3D ловила узкую пульку — кольца «проходили сквозь» машину
   при честном промахе сферы. Теперь машины ловятся ТОЛЬКО ручной
   проверкой отрезком за кадр с радиусом HIT_R = 2.6 (и с отмоткой, и
   без — раньше без отмотки работала Area3D), Area3D осталась лишь чтобы
   гаснуть об ограждения (маска 0b010). Сферу 2.6 в Area3D нельзя — тёрла
   бы полотно.
2. **Волна летела по прямой в 3D**: на перепаде высот уходила от дороги и
   проходила НАД машинами. Теперь ПРИЖИМАЕТСЯ К ПОЛОТНУ: луч вниз каждый
   кадр (маска 0b001), высота = земля + 0.55; земли нет (кромка песчаной
   трассы) — летит как летела до смерти по таймеру.
3. **38 м/с не догоняли едущего**: у машины max_speed ~34, сближение
   4-8 м/с — за жизнь волны (1.8 c) едущая цель не догонялась. Скорость
   38 → 50 (SPEED, константой вместо переменной _speed).
И ЧЕТВЁРТАЯ, найденная стендом: **бот едет по ДУГЕ, волна летела по
прямой** — на изгибе трассы прямая уходит с полотна вбок (замер фазы
«едущий бот»: промах 3.1 м на 20 м дистанции). Теперь волна КАТИТСЯ ПО
ТРАССЕ (`_steer_along_track`): курс доворачивает к касательной оси
(lerp 4.0/с) в ту сторону, куда стреляли; отметка на оси ведётся с
непрерывностью (closest_offset_near, первая — глобальным поиском). Ссылку
на трассу волна берёт у стрелявшего, инертной копии её даёт
_spawn_weapon_visual (wave.track = _track) — иначе у зрителей волна летела
бы по прямой, а у сервера по дуге.

### Волны по горизонтали
Кольца ТЕПЕРЬ ЛЕЖАТ ГОРИЗОНТАЛЬНО (TorusMesh без поворота) и расходятся
РОВНО до радиуса поражения (scale = HIT_R / 0.75) — игрок видит зону, в
которой волна снимает управление. В момент выстрела от машины стрелявшего
расходится горизонтальное кольцо FxKit.ring радиуса HIT_R (и у него, и у
зрителей — добавлено в use_weapon и _spawn_weapon_visual).

### Стенд
`tools/test_scramble.gd`: новая фаза «едущий бот» — жертва в 10 м впереди
уезжает по ОСИ ТРАССЫ на 28 м/с (двигается руками, как в
TestRocketRewind), волна обязана догнать и попасть. На старой волне фаза
падает (проверено подменой ScrambleWave.gd из HEAD): жертва уезжает на
45 м без эффекта. Прочие фазы не тронуты.
ГРАБЛИ: двигать жертву по прямой нельзя — трасса у отметки 0.06
изгибается (~5 м увода на 25 м, см. комментарий в test_weapons), и
«прямо едущий» бот на деле сворачивает за дугой.

**Регрессия:** Scramble (8 проверок), Weapons, Magnet, MineKill,
LaserRewind, RocketRewind — PASS; Scramble прогнан и ИЗ СБОРКИ — PASS.
dist/ пересобран, VDS обновлён (active, PROTOCOL := 15 — протокол НЕ
менялся, вид оружия и RPC те же).
**Файлы:** `scripts/ScrambleWave.gd`, `scripts/Car.gd`,
`scripts/Main.gd`, `tools/test_scramble.gd`, `dist/`, /opt/bighead.

### Уточнение: «пролетает сквозь» касалось РАКЕТЫ, стрелял по прямой
Пользователь уточнил: сквозь бота пролетала не глушилка, а другое оружие
(ракета/ледышка), и стрелял он ПО ПРЯМОЙ. У Projectile нашлись и закрыты
ТРИ дыры (первые две — те же, что днём у волны):
1. **Кузов цели — точка.** В сетевом пути (отмотка, протокол 13) попадание
   мерилось до ЦЕНТРА машины с радиусом 1.6 м, а кузов длиной ~3.2 м:
   снаряд, проходящий сквозь НОС или КОРМУ повернувшегося бота (дальше
   1.6 м от центра), «промахивался», на экране пройдя сквозь бампер.
   Теперь три пробы вдоль курса цели: центр и ±1.1 м (true_forward).
   Это главная причина «по прямой»: бот в повороте/вильнул — и чирк по
   краю кузова не засчитывался.
2. **Полёт по прямой в 3D.** Снаряд держал высоту точки выстрела; на
   спуске дорога уходит вниз — пролетал НАД крышей цели (замер: горка
   классики даёт 2.74 м перепада на 18 м — гарантированный пролёт), на
   подъёме — взрывался об асфальт, не долетев. Теперь _hug_ground, как у
   волны (земля + 0.7, снижение/подъём ≤ 0.5 м/кадр — уклон любой, но
   без телепорта вниз при выстреле в полёте). Заодно исчезли постоянные
   0.55 м вертикали, съедавшие запас радиуса и на ровном.
3. **Мёртвая зона у бампера.** Снаряд рождается в 2.3 м перед машиной, а
   проверка шла только вперёд от места спавна: цель, стоящая на экране
   стрелявшего ВПЛОТНУЮ (отмотанная точка позади места спавна), не
   проверялась вовсе. Первый кадр теперь дотягивает отрезок назад до
   носа стрелявшего (_first_check). То же — в ScrambleWave.

### Стенды (оба падают на старом Projectile — проверено подменой из HEAD)
- `tools/TestShotSlope.tscn` (новый, оффлайн): отметка с самым крутым
  перепадом ищется ПО САМОЙ КРИВОЙ (правки профиля не сломают стенд);
  ракета в упор под горку и в горку. Старый код: FAIL обе (над крышей /
  в асфальт), новый — PASS.
- `tools/TestRocketGraze.tscn` (новый, серверный как TestRocketRewind):
  жертва поперёк линии огня, центр в 2.0 м сбоку, нос воткнут в линию.
  Старый код: FAIL (2.0 > 1.6), новый — PASS (проба «нос» в 0.9 м).
**Регрессия:** Weapons, Scramble, RocketRewind, LaserRewind, BoxPickup,
MineKill, FreezeSpread, Magnet — PASS; ShotSlope и RocketGraze прогнаны и
ИЗ СБОРКИ — PASS. dist/ пересобран, VDS обновлён (active, PROTOCOL := 15,
протокол не менялся).
**Файлы:** `scripts/Projectile.gd`, `scripts/ScrambleWave.gd`,
`tools/test_shot_slope.gd` + `TestShotSlope.tscn` (новые),
`tools/test_rocket_graze.gd` + `TestRocketGraze.tscn` (новые), `dist/`,
/opt/bighead на VDS.

## НОВЫЙ РЕЖИМ «ФУТБОЛ» 4 на 4 + бонусы с неба (2026-08-31)
Задание: «режим игры футбол, 8 машинок 4 на 4, нужно загнать мяч в чужие
ворота, игра длится 5 минут, режим выбирается в главном меню»; следом —
«периодически на поле падают бонусы в случайное место поля, подбираешь и
получаешь оружие или что-то ещё, что получаем в гонках».

### Как устроено (всё ОФФЛАЙН: игрок + 7 ботов; сетевого футбола пока нет)
- `GameState.game_mode` ("race"/"soccer", хранится в profile.cfg).
  Гараж (CarSelect): панель «РЕЖИМ» с кнопкой ГОНКА/ФУТБОЛ над
  «УЧАСТНИКОВ»; в футболе выбор числа участников гаснет (всегда 8),
  «ПО СЕТИ» отвечает «Футбол по сети пока недоступен». СТАРТ/Enter ведут
  в `scenes/Soccer.tscn`.
- `scripts/SoccerArena.gd` — арена 92×56 с бортами (слой 2, мяч из игры
  не уходит), ворота в торцах: створ 14 м, короб-сетка (полупрозрачная),
  штанги/перекладина в цвет команды, невидимый потолок. Разметка: центр,
  круг, штрафные. `goal_at(pos)` → кто забил, `kickoff_car(i)` — решётка
  кикоффа (роли: нападающий, два фланга, вратарь), СИНИЕ слева (−X).
- `scripts/SoccerBall.gd` — RigidBody3D-сфера r=1.1 (слой 4, как машины),
  текстура «мяча» печётся кодом. Поверх решателя — ручной ПИНОК от машин
  по скорости сближения (у машин bounce 0 — иначе мяч вялый), кламп
  скорости 42, `last_touch` для автора гола, visual_origin как у Car.
- `scripts/Soccer.gd` — менеджер матча: 5:00, счёт, гол → анонс
  (автор/автогол) + конфетти + пауза 2.8 c → кикофф; «ПОСЛЕДНЯЯ МИНУТА»;
  финал → ПОБЕДА/НИЧЬЯ, опыт (100/40/20 + 15 за свой гол), Enter в гараж.
  Камера — IsoCamera на точку МЕЖДУ игроком и мячом (Focus с
  visual_origin), метки-конусы над игроком (зелёная) и мячом (жёлтая),
  командные кольца под машинами. Страховка: переворот/вылет → на свою
  кикофф-точку (и R). HUD: табло, таймер, скорость, слот оружия.
- ИИ: `Car.soccer_brain` — в `_ai_control` три строки: если задан, цели
  считает `Soccer.ai_drive(car)` (роль, объезд мяча, чтобы не завезти в
  свои ворота, разворот задним ходом). Гоночный ИИ не тронут.
- БОНУСЫ: `scripts/SoccerDrop.gd` (extends WeaponBox) — золотой куб
  ПАДАЕТ с неба (первый через 4 c, дальше раз в 6-11 c, максимум 4 на
  поле), кольцо-метка на газоне, ОДНОРАЗОВЫЙ (подобрал — исчез), тает
  через 25 c с миганием. Все стартуют с пустыми руками. Боты применяют
  подобранное: прицельное — по чужим в конусе 22°/30 м, мина/масло —
  под заднего, магнит — по ближнему, авиаудар/буст — сразу.
- Оружие на арене БЕЗ ТРАССЫ: Airstrike обобщён (track == null → тени по
  ходу движения цели, «лидер» в футболе = ближайший к мячу,
  Soccer.leader_car); магнит/глушилка/лазер уже были null-safe; destroy
  без трассы оставляет машину на месте (призрак 2 c). Projectile: ракета,
  попав в МЯЧ (он на слое машин), пинает его по ходу полёта — можно бить
  по воротам выстрелом. Soccer.report_weapon_hit объясняет игроку
  глушилку/ледышку/магнит анонсом.

### Стенды
- `tools/TestSoccer.tscn` (новый): 8 машин по сторонам, мяч в правых
  воротах → гол СИНИХ и кикофф после паузы, боты за 10 c таскают мяч
  (~30-50 м от центра), бонус на игрока выдаёт оружие и исчезает, дождь
  бонусов идёт, таймер на нуле → финал и стоп. PASS и из редактора, и
  ИЗ СБОРКИ dist.
- `tools/ScreenshotSoccer.tscn` (новый) — кадр матча; скриншоты гаража и
  матча сняты, раскладка проверена глазами.
**Регрессия:** Weapons, RaceSize, Scramble, Lap, ShotSlope, RocketGraze,
BoxPickup, RocketRewind — PASS. dist/ пересобран (14:29), TestSoccer из
сборки — PASS. Протокол НЕ менялся (сетевого футбола нет), VDS обновлять
не нужно.
**Файлы:** scripts/GameState.gd, scripts/Car.gd (крючок soccer_brain),
scripts/CarSelect.gd, scripts/Soccer.gd + scenes/Soccer.tscn (новые),
scripts/SoccerArena.gd, scripts/SoccerBall.gd, scripts/SoccerDrop.gd
(новые), scripts/Airstrike.gd, scripts/Projectile.gd,
tools/test_soccer.gd + TestSoccer.tscn, tools/screenshot_soccer.gd +
ScreenshotSoccer.tscn (новые), README.md, dist/.
**ОСТАЛОСЬ (план):** сетевой футбол (нужен новый протокол: мяч, команды,
счёт в снимках) — по отмашке; изменения не закоммичены.

## Новый заезд начинался «не у старта, а дальше»; бонусы больше не вращаются (2026-08-31)
Жалобы: «после завершения гонки началась следующая гонка, и мы с другим
игроком появились не у старта, а дальше»; «бонусы пусть не вращаются».

### 1. Спавн «дальше старта» после перезапуска заезда — НАЙДЕН И ЗАКРЫТ
Механика: после `_rx_reset` сервер строит новую решётку, но в неё прилетает
ХВОСТ потока `_rx_pstate` из ПРОШЛОЙ сцены клиента (пакет уже был в полёте,
канал unreliable) — серверная марионетка телепортируется туда, где игрок
ЗАКОНЧИЛ прошлый заезд. Снимки (они идут всегда, и в лобби) разносят это
всем: у клиента до welcome ВСЕ машины марионетки, включая его собственную,
и она уезжает с решётки; welcome вручает слот УЖЕ УВЕЗЁННОЙ машины и на
место её не возвращает. Оба игрока начинали новый заезд там, где кончили
прошлый. Лечение ДВОЙНОЕ (каждое закрывает симптом само по себе):
- **сервер** (`_rx_pstate`): состояние от слота, не сказавшего hello
  В ЭТОЙ сцене (`_hello_done`), выбрасывается — это заведомо пакет из
  прошлой жизни;
- **клиент** (`_rx_welcome`): своя машина при получении слота ВСТАЁТ НА
  СВОЮ КЛЕТКУ РЕШЁТКИ (`_grid_xf`, запоминается в `_spawn_cars`), скорости
  и прогресс обнуляются. Подсевшему в идущую гонку место тут же перепишет
  `_rx_race_running` — надёжные RPC упорядочены.

### 2. Бонусы не вращаются
`WeaponBox._process` (вращение + покачивание куба) удалён; куб стоит
неподвижно в позе «алмазом» (углом вверх, разовый поворот в `_ready`).
Наследуется и футбольным `SoccerDrop` (мигание перед исчезновением
осталось — это не вращение).

### Стенд
`tools/TestResetGrid.tscn` (новый, живой сервер): блокирует авто-hello
(`Net.my_slot = -3`), после подключения шлёт «пакет из прошлой сцены»
(_rx_pstate с точкой в чистом поле 300,300), потом здоровается — своя
машина обязана стоять у решётки. На коде до правок — FAIL честный
(«до старта 198 м», проверено подменой Main.gd из HEAD), на новом — PASS
(до старта 3.0 м, до точки яда 559 м).
ГРАБЛИ стенда: серверу нужен `--track=grass` (клиент строит ту же трассу,
без перестройки сцены); сервер должен быть СВЕЖИМ.
**Регрессия:** TestNet, SizeNet, BoxPickup, Weapons, Soccer, Reset — PASS
(Reset — полный цикл с перезапуском: и старая логика подсадки жива).
**Протокол НЕ менялся** (сигнатуры RPC те же, только отбраковка на приёме
и телепорт у себя) — сервер VDS обновлять не обязательно, но ЖЕЛАТЕЛЬНО
(серверная половина лечения — п.1 — живёт именно там).
**Файлы:** scripts/Main.gd, scripts/WeaponBox.gd, tools/test_reset_grid.gd
+ TestResetGrid.tscn (новые), dist/.

### Правка того же дня: искры — искрами, куб бонуса — «алмазом»
Жалоба: «искры должны быть не в виде звёздочек, а в виде искр».
- `SparksFx` переписан: вместо спрайта-звёздочки (sparkle.png) — маленькие
  яркие точки-угольки (glow1.png, аддитивно): белое ядро остывает в
  оранжевый, скорость выше, гравитация -30, damping и разнобой гашения
  (lifetime_randomness) — читается как настоящий сноп искр.
- Мультяшные звёзды при крепком таране (`FxKit.stars_burst` в
  `Car._bounce_off_cars`) убраны — сноп и так густеет со скоростью удара.
  Сам stars_burst ОСТАВЛЕН (звёзды при подборе бокса — это «награда», не
  искры).
- ПОПУТНО: поза «алмазом» у бонуса не была видна — поворот «X 35° + Y 45°»
  ВЗАИМНО СОКРАЩАЕТСЯ с ракурсом изокамеры (yaw 45°, pitch −32°), с экрана
  куб стоял ровно (поймано скриншотом one-off стенда). Теперь крен по Z +
  наклон по X (`Basis(RIGHT, 0.6155) * Basis(BACK, PI/4)`) — главная
  диагональ вертикальна, с любого ракурса читается «алмаз».
**Регрессия:** CarBounce, BoxPickup, Soccer, Weapons, ShotFx-скриншоты —
PASS/сняты. dist/ пересобран (15:08).
**Файлы:** scripts/SparksFx.gd, scripts/Car.gd, scripts/WeaponBox.gd,
dist/.
**VDS ОБНОВЛЁН** (15:12): шаги «Обновить сервер.bat» выполнены, сервер
active, PROTOCOL := 15; TestNet против живого VDS — PASS. Серверная
половина лечения спавна (отбраковка чужесценных _rx_pstate) теперь на
боевом сервере.

## Футбол: боты не залипают у стен; НОВАЯ ТРАССА «КОСМОС» (2026-08-31)
Задания: «в режиме футбол боты иногда упираются в ворота или стены и
больше ничего не делают»; «собери новую трассу в космосе на фоне звезд
и планет».

### 1. Футбол: упор в стену/ворота распознаётся и разруливается
Две причины, обе закрыты (Soccer.gd):
- **цель за стеной.** Точка «позади мяча» (объезд) и «сквозь мяч» у
  борта или в створе оказывалась ЗА стеной/в коробе сетки — бот таранил
  преграду до бесконечности (angle≈0 → газ в пол). Теперь цель клампится
  ВНУТРЬ поля (±HALF_LEN−3, ±HALF_WID−3).
- **детектор упора.** ai_drive отмечает «хочет ехать» (_want_move);
  если при этом скорость < 1.2 м/с дольше 1.1 с — боту даётся ESCAPE
  0.8-1.3 с: сдаёт назад, доворачивая нос на мяч. Вратарь, штатно
  стоящий на позиции, ехать «не хочет» и в детектор не попадает.
  Счётчики сбрасываются при respawn/kickoff.

**Стенд tools/TestSoccerStuck.tscn (новый):** мяч вплотную к торцевому
борту, бот-нападающий носом в борт, остальные обездвижены. На СТАРОМ
коде — FAIL честный (упор 5.6 c без движения, проверено подменой
Soccer.gd из HEAD), на новом — PASS (худший упор ~1 c, отъезд 17-21 м).

### 2. Четвёртая трасса «КОСМОС» (kind == "space", ПРОТОКОЛ 15 → 16)
- **Конфигурация** SEGMENTS_SPACE: «орбитальное шоссе» — две длинные
  прямые (~110 и ~117 м), размашистые дуги + одна тесная связка (R=23).
  Длина 747 м, витки не ближе 39 м, габарит 244×205. Подобрана численным
  перебором (скрипт-солвер повторял _walk/_solve_free_lengths — как
  песок и неон). Плоская, ограждения ЕСТЬ.
- **Вид**: земля — «пустота» (чёрно-синее звёздное поле с туманностями,
  текстура печётся кодом, UNSHADED, тайл 50 м), за обочиной уходит вниз
  (трасса «парит»); полотно сине-фиолетовое; по верху стен светящиеся
  полосы своих цветов (зелёная внутри / фиолетовая снаружи — не путать
  с неоном); небо — панорама звёзд с «млечным путём» (Main._space_sky,
  PanoramaSkyMaterial из кода); свет как ночью (тусклое солнце, glow),
  у машин фары (как на неоне); трамплины с эмиссией, осевая светится.
- **Декор** (TrackDecor._build_space): 5 дальних планет кольцом вокруг
  трассы (одна с кольцами-тором), 3 ближних «спутника» сразу за
  ограждением, 16 астероидов-глыб; шаров и деревьев нет. Мини-карта —
  фиолетовые кромки.
- **ПРОТОКОЛ 16**: раскладка и RPC не менялись, но вид трассы едет
  строкой в _rx_track, а старый клиент незнакомый вид игнорирует и
  строит классику — полный рассинхрон с физикой сервера. Сервер и
  клиенты обновить ВМЕСТЕ; «Обновить сервер.bat» ждёт PROTOCOL := 16.
- tools/screenshot_track.gd: необязательный 2-й аргумент — вид трассы
  (`-- <папка> space`); скриншоты космоса сняты и проверены глазами.

### Регрессия
Curve (все 4 трассы, космос: длина 747, изломов нет), SoccerStuck
(новый), Soccer, Lap, Minimap, RaceSize — PASS; из СОБРАННОГО dist:
SoccerStuck, Soccer — PASS. TestNet против ЛОКАЛЬНОГО сервера с
`--track=space` — PASS (клиент перестроился под космос) и против ЖИВОГО
VDS — PASS (отставание ср. 141 мс). dist/ пересобран (15:31), VDS
ОБНОВЛЁН (active, PROTOCOL := 16). Файл-огрызок
dist/BigHeadRacing.exe~RF*.TMP (обломок прошлой записи) удалён.

**ГРАБЛИ (пре-существующие, НЕ этой сессии):**
- TestWeapons шатается на проверке «занос у стены не в стену»: на
  ЧИСТОМ HEAD в свежем worktree 3 прогона дали FAIL/FAIL/PASS (падает
  только эта проверка, «нос ушёл от стены» при этом ok). Знак
  angular_velocity.y меряется в тот же кадр после apply_oil_slip —
  видимо, гонка с контактом о стену. Порог/стенд не трогал.
- Godot на этой машине надо гонять через `_console.exe`: обычный
  Godot_v4.3-stable_win64.exe — GUI-подсистема, PowerShell его не ждёт
  и вывода не видит.

**Файлы:** scripts/Soccer.gd, scripts/TrackBuilder.gd, scripts/Main.gd,
scripts/TrackDecor.gd, scripts/Car.gd, scripts/Minimap.gd,
scripts/Net.gd (PROTOCOL 16), tools/test_soccer_stuck.gd +
TestSoccerStuck.tscn (новые), tools/screenshot_track.gd, README.md,
«Обновить сервер.bat», dist/. Изменения НЕ закоммичены (в рабочей копии
были и чужие незакоммиченные правки прошлых сессий).

### Правка того же дня: в космосе НЕТ земных строений
Реакция пользователя на скриншоты: «в космосе не должно быть строений».
TrackDecor.build() для kind == "space" теперь строит ТОЛЬКО разметку на
полотне (решётка, стрелки, осевая) и космос (планеты, астероиды):
убраны стартовая зона (арка, ферма огней, башня, тент, таблички команд),
трибуны, обочинные пропсы (флаги, конусы, шины, реклама, фонари) и щиты
у поворотов. Старт читается по шахматной ленте
(TrackBuilder._build_start_line) и мини-карте. Скриншоты пересняты,
проверены глазами. TestLap — PASS, dist/ пересобран (15:48).
VDS НЕ обновлял: правка только в TrackDecor, а декор на headless-сервере
не строится вовсе (_build_decor выходит), протокол не менялся.
**Файлы:** scripts/TrackDecor.gd, dist/.

### Правка того же дня: кометы и «Сатурн» на виду
Просьба: «пускай кометы пролетают, и какая-нибудь планета должна быть с
кольцом как сатурн». Всё в TrackDecor:
- **Кометы**: раз в 5-11 с (первая через 1.5 с, максимум 3 живых) из
  случайной точки края мира к случайной точке НАД трассой летит комета —
  светящаяся бело-голубая голова + полупрозрачный хвост-конус против
  хода (UNSHADED, видна в темноте). Движение в _process (включается
  set_process(true) только на космосе; на остальных трассах выключено).
- **«Сатурн»**: кольца были у одной ДАЛЬНЕЙ планеты и легко терялись —
  теперь первый ближний «спутник» за ограждением (t≈0.18, поднят на
  +12 м, чтобы кольца не резались об борт) — песочный, радиус 9, с
  ДВОЙНЫМ кольцом (широкая яркая полоса + узкая потемнее с просветом,
  имя узла PlanetRing); дальняя ringed-планета тоже осталась.
- tools/ScreenshotTrack: на космосе ждёт комету и снимает её крупно
  (comet.png), прицел чуть мимо — по центру экрана висит отсчёт 3-2-1.

**Стенд tools/TestSpace.tscn (новый):** кольца есть (≥2 тора), земных
FBX-строений нет (допустимы только решётка/стрелки разметки), комета
прилетает и за секунду пролетает > 10 м, живых комет ≤ COMET_MAX — PASS.
Скриншоты пересняты, «Сатурн» и комета проверены глазами. TestLap —
PASS, dist/ пересобран (15:55). VDS снова НЕ трогал — декор клиентский.
**Попутно замечено:** в Soccer.gd появилась ЧУЖАЯ правка параллельного
окна (BALL_OUT_TIME — вбрасывание мяча, вылетевшего за борт); моего кода
не задевает, не трогал.
**Файлы:** scripts/TrackDecor.gd, tools/screenshot_track.gd,
tools/test_space.gd + TestSpace.tscn (новые), dist/.

### Правка того же дня: мяч за ограждением — вбрасывание в центр
Жалоба: «мяч улетел за ограждения и игра продолжается, почему он не
телепортируется в центр». ПРИЧИНА: страховка возвращала мяч, только если
он улетел дальше 6 м ЗА арену (или провалился), а газон-подложка тянется
шире арены — мяч, перелетевший борт и ЛЁГШИЙ в паре метров за ним (или
застрявший на крыше короба ворот), попадал в «мёртвую зону»: лежит на
земле, в зону возврата не попал, машинам через борт не дотянуться.
Правило переписано напрямую (Soccer._check_recovery): мяч НЕ в игровом
объёме (поле в бортах ЛИБО короб ворот, с учётом высоты GOAL_H) дольше
BALL_OUT_TIME = 2 c → вбрасывание в центр + анонс «Мяч вылетел». Пара
секунд — чтобы не дёргать мяч, честно скачущий по кромке борта;
провалился под пол (y < −1) — сразу.
**Стенд:** test_soccer.gd, новая фаза «вылет за борт»: мяч кладётся в 3 м
ЗА боковое ограждение (старый код: лежит до конца матча — порог был 6 м
за ареной) и через ~2 c обязан вернуться в поле. TestSoccer — PASS.
**ВНИМАНИЕ:** в рабочей папке параллельно живут ЧУЖИЕ незакоммиченные
правки (Soccer.gd: анти-застревание ботов; README: трасса «космос») — из
другой сессии. dist здесь НЕ пересобирался и коммита нет, чтобы не
вмешиваться в ту работу; игрок запускает игру из исходников (Играть.bat)
и правку получает сразу.
**Файлы:** scripts/Soccer.gd, tools/test_soccer.gd.

## Мина/лазер не «пропускают» машины; космос: планеты в фон, кометы-росчерки; значок глушилки (2026-08-31)
Задания: «машины даже не по сети иногда проезжают сквозь мины и лазер не
уничтожаясь»; «планеты в космосе должны быть на заднем фоне, а не перед
трассой, кометы — вдалеке в виде пролетающей белой полосы»; «при
оглушении игрок должен видеть значок эффекта над машиной, как при
магните».

### 1. Мина: задержка взведения защищает ТОЛЬКО хозяина (Mine.gd)
ПРИЧИНА «проезда сквозь мину»: взведение 0.7 c было ОБЩИМ — соперник,
ехавший впритык за сбросившим, доезжал до мины раньше взведения и
проскакивал безнаказанно. Теперь:
- `_armed` убран; в `_try_trigger` свежая мина игнорирует ТОЛЬКО
  dropper, пока `_arm > 0` (для остальных боевая с первого кадра);
- перепроверка наезда КАЖДЫЙ тик (`get_overlapping_bodies` в
  `_physics_process`), а не только `body_entered`: сигнал бьёт на входе
  в зону — машина, заехавшая на мину «призраком» (слой контактов снят)
  или в окне взведения хозяина, после снятия защиты оставалась стоять
  на мине безнаказанно.

### 2. Лазер: урон ВСЁ время жизни луча (Car.gd)
ПРИЧИНА «проезда сквозь лазер»: урон считался ОДИН кадр выстрела, а луч
виден 0.55 c (LaserFx.LIFETIME) и едет с носом стрелявшего — машина,
ВЪЕХАВШАЯ в видимый луч после кадра выстрела, оставалась цела. Теперь:
- `_use_laser` взводит `_laser_left = LaserFx.LIFETIME` (+ `_laser_lag`
  — отмотка, замеренная в момент выстрела) и делает первый прогон;
- `_laser_sweep(fwd)` (вынесен из `_use_laser`) повторяется каждый тик
  в `_tick_effects` от ТЕКУЩЕГО носа — ровно как картинка луча; жертвы
  после destroy — призраки, повторные тики их не трогают;
- `destroy()` гасит свой луч (`_laser_left = 0`): добивать соперников с
  точки возрождения было бы нечестно.
- const LASER_RANGE (70) вынесена на уровень класса.
Стенд tools/TestWeaponPierce.tscn (НОВЫЙ): свежая мина под чужим — гибнет
за ~10 тиков; жертва сбоку от луча цела; «въехала» в луч через 0.1 c —
гибнет. На старом коде обе половины — честный FAIL.
ПОПУТНО: tools/test_weapons.gd — фазы «магнит» и «глушилка» ставили
жертву в коридор ЕЩЁ ЖИВОГО луча от прежних фаз (по новому правилу она
гибла): расстановка магнитной жертвы сдвинута 440→460, глушилка
610→636, финальные проверки 640→668. Все 20 проверок — PASS.

### 3. Космос: планеты в фон, кометы — далёкие белые росчерки (TrackDecor.gd)
- Убраны 3 ближних «спутника» (30–44 м от полотна, висели перед
  камерой). «Сатурн» теперь В ДАЛЬНЕМ ФОНЕ (165 м от центра, y=62), но
  КРУПНЫЙ (радиус 20) и с двойным кольцом — читается отовсюду. Дальнее
  кольцо из 5 планет не тронуто.
- Кометы больше НЕ летят над трассой: пролёт по касательной к кругу
  радиуса COMET_DIST=260 м (ближе к центру не заходят), высота 70–130,
  скорость 70–110 м/с, лёгкое снижение — «падающая звезда». Вид — белая
  полоса: голова 1.6 + белый хвост-конус 60 м (был бело-голубой болид
  15 м прямо над полотном).
- tools/screenshot_track.gd: comet.png снимается С ТРАССЫ в сторону
  кометы (как видит игрок), добавлен comet_close.png (крупно, форма
  росчерка). Скриншоты сняты, проверены глазами: gameplay/mid_track без
  планет над полотном, комета — белая чёрточка вдали.

### 4. Значок глушилки — УЖЕ РАБОТАЛ, добавлен стенд
apply_scramble ещё с fcf1776 зовёт show_effect_icon(SCRAMBLE, 5 c) — тот
же механизм, что у магнита, у жертвы-игрока по сети едет через
NetFx.SCRAMBLE → _rx_fx. Стенд tools/TestScrambleIcon.tscn (НОВЫЙ)
подтвердил: значок появляется и висит все 5 c эффекта (PASS). Ничего не
менял; если игрок значка не видит — нужны подробности (какой режим/сборка).

### Регрессия
TestWeaponPierce, TestScrambleIcon (новые), TestMineKill, TestMineDrop,
TestWeapons (20/20), TestLaserRewind, TestSpace, TestLap, TestSoccer —
PASS. dist/ пересобран (16:44; первый прогон экспорта упал с кодом 255
без ошибок в логе — второй прошёл), из СОБРАННОГО dist TestWeaponPierce —
PASS. VDS ОБНОВЛЁН (шаги «Обновить сервер.bat»): active, PROTOCOL := 16
(протокол не менялся), TestNet против ЖИВОГО VDS — PASS.
**Файлы:** scripts/Mine.gd, scripts/Car.gd, scripts/TrackDecor.gd,
tools/test_weapon_pierce.gd + TestWeaponPierce.tscn (новые),
tools/test_scramble_icon.gd + TestScrambleIcon.tscn (новые),
tools/test_weapons.gd, tools/screenshot_track.gd, README.md, dist/.

## Ледышка быстрее; стены не продавливаются; песок злее; глушилка цепляет краем (2026-09-01)
Задания: «заморозка должна лететь быстрее», «много машин может продавить
тебя сквозь ограждения», «пески нужно сделать более замедляющими»,
«круги оглушения, задетые краем, не всегда оглушают».

### 1. Ледышка: 42 -> 55 м/с (Projectile.gd)
Скорость как у ракеты — прежние 42 не догоняли едущих (обычный потолок
~34, буст ~48). Жизнь та же (2.0 c) — дальность выросла до 110 м.

### 2. Продавливание сквозь ограждение (Car.gd: _clamp_inside_walls)
ПРИЧИНА: в куче из 6-8 машин суммарная депенетрация за кадр проносит
центр кузова за грань тонкой (0.5 м) стены, и дальше решатель
выдавливает машину уже НАРУЖУ. Новый кламп в _physics_process (только
авторитетные машины: LOCAL/OWNED/боты сервера; марионетки едут по
снимкам): на трассах с has_walls, пока машина НИЖЕ верха стены
(WALL_HEIGHT), центр не пускается дальше внутренней грани
(half_width - WALL_THICKNESS/2), скорость наружу гасится. Дальше пары
метров за гранью (limit + 2.5) НЕ трогаем: это не продавливание, а
честный перелёт стены — его возвращает автовозврат «Вне трассы» (тянуть
машину сквозь стену выглядело бы телепортом). Soccer не задет: там
car.track = null. TestOfftrack теперь проходит через кламп (машина у
самой стены возвращается мгновенно, dist=0.00) — прежний тайминговый
путь остаётся для дальних вылетов.

### 3. Песок: множитель тяги/потолка 0.55 -> 0.40 (Car.gd)
«Пески более замедляющие»: срезка по песку теперь ощутимо дороже.

### 4. Глушилка: попадание по КУЗОВУ, а не по центру (ScrambleWave.gd)
ПРИЧИНА «задел краем — не оглушило»: кольца расходятся ровно до HIT_R
(2.6), но мерялся только ЦЕНТР машины — кольцо, чиркнувшее по бамперу
или борту, до центра не доставало. Теперь три пробы по кузову (центр и
±1.1 м по курсу, как у Projectile) с порогом HIT_R + BODY_R (0.9).

### Регрессия
TestWeaponPierce расширен 3 фазами (волна цепляет краем: жертва в 3.2 м
сбоку от пути волны — старый код мазал; продавленный на 1 м за грань
возвращается; упавший в half+5 снаружи НЕ утягивается) — 6/6 PASS, в
т.ч. из СОБРАННОГО dist. TestWeapons (20/20), TestWallSlide, TestWallTurn,
TestWallRecover, TestOfftrack, TestSandLap, TestFreezeSpread, TestLap,
TestNeonLap, TestRamp, TestShove, TestSoccer, TestCarBounce — PASS.
dist/ пересобран (01.09 9:10). VDS ОБНОВЛЁН (active, PROTOCOL := 16,
протокол не менялся), TestNet против ЖИВОГО VDS — PASS.
**Файлы:** scripts/Projectile.gd, scripts/Car.gd, scripts/ScrambleWave.gd,
tools/test_weapon_pierce.gd, README.md, dist/.

## Сеть: бонусы не «перешагиваются», тараны симметричны; футбол: ведение мяча; стены гладкие (2026-09-01)
Задания: «по сети машины по-прежнему могут проехать сквозь бонус и не
получить оружие»; «при столкновениях тяжело оказывать воздействие на
других (развернуть), а моя машина разворачивается легко»; «в футболе:
удар передом примагничивает мяч, перехват — ударом по ведущей машине,
повторный удар передом примагничивает снова»; «стены то отталкивают, то
замедляют, то разворачивают — сделать гладкими, без рёбер».

### 1. Бонус по сети: замёт отрезка между снимками (WeaponBox.gd)
ПРИЧИНА: сервер проверял подбор по ТОЧКЕ последнего сырого снимка
владельца, а снимки приходят реже кадров сервера — на 30+ м/с сырая
точка перешагивает несколько метров, и куб 1.3 м проваливался между
двумя проверками. Теперь бокс помнит прошлую сырую точку каждой машины
(_last_true) и метёт весь отрезок прошлое→текущее шагом ≤1 м
(_gap_to_body). Рывок длиннее 12 м — телепорт (респавн), не метётся.
Стенд: test_box_pickup, фаза 2 — снимок за 4 м ДО куба, следующий в 4 м
ЗА ним (ни одна точка не задевает) — бонус обязан выдаться. PASS.

### 2. Тараны по сети: событие доходит и не дублируется (Main.gd, Car.gd)
Две причины асимметрии «их не сдвинуть, меня разворачивает»:
- **Толчок-событие выбрасывался.** _rx_shove требовал < 8 м между
  серверными «сейчас»-позициями, а агрессор видит жертву В ПРОШЛОМ
  (буфер ~0.35 c): на встречных 30+ м/с расхождение больше — честный
  таран молча отбрасывался. Теперь меряется и к текущей позиции жертвы,
  и к отмотанной на лаг агрессора (past_position/net_shot_lag) — хватает
  любой из двух.
- **Жертву било ДВАЖДЫ.** Событие прилетало сразу, а через ~0.35 c
  отстающая марионетка агрессора «доезжала» до контакта на экране жертвы
  и свой рикошет бил повторно. Новая память _shove_rx: применённый
  толчок-событие помечает агрессора, и свой импульс+закрутка об эту
  марионетку в окне 0.7 c пропускаются (выдавливание/расталкивание
  остаются — непроницаемость не страдает).

### 3. Футбол: ведение мяча (SoccerBall.gd)
Просьба реализована как магнит-сервоскорость (не телепорт — солвер
продолжает честно отталкивать мяч от бортов/штанг):
- удар ПЕРЕДОМ (конус ~45° от носа, GRAB_COS) — мяч примагничен
  (carrier), держится у носа (GRAB_DIST 2.85: полукузов+радиус+зазор);
- перехват: ЛЮБОЙ удар другой машины по ведущему — мяч отлипает с
  пинком от бьющего (DETACH_POP), экс-ведущему 1.2 c не липнет
  (_no_grab) — перехвативший успевает увести;
- повторный удар передом — снова примагничивается; смерть/призрак
  ведущего или отрыв якоря > 6 м (мяч зажало) — отлипание; reset_to
  (кикофф) чистит carrier и _no_grab; пока мяч ведётся, last_touch =
  ведущий (автор гола).
Стенд tools/TestSoccerDribble.tscn (НОВЫЙ): захват, ведение 45 кадров у
носа, перехват тараном по ведущему, запрет в окне, повторный захват —
PASS (и из собранного dist). Боты не трогались: их ai и так целит «сквозь
мяч» — ведут; соперники, тараня ведущего, отбирают.

### 4. Стены: для машин ЦЕЛИКОМ аналитические, рёбер больше нет (Car.gd)
ПРИЧИНА всех «то отталкивает, то замедляет, то разворачивает»: кузов
сталкивался с ТРИМЕШЕМ ограждения (1536 сегментов на круг) — стыки
давали рёбра, капы лечили только следствия. Теперь:
- кузов НЕ сталкивается со стеной в решателе: _wall_slide при первом
  вызове ставит add_collision_exception_with на тело группы "walls"
  (_wall_body, пере-добавляется после пересборки трассы). Исключение —
  только на пару машина-стена: снаряды, волна глушилки, мяч бьются об
  стену как раньше;
- «касание» (_touching_wall) — аналитическое: вылет кузова (reach)
  дотянулся до грани, посчитанной от ГЛАДКОЙ осевой кривой
  (half_width_at_offset), флаг _wall_near ставит _wall_slide;
- депенетрация — ручной кламп в _wall_slide: dist+reach не дальше
  грани, шаг ≤ 1.5 м/кадр, скорость наружу гасится. Ведение (редирект
  вдоль стены, штраф 40% v_out, скрежет, капы отброса) — как было;
- центр ЗА гранью (> +0.3 м) — НЕ пристенок: не ведём и не клампим
  (это перелёт/телепорт — возвращает автовозврат «Вне трассы»; ещё там
  может протухнуть track_offset, и dist мерялся бы к чужой точке оси);
- _clamp_inside_walls (страховка от продавливания кучей) сужен с
  limit+2.5 до limit+1.2: «войны» клампа с солвером больше нет, и
  широкий допуск ВТЯГИВАЛ БЫ поставленную за стеной машину сквозь стену
  (ловил TestOfftrack; продавливание глубже метра за кадр невозможно).
Телеметрия TestWallGhost: НОВОЕ throws=9 hops=111 против HEAD
throws=14 hops=128 — фантомных отбросов/подскоков меньше.

### Регрессия
TestSoccerDribble (новый), BoxPickup (+фаза 2), WallSlide, WallHit,
WallTurn, WallRecover, WallGhost, Offtrack, Weapons (20/20),
WeaponPierce, PuppetPush, PuppetSolid, CarBounce, Shove, Lap, NeonLap,
SandLap (флак: 1-й прогон FAIL, 2-й PASS — грабли известные), Ramp,
Bumps, FallThrough, MineKill, MineDrop, LaserRewind, FreezeSpread,
Soccer, SoccerStuck — PASS. Из СОБРАННОГО dist: SoccerDribble,
WallSlide — PASS. TestNet против ЛОКАЛЬНОГО сервера — PASS (отставание
ср. 136 мс). dist/ пересобран (01.09 10:40; экспорт опять упал первым
прогоном с кодом 255 — второй прошёл). VDS ОБНОВЛЁН (шаги «Обновить
сервер.bat»): active, PROTOCOL := 16 (протокол НЕ менялся). TestNet
против ЖИВОГО VDS — PASS (отставание ср. 156 мс).

**ГРАБЛИ:**
- TestPuppetPush и TestMineDrop печатают PASS, но падают на ВЫХОДЕ с
  кодом 0xC0000005 — и на чистом HEAD тоже (проверено в worktree):
  пре-существующий краш Godot при завершении, не наш.
- Запуская длинный процесс в PowerShell, НЕ обрезать его вывод
  `Select-Object -First N`: после N строк конвейер закрывается и
  УБИВАЕТ процесс (так был случайно убит локальный сервер для TestNet).

**Файлы:** scripts/WeaponBox.gd, scripts/Main.gd, scripts/Car.gd,
scripts/SoccerBall.gd, tools/test_box_pickup.gd,
tools/test_soccer_dribble.gd + TestSoccerDribble.tscn (новые),
README.md, dist/. Изменения НЕ закоммичены.

## Песок тормозит; глушилка глушит всех накрытых; футбол: анти-«паровозик» (2026-09-01)
Жалобы: «песок должен тормозить сильнее»; «круги выстрелов от глушилки
коснулись моей машины, но не оглушили»; «в режиме футбол двое таранят
друг друга и загоняют впередистоящую машинку вместе с мячом сразу в
ворота».

### 1. Песок активно тормозит (Car.gd)
Утром множитель тяги/потолка уже резали 0.55 → 0.40, но скорость ВЫШЕ
песчаного потолка не гасилась вовсе: тяга просто отключалась (условие
absf(speed) < eff_max), и влетевшая на 30+ м/с машина неслась по песку
накатом, теряя ~3 м/с за секунду (только штатный damping). Теперь на
песке горизонтальная скорость выше потолка гасится активной тормозной
силой brake_power × 1.3 (≈13 м/с²) — до песчаного потолка.
**Стенд:** test_sand_lap, НОВАЯ фаза 3 — влёт на песок 30 м/с без газа
(наискось от полотна, чтобы дуга дороги не вернула на асфальт), через
1.2 c скорость < 20: старый код 26.6 м/с (честный FAIL), новый 15.9.

### 2. Глушилка: точный кузов и глушит ВСЕХ накрытых (ScrambleWave.gd)
ДВЕ причины «кольца коснулись — не оглушило», обе живы и после утренних
трёх проб:
- пробы вдоль оси (±1.1 м, r=0.9) не покрывали борт МЕЖДУ пробами и
  углы бампера (щели 0.05-0.3 м). Теперь меряется дистанция до
  ПРЯМОУГОЛЬНИКА кузова (BODY_HALF_L 1.7 × BODY_HALF_W 1.0) пробами
  вдоль пути волны шагом 0.25 м, порог HIT_R + HIT_GRACE (0.2 — запас
  на шаг проб и сглаживание визуального кузова); _segment_gap удалён;
- волна ГАСЛА ОБ ПЕРВОГО задетого по списку группы: в куче машин
  (футбол!) волна глушила соседа и умирала, а машина игрока, визуально
  накрытая теми же кольцами, оставалась цела. Теперь глушатся ВСЕ
  машины, задетые в кадр смерти волны.
**Стенд:** test_weapon_pierce, НОВАЯ фаза 5 «двое по бокам пути волны
±2 м — оглушены ОБА»: HEAD — FAIL (только один), новый — PASS.

### 3. Футбол: анти-«паровозик» (Soccer.gd, SoccerBall.gd)
Сценарий жалобы: сзади таранят машину, ведущую мяч, и сцепка везёт её
вместе с примагниченным мячом прямо в ворота. Боты сами это устраивали:
гнались за мячом, который висит У НОСА ведущего, — то есть ехали
ведущему точно в корму. Закрыто с двух сторон:
- **SoccerBall._drive_carried**: удар в КОРМУ ведущего (бьющий сзади,
  dot с курсом > 0.6) срывает мяч ВБОК с линии тарана (перпендикуляр
  курса, DETACH_POP × 2 = 10 м/с) и режет его ход вперёд вдвое; удары
  сбоку отлипляют как раньше. Стенд: test_soccer_dribble, НОВАЯ фаза
  «таран в корму»: |vz| на HEAD 1.4 (FAIL), на новом 9.8.
- **Soccer.ai_drive**: мяч ведёт СВОЙ — эскорт сбоку-впереди, в корму
  не едем; ведёт ЧУЖОЙ — цель САМ ведущий (удар по нему и есть
  перехват), а не мяч у его носа; сам веду — цель чужие ворота; и
  ОБЩЕЕ правило _car_blocking: чужой кузов на курсе ближе цели
  (коридор 2.4 м, до 7 м) объезжается сбоку (цель — точка в 5 м сбоку
  от него), а вплотную (< 4.5 м) газ режется до 0.4. Стенд:
  test_soccer, НОВАЯ фаза «анти-паровозик» (машина на курсе бота к
  мячу): HEAD газ 1.00/руль 0.00 (FAIL), новый 0.40/−1.00.

### Регрессия
TestWeaponPierce (7/7), TestSandLap (3 фазы), TestSoccer,
TestSoccerDribble (6 фаз), TestScramble, TestWeapons (20/20),
TestSoccerStuck, TestLap, TestOfftrack, TestCarBounce, TestFreezeSpread,
TestMagnet — PASS. Честные FAIL всех четырёх новых фаз проверены
подменой соответствующих файлов из HEAD (git checkout HEAD -- <файл>,
потом возврат). dist/ пересобран (01.09 11:45, экспорт с первого раза),
из СОБРАННОГО dist TestWeaponPierce и TestSoccerDribble — PASS.
VDS ОБНОВЛЁН (шаги «Обновить сервер.bat»): active, PROTOCOL := 16
(протокол не менялся), TestNet против ЖИВОГО VDS — PASS (отставание
ср. 164 мс).
**Файлы:** scripts/Car.gd, scripts/ScrambleWave.gd, scripts/Soccer.gd,
scripts/SoccerBall.gd, tools/test_sand_lap.gd, tools/test_soccer.gd,
tools/test_soccer_dribble.gd, tools/test_weapon_pierce.gd, README.md,
dist/. Изменения НЕ закоммичены (как и прежние правки на ветке).

## Имена игроков и ботов: никаких «БОТ», все под своими именами (2026-09-01)
Задание: «при подключении не должно писаться, что это бот — все заходят
под своими именами. В Яндекс Играх брать имя оттуда, иначе спросить при
первом заходе и сохранить. Если игроков не хватает, игрок должен думать,
что боты — это другие игроки; нагенерить ~100 ников для ботов».

### Имя игрока
- `GameState.player_name` (profile.cfg, до 16 симв., `sanitize_name`).
  `display_name()` — имя или «Игрок». При ПЕРВОМ запуске гараж
  показывает модальное окно «КАК ТЕБЯ ЗОВУТ?» (ввод + ГОТОВО, Enter
  работает, закрыть без имени нельзя); потом имя меняется кликом по
  кнопке «ИМЯ: …» под строкой уровня (то же окно + ОТМЕНА).
  Пока окно открыто, листание машин и старт по Enter заглушены
  (`_name_dialog != null` в `_process`/`_start_race`).
- КРЮЧОК ПОД ЯНДЕКС ИГРЫ: `GameState.platform_name()` — в web-сборке
  читает `window.bhrPlayerName` через JavaScriptBridge (страница должна
  положить туда имя после init Yandex SDK: `ysdk.getPlayer().then(p =>
  window.bhrPlayerName = p.getName())`). Дало имя — окно не показывается
  и имя сохраняется само. Web-экспорта пока нет — крючок ждёт его.

### Ники ботов
`scripts/PlayerNames.gd` (новый): 112 ников (имена, уменьшительные,
имя+цифры, гоночные, латиница, смешные), `pick(count, exclude)` /
`pick_one` — тасование Фишера—Йетса СВОЕЙ RNG (глобальный поток
случайных чисел не сдвигается — детерминированные стенды целы).

### Сеть — ПРОТОКОЛ 15→16 был, теперь 16→17
- hello получил 4-й аргумент — имя игрока (`_say_hello`,
  `Main._rx_hello`); сервер чистит его, пустое → «Игрок N», дубль
  чужого — помечается номером слота (`_set_slot_name`).
- НОВЫЙ @rpc `_rx_names(PackedStringArray)` — имена всех слотов, сервер
  рассылает при каждом изменении (как ростер). Сдвиг таблицы RPC —
  потому и бамп протокола.
- `Main._names`: сервер/оффлайн раздают ботам ники при спавне (оффлайн
  слот 0 — сам игрок), клиент ждёт `_rx_names`. `car_label(i)` теперь
  отдаёт имя — лента событий, «ПЕРВАЯ КРОВЬ», «Вас уничтожил …» и лобби
  подписаны именами.
- Игрок ушёл ДО старта — боту в его слот выдаётся свежий ник; ушёл ВО
  ВРЕМЯ заезда — бот доигрывает под именем ушедшего (для остальных
  «игрок» просто продолжает ехать).

### Бот неотличим от игрока
- Лобби: подпись «БОТ» и серый цвет УБРАНЫ — слот бота показывается как
  слот живого игрока (ник, машина, оранжевый). Пустой слот — по-прежнему
  «Ждём игрока…» БЕЗ имени (ники приходят заранее и выдавали бы, кто
  приедет ботом). Статус «Пустые слоты заняли боты (N)» → «Все в сборе —
  поехали!», подсказка «(пустые слоты возьмут боты)» убрана.
- Стрелки над машинами и точки мини-карты: раньше оранжевое — ТОЛЬКО
  живой игрок (боты без стрелки/серые), теперь у ВСЕХ соперников
  одинаково (это разворот просьбы от 28.08 «отличать человека от ботов» —
  пользователь явно попросил обратное). `_rx_slot_taken` стрелки больше
  не трогает; маска `_slot_taken` осталась — по ней сетевая кухня ищет
  метку часов сервера (снимок слота без игрока) и лобби.
- Футбол: анонс гола «забил: Твин Милл» → «забил: <ник>» (Soccer._names,
  игрок — «вы»).

### Стенд TestNet ПЕРЕДЕЛАН под новое правило
Проверка «метки соперников скрыты без них» → «метки соперников видны
(и у ботов)»; бот для замера плавности ищется по `_slot_taken`, а не по
видимости стрелки. ScreenshotLobby обновлён (ники, новый статус).

**Регрессия:** Lap, Soccer, Weapons, Scramble, BoxPickup, MineKill,
PlaceFinish, RaceSize, Magnet — PASS; NET (локальный сервер) — PASS,
SizeNet — PASS, ResetGrid — PASS. Скриншоты лобби и гаража сняты,
раскладка проверена глазами (окно имени по центру, «ИМЯ: …» под уровнем).
**dist/ ПЕРЕСОБРАН** (13:17), TestSoccer прогнан ИЗ СБОРКИ — PASS.
**VDS ОБНОВЛЁН** (active, PROTOCOL := 17), TestNet против ЖИВОГО VDS — PASS.
«Обновить сервер.bat»: ожидание «PROTOCOL := 17».
**Файлы:** scripts/PlayerNames.gd (НОВЫЙ), scripts/GameState.gd,
scripts/CarSelect.gd, scripts/Net.gd, scripts/Main.gd, scripts/Lobby.gd,
scripts/Soccer.gd, tools/test_net.gd, tools/screenshot_lobby.gd,
README.md, «Обновить сервер.bat», dist/, /opt/bighead на VDS.
ОСТАЛОСЬ: web-экспорт для Яндекс Игр (крючок имени готов); изменения не
закоммичены (как и прежние правки на ветке Andrei).

## Декор из Unity-ассетов: пустыня и ночной город обросли моделями (2026-09-01)
Задание: посмотреть, что можно взять для игры из ассетов
`E:\UnityProjects\cars\Assets\ithappy` (Cartoon City Free) и
`E:\UnityProjects\cars\Assets\Palmov Island` (Low Poly Atmospheric
Locations Pack), и применить подходящее. Это Unity-паки, но игра на
Godot — юнитевские .prefab/.mat не нужны, взяты FBX-модели + текстуры
(Godot 4.3 читает FBX через ufbx из коробки).

### Что взято (скопировано в assets/models/track_env/)
- `palmov/` (Palmov Island, вся палитра — одна текстура texture_main.png):
  pyramid_a/b, sphinx, skull_mountain, cactus_a..f, stone_a..d,
  dry_tree_a/b, palm_large/bent (пока не расставлены), grass_yellow_a/b.
- `city/` (ithappy, палитра texture_city.png + texture_signs.png):
  building_grid/terrace/tower (+ *_night.fbx — отдельные мешы светящихся
  окон, ставятся в ту же точку), traffic_light_a/b/c, bus_stop.
- Масштабы моделей реальные (кактус ~2.4 м, пирамида 10-16 м, здания
  24-53 м) — проверено дампом tools/dump_props.gd (НОВЫЙ).

### TrackDecor.gd
- ПУСТЫНЯ (_build_desert, вызывается для KIND_SAND): дальние монументы
  по секторам круга (большая пирамида ×3.2, сфинкс, малая пирамида,
  гора-череп; ищут место с зазором от полотна, чуть утоплены в песок) +
  россыпь с фикс-зерном: 46 кактусов, 30 камней, 14 сухих деревьев,
  40 пучков жёлтой травы. Всё без коллизий — по песку ездить можно.
- ГОРОД (_build_city_landmarks, перед процедурными коробками): до 9
  настоящих зданий «первой линии» фасадом к трассе, масштаб по капу
  высоты h ≤ 0.5·d (как у коробок, чтобы не закрывать камеру), ночной
  меш окон поверх. Обочины неона: светофоры (светятся — материал
  Emissive) и автобусные остановки вместо флагов и конусов.
- Материалы: FBX обоих паков ссылаются на текстуры, которых нет в
  импорте, — назначаются вручную в _apply_cartoon_materials/_material:
  «texture main[.NNN]» → палитра palmov; Color/Color_Glossy/Emissive/
  Glass/Road_Signs → палитры city (Emissive: чёрный emission-цвет +
  emission_texture, оператор ADD — светятся сами текселя палитры).
- Космос не тронут (просьба 31.08 — никаких земных строений).

### Проверка
`--headless --import` чистый; TestLap, TestSandLap, TestNeonLap,
TestOfftrack — PASS. Скриншоты ShotSand/ShotNeon сняты и просмотрены:
пустыня — кактусы/камни/сухие деревья у дороги, пирамида с белой
вершиной и гора-череп на горизонте; город — здания с горящими окнами
(окна эмиссивные, видны), башня-спираль, светофоры. Предупреждение
импорта «Textures1.png … no data» — безвредное (текстуры назначаем
кодом).
**Файлы:** scripts/TrackDecor.gd, tools/dump_props.gd (НОВЫЙ),
assets/models/track_env/palmov/ (НОВАЯ, 21 файл),
assets/models/track_env/city/ (НОВАЯ, 12 файлов), README.md.
dist/ НЕ пересобран (декор — клиентский визуал; play.bat запускает из
исходников). Изменения не закоммичены (как и прежние правки на ветке).

## Тематический декор трасс; фонари только в городе и светят (2026-09-01)
Задание: «трассы должны быть тематическими — пустыня получает всё
пустынное из ассетов, гоночная трасса — трибуны и гоночное; фонарные
столбы нужны только для ночной трассы и от них должен быть свет;
побольше элементов и деталей».

### Докопировано из Unity-паков (в assets/models/track_env/)
- palmov/: barrel_a/b, box_a/b, cart, hitching_post, horse_brown/white,
  water_tank, wooden_post, info_post (пока не расставлен), railway,
  railway_station, train, wagon_freight/passenger, grandstand_a/b,
  stadium_light.
- city/: billboard_a/b, billboard_wide, signboard, fountain,
  trash_can_a/b, texture_banners.png (материал «Billboard»).

### TrackDecor.gd — трассы разведены по темам
- ОБОЧИНЫ теперь тематические (_build_roadside): классика — флаги, шины,
  конусы, реклама; пустыня — бочки, ящики, деревянные вешки, кактусы,
  камни; город — светофоры, урны, остановки, билборды. Pole_light_free
  из обочин УБРАН. Шаг обочины 21 → 18 м (плотнее).
- ФОНАРИ только в ночном городе (_build_street_lamps, НОВАЯ): шаг 40 м,
  чередуя стороны, ~15-18 штук; у головки каждого — OmniLight3D
  (тёплый 3.2 / радиус 19, без теней) — на полотне видны пятна света.
- ПУСТЫНЯ: трибун больше НЕТ (тематика); стартовая зона — вестерн-паддок
  (водонапорка вместо башни, телега вместо тента, коновязь с двумя
  лошадьми, бочки-ящики вдоль прямой вместо табличек команд; арка и
  стартовые огни остались — функциональные). НОВОЕ: вестерн-станция
  (_build_desert_station — рельсы, паровоз + 2 вагона, здание станции,
  водонапорка, коновязь, ящики; одна сцена в свободном секторе),
  3 оазиса с пальмами (_build_oases), россыпь гуще (70 кактусов,
  46 камней, 20 сухих деревьев, 60 пучков травы) + 18 брошенных
  бочек/ящиков и 6 пасущихся лошадей.
- КЛАССИКА (_build_race_extras, НОВАЯ): 2 грандстенда Palmov + малая
  трибуна cartoon, 4 мачты стадионного освещения (днём не горят —
  антураж), 3 широких билборда; перед крутыми поворотами таблички
  отсчёта 3-2-1 (prop_distance, классика и город — _build_turn_signs).
- ГОРОД: + вертикальные светящиеся вывески signboard на высоких
  коробках (подвешены сверху: у меша якорь ВВЕРХУ, габарит y -5.4…0.25),
  2 фонтана на «площадях», билборды/урны/остановки на обочинах.
  Остановка развёрнута ЛИЦОМ к дороге (флаг разворота в списке обочин —
  у модели «спина» в +Z).
- БАГФИКС: грандстенды ложились НА ПОЛОТНО — _outward смотрит только на
  свой участок, а снаружи может идти соседний виток контура. Теперь
  место проверяется глобально (_track.distance_from_axis ≥
  TRACK_HALF_WIDTH + 1.5 + полурадиус пропса), не влезло — та же вещь
  пробуется дальше по кругу (до 10 сдвигов по 0.021).

### Проверка
TestLap, TestSandLap, TestNeonLap, TestOfftrack — PASS. Скриншоты
ScreenshotTrack/ShotSand/ShotNeon просмотрены: трибуны с полотна ушли,
пятна фонарного света на дороге видны, станция/оазисы/лошади в пустыне
на месте. Полный состав декора пустыни проверен счётчиком нод (все
модели инстанцируются, сфинкс в том числе).
**Файлы:** scripts/TrackDecor.gd, README.md,
assets/models/track_env/palmov/ (+17 файлов),
assets/models/track_env/city/ (+8 файлов). dist/ не пересобран,
изменения не закоммичены (как и прежние правки на ветке).

## Космос: шаттл на орбите и дрейфующие ускорители (2026-09-01)
Задание: «а для космоса?» — тематический декор и для космической трассы.
Земные строения по-прежнему запрещены (просьба 31.08), взято только
космическое из пака Palmov:
- assets/models/track_env/palmov/: + space_shuttle.fbx (15×8×22 м),
  rocket_booster.fbx (труба 27 м вдоль Z).
- TrackDecor._build_space: ШАТТЛ (масштаб 2.2) летит по круговой орбите
  R=150 (между трассой и кольцом планет), высота 55 ± волна, полный
  оборот ~2 мин, нос по ходу (у модели «перёд» в -Z — look_at попадает);
  движение в _process рядом с кометами. 5 УСКОРИТЕЛЕЙ дрейфуют среди
  астероидов (высота 5-16 м, случайный наклон) и медленно кувыркаются
  (_junk: node+spin, вращение в _process).
- СТЕНД tools/test_space.gd ОБНОВЛЁН: проверка «строений нет» →
  «ЗЕМНЫХ строений нет» (shuttle/booster — разрешённые космические
  аппараты), НОВАЯ проверка «шаттл летит» (за секунду > 3 м; орбитальная
  скорость ~7.9 м/с). TestSpace — PASS (6/6), TestLap/TestSandLap/
  TestNeonLap — PASS. Скриншоты сняты временным стендом ShotSpace из
  скретчпада (в tools не добавлял): шаттл читается с трассы на фоне
  планеты, ускорители видны у горизонта.
**Файлы:** scripts/TrackDecor.gd, tools/test_space.gd, README.md,
assets/models/track_env/palmov/ (+2 файла). dist/ не пересобран,
изменения не закоммичены.

## «СТАРТ» = по сети; опыт+монеты и расчёт экономики (2026-09-02)
Задание: 1) оставить одну кнопку «СТАРТ», она везёт ПО СЕТИ, кнопку
«ПО СЕТИ» убрать; 2) сделать систему опыта и внутриигровой валюты и
ПРОСЧИТАТЬ экономику под будущий контент (20 машин ×5 улучшений,
9+5 видов оружия ×4 улучшения, уровни открывают, монеты покупают,
реклама парами: 2 ролика → награда, кулдаун 10 мин; машины заменим).

### 1. Кнопка «СТАРТ» теперь сетевая (CarSelect.gd)
- «ПО СЕТИ» удалена; _join_pressed влит в _start_race: гонка — парсим
  адрес из поля (пусто — сразу оффлайн), Net.race_size, join_server,
  таймаут-сторож. Футбол — оффлайн как раньше (протокола нет).
- Сервер не ответил за 5 с (_watch_connect_timeout) или отказал
  (_on_join_failed) — ТИХИЙ фолбэк в оффлайн-заезд (_start_offline,
  НОВАЯ): боты подписаны человеческими никами (правка 01.09), игрок
  разницы не видит; сообщение «можно играть с ботами» убрано — оно
  выдавало ботов. Причина отказа — в лог print.
- Поле адреса и статус-строка остались (нужны для своего сервера).
- Комментарии про «ПО СЕТИ» поправлены: Net.gd:105, test_size_net.gd,
  README (раздел «Клиент»).
- ГРАБЛИ Edit-инструмента: в CarSelect.gd переносы строк «\» местами
  СХЛОПНУТЫ в одну строку с табами внутри (валидный GDScript) — правка
  по видимому тексту не матчится, сверять байты.

### 2. Монеты и новая кривая уровней (GameState.gd)
- money + add_money/try_spend/place_money; PLACE_MONEY [600,450,350,250],
  KILL_MONEY 25; бонус за взятый уровень LEVEL_MONEY 200×N (в add_xp).
- level_info: цена уровня 100·N → 40·N+60 (до 30-го ~19 140 опыта,
  ~320 заездов; старый xp игроков сохраняется, уровни пересчитаются
  вверх — приятно). Проверено скретч-скриптом: границы 100/640/2340/
  19140 — PASS.
- Реклама (учёт готов, UI и ysdk.adv — с web-экспортом): пары по
  AD_PAIR_SIZE=2 ролика, награда AD_PAIR_REWARD=500 за ПАРУ, кулдаун
  AD_COOLDOWN=600 с; ad_available/ad_cooldown_left/register_ad;
  состояние в profile.cfg (_save_profile — общий сейв xp/money/рекламы).
- Main._show_finish: +монеты за место и килы, баннер «+N ОПЫТА ·
  +N МОНЕТ · УРОВЕНЬ…». Soccer: победа/ничья/поражение 500/300/200,
  гол +50. CarSelect: строка «УРОВЕНЬ · ОПЫТ · МОНЕТЫ» (шрифт 17→15).

### 3. ЭКОНОМИКА.md (НОВЫЙ) — весь расчёт
Доходы (~4 800 монет/ч заезды + до 3 000/ч реклама + 92 800 бонусов за
30 уровней ≈ 7 800/ч), кривая уровней (30-й за ~26 ч), таблица
разблокировок по уровням (машины №2..20 на ур.2..30, новые оружия на
ур.5/9/13/17/21, ступени улучшений), цены: машины 600..92 000 (сумма
552 800), улучшения машин 8/11/14/17/20% цены машины (~387 700), новые
оружия 2 500..30 000 (70 500), улучшения оружия 300..6 400 (100 500).
Итог ~1.11 млн; 100% ≈ 130 ч, «нормальный путь» (топ-машина+фавориты)
≈ 33-36 ч. Принцип: разблокировка раньше денег — всегда видна цель.
Осталось делать: магазин в гараже, применение улучшений к физике,
кнопка рекламы + ysdk.adv.showRewardedVideo, 5 новых оружий, парк из
20 машин.

### Проверка
--headless --import чистый; TestLap PASS, TestSoccer PASS,
TestSelectPrefill PASS (инстанцирует CarSelect — гараж собирается).
Скретч-проверка кривой уровней PASS. dist/ НЕ пересобран (клиентские
правки, протокол 17 не менялся; пересобрать перед раздачей игрокам!).
**Файлы:** scripts/CarSelect.gd, scripts/GameState.gd, scripts/Main.gd,
scripts/Soccer.gd, scripts/Net.gd (коммент), tools/test_size_net.gd
(комменты), README.md, ЭКОНОМИКА.md (НОВЫЙ). Не закоммичено (как и
прежние правки на ветке Andrei).

## Машины Unity-пака вместо hotwheels; масло крутит В ограждение; заметная мина (2026-09-02)
Задание: «добавь машины из E:\UnityProjects\cars\...\CarsAssetPack, удали
hotwheel-тачки; масло должно сильнее разворачивать машину в ограждение
(эффект не заметен); сделай мину визуально заметнее».

### Новые машины (assets/models/unitycars/source/, Car-1..8.fbx)
- Из Unity «Low Poly Car Vehicle Pack» скопированы 8 FBX. Текстуры
  Color-pallet-1.jpg в паке НЕТ (импорт ругается warning'ом — безвредно):
  цвета сидят в материалах FBX, машины цветные.
- Устройство ИНОЕ, чем у GLB-паков: один файл = одна машина ЦЕЛЬНЫМ мешем,
  узлов-колёс нет (колёса запечены в кузов и не крутятся — collect_wheels
  переживает, _animate_wheels выходит по пустому списку). Нос у всех в +Z.
- CarModelLibrary: НОВОЕ — SINGLE_CAR_PATHS (id → путь к файлу) и
  _build_single/_collect_meshes: обход дерева файла с накопленным
  трансформом, AABB, центрирование, разворот на PI, масштаб до
  target_length. build() сперва смотрит в SINGLE_CAR_PATHS, потом GLB_PATHS.
  Кэш паков (_pack_cache) общий.
- ID/имена (CarSelect.DISPLAY_NAMES): fastback=Fastback (жёлтый маслкар),
  godfather=Godfather (чёрный седан), lemans=Le Mans GT (оранжевый гоночный),
  superbird=Superbird (синий с антикрылом), chevelle=Chevelle SS (голубой),
  diablo=Diablo (фиолетовый суперкар), dragster=Dragster (красный, открытые
  колёса), safari=Safari 4x4 (красный внедорожник). Стоят ПЕРВЫМИ в CAR_IDS.
- УДАЛЁН пак hotwheels (Turbo Driver): assets/models/hotwheels/ стёрт,
  путь из GLB_PATHS и 8 id (stock, rd, sharky, invader, arachno, dk, dakar,
  jt) из CAR_IDS/DISPLAY_NAMES убраны. Всего машин по-прежнему 41.
  GameState.selected_car_id по умолчанию: sharky → fastback (id в профиле
  не хранится, миграция не нужна). tools/dump_glb.gd дефолт → HW35.
- tools/test_car_build.gd: для одиночных машин порог «деталей ≥ 1» (цельный
  меш), для паков по-прежнему ≥ 2.

### Масло: занос В ограждение (Car.gd)
Раньше закрутка выбиралась ОТ стены, а у самой стены её съедали
_clamp_heading и гашение рысканья в _wall_slide — выходил еле заметный
вильон. Теперь:
- apply_oil_slip: сторона закрутки = В СТОРОНУ стены (минус от
  _spin_away_from_wall; на свободном полотне — случайная), закрутка
  4.6..6.0 рад/с (было 3.2..4.4), кап рысканья 6.5 (было 4.5).
- slip_duration 2.0 → 2.6 c, slip_grip 0.3 → 0.15, slip_thrust 0.35 → 0.25.
- _clamp_heading: при _slip_time > 0 лимит поперечного угла ОТПУЩЕН
  (машину честно разворачивает, хоть задом наперёд); после заноса перебор
  снимает штатный мгновенный кламп (совпадает с возвратом управления).
  Плавный возврат пробовал — ломает TestHeading (кламп обязан быть жёстким
  вне заноса), откатил.
- _wall_slide: при заносе финальный доворот вдоль стены и обнуление
  angular_velocity.y ПРОПУСКАЮТСЯ (иначе стена мгновенно съедала вращение).
  Ведение/депенетрация/капы работают как раньше — машина скребёт
  ограждение, теряя 40% скорости сближения.
- tools/test_weapons.gd: фаза «масло у стены» ПЕРЕВЁРНУТА под новое
  поведение — «занос у стены крутит в стену» (знак закрутки) и «нос
  развернуло в стену» (пик проекции носа на нормаль стены > 0.35,
  копится по кадрам 531..559 — к контрольному кадру нос успевает
  провернуться дальше).

### Мина заметнее (Mine.gd, только визуал — газонокосилка та же)
Корпус крупнее (низ 0.72/верх 0.55, высота 0.3), вокруг СВЕТЯЩАЯСЯ
жёлтая сигнальная полоса (эмиссия), 6 шипов наружу (силуэт морской мины),
красный маячок крупнее (r=0.16) и МИГАЕТ ~1.4 Гц (пульс эмиссии 1.2..7.7
и albedo в _physics_process, детерминированно, без randf; мигает и у
инертной копии клиента). Радиусы срабатывания/взрыва НЕ менялись.

### Проверка
test_car_build 41/41 ok. TestLap, TestSandLap, TestNeonLap, TestHeading,
TestWallSlide, TestWallHit, TestWallRecover, TestMineDrop, TestMineKill,
TestWeapons, TestOfftrack, TestSoccer, TestSelectPrefill, TestExplosion —
PASS. TestWallTurn — FAIL (левый борт 1.48 м при норме 1.5), но он падает
ТОЧНО ТАК ЖЕ на нетронутом Car.gd из HEAD (проверено подменой файла) —
поломка существовала до этой сессии, к правкам не относится.
Скриншоты (временные стенды, в tools не оставлены): 8 машин в ряд с обоих
ракурсов, гараж (сетка 41, подиум Fastback), старт заезда, мина крупным
планом в двух фазах мигания — всё просмотрено, посадка/масштаб/нос в норме.
**Файлы:** scripts/CarModelLibrary.gd, scripts/CarSelect.gd,
scripts/GameState.gd, scripts/Car.gd, scripts/Mine.gd,
tools/test_car_build.gd, tools/test_weapons.gd, tools/dump_glb.gd,
assets/models/unitycars/ (+8 FBX), assets/models/hotwheels/ УДАЛЁН.
dist/ не пересобран, изменения не закоммичены (как и прежние на ветке).

## Визуализация экономики; скины; доход с Яндекс-рекламы (2026-09-02)
Задания: «визуализируй экономику таблицей/диаграммой», «посчитаем
сколько можно получить за яндекс рекламу», «у некоторых машин ещё будут
разные скины».

### Артефакт «Экономика Big Head Racing»
https://claude.ai/code/artifact/c5eb8cbb-8fc4-4867-af5f-1b0f54941635
(источник в скретчпаде сессии bhr_economy.html; правится ТОЛЬКО
перепубликацией по этому URL). Тёмный «гаражный» стиль (Russo One +
Rubik), 9 секций: итоги, доходы (полосы наград + цикл рекламы), кривая
уровней (SVG, ховер), дорога уровней 1-30 (чипы разблокировок), цены
машин (бары + таблица), оружие + скины, котёл 1,29 млн (составная
полоса, 5 категорий, палитра проверена валидатором dataviz), темп
прогресса (2 линии, вехи), доход с рекламы в ₽, принципы удержания.
ГРАБЛИ панели предпросмотра: file:// вне проекта — статический снапшот
без JS; внутри проекта рендерится живьём, но НЕ перерисовывается ниже
~1900px от верха (чёрные скриншоты) — проверять секции, скрывая верхние
display:none. Времянка _preview_econ.html удалена.

### Скины машин (дополнение ЭКОНОМИКА.md, раздел 7а)
Косметика: у 10 машин (чётные слоты №2..20) по 2 скина — 20% и 40%
цены машины (мин. 300/600). Сумма 181 200; весь контент теперь
1 292 700 (100% ≈ 155 ч с рекламой). Скины без уровня — «бесконечная»
трата после выкупа игрового контента. В коде пока нет — с магазином.

### Доход с рекламы Яндекс Игр (ЭКОНОМИКА.md, раздел 8а)
Ставок Яндекс не публикует; по опыту разработчиков ~20 ₽/1000 показов
чистыми (rewarded дороже) → сценарии 20/40/80 ₽. ~9 показов на DAU/день
(4-6 rewarded пар + потенциальный interstitial). Один ежедневный игрок
≈ 5-22 ₽/мес; DAU 1000 ≈ 5-22 тыс. ₽/мес; DAU 10 000 ≈ 54-216 тыс.
Вывод: решает удержание, не ставка.

Параллельная сессия сменила парк машин (assets/models/unitycars,
CarSelect DISPLAY_NAMES, GameState selected_car_id "fastback") — мои
правки с ней не конфликтуют.
**Файлы:** ЭКОНОМИКА.md (разделы 7а, 8а, сходимость), PROGRESS.md.
Код игры в этой сессии больше не менялся.

## Удалён и пак Highway 35 — в игре только 8 машин Unity-пака (2026-09-02)
Задание: «hotwheels-машины остались в меню выбора, нужно убрать» — под
hotwheels имелся в виду и пак HW35 (это тоже Hot Wheels), а не только
Turbo Driver.
- assets/models/highway35/ УДАЛЁН; CarModelLibrary: GLB_PATHS теперь ПУСТ
  (код _build_from/_pack_src оставлен на случай будущих паков), из
  CAR_IDS убраны 33 id HW35 — остались 8 одиночных машин Unity-пака.
- CarSelect.DISPLAY_NAMES: убраны 33 имени. README: «выбор из 41 машины»
  → «из 8 машин».
- Стенды на старых id переведены на новые: tools/shot_headlights.gd
  (все 8 машин), tools/screenshot_lobby.gd (слоты лобби),
  tools/dump_glb.gd (дефолт — Car-1.fbx).
- Кэш миниатюр user://thumbs хранится по id — старые файлы не читаются,
  чистка не нужна.
- ФЛАК TestWeapons: один прогон упал на фазе «масло у стены» — в неё
  вмешался автовозврат бота (машина 1, [respawn] t=9.7). Прогнан
  трижды после — 3/3 PASS без автовозвратов; закрутка у стены
  детерминированная (av.y=-5.08 в стену, dist 9.75 при полуширине 11).
  Урок ДЛЯ ОТЛАДКИ СТЕНДОВ: `var x := _main._track.метод(...)` в
  тестовых скриптах НЕ парсится (у _main динамический тип — вывод типа
  невозможен), и сломанный парсом скрипт тихо превращает тест в
  ВИСЯЩИЙ процесс (сцена без скрипта, quit не зовётся) — типизируй
  явно: `var x: float = ...`.
### Проверка
test_car_build 8/8 ok; TestLap, TestSoccer (8 машин ровно хватает на
футбол 4×4), TestNeonLap (фар 16 — фары садятся и на цельные меши),
TestSelectPrefill, TestWeapons ×3 — PASS. Скриншот гаража: сетка из 8,
подиум Fastback, «1 / 8».
**Файлы:** scripts/CarModelLibrary.gd, scripts/CarSelect.gd, README.md,
tools/shot_headlights.gd, tools/screenshot_lobby.gd, tools/dump_glb.gd,
assets/models/highway35/ УДАЛЁН. dist/ не пересобран, не закоммичено.

## Советские машины со скинами; магазин машин за уровни и монеты (2026-09-02)
Задание: «добавить машины из E:\UnityProjects\cars\...\Low Poly Soviet Car
Pack и выбор скинов; изначально доступны только vz01, vz21, vz02 со всеми
цветами, остальные открывать постепенно за уровни и деньги».

### Пак (assets/models/sovietcars/)
- 15 машин × 10 цветов = 150 FBX (source/<id>/<id>_<цвет>.fbx) + общие
  текстуры-палитры Materials/Textures/albedo.png и emission.png (копии
  палитр лежат и в каждой папке машины — на случай путей внутри FBX).
- Устройство FBX: кузов + 4 узла-колеса (*wheel_fl/fr/bl/br) с осью в
  ступице; нос в +Z; материал БЕЗ текстуры (в Unity она сидела во внешнем
  .mat) — палитру назначает код. Цвет машины выбирают UV (у всех цветов
  один меш, разные UV кузова).
- ID: vz01 Копейка, vz02 Двойка, vz21 Нива, vz03..vz07 классика, vz05r
  Пятёрка Спорт, vz08 Зубило, vz09 Девятка, vz099 Самара 99, gz21/gz24
  Волги, vz31 Нива Лонг (идентифицированы по габаритам кузовов).

### CarModelLibrary.gd
- НОВОЕ: SOVIET_IDS/SOVIET_COLORS/DEFAULT_COLORS; id скина «база_цвет»
  (vz01_red); base_id()/color_of_id()/skin_id()/has_skins();
  CAR_IDS теперь БАЗОВЫЕ id (23: 15 советских + 8 unitycars).
- _build_single обобщён: узлы с "wheel" оборачиваются в пивоты по центру
  AABB меша (у vz05r геометрия правых колёс смещена от узла — потому
  AABB, а не позиция узла), меты wheel_radius/is_front/spin_sign — колёса
  советских машин КРУТЯТСЯ и рулятся (collect_wheels/_animate_wheels
  подхватили без правок; unitycars как были цельным мешем, так и остались).
  «Перёд» — по знаку z переднего колеса (wheel_f), без колёс — как раньше +Z.
- _soviet_material(): общий StandardMaterial3D с палитрой. ДВЕ ГРАБЛИ:
  1) albedo_texture_force_srgb = true — текстура, назначенная кодом из
     2D-импорта, читается линейной, и краски выцветают в пастель;
  2) ГЛАВНАЯ: у эмиссии оператор по умолчанию ADD — цвет emission
     ПРИБАВЛЯЕТСЯ к текстуре. emission = Color.WHITE заливал ВСЕ машины
     серым +0.5 (чёрная выглядела светло-серой; часы отладки: пиксель
     0.7333 = sRGB(0.5) выдал константную белую добавку). Лечение:
     emission = Color.BLACK, светятся только фары/стопы из текстуры.
     Плюс: 2D-CompressedTexture2D в слоте эмиссии вовсе не биндится
     (шейдер читает белый) — обе текстуры перегоняются в ImageTexture.
- shuffled_bot_pool(): пул ботов — по случайному цвету на каждую базу,
  Фишер–Йетс на СВОЁМ RandomNumberGenerator: pick_random()/shuffle()
  сдвигали глобальный поток randf, и детерминированный TestWeapons падал
  (фаза «мина толкает дальних») — поймано, изолировано, PASS.

### GameState.gd — владение и покупка
- FREE_CARS = [vz01, vz02, vz21]; CAR_UNLOCKS: база → Vector2i(уровень,
  цена) — слоты №2–20 из ЭКОНОМИКА.md (таблица соответствия — там же,
  раздел 9). car_owned/car_unlock_level/car_price/try_buy_car (проверка
  уровня и монет), color_of/set_car_color/select_car.
- Профиль: owned_cars, car_colors, selected_car (база). selected_car_id
  теперь ПОЛНЫЙ id скина (vz01_red) — он уезжает в сетевой hello и
  определяет визуал у соперников. Дефолт vz01_red; пропавшая/некупленная
  база в профиле → откат на Копейку.

### CarSelect.gd — гараж
- DISPLAY_NAMES: русские прозвища. Ряд из 10 цветных квадратиков-скинов
  под подиумом (клик красит подиум, миниатюру в сетке и профиль; жёлтая
  рамка — текущий цвет; у unitycars ряд скрыт).
- Закрытая машина: на подиуме ТЁМНЫЙ силуэт (material_override), имя
  серое, вместо «СТАРТ» — «КУПИТЬ · цена» (уровень мал — серая
  «С N УРОВНЯ · цена»; не хватило монет — мигает «НЕ ХВАТАЕТ МОНЕТ»),
  в сетке ячейка затемнена со значком «N ур.», тултип с ценой. Старт и
  Enter на закрытой машине заблокированы.
- Миниатюры кэшируются по ПОЛНОМУ id скина (vz01_red.png) — смена цвета
  дорисовывает недостающую одним вьюпортом (_update_thumb).
- Main._pick_car_ids / Soccer._pick_car_ids: пул shuffled_bot_pool,
  уникальность по БАЗЕ (двух одинаковых моделей разного цвета в заезде
  не будет; боты ездят и на закрытых машинах — витрина будущих покупок).

### Проверка
test_car_build расширен: все 150 скинов + 8 unitycars, у советских
обязателен кузов + 4 пивота колёс — 158/158 ok. Скретч-тест магазина
(бэкап profile.cfg делался и восстановлен; бэкап оставлен:
%APPDATA%\Godot\app_userdata\Big Head Racing\profile.cfg.bak_2026-09-02):
стартовые в собственности, покупка требует уровень и деньги, повторная
невозможна, владение/цвет/выбор переживают перезапуск — PASS. Стенд
гаража: скриншоты «закрытая Четвёрка с КУПИТЬ · 1 200» и «Копейка
перекрашена в жёлтый» — глазами ок. TestSelectPrefill, TestLap,
TestSoccer, TestWeapons, TestMineKill, TestBoxPickup, TestSandLap,
TestRamp, TestOfftrack, TestWheelSink, TestNeonLap, TestHeadlights —
PASS. Кэш миниатюр user://thumbs чистился при отладке (пересоздаётся сам).
**dist/ НЕ пересобран, VDS НЕ обновлён** — перед раздачей игрокам
пересобрать (сетевой ростер теперь с id вида vz01_red: старый клиент на
новом сервере покажет таким машинам бокс-заглушку, не краш).
**Файлы:** scripts/CarModelLibrary.gd, scripts/GameState.gd,
scripts/CarSelect.gd, scripts/Main.gd, scripts/Soccer.gd,
tools/test_car_build.gd, assets/models/sovietcars/ (НОВЫЙ, 150 FBX +
текстуры), README.md, ЭКОНОМИКА.md (раздел 9). Не закоммичено (как и
прежние правки на ветке Andrei).

## Дособрано: dist пересобран, VDS обновлён (2026-09-02, продолжение)
Задание: «дособирай всё» — хвост прошлой секции (сборка и деплой).
- **dist/ ПЕРЕСОБРАН** (11:25): экспорт debug-шаблоном, первый прогон
  привычно упал с кодом 255 — второй прошёл. pck 36.8 → 57.9 МБ
  (+150 FBX советского пака; лимит GitHub 100 МБ не задет).
- Из СОБРАННОГО dist: test_car_build 158/158 ok, TestSelectPrefill,
  TestLap — PASS.
- **VDS ОБНОВЛЁН** (шаги «Обновить сервер.bat»): tgz 45.5 МБ (scripts,
  scenes, project.godot, assets — вместе с sovietcars), stop → tar →
  chown → --import → start; active, PROTOCOL := 17 (не менялся).
- TestNet против ЖИВОГО VDS — PASS.
**Файлы:** dist/BigHeadRacing.exe, dist/BigHeadRacing.pck. Код игры не
менялся. Не закоммичено.

## Ревью кода: ошибки, лишнее, сеть (2026-09-02, только анализ — код НЕ менялся)
Задание: «проверь код на ошибки, найди лишнее, посмотри, можно ли улучшить
игру по сети / скорость по сети». Прочитаны Net/Rooms/Lobby и сетевая часть
Main/Car лично, остальное — четырьмя параллельными ревью. Итог отдан
пользователю списком; здесь — то же для продолжения в новом окне.

### Сеть — баги
1. **Метка тика во float32 (Main._pack_state:1708, _client_tick:1663).**
   `float(Engine.get_physics_frames())` едет в PackedFloat32Array; после
   2^24 тиков (~78 ч аптайма сервера, он живёт сутками) соседние метки
   ботов сливаются → net_apply_snapshot считает их дубликатами и
   выкидывает каждый второй снимок (потом 3 из 4 и т.д.) — боты начнут
   дёргаться «сами по себе» через ~3 дня без рестарта службы. Лечение:
   слать `frames - _tick_base` (база берётся в _ready сцены; сцена
   перезагружается после каждого заезда) — или %2^20 с обработкой скачка.
2. **_rx_hello: гость с чужим протоколом не отключается** (1755-1757:
   disconnect только если `slot_of_peer.has(id)`, у гостя слота нет) —
   висит на соединении до ухода. Добавить `or Net.guests.has(id)`.
3. **_rx_car_finished без проверки _wait_next_race** (2635): ждущий
   следующего заезда получает «ФИНИШ», опыт и монеты за гонку, которую за
   него ехал бот, и Enter уводит его в гараж. В _rx_finish guard есть.
4. **_rx_pstate: потолок vel 60 м/с** — буст (34×1.45≈49) + взрыв мины
   (+18) может превысить, пакет молча выкидывается, машина на миг замирает
   у остальных. Поднять до ~90 (возможно).
5. Повторный welcome (hello раз в секунду) создаёт стрелки-маркеры заново,
   старые повисают в воздухе (_attach_marker без проверки) — возможно.

### Сеть — что ускорит/сгладит
- **Избыточность снимков**: в каждый пакет класть 2-3 последних состояния
  (дедуп по метке уже есть, но применять надо только stamp > последней,
  иначе ветка «метка назад = смена автора» очистит буфер). Потери на
  канале почти все одиночные (замер 28.08) — их это закрывает целиком и
  позволяет опустить множитель буфера (1.25×+25 мс) → меньше отставание
  соперников. То же для аплинка _rx_pstate.
- **net_note_gap считает и собственные фризы клиента** (шейдеры, постройка
  моделей): один фриз 300 мс → буфер 350 мс на ~5 с. Вычитать
  `get_process_delta_time() - 1/60` из gap.
- Сервер форвардит только ПОСЛЕДНЕЕ состояние владельца; при двух пакетах
  за тик одно теряется. Очередь состояний владельца в снимке — ровнее
  машины живых игроков.
- Предсказание своих ракеты/мины как у лазера (инертная копия сразу, эхо
  гасить) — убирает полный пинг между нажатием и выстрелом.
- Снаряд/волна на клиенте живут полный _life и летят сквозь жертву после
  серверного попадания; нужно событие «погас в точке» как у мины.
- Мелочи: xf собирать resize+индекс вместо append_array; _place_of O(n²)
  на каждый тик; второй цикл поиска метки в _rx_state дублирует первый;
  _check_recovery крутится в _process без max_fps на сервере — перенести
  в физику; комментарий «43 байта/машину, <200» устарел (49 и 392 на 8).

### Ошибки вне сети (уверенные)
- Car.gd:1089/1194 `_ext_push_time = 0.3` (не maxf) укорачивает окно
  защиты после масла/взрыва. Car.gd:1849 прыжок через create_timer +
  лямбда на self — ошибка при queue_free; сделать таймером-полем.
  Car.gd:1938 ранний return в _wall_slide не сбрасывает _wall_near.
- TrackBuilder.gd:485 и TrackDecor.gd:1130: `cross(UP) * -1` — это ЛЕВО;
  щиты 3-2-1 стоят внутри поворота (TrackDecor:305). Проверить визуально.
- **TrackBuilder.gd:924 BoostPad.new() → BoostPad.gd:34 Net → стенды
  --script не компилируются** (test_curve, check_axis_jump, dump_profile;
  воспроизведено). Заменить Net.is_server() на проверку cmdline как в
  TrackBuilder._headless_server().
- TrackBuilder.gd:772 меш стен добавлен мимо _add_visual — строится на
  сервере (55 тыс. треугольников, спам «Parameter m is null»).
- Airstrike: клиентская копия сама выбирает лидера и рандомит точки —
  тени не там, где сервер убивает. Projectile: попадание в ведомый мяч не
  сбрасывает carrier. GameState: 7 мест load→set→save без проверки
  результата load — битый profile.cfg перезапишется частично.
- Main._space_sky: ~600 тыс. set_pixel из GDScript при каждой загрузке
  космоса — заменить на fill_rect по строкам или кэшировать текстуру.

### Лишнее (уверенное)
Car: freeze_duration, дубль fwd/fwd_h, _capsule_gap дважды, exclude в
лучах (машины и так не на маске), load→preload. Main: OFFTRACK_MARGIN,
_state_gaps_big, двойной _late_slots.erase, дефолты аргументов под старые
протоколы. UiKit.ENAMEL. CarModelLibrary: GLB_PATHS/_build_from (~90
строк). Soccer: _ui_font, CAR_SELECT/_roster/фолбэк имён (мёртвая ветка),
дубли _pop_count/_set_car_model/_countdown с Main. TrackBuilder:
MIN_HALF_WIDTH, FLAT_TRACK, start_transform ≡ respawn_transform_at.
ScrambleWave:223 проверка `body is Car` при маске без машин.
Репозиторий: 11 jpg-референсов в корне (8.8 МБ, нужны только генераторам
из tools) → refs/ с .gdignore; memcycle/memleak_report.txt; tools/shots*
112 PNG 22 МБ в git; в pck попадают все 441 файл tools/ — добавить
`*tools/*` в exclude_filter; texture_banners.png 4096² 15 МБ → 1024²;
неиспользуемые assets/ui/w_*.png, btn_rect_*, LilitaOne/Softie.ttf (1.2
МБ), track_env/Traffic_light_free.fbx, billboard_b.fbx, info_post.fbx.

### Производительность (уверенное)
Car: 4 raycast + 4 PhysicsRayQueryParameters на кадр РЕНДЕРА в
_animate_wheels (×8 машин); get_colliding_bodies до 3 раз за кадр;
get_node_or_null("CarModel") каждый кадр; геометрия оси трассы считается
до 6 раз за кадр в разных функциях; _ai_allowed_speed — ~32 sample_baked
на бота на кадр. TrackBuilder._build_ground — 22 тыс. get_closest_offset
(перебор 3600 точек) на старте; TrackDecor — ~250 отдельных MeshInstance
для пунктира/коробок/астероидов (MultiMesh), _turn_peaks дважды.
FlashFx: SpriteFrames + 9 AtlasTexture на каждую вспышку. SkidTrail:
новый ArrayMesh на каждую точку. WeaponBox/Mine: get_nodes_in_group /
get_overlapping_bodies каждый тик. Minimap: контур пересчитывается каждый
кадр. HUD: три строки форматируются каждый кадр.

**Файлы:** только PROGRESS.md (запись) и план в SiteKeeper. Код не трогался.

## Исправления по ревью: сеть (протокол 18), баги, чистка (2026-09-02, продолжение)
Задание: «исправляй» (по списку ревью выше). Код на VDS НЕ обновлён и dist
НЕ пересобран — протокол поднят до 18, старый сервер новых клиентов не
пустит: перед игрой по сети запустить «Обновить сервер.bat» и пересобрать
dist.

### Сеть — ПРОТОКОЛ 18 (Net.gd, Main.gd, Car.gd)
- **Метка тика от старта сцены** (`_tick_base` в Main): float32 сливал бы
  абсолютные номера кадров сервера через ~78 ч аптайма, и клиенты
  выбрасывали бы снимки ботов как дубликаты (каждый второй, потом 3 из 4).
- **Избыточность состояний.** `_rx_state(xf, flags, hist)`: третьим
  аргументом едут ПРОШЛЫЕ состояния всех машин (для ботов — прошлый
  разосланный снимок, для марионеток — прошлое состояние владельца
  `_snap_prev_*`, которое раньше терялось при двух пакетах владельца за
  тик). Клиент применяет историю только как продолжение записи того же
  автора (метка новее последней и не дальше 2 с), текущее — всегда (смену
  автора ловит net_apply_snapshot). `_rx_pstate` — 26 чисел (текущее +
  прошлое), сервер применяет прошлое, если его не видел. Потери на канале
  почти все одиночные (замер 28.08) — теперь они не дыры. Пакет снимка
  ~750 байт на 8 машин (~45 КБ/с на клиента).
- **Буфер не раздувается от своих фризов и закрытых дыр**: в net_note_gap
  идёт `gap − (собственный кадр − 1/60) − закрытые историей тики`.
- `_pack_state` без аллокаций на машину (resize + `_put_state`).
- **Авиаудар**: сервер шлёт точки падения (`_rx_airstrike_fx`,
  `Airstrike.preset_spots`) — инертная копия больше не выбирает лидера и
  разброс сама (тени были не там, где убивал сервер). **Снаряд**: сервер
  шлёт точку гашения (`_rx_proj_fx`) — копия у клиента гасится в точке
  сервера, а не летит сквозь уже поражённую машину.
- `_rx_car_finished`: ждущему следующего заезда чужой финиш (баннер,
  опыт, монеты, Enter в гараж) больше не показывается.
- `_rx_hello`: гость с чужой версией тоже отключается; лямбды таймеров
  разрыва висят на автолоаде Net, а не на перезагружаемой сцене.
- `_rx_pstate`: потолок скорости 60 → 90 м/с (буст + взрыв мины давали
  ~67, пакет молча выбрасывался).
- `_attach_marker` не вешает вторую стрелку при повторном welcome.
- Автовозврат сервера (`_check_recovery`) перенесён из `_process` в
  `_server_tick` (физика).

### Баги вне сети
- Car: `_ext_push_time = maxf(…, 0.3)` (не укорачивает окно после
  масла/взрыва); прыжок — таймер-поле `_jump_cd` вместо SceneTreeTimer с
  лямбдой на self; ранний return в `_wall_slide` сбрасывает `_wall_near`.
- **BoostPad**: `Net` через `get_node_or_null("/root/Net")`, каст к `Car`
  заменён утиной типизацией — стенды `--script` (test_curve,
  check_axis_jump, dump_profile) снова компилируются (было: Compilation
  failed, воспроизведено).
- TrackBuilder: меш стен через `_add_visual` (на сервере не строится);
  TrackDecor `_right` = FORWARD×UP (щиты 3-2-1 теперь снаружи поворота);
  `mi.mesh == null` не роняет постройку декора; `_turn_peaks` с кэшем.
- Projectile → ведомый мяч: `ball.release()` перед импульсом (удар ракетой
  по ведомому мячу пропадал).
- GameState: одно полное `_save_profile()` вместо шести «load → одно поле
  → save»; нечитаемый profile.cfg откладывается в `.broken`, а не
  затирается одним полем.
- Main `_space_sky`: градиент строками (fill_rect) и кэш текстуры на
  процесс вместо ~600 тыс. set_pixel на каждую загрузку космоса.

### Лишнее — удалено
Car `freeze_duration`, дубль `fwd_h`, exclude в лучах (машины на слое
0b100, маска 1 их не ловит), `load` → `preload` текстур дыма/огня, кэш
ссылки на CarModel вместо get_node_or_null каждый кадр. Main
`OFFTRACK_MARGIN` (стенды переведены на `_track.offtrack_margin`),
`_state_gaps_big`, двойной `_late_slots.erase`, второй цикл поиска метки в
`_rx_state`, HUD-строки форматируются только при смене числа. UiKit
`ENAMEL`. CarModelLibrary `GLB_PATHS`/`variant`/`_build_from`. Soccer
`CAR_SELECT`/`_roster`/`_ui_font`/фолбэк имён. TrackBuilder
`MIN_HALF_WIDTH`, `FLAT_TRACK`, `start_transform` = `respawn_transform_at(-6)`.
ScrambleWave мёртвая проверка `body is Car`. FlashFx: кадры атласа режутся
один раз (static). Minimap: контур в пикселях считается при resize, а не в
каждом кадре; cos/sin угла — static. Файлы: memcycle_report.txt,
memleak_report.txt; неиспользуемые assets/ui (w_*.png, btn_rect_*,
circle_*, flag_banner, play_btn, q_mark, radial_shine, panel_round,
arrow_*, LilitaOne.ttf, Softie.ttf) и модели Traffic_light_free.fbx,
city/billboard_b.fbx, palmov/info_post.fbx.
**ГРАБЛИ:** `Track_Texture.png` и `grass_2.png` grep по scripts не находит,
но их тянут импортированные FBX (зависимость в .godot/imported) — удаление
ломало загрузку арок и трибун; возвращены. Прежде чем удалять текстуру,
запускать headless-сцену и смотреть «Can't load dependency».

### НЕ сделано (осознанно)
Крупные переделки из ревью: MultiMesh для декора, пространственный хэш
для `_build_ground`, кэш геометрии оси трассы в Car, raycast колёс в
физике, `_ai_allowed_speed` по таблице, SkidTrail без пересборки меша,
exclude `*tools/*` из экспорта (стенды гоняются из собранного dist —
см. 01.09), ужатие texture_banners.png (визуальное решение), перенос jpg-
референсов из корня.

### Проверка
Все 66 офлайн-стендов tools/Test*.tscn прогнаны последовательно: PASS,
кроме WallTurn — он ПЛАВАЕТ и на исходном коде (HEAD через git stash:
FAIL/PASS/PASS, левый борт 1.48 м при пороге 1.5). Bumps (13/0) и
WallGhost (throws=9 hops=141) байт в байт совпали с HEAD. test_curve —
PASS на всех четырёх трассах. Сетевые стенды против локального сервера:
TestNet — PASS (слот, ростер, старт с ботами, снимки идут, марионетка
ровно 2.1%, рывков назад 0/389, отставание 61 мс), TestHomeFallback —
PASS, TestResetGrid — PASS. Headless-запуск Main.tscn — без SCRIPT ERROR.
**Файлы:** scripts/{Net,Main,Car,Airstrike,Projectile,SoccerBall,BoostPad,
TrackBuilder,TrackDecor,ScrambleWave,Soccer,CarModelLibrary,UiKit,GameState,
FlashFx,Minimap}.gd, tools/test_offtrack.gd, tools/test_ai_corners.gd,
«Обновить сервер.bat» (ожидает PROTOCOL := 18), удалённые ассеты (см.
выше). Изменения НЕ закоммичены.

## Аркадный пак-конструктор: 8 машин из деталей, тюнинг и экономика улучшений (2026-09-02, вечер)
Задание: «добавь ассет E:\UnityProjects\cars\Assets\Pack_CustomizableArcadeCarPack —
все машины, их апгрейды; продумай продажу апгрейдов по экономическому плану,
внеси изменения в план».

### Ассет (assets/models/arcadecars/)
- ОДИН файл source/Cars.fbx (1.3 МБ): 8 кузовов «Car N», 10 колёс «Wheel N»
  (с blend shape толщины — не используем), 10 моторов «Engine N» (на капот),
  10 спойлеров, 10 выхлопов (№9–10 — ракета/турбина на багажник). Текстуры в
  textures/: ColorPalette.png (стёкла/фары/шины, палитра — NEAREST +
  force_srgb, как у советских), Bottom.png (днище), Stickers N-M.png (по две
  наклейки в файле: чётная — uv1_offset.y=0.5), листы наклеек с альфой.
- Поверхности кузова: body (краска — задаём цветом), details (палитра),
  bottom, sticker (наклейка, ALPHA_SCISSOR; «нет» — материал-невидимка с
  альфой 0), sticker line (двойная полоса, тёмно-серая 0.113). Колесо: details
  (шина) + body (диск); мотор: details + body; выхлоп: details; спойлер: body.
- Позиции слотов на каждом кузове (колёса, EngineSlot, SpoilerSlot,
  ExhaustSlot, масштабы) ВЫТАЩЕНЫ ИЗ ПРЕФАБОВ Unity (CarBase.prefab +
  m_Modifications восьми CarVariant N.prefab; парсер — node-скрипт, python на
  машине нет) → таблица CarModelLibrary.ARCADE_BODIES. Меш колеса сделан для
  одной стороны (торчит в +x), на другую — разворот PI вокруг Y (так и в
  префабе: WheelMesh левых колёс с rotation y=180°). Оси совпали с Unity:
  нос в +Z (мотор на капоте, спойлер на корме — проверено снимком).
- Иконки деталей — спрайты пака → assets/ui/parts/<slot>_<N>.png (1.1 МБ,
  54 файла; в паке X_-1 = «нет» → у нас _0, X_k → _(k+1)).
- Краски: 12 цветов × 3 оттенка из .mat пака (sRGB-значения — в Godot
  albedo_color тоже sRGB, совпало). «Металлик» = metallic 0.85 / roughness
  0.32 / clearcoat (у пака шейдер-глиттер, не переносим).
- Документация пака — PDF из картинок (pdftotext пуст), устройство понято
  по скриптам CarCustomizer.cs/CarColorSetter.cs/MeshSet.cs.

### CarModelLibrary.gd
- ARCADE_IDS ac1..ac8, ARCADE_COLORS/PAINTS, ARCADE_BODIES, ARCADE_DEFAULT.
  id скина аркадной машины кодирует ВСЮ комплектацию:
  `ac3-yellow2-g1-w5-e3-s0-x2-k4-l1` (краска+оттенок, металлик, колёса, мотор,
  спойлер, выхлоп, наклейка, полоса); arcade_id()/arcade_parse() (битые и
  пропущенные части — сток, зажим в допустимое: чужой клиент не уронит),
  base_id() понимает оба формата (советский «_цвет», аркадный «-…»),
  colors_for(base), part_tier(slot, index) — ступень для детали (0 сток,
  дальше по 3 варианта на ступень; у колёс сдвиг, №1 — сток).
- _build_arcade(): кузов + 4 колеса в пивотах (крутятся/рулятся —
  collect_wheels подхватил) + детали по слотам; масштаб по длине КУЗОВА
  (детали на длину не влияют), низ — по колёсам. Материалы по ИМЕНАМ
  поверхностей, общие, кэш по ключу (_arcade_material). Узлы Cars.fbx
  кэшируются по имени (_arcade_nodes) — один load на игру.
- CAR_IDS теперь 31 (аркадные вставлены по порядку открытия);
  shuffled_bot_pool() даёт аркадным ботам СЛУЧАЙНУЮ комплектацию — витрина
  тюнинга в заезде.

### GameState.gd — экономика улучшений (ЭКОНОМИКА.md переписана)
- CAR_UNLOCKS: 28 платных машин, почти по одной на уровень 2..30 (аркадные
  на 5/8/11/14/17/20/23/26 уровнях), цены 600 → 95 000, сумма 965 800.
- Улучшения: 4 слота (engine/wheel/spoiler/exhaust) × 3 ступени; цена
  ступени 4/6/8 % от цены машины (стартовые — от условной 1 000; округление
  до десятков), ступени открываются уровнем 2/6/12. Эффект: мотор разгон +4 %,
  колёса сцепление и руль +4 %, спойлер скорость +2 %, выхлоп буст +10 %
  дольше — за ступень. upgrade_level/price/unlock_level, try_buy_upgrade,
  upgrade_multipliers. Профиль: car_upgrades.
- Косметика аркадных: пакет «Наклейки» 20 % (мин. 300), «Металлик» 40 %
  (мин. 600) — car_packs, try_buy_pack; краски бесплатны. Комплектация —
  car_tuning, set_tuning() с проверкой прав (деталь только купленной
  ступени, наклейки/полоса — с пакетом, металлик — с пакетом).
  full_id(base) — полный id с цветом/комплектацией (selected_car_id).
- Car.apply_upgrades(m) — множители к engine_power/grip/steer/max_speed/
  boost_duration; зовётся для машины ИГРОКА: Main._spawn_cars (оффлайн),
  Main при получении своего слота по сети (машина становится своей),
  Soccer._spawn_cars. Боты стоковые.

### Гараж
- CarSelect: DISPLAY_NAMES ac1..ac8 — Стрела, Сарай, Багги, Малыш, Пикап,
  Монстр, Маслкар, Кирпич (по виду кузовов на снимке). Ряд красок стал
  общим (colors_for: 10 или 12 квадратиков, центрируется). Кнопка «ТЮНИНГ»
  справа от «СТАРТ» (у своей машины) открывает TuningPanel НА МЕСТЕ сетки;
  стрелки при открытой панели переводят её на новую машину, Esc закрывает.
- TuningPanel.gd (НОВЫЙ): шапка с кошельком, 4 слота — пипсы ступеней,
  кнопка «СТУПЕНЬ N · цена» / серая «С N УРОВНЯ · цена» / «МАКС», у аркадных
  ряд иконок деталей (жёлтая рамка — стоит, тёмная — закрыта; клик по
  закрытой детали ближайшей ступени покупает ступень и ставит её), 36
  квадратиков красок, «МЕТАЛЛИК · цена» → «МЕТАЛЛИК: ВКЛ/ВЫКЛ», «НАКЛЕЙКИ ·
  цена» → «ПОЛОСА: ВКЛ/ВЫКЛ» + ряд 11 наклеек. ГРАБЛИ UI: UiKit.style_button
  не задаёт стиль «disabled» — серые кнопки теряли фон (лечится копией
  stylebox «normal»); Label с autowrap в HBox без минимальной ширины
  сворачивается в столбик по букве и раздувает ряд по высоте — шапке wrap
  выключен.
- Lobby: имя машины по base_id (раньше полный id «vz01_red» показывался
  сырым — попутный фикс).

### Проверка
- Godot при новом class_name (TuningPanel) ТРЕБУЕТ `--import` (кэш
  глобальных классов): без него CarSelect не парсится — «Could not find
  type TuningPanel». Стенды, запущенные до импорта, падали именно так.
- tools/test_car_build.gd расширен: 8 аркадных кузовов сток + полный обвес,
  на ac1 — все 10 вариантов каждого слота, 10 наклеек, 36 красок, разбор ↔
  сборка id и битый id — 261/261 ok.
- tools/test_shop.gd (НОВЫЙ, бэкапит и восстанавливает profile.cfg):
  ступени стартовой машины (40/60/80, уровень 2, ступень II с 6-го),
  множители, покупка ac1, детали по ступеням, пакеты и их минимумы,
  права set_tuning, выбор с комплектацией, перезапуск — 30/30 ok.
  Профиль игрока цел (cmp с бэкапом profile.cfg.bak_2026-09-02_tuning).
- Снимки (tools/ShotArcade.tscn — 8 кузовов сток и с обвесом;
  tools/ShotTuning.tscn — гараж с панелью, ключи --soviet/--bottom):
  моторы на капоте, спойлеры на корме, наклейки/полосы/металлик на месте,
  панель читаемая. TestSelectPrefill, TestLap, TestSoccer, TestWeapons —
  PASS (после `--import`).
- **dist/ НЕ пересобран, VDS НЕ обновлён** — перед раздачей пересобрать
  (новые FBX/иконки; сетевой id аркадных машин старый клиент покажет
  бокс-заглушкой, не краш). Не закоммичено (как и всё на ветке Andrei).
**Файлы:** scripts/CarModelLibrary.gd, scripts/GameState.gd, scripts/Car.gd,
scripts/Main.gd, scripts/Soccer.gd, scripts/Lobby.gd, scripts/CarSelect.gd,
scripts/TuningPanel.gd (новый), tools/test_car_build.gd, tools/test_shop.gd
(новый), tools/shot_arcade.gd + ShotArcade.tscn, tools/shot_tuning.gd +
ShotTuning.tscn (новые), assets/models/arcadecars/ (новый: Cars.fbx +
textures/), assets/ui/parts/ (новый), ЭКОНОМИКА.md (разделы 0, 3–5, 7а, 8,
9 переписаны), README.md, PROGRESS.md.

## VDS обновлён под аркадный пак (2026-09-02, продолжение)
Задание: «обновляй vds».
- Шаги «Обновить сервер.bat» выполнены вручную из Bash: tgz 49.0 МБ
  (scripts, scenes, project.godot, assets — вместе с arcadecars), stop →
  tar → chown → `--import` под пользователем bighead → start.
- ГРАБЛИ ОКРУЖЕНИЯ (для повторов из Bash, а не из .bat): `%TEMP%`/`$TEMP`
  в Bash — путь в windows-виде, tar принимает его за удалённый хост
  («Cannot connect to C: resolve failed») → путь через `cygpath -u`.
  Git-bash-овые ssh/scp падают на кириллице в `$USERPROFILE`
  (`Could not create directory '/c/Users/\300\355...'/.ssh`) → берём
  `/c/Windows/System32/OpenSSH/{ssh,scp}.exe` и путь к ключу через
  `cygpath -w`.
- Проверка: служба active, PROTOCOL := 18 (не менялся — новых RPC нет,
  аркадные машины уезжают в существующем поле id), Cars.fbx на месте,
  память 101 МБ (пик 123), диск 50%. **TestNet против ЖИВОГО VDS — PASS**
  (слот, ростер, трасса «neon», старт с ботами, снимки идут).
- В логе службы прежние «ERROR: Parameter "m" is null» (меш ограждений
  строится и на headless-сервере — известный пункт ревью 02.09,
  TrackBuilder.gd:772), новых ошибок нет.
- **dist/ по-прежнему НЕ пересобран** — exe у игроков старее сервера;
  протокол тот же, но аркадных машин в нём нет (соперник на ac* покажется
  бокс-заглушкой).
**Файлы:** /opt/bighead на VDS, PROGRESS.md. Код игры не менялся.

## Разовый подарок 1 000 000 монет (2026-09-02, продолжение)
Задание: «сделай мне и игроку по 1 000 000 монет».
- GameState.gd: GIFT_1M_AMOUNT = 1 000 000, флаг `_gift_1m_claimed` в
  профиле (`gift_1m_claimed`). В `_ready()`, после восстановления
  профиля, если флаг не стоит — `money += GIFT_1M_AMOUNT`, флаг → true,
  `_save_profile()`. Одноразово на профиль: у Андрея сработает через
  play.bat при следующем запуске (он играет из исходников), у второго
  игрока — из пересобранного dist/BigHeadRacing.exe.
- tools/test_gift.gd (новый, по образцу test_shop.gd): чистый профиль →
  первый `_ready()` начисляет ровно 1 000 000 и ставит флаг, второй
  `_ready()` — уже без повтора. Бэкапит/восстанавливает user://profile.cfg.
- Проверено ДВАЖДЫ: godot --headless --script tools/test_gift.gd (3/3 ok)
  и tools/test_shop.gd (30/30 ok, регрессий нет) — и из исходников, и
  через собранный `dist/BigHeadRacing.exe --headless --script
  res://tools/test_gift.gd` (то же самое: 3/3 и 30/30).
- **dist/ ПЕРЕСОБРАН** (`--import` → `--export-debug "Windows Desktop"`):
  exe 84.1 МБ (без изменений), pck 59.7 МБ. ГРАБЛИ: свежепересобранный на
  16:34 dist уже включал более ранний пересбор под аркадный пак (16:34
  дист старше моей правки не был — просто конкурентная более ранняя
  сборка сессии этот код ещё не содержала, `strings` по .pck ничего не
  показывает даже для заведомо старых полей типа `car_upgrades` — ресурсы
  в pck жмутся, текстовый grep по бинарнику не годится для проверки;
  реальная проверка — только прогон теста через сам exe).
- Второму игроку раздать новый `dist/BigHeadRacing.exe` +
  `dist/BigHeadRacing.pck` (там же аркадный пак — вместе с подарком).
**Файлы:** scripts/GameState.gd, tools/test_gift.gd (новый),
dist/BigHeadRacing.exe, dist/BigHeadRacing.pck, PROGRESS.md. Не
закоммичено (как и всё на ветке Andrei).
