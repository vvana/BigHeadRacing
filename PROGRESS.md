# Прогресс: Big Head Racing — гонки в стиле Rock'n'Roll Racing

## Цель
Аркадная изометрическая гонка с оружием в духе Rock'n'Roll Racing (SNES).
Выбранный стек: **Godot 4.3+ / GDScript**, подход — **3D с фиксированной
изометрической ортокамерой** (низкополи-машины, физика прыжков «из коробки»).

## Сделано (2026-08-12)
Создан рабочий стартовый скелет в `D:\h\Big Head Racing`:

- `project.godot` — конфиг, главная сцена `scenes/Main.tscn`, autoload InputSetup.
- `scripts/InputSetup.gd` — регистрация управления кодом в InputMap
  (accelerate/brake/steer/handbrake + заделы fire/jump). Раскладку держим
  в коде, а не в project.godot — нет проблем с сериализацией InputEventKey.
- `scripts/Car.gd` — аркадная машина: RigidBody3D + 4 луча-«колеса» с пружинной
  подвеской (raycast suspension), аркадное сцепление с гашением бокового сноса,
  дрифт на ручнике (Space), прыжок (Shift, фишка RnRR), выравнивание в воздухе,
  зеркалирование руля на заднем ходу. Вперёд = -Z (конвенция Godot).
- `scripts/TrackBuilder.gd` — процедурная кольцевая трасса из Curve3D
  (замкнутый контур ~76×56 м): земля, тёмное полотно, красные стены по обеим
  сторонам (StaticBody3D), 2 жёлтых трамплина, стартовая линия;
  `start_transform()` отдаёт точку спавна.
- `scripts/IsoCamera.gd` — ортографическая камера, pitch -32° / yaw 45°,
  НЕ вращается с машиной (как в оригинале), плавное слежение lerp.
- `scripts/Main.gd` — сборка мира целиком из кода: свет+среда (красноватая
  «чужая планета»), трасса, машина из BoxMesh (корпус/кабина/жёлтый нос-маркер
  направления), камера, HUD (скорость км/ч + подсказка управления).
- `scenes/Main.tscn` — минимальная сцена, только ссылка на Main.gd.

Всё процедурное, внешних ассетов нет — проект должен запускаться сразу.

## Осталось
- ~~Установить Godot~~ ГОТОВО: Godot 4.3-stable в `D:\Soft\Godot\Godot_v4.3-stable_win64.exe`
  (+ _console.exe для отладки). Запуск игры:
  `D:\Soft\Godot\Godot_v4.3-stable_win64.exe --path "D:\h\Big Head Racing"`
- НОВАЯ МАШИНА (2026-08-13): проект теперь в `E:\UnityProjects\BigHeadRacing`,
  Godot 4.3-stable скачан в `E:\Soft\Godot\Godot_v4.3-stable_win64.exe`
  (+ `_console.exe`; на D: не было места). Прогнан `--headless --import`
  (exit 0) и тесты: test_curve PASS, TestLap PASS (худший ИИ 636 м).
  Запуск игры:
  `E:\Soft\Godot\Godot_v4.3-stable_win64.exe --path "E:\UnityProjects\BigHeadRacing"`
- ~~Проверить запуск~~ ГОТОВО (2026-08-12): headless-прогон 300 кадров чистый
  (exit 0, script-ошибок нет). Исправлен `Curve3D.closed` (нет в 4.3) →
  замыкание дублем первой точки. Шум «Parameter m is null» в headless —
  безвредный dummy-рендерер.
- Пользователь пробует ездить; подтюнить физику (grip/engine_power/steer_speed)
  по ощущениям.

## Модели машин (2026-08-12)
- Скачан пак «hot-wheels-turbo-driver-cars» (Sketchfab) →
  `assets/models/hotwheels/source/Turbo Driver Cars.glb` (+ textures/).
- В GLB 8 машин × 2 ряда (z=+1 обычные, z=-1 «TSH»-вариант), все детали
  машины стоят в одной точке-слоте, геометрия в вершинах. Модели смотрят в +Z.
- `scripts/CarModelLibrary.gd`: static `build(car_id, target_length, base_y,
  variant)` — выдёргивает машину из GLB, центрует, разворачивает на PI,
  масштабирует. ID: stock, rd, sharky, invader, arachno, dk, dakar, jt.
- Main.gd: игрок ездит на "sharky" (Sharkruiser); боксы остались фолбэком
  (`_build_placeholder_visual`). `tools/dump_glb.gd` — отладочный дамп GLB
  (путь к GLB можно передать после `--`).
- После добавления нового class_name нужен `--headless --import` (кэш классов).
- Второй пак «planet-hot-wheels-highway-35-cars» (Sketchfab) →
  `assets/models/highway35/source/HW35 cars.glb`: +35 машин Highway 35 /
  World Race (deora, twinmill, krazy8s, backdraft, vulture, …).
  Особенность: узлы Z-up (повёрнуты), «перёд» уже в -Z.
- CarModelLibrary обобщён: список GLB_PATHS, AABB с учётом поворота узла,
  автоопределение «переда» по колесу wheel_front (z>0 → разворот на PI).
  Все ID — в CarModelLibrary.CAR_IDS (41 шт.).
- `tools/test_car_build.gd` — тест сборки всех машин: 41/41 ok.

## Экран выбора машины (2026-08-12)
- `scripts/GameState.gd` — autoload, хранит `selected_car_id` между сценами.
- `scripts/CarSelect.gd` + `scenes/CarSelect.tscn` — подиум, вращающаяся
  машина, названия (DISPLAY_NAMES), листание ←→/AD, Enter/Space — в гонку.
  Это теперь стартовая сцена (run/main_scene).
- Main.gd: машина игрока из GameState; Esc в гонке — назад к выбору.
- Сетка всех машин (правая половина экрана): миниатюры рендерятся в
  SubViewport (own_world_3d, transparent_bg, UPDATE_ONCE + await
  frame_post_draw) один раз за запуск, кэш в GameState.car_thumbs.
  Навигация ←→↑↓/AD, клик мышью выбирает, повторный клик/Enter — старт.
- Ускорение миниатюр: рендер пачками по 8 вьюпортов за кадр + дисковый кэш
  PNG в user://thumbs (%APPDATA%\Godot\app_userdata\Big Head Racing\thumbs).
  Первый запуск ~5-6 кадров, дальше — мгновенно с диска. При замене паков
  моделей кэш надо удалить вручную.

## Вращение колёс (2026-08-12)
- CarModelLibrary: каждое колесо оборачивается в пивот "WheelPivot_*"
  в центре ступицы (геометрия деталей запечена от центра машины, поэтому
  без пивота крутить не вокруг чего). Меты пивота: wheel_radius (мировой),
  is_front, spin_sign (-1 при развороте контейнера на PI).
- Car.gd: collect_wheels(model) + _animate_wheels() — spin по скорости
  качения (rotation.x += speed/radius), передние поворачиваются рулём
  (rotation.y = _steer_visual, сглаженный lerp).

## Гладкая трасса (2026-08-12)
- TrackBuilder переделан с коробок на непрерывные меши (SurfaceTool):
  полотно — одна лента-стрип по кривой; ограждение — два цельных бортика
  (грань к трассе + наружная + верх), нормали заданы вручную, cull off.
- Коллизия стен — ConcavePolygonShape3D по тем же треугольникам
  (backface_collision = true). SAMPLES = 256 на круг.
- `_sample_frames()` — общий предрасчёт точек/перпендикуляров контура.
- Фикс «застрял в стене»: у Car включён continuous_cd (тонкая concave-стена
  на скорости тоннелировалась), + действие "respawn" (R) — телепорт на
  ближайшую точку оси трассы (TrackBuilder.respawn_transform по
  get_closest_offset), скорости обнуляются.
- Фикс «проваливается под асфальт при прыжке»: пружина стала прогрессивной
  (×(1+2c²) при сжатии c), демпфер 8→11, коллизионный бокс продлён вниз
  до -0.3 (посадка модели -0.35) — жёсткий упор при пробое подвески.
- Фикс «Shift-прыжок не работает»: прыжок срабатывал, но импульс был
  jump_impulse*mass*0.1 → взлётная 0.7 м/с (2 см, невидимо). Убран 0.1,
  jump_impulse теперь = взлётная скорость в м/с (6.0 → ~1.8 м высоты).
  Клавиши-модификаторы в InputMap матчатся нормально (проверено скриптом).
- Фикс «застрял на трамплине»: продлённый вниз бокс втыкался углом в наклон.
  Коллизия теперь «санки» (ConvexPolygonShape3D): днище -0.28 как упор,
  нос/корма скошены (кромка y=0.05 на z=±1.5) — заезжает на наклон.
  Коллизию строит сам Car._build_collision() (Main больше не делает).
- Регрессионный автотест: `tools/TestRamp.tscn` + test_ramp.gd — машина
  газует через трамплин 6 с, PASS/STUCK + телеметрия по секундам.
  Запуск: `godot --headless --path . res://tools/TestRamp.tscn`
- Фикс «R не работает»: сам механизм исправен (автотест
  tools/TestRespawn.tscn — PASS, инжект клавиши в загруженный Main), но
  respawn возвращал в БЛИЖАЙШУЮ точку оси — т.е. обратно в ту же ловушку
  (трамплин стоит на оси). Теперь respawn_transform выбрасывает на +6 м
  вперёд по ходу трассы.
- R всё равно не срабатывал у пользователя в окне (в headless-тесте — да).
  Main переведён с _unhandled_input на прямой опрос Input в _process
  (как езда). Плюс автовозврат: >11 м от оси трассы → жёлтое предупреждение
  и через 3 с _respawn() сам. TrackBuilder.distance_from_axis() — хелпер.

## Механика Rock'n'Roll Racing (2026-08-12) — БОЛЬШОЙ ЭТАП
- Физика перенастроена под RnRR: engine 110 (сотня ~2.5 c), brake 80,
  steer 3.4/2.2 (руль почти не слабеет на скорости), grip 14 (едет куда
  смотрит, дрифт только с ручником). Упругие столкновения: bounce 0.4 у
  машин + bounce 0.5 у стен (рикошет).
- Car.gd реструктурирован: общий _drive() для игрока и ИИ; _ai_control —
  едет к точке оси трассы (lookahead 6+0.45*v), стреляет по цели в конусе
  22° / 22 м, кидает мину под соперника ближе 10 м сзади; ai_rubber.
- Бой: shoot() — Projectile.gd (Area3D, 45 м/с, 30 dmg, живёт 1.4 с);
  drop_mine() — Mine.gd (взвод 0.7 c, 35 dmg, живёт 25 c); take_damage с
  толчком; _explode() — вспышка FlashFx.gd, невидим+freeze 2 c, респавн
  с полным HP. Боезапас: 6 снарядов + 3 мины, рефилл каждый круг.
- Main.gd = менеджер гонки: решётка 2×2 (игрок впереди слева), 3 ИИ на
  случайных машинах (не как у игрока, ±разброс характеристик), отсчёт
  3-2-1-GO (controls_enabled), прогресс по накопленному пути вдоль кривой
  (защита от среза — прогресс по ближайшей точке оси), круги (4), позиция,
  «резинка» ИИ ±(0.85..1.3) от разницы прогресса, финиш → место + Enter в
  гараж. HUD: скорость, круг, позиция, HP-бар с цветом, боезапас.
- InputSetup: + "drop" [L, C]. Управление: Ctrl/J огонь, L/C мина.
- Off-track: игрок 2 с с предупреждением, ИИ тихо через 1 с.
- Тесты PASS (test_ramp обновлён: controls_enabled = true).
- Фикс «машины стоят за бортом и не возвращаются» (2026-08-12): порог
  вылета был TRACK_HALF_WIDTH+4 = 11 м, а машина у наружной грани стены —
  всего 8.1 м от оси (замерено тестом) → детект не срабатывал НИКОГДА.
  Теперь OFFTRACK_DIST = TRACK_HALF_WIDTH+0.5 = 7.5 (на полотне максимум
  ~6.2). Плюс WALL_HEIGHT 1.2 → 1.7 (высота прыжка ~1.8) — реже улетают.
  Новый тест `tools/TestOfftrack.tscn` ставит машину за борт и проверяет
  авто-возврат.
- Фикс «перевернулась и так лежит» (2026-08-12): _check_offtrack →
  _check_recovery, три причины возврата — вылет (2 с игрок / 1 с ИИ),
  переворот (up_dot < 0.35, 1.5 с), застревание (v < 1.2 м/с, 5 с; только
  при controls_enabled, иначе решётка на старте считается застреванием).
  Игроку в HUD пишется причина. Выравнивание в воздухе усилено 4→14 +
  демпфер крена/тангажа 2.5 — реже приземляется на крышу.
  Тест `tools/TestFlip.tscn` переворачивает машину и ждёт восстановления.
- Уточнение (2026-08-12): отсчёт «Перевернулся!» стартовал ещё в полёте
  (кувырок на прыжке). Теперь нужны три условия: up_dot < 0.35 И машина
  у земли (Car.is_near_ground() — луч строго вниз по МИРУ, а не по оси
  кузова: у машины на крыше лучи подвески смотрят в небо) И |v.y| < 2.5.
  TestFlip расширен: фаза 1 — кувырок в воздухе (ложного отсчёта быть не
  должно), фаза 2 — лежит на крыше на земле (должна восстановиться).

- Стартовая черта → финишный створ (2026-08-12): узел "FinishGate" на
  нулевой отметке кривой — шахматная лента 14×2 клетки на асфальте,
  две металлические опоры (снаружи ограждения, h=5) и клетчатый баннер
  16×2 над дорогой. Только визуал, коллизий нет.

## Трасса с перепадами высот (2026-08-12)
- Контрольные точки кривой получили Y (-2..+7 м): подъёмы, спуски, гребни.
- ВАЖНО: дорога теперь StaticBody3D с СОБСТВЕННОЙ trimesh-коллизией —
  раньше машины ездили по плоскому боксу земли, при рельефе это ломалось.
- Земля — сетка 56×56 на 190 м с trimesh-коллизией. Высота узла:
  `_ground_height()` = вровень с полотном в радиусе TRACK_HALF_WIDTH+5
  (ровная обочина), дальше склон вниз 0.28/м + синусные холмы с
  нарастающим весом. Высоты считаются по узлам сетки (не по 4 на ячейку).
- `_rights` теперь строго горизонтальны (полотно без бокового крена).
- Тест `tools/TestLap.tscn`: гонка едет сама 60 с, проверяет что худший ИИ
  проехал > полкруга. Факт: ИИ делают ~3 круга (круг = 221 м).
- Фикс «пятна грунта на асфальте» (z-fighting): обочина была вровень с
  полотном, а сетка земли (3.4 м/ячейка) грубее дороги → на выпуклых
  местах треугольники земли пробивались наверх. Теперь GROUND_DROP = 1.2:
  обочина ниже полотна, ограждение продлено вниз юбкой (GROUND_DROP+0.4),
  так что ступенька не видна. GROUND_RES 56 → 72.
- test_ramp: критерий сменён на МАКСИМУМ пройденного (после трамплина
  машина на полном газу без руля бьётся в стену поворота и откатывается —
  это не «застряла на трамплине»).

## Взрыв без исчезновения (2026-08-12)
- Было: visible=false + freeze на 2 с → «машина пропала», потом появлялась
  с высоты 1.0 м и подпрыгивала.
- Стало: машина всегда видима. При подрыве — вспышка + подброс
  (5.5*mass вверх) и случайная закрутка, управление снято (alive=false),
  через 1.6 с сама встаёт на трассу с полным HP.
- Высота респавна/старта 1.0 → 0.62 м (примерно длина покоя подвески),
  чтобы не падать и не подскакивать.
- Тест `tools/TestExplosion.tscn`: следит, что visible ни разу не стал
  false, машина умерла и возродилась с HP 100 (HP проверяется в момент
  возрождения — иначе ИИ успевает подстрелить и тест врёт).

## Гладкая петля без изломов (2026-08-12)
- Причина «уголков» на трассе: касательные были фиксированной длины
  (normalized * 6.0) независимо от расстояния между опорными точками —
  на длинных участках кривая почти прямая, а в точке резко ломается.
- Теперь: касательная = (next - prev) / 6 (Катмулл-Ром, пропорционально
  шагу), а точки задаются по углу процедурно (14 шт., радиус
  33 + 6·sin2a + 2.5·sin(3a+1.2), высота 3.2·sin(2a+0.5) + 1.8·sin3a + 1)
  — широкие дуги, лёгкие шиканы, подъёмы/спуски. Длина круга 219 м.
- Тест `tools/test_curve.gd` (SceneTree-скрипт): максимальный поворот
  между сэмплами; PASS < 6° на шаге 0.55 м. Факт: 2.76°.

## Ступенчатый рельеф как в RnRR (2026-08-12)
- Вместо синусоидальных холмов — профиль плато: `HEIGHT_KEYS` (доля круга →
  высота) + `_profile_height()` со smoothstep на переходах. Сейчас:
  0 м старт → подъём → плато 7 м → РЕЗКИЙ сход до 1 м → плато 1 м →
  подъём → плато 4.5 м → сход до 0. Точек кривой 14 → 40 (иначе плато
  размазывается).
- Трамплины перенесены на кромки обрывов (t = 0.44 и 0.875) — слетаешь
  с плато вниз, как в оригинале.
- test_curve разделён: излом В ПЛАНЕ (дефект, лимит 6°, факт 1.71°) и
  перелом ПРОФИЛЯ (задумка — кромки плато, лимит 20°, факт 8.49°).

## Живость + руление в полёте + лимит разворота (2026-08-13)
- Физика «поживее»: mass 120 → 250, max_speed 26 → 34, engine 110 → 150,
  brake 80 → 100, steer_speed_min 2.2 → 2.6 (радиус на скорости ~13 м).
  ИИ на TestLap проезжают 808 м/мин против 636 раньше.
- Руление в полёте: в воздушной ветке _drive() рысканье от руля
  (air_steer_speed 2.2, мягче земли, lerp 6.0), выравнивание к горизонту
  как было — оно рысканье не трогает.
- Лимит разворота: Car._clamp_heading(delta) — угол «нос машины vs
  касательная трассы в ближайшей точке» не может превышать
  max_track_angle_deg (80°). Лишний угол снимается rotate() сразу, а
  рысканье урезается ПРЕДИКТИВНО ((sign*limit - ang)/delta): кламп идёт до
  интеграции физики, без предикции машина проскакивала бы предел на кадр
  и дребезжала на границе. Работает и для ИИ; пропускается при alive=false
  (закрутка взрыва) и без track.
- Тест `tools/TestHeading.tscn`: фаза 1 — разворот на 180° (должно
  довернуть до 80°), фаза 2 — 2 с насильного рысканья 6 рад/с (макс. угол
  за все кадры ≤ 80°+1). Факт: ровно 80.0°.

## Скольжение вдоль ограждений вместо отскока (2026-08-13)
- Было: упругие стены (bounce 0.5) — машина рикошетила к оси трассы.
- Стало: стены bounce 0.0 + группа "walls"; Car._wall_slide(delta) при
  контакте (contact_monitor + get_colliding_bodies) проецирует скорость
  на касательную трассы (вертикаль не трогает — прыжок жив) и доворачивает
  корпус вдоль стены (wall_align_speed 7.0, экспоненциально).
- ВАЖНО: доворот держится ещё 0.35 с после отрыва (_wall_align_time) с
  полным гашением angular_velocity.y — удар УГЛОМ кузова о стену
  закручивает машину, и без окна остаточное вращение разворачивало её
  обратно поперёк (в тесте угол был 67° вместо <30°).
- Упругость машина-машина осталась (bounce 0.4 у Car).
- Тест `tools/TestWallSlide.tscn`: пуск в стену под 55° на 20 м/с;
  критерии — отскок к оси < 3 м/с на всех кадрах после касания
  (факт 2.67 — медленный дрейф, не рикошет) и финальный угол к трассе
  < 30° (факт 19.2°). Касание детектится через car._touching_wall(),
  НЕ по расстоянию до оси: нос касается стены, когда центр ещё в ~5.2 м.

## Руль у стены + не заезжать на ограждение (2026-08-13)
Жалобы: «вдоль стенки невозможно рулить», «можно наехать на ограждение
и перевернуться». Причины: _wall_slide глушил ЛЮБОЕ рысканье и проецировал
всю скорость на касательную (руление от стены умирало), а rotate() у стены
вклинивал угол кузова в стену — депенетрация подбрасывала машину вверх
(заезд на стену → переворот). Переделано направленно:
- Скорость: гасится только компонента В стену (v_out = v·n, n — от оси к
  стене); вдоль и ОТ стены — не трогаем.
- Клапан подскока: при контакте v.y обрезается до 1.0 — на стену больше
  не заезжает. После прыжка (_jump_time 0.6 с) клапан отключён, чтобы
  прыжок у стены работал.
- Антипереворот: при контакте — та же выравнивающая пара сил, что в
  полёте (up→горизонт 14.0 + демпфер крена/тангажа 2.5).
- Руление: Car._yaw_cmd_sign (знак рысканья, которое просит руль,
  ставится в _drive). Если руль просит рысканье ПРОЧЬ от стены — доворот
  и гашение полностью пропускаются. Иначе доворот к касательной +
  angular_velocity.y = 0 (без руля вращение у стены — это закрутка от
  удара, её гасим; заодно закрыт регресс «закрутило от удара углом →
  раскрутило до лимита 80°»).
- TestWallSlide теперь двухфазный: фаза 1 — скольжение без отскока
  (отскок < 3 м/с, угол < 30°; факт 2.67 и 8.8°); фаза 2 — руль от стены
  через Input.action_press("accelerate"/"steer_*"), критерий — хоть на
  одном кадре нос ушёл от стены (fwd·n < -0.1) или машина покинула
  пристенок (dist < 4). Проверять только последний кадр нельзя: машина
  успевает пересечь трассу и «уткнуться» в противоположный борт.

## Самовольный разворот на взлёте/падении/прямой — исправлен (2026-08-13)
- Жалоба: «при подлёте, падении и иногда при прямой езде машина
  разворачивается, хотя я не рулю». Причина: рысканье гасилось ТОЛЬКО
  на земле при |speed| > 0.5 (lerp к нулю в ветке руля). В воздухе руль
  работал лишь при |steer| > 0.01, а без руля закрутка от кромки
  трамплина/угла кузова не гасилась вовсе — машина летела вращаясь.
- Теперь в _drive рысканье без руля затухает везде: в полёте lerp к
  steer*air_steer_speed идёт всегда (при steer=0 цель — ноль, 6.0/с);
  на земле при |speed| <= 0.5 — lerp к нулю (10.0/с).
- Тест `tools/TestAirSpin.tscn`: машине в полёте дают ω.y = 1.5 рад/с
  без руля; через 0.65 с должно быть < 0.3 (факт 0.021).
- Замечание: TestLap изредка печатает PASS, но процесс падает на выходе
  (exit -1073741819, access violation при завершении headless). Плавающий
  сбой Godot при выгрузке, на игру не влияет; смотреть на печать PASS,
  а не только на код выхода.

## Стены/приземления не замедляют + запрет езды по стене (2026-08-13)
Жалобы: «о стену и при приземлении машина замедляется», «иногда едет по
ограждению вертикально». Сделано:
- Слои коллизий: стены на layer 2, у Car collision_mask = 0b11, а ЛУЧИ
  ПОДВЕСКИ и is_near_ground — mask 1. Кузов со стеной сталкивается, но
  колёса стену «дорогой» не считают → вертикальная езда по стене убита.
- _wall_slide переделан в «ведение»: в динамической зоне у стены (вылет
  кузова в сторону стены: нос 1.5 + борт 0.85, проекции на нормаль, +
  упреждение v_out*3*delta) вся горизонтальная скорость перенаправляется
  вдоль касательной С СОХРАНЕНИЕМ МОДУЛЯ (не ниже _recent_hspeed).
  Ключевое: перехват ДО контакта — решатель съедает нормальную
  составляющую в тот же шаг, задним числом скорость уже не вернуть
  (первая версия с узкой полосой 1.4 м это и ловила: нос доставал до
  стены раньше входа центра в полосу → отскок 6.7 и просадка до 8.5).
- _recent_hspeed — «недавняя» горизонтальная скорость: максимум с
  затуханием 30 м/с² (в окне защиты приземления — 5 м/с²: серия
  кадров-укусов при скрежете иначе сползает и память). Мгновенная память
  затиралась кадром, где скорость уже съедена.
- _protect_landing_speed: 0.25 с после касания (контакт = 2 колеса ИЛИ
  корпус трётся о StaticBody не-стену — при жёсткой посадке корпус
  чиркает раньше колёс) модуль горизонтальной скорости не ниже
  _recent_hspeed. Направление не трогаем — руль работает.
- Трение корпуса 0.5 → 0.15: сцепление и так аркадной силой в _drive,
  трение материала только ворует скорость при скрежете.
- Прыжок через стену жив: ведение отключено 0.6 с после прыжка.
- Тесты: TestLanding (подброс на 3 м на 20 м/с; после посадки, пропуская
  4 переходных кадра, скорость ≥ 17; факт 17.7). TestWallSlide фаза 1
  ужесточена: мин. скорость у стены ≥ 16 (факт 18.8), отскок 0.00,
  угол 2.2°; «пристенок» детектится по _wall_align_time > 0 (машина
  теперь может вообще не коснуться стены). Эффект на игру: ИИ на
  TestLap проезжают 1011 м/мин (до правок дня — 636).

## Подбрасывание носа на ровном/подъёме — сглажено (2026-08-13)
- Жалоба: «машину иногда подбрасывает передом вверх на ровных местах или
  подъёме». Причины: кромка кузова цепляет стыки тримеша дороги на
  переходах профиля (трение уже снижено 0.5 → 0.15, это часть лечения),
  а резкий пик силы прогрессивной пружины на кадре-стыке даёт рывок
  тангажа, который нечему гасить.
- В _physics_process при on_ground добавлено гашение крена/тангажа:
  apply_torque(-spin_xz * 3.0 * mass * 0.1), рысканье не трогается.
  Следование рельефу сохраняется (подвеска сильнее демпфера), гаснут
  только резкие рывки. Все 10 тестов PASS (TestRamp — трамплины
  по-прежнему подбрасывают как надо: speed 20.5 на вылете).

## Подскоки и «после нырка носом едет назад» — исправлены (2026-08-13)
- Жалоба: «всё равно есть подскоки; при нырянии носом машину отталкивает
  назад и она ЕДЕТ назад». Две причины:
  1) Материальный bounce 0.4 у кузова действовал не только между
     машинами, но и о ДОРОГУ — каждый чирк корпуса о полотно давал
     рикошет-подскок, а удар носом о склон — отброс назад.
  2) _protect_landing_speed запоминала только МОДУЛЬ скорости: когда удар
     носом разворачивал вектор, защита «восстанавливала» задний ход и
     машина уезжала назад с прежней скоростью.
- Сделано: bounce 0.0; рикошет машина-машина теперь вручную —
  _bounce_off_cars(): разовый импульс от соперника в момент НОВОГО
  контакта (по словарю _touch_cars), пропорционален скорости сближения
  (×0.4), только по горизонтали.
- Память скорости хранит и направление: _recent_hdir снимается на пике
  (ДО удара). Восстановление: если текущее направление живо (dot > 0) —
  масштабируем его (руль работает); если развёрнуто/убито — возвращаем
  запомненный курс. Порог памяти 2 м/с.
- reset_speed_memory() при респавне (Main._respawn_car и _explode) —
  иначе защита «вернула» бы скорость, которой уже нет.
- Тест `tools/TestDive.tscn`: пикирование носом 35° на 18 м/с с 4 м;
  через ~1.5 с скорость ВДОЛЬ исходного курса должна быть > 5 м/с
  (факт 11.3 — раньше уезжала назад). ИИ на TestLap: 1044 м/мин.

## Стена слегка тормозит + быстрая посадка (чувство массы) (2026-08-13)
- По просьбе пользователя: ограждение должно «слегка притормаживать»,
  а после подлёта машина должна быстрее возвращаться на землю.
- Стена: при перехвате ведением теряется 25% скорости СБЛИЖЕНИЯ
  (0.25*v_out, разово — после перенаправления v_out≈0) + скрежет
  2.5 м/с² пока есть фактический контакт. ВАЖНО: память _recent_hspeed
  срезается вслед (minf), иначе она возвращала штраф обратно.
- Прижим в полёте: в _physics_process при не-on_ground доп. сила вниз —
  14g-доп при падении (vy<0), 5 при взлёте (прыжок не душим;
  jump_impulse 6.0 → 7.5, высота ~1.9 м — стену 1.7 всё ещё можно
  перепрыгнуть). Сила НАРАСТАЕТ за 0.3 с полёта (clampf(_air_time/0.3)):
  без этого микро-подлёты на стыках полотна получали полный прижим,
  машину вбивало в асфальт серией ударов и скорость съедалась
  (TestLanding падал с 17.6 до 15.8, TestRamp с 74 до 46 м).
- Отладка TestLanding по телеметрии: посадка стоит ~1.4 м/с (норм), а
  «просадку до 15.8» давал штраф СТЕНЫ — трасса в плане кольцевая, после
  посадки машина по хорде сближается с внешним бортом, срабатывает
  ведение. Тест теперь обрывает замер при _wall_align_time > 0 и меряет
  кадры 5..13 после касания (факт 18.3). TestWallSlide фаза 1: норма
  скорости у стены теперь ДИАПАЗОН 12..18.5 (тормозит слегка, не в ноль;
  факт 15.1).

## Широкая трасса + маркер игрока (2026-08-13)
- TRACK_HALF_WIDTH 7 → 9 (полотно 18 м). Всё остальное подстроилось
  само: пороги вылета, стартовая клетка, обочина, тесты — везде
  константа. Тесты все PASS (TestRamp: машина после трамплина теперь
  упирается в стену поворота на 39 м — критерий «максимум пройденного»
  это учитывает).
- Маркер игрока: ЗЕЛЁНЫЙ конус остриём вниз над машиной (y=2.4),
  unshaded + emission, cast_shadow OFF (тень от стрелки на машине
  мешала), лёгкое покачивание по высоте (0.12 м, 3 Гц). Вращение убрано
  по просьбе пользователя — отвлекало. Строится в
  Main._build_player_marker(), живёт ребёнком машины игрока.

## Прилипание к дороге — не подпрыгивает, нос не закидывает (2026-08-13)
- Жалоба: «при ударе об землю снова подпрыгивает, иногда забрасывая нос
  вверх; нужно более аркадное управление». Источники: отбой подвески и
  депенетрация кузова дают скорость вдоль нормали полотна НАРУЖУ;
  неравномерное сжатие пружин перед/зад на посадке закидывает нос.
- Сделано (в _physics_process при alive и on_ground):
  1) _ground_normal — средняя нормаль из лучей подвески (копится в
     _apply_suspension).
  2) Прилипание: v·n > 0.4 → лишнее срезается. Подъёмы/трамплины целы:
     на поверхности скорость идёт ВДОЛЬ неё (v·n≈0), на кромке колёса
     теряют контакт (кламп только при on_ground), после прыжка — клапан
     _jump_time.
  3) Выравнивание кузова к ПЛОСКОСТИ ДОРОГИ (не к горизонту!):
     torque = up×n*10 + гашение качки spin_xz*4 (было просто -spin*3).
- Все тесты PASS. TestRamp 39 м с остановкой у стены — это эффект
  ШИРОКОЙ трассы (без руля машина после трамплина утыкается в поворот),
  не прилипания: значение совпадает с прогоном до правки.

## Подскок разрешён, но параллельно земле (2026-08-13)
- Уточнение пользователя: отскок от земли — это ок (аркадный подскок),
  главное чтобы кузов оставался ПАРАЛЛЕЛЬНЫМ земле (не задирал нос).
- Клапан прилипания ослаблен: v·n срезается не свыше 0.4, а свыше
  3.0 м/с (подскок виден, дикие выбросы депенетрации по-прежнему
  отрезаны).
- Полётная ветка _drive: первые 0.5 с полёта корпус выравнивается к
  НОРМАЛИ ПОСЛЕДНЕЙ ОПОРЫ (_ground_normal, память из подвески) с
  плавным переходом к горизонту дальше (lerp по _air_time/0.5) —
  подскок над склоном идёт параллельно склону, дальний полёт как
  раньше к горизонту. Выравнивание к плоскости дороги на земле
  (up×n*10, качка *4) осталось. Все 11 тестов PASS.

## Ведение у стены — только при фактическом касании (2026-08-13)
- Жалоба: «едешь вдоль ограждения — машина, НЕ КАСАЯСЬ его,
  поворачивает вдоль, будто коллайдеры кривые». Причина: зона ведения
  имела запас 0.15 м + упреждение 3 кадра сближения — при плавном
  подруливании перехват срабатывал в десятках см от стены.
- Теперь запас 0, упреждение ровно 1.2 кадра: перехват всё ещё ДО
  решателя (скорость не съедается), но визуально машина реально
  касается ограждения (при мягком сближении зазор — миллиметры,
  дальше корпус дожимается до фактического контакта). Все 11 PASS.

## «Невидимая стена» при подлёте вдоль ограждения — исправлена (2026-08-13)
- Жалоба: «при подлёте вдоль стенки машина останавливается, будто
  невидимая стена поверх ограждений тормозит». Причина: после прыжка
  ведение у стены отключалось на 0.6 с (клапан _jump_time — чтобы можно
  было перепрыгнуть ограждение), и если в полёте машина задевала стену,
  контакт шёл БЕЗ перехвата — решатель мгновенно съедал скорость.
- Теперь ведение отключается не по таймеру прыжка, а ПО ВЫСОТЕ: если
  днище (y-0.3) выше кромки ограждения (axis_pos.y + WALL_HEIGHT) —
  ведения нет, стену можно перелетать; ниже кромки ведение работает
  всегда, в том числе в полёте и сразу после прыжка. Клапан _jump_time
  остался только на зажиме вертикали при контакте (прыжок у стены жив).
- ГРАБЛИ: первая правка сослалась на _wall_guiding, которого в текущей
  версии Car.gd нет (тест смотрит _wall_align_time) → скрипт не
  компилировался, ВСЕ машины ломались, тесты падали/висли. Симптом в
  тестах: «угол 0.0, пристенок=false» (машина так и стояла на решётке).
  Прогонять хотя бы один тест БЕЗ 2>$null — parse-ошибки видны только
  в stderr. Все 11 тестов PASS.

## «Тяжело вывернуть от ограждения» — исправлено (2026-08-17)
- Жалоба: «еду вдоль ограждения слева — тяжело вывернуть вправо, будто
  что-то мешает». Диагностический тест `tools/TestWallTurn.tscn`
  (test_wallturn.gd) воспроизвёл: три одинаковых манёвра (газ + руль от
  стены 1.5 с на 20 м/с) — правый борт, левый борт, середина трассы.
  До фикса у левого борта машина за 0.75 с уходила от стены на 0.16 м
  (в контроле — 6.4 м): «прилипала» на ~1.2 с.
- Две причины, обе в _wall_slide:
  1) Ведение при v_out > 0 ставило ВСЮ скорость строго на касательную.
     Трасса кривая — на кадре, где стена «подворачивает» к машине,
     появляется новое сближение, ведение снова ставит скорость на рельс
     и стирает боковую скорость, которую руль успел наработать. Итог:
     против кривизны не вырваться.
  2) Чтобы отвернуть нос от стены, корма должна вдавиться в стену —
     контактные импульсы решателя гасят рысканье (руль набирает его
     медленным lerp, контакт съедает быстрее).
- Фикс: если руль просит рысканье ПРОЧЬ от стены (steering_away — тот же
  признак, что и раньше пропускал доворот), ведение выпускает машину
  ПОД УГЛОМ 22° от ограждения, а не вдоль. Пары кадров хватает, чтобы
  отойти от стены, контакт рвётся, дальше рулится свободно. Заодно
  fwd/into_sign считаются один раз в начале _wall_slide (раньше — в
  хвосте функции повторно).
- После фикса: левый борт 3.66 м за 0.75 с, правый 9.09 м. Критерий
  теста: за 0.75 с руления от стены уйти ≥ 1.5 м у обоих бортов.
  Все 14 тестов PASS (TestWallSlide не задет: фаза 1 без руля —
  ведение вдоль как было; фаза 2 «руль от стены» стала только легче).

## Закрутка от столкновений машин (2026-08-18)
- Запрос: «врезаешься в другую машину — она должна сместиться или
  повернуть, например поддеваешь зад — и она поворачивает».
- Было: рикошет машина-машина (_bounce_off_cars) — только ЦЕНТРАЛЬНЫЙ
  импульс (центр-в-центр), момента вращения он не даёт; а руление в
  _drive каждый кадр жёстко переписывает angular_velocity.y (lerp 10/с)
  и съедает любую закрутку от удара за пару кадров.
- Сделано (Car.gd):
  1) В _bounce_off_cars при новом контакте, кроме толчка, — закрутка:
     момент = плечо × отн. скорость соперника (плечо — от центра к
     сопернику, не длиннее полукорпуса 1.5 м; rel обрезана 15 м/с);
     spin = момент.y * bump_spin (экспорт, 0.25), порог |spin| > 0.15,
     ИТОГОВАЯ angular_velocity.y клампится ±3.0 — серия контактов
     (удар-отскок-удар) не раскручивает волчком.
  2) Окно _bump_spin_time = 0.6 с: руление в _drive ослаблено
     (lerp 10 → 2.5 на земле, 6 → 2 в воздухе), иначе руль съедал бы
     закрутку мгновенно. Работает для игрока и ИИ одинаково.
- Центральный таран momenta не имеет (плечо ∥ скорости) — только толкает.
- Тест `tools/TestCarBounce.tscn` (test_car_bounce.gd): фаза 1 — таран
  15 м/с в УГОЛ кормы стоящей машины (смещение 0.7 м) — развернуло на
  67° и толкнуло до 12 м/с; фаза 2 — таран строго в центр — 7.5°
  (почти не крутит), толчок 16 м/с. Критерии: угол ≥ 20°, центр < 8°,
  толчок ≥ 1.5 м/с. Лишние ИИ паркуются на другом конце трассы с
  ammo=0 (иначе подстрелят жертву и тест соврёт). 2026-08-18: в тест
  добавлен seed(1234) — атакующий случаен, порог «центр < 8°» без
  seed изредка плавал (9.9°) и тест мигал.

## Кап боковых пинков на ровной езде (2026-08-19)
- Уточнение пользователя: не разворот задом наперёд, а «просто угол
  направления менял» — резкий сдвиг курса на ровном месте. Это боковая
  составляющая того же фантомного удара о ребро тримеша: капы стояли
  на вертикаль (vn), тангаж/крен и пристенок, а боковой пинок по
  курсу — нет.
- Фикс (Car.gd): на установившейся ровной езде (та же ветка, где
  allowed = 0.6) БОКОВОЕ изменение горизонтальной скорости за кадр
  ограничено 0.6 м/с (честная физика даёт максимум ~0.3: грип 14 →
  a = снос×1.4, за кадр 0.23 м/с; кап с двойным запасом). Продольная
  (газ/тормоз) не трогается. Исключения: пристенок (своё ведение),
  окно 0.3 с после честного толчка — новый таймер _ext_push_time
  ставится в _bounce_off_cars (контакт машин) и take_damage (снаряд/
  мина/взрыв). _prev_hvel обновляется В КОНЦЕ _physics_process.
  После телепорта/респавна кап молчит сам: активен только при обеих
  скоростях (прошлой и текущей) > 5 м/с и _ground_time > 0.35.
- TestBumps считает «kicks» (смена угла курса > 2.5°/кадр на плато вне
  пристенка): ИИ-сценарий 2 → 1 (пинки редки и потому «внезапны» в
  игре; метрика главное подтверждает — кап не даёт ложных срабатываний
  на честном рулении). Все 17 тестов + WallGhost без изменений PASS.

## Ведение у стены не «выстреливает» машину (2026-08-19)
- Жалоба: «всё равно отталкивается странно от ограждений, иногда
  внезапно вдоль дороги может поменять направление».
- Проверено и ОТПАЛО: перескок get_closest_offset на соседний виток
  трассы у стены (tools/check_axis_jump.gd — 0 конфликтных зон);
  удары в стену под 55-88° (новый tools/TestWallHit.tscn — разворотов
  нет). Осталась логика самого ведения (_wall_slide):
  1) «свободная» ветка (контакт есть, но v_out <= 0): направление
     бралось из МГНОВЕННОЙ скорости, которую решатель мог только что
     развернуть ударом о ребро, а величина НАКАЧИВАЛАСЬ из памяти
     _recent_hspeed до полной — машину «выстреливало» в случайную
     сторону на полной скорости. Теперь при движении от стены скорость
     НЕ переписывается вовсе (фантомные броски гасят капы: внутрь
     ≤ 1.2 м/с, vy ≤ 1.0, крен/тангаж ≤ 1.5 рад/с);
  2) знак «вдоль» на рельсе: при |продольной| < 2 м/с сигнал ненадёжен
     (мог быть искажён ударом) — берём память направления _recent_hdir,
     затем нос машины (он ограничен ±80° к оси, назад не смотрит).
     Раньше слабый/искажённый сигнал мог развернуть машину задом
     наперёд вдоль трассы на запомненной скорости.
- Все 17 тестов PASS + WallGhost (far_frames 157, без изменений) +
  WallHit (0 кадров «назад»). TestWallGhost научился считать развороты
  продольной скорости (rev=0).

## Кузов больше не трогает полотно — урок из WaterSlides (2026-08-19)
- Жалоба (повторная): «и отскакивает от ограды, и подбрасывает перед
  на ровном месте, и прыгать иногда начинает с потерей скорости».
  Просьба пользователя: посмотреть, как строились горки в WaterSlides.
- Уроки из E:\UnityProjects\WaterSlides\ПРОГРЕСС.md: (1) главный —
  НЕ ДАВАТЬ телу касаться фасеточного меша (там тело на горке вообще
  переведено на путевую модель, а «виртуальный пол» правит только
  скорость вдоль нормали); (2) скорость «в поверхность» срезать ДО
  солвера (у нас это уже делает _wall_slide перехватом за кадр).
- Сделано (Car.gd):
  1) днище «санок» поднято -0.28 → -0.12 (клиренс ~29 см): при обычной
     езде кузов НЕ касается дороги ВООБЩЕ — рёбра тримеша полотна ему
     безразличны; упор от провала срабатывает раньше пробоя пружин.
     Замер (TestBumps, счётчик кадров контакта кузов-дорога за 90 с):
     251 (-0.28) → 156 (-0.18) → 82 (-0.12, в осн. посадки).
     Подскоки на плато: 15 шт (vy до 7.6) → 4 шт (vy ≤ 1.15, все в
     зоне приземления после сходов).
  2) капы УГЛОВОЙ скорости («подбрасывает перед» — это тангаж, линейные
     капы его не трогали): на установившейся ровной езде крен/тангаж
     ≤ 1.2 рад/с (трамплины не задеты: там нормаль опоры наклонена и
     ветка выключена, порог 0.995 > cos 9°); в пристенке ≤ 1.5 рад/с.
- TestBumps научился считать контакты кузова с дорогой (body_contacts).
- Все 17 регрессионных тестов PASS (включая Landing/Dive/Ramp/Flip —
  упор днища и посадки не пострадали); WallGhost: far_frames 157
  (стабильно), вылеты за 2 м от стены не выросли.
- Если жалобы останутся: следующий шаг — радикальный, по образцу
  WaterSlides: дорога на отдельный слой (4), кузов с ней не
  сталкивается вовсе (лучи подвески видят 1|4), а «виртуальный пол»
  в коде держит кузов над полотном; минус — перевёрнутая машина
  ляжет «в дорогу» на 1.2 м до автопереворота.

## Колёса проваливались сквозь дорогу и притормаживали (2026-08-20)
- Жалоба: «в некоторых местах колёса проваливаются сквозь текстуру
  дороги, вроде бы даже притормаживают». Диагностика новым тестом
  `tools/TestWheelSink.tscn` (test_wheelsink.gd: ИИ едет 90 с, по 100
  корзинам круга меряется утопание НИЗА визуального колеса под полотно,
  прожатие подвески, контакты кузова, мин. скорость): утопание до
  0.39 м в ложбинах профиля и зонах приземления, строго коррелирует
  с прожатием подвески 0.8-0.99 и контактами днища (машина скребла
  полотном → «притормаживает»: v_min 8-17 при обычных 30+).
- Причина: кузов — жёсткое тело, колёса запечены в модель (низ на
  -0.35), а подвеска в ложбинах прожималась почти до упора — кузов
  проседал до асфальта, колёса визуально уходили под него, днище
  (-0.12) тёрлось о дорогу.
- Фиксы (Car.gd):
  1) Пружина: к прогрессии добавлен член 12·c⁶ — жёсткий «упор» у дна
     хода. Прожатие покоя (~0.25) не задето (добавка там ничтожна),
     обычная езда не изменилась, но в ложбинах кузов больше не доседает
     до касания днищем. Скорость в ложбинах теперь НЕ проседает
     (t=0.62-0.70: v_min 27-31 против прежних 12-26).
  2) Кламп визуальных колёс: _animate_wheels перенесена из
     _physics_process в _process (кадр рисуется ПОСЛЕ решателя — кламп
     должен видеть конечное положение кузова). Каждый кадр луч вниз от
     ступицы: если низ колеса ушёл бы под полотно, пивот приподнимается
     на глубину утопания (вверх мгновенно, вниз плавно 12/с). Упреждение
     на пару шагов решателя по скорости ТОЧКИ колеса (vy кузова +
     тангаж: при посадке носом колесо ныряет быстрее центра) — без него
     на жёсткой посадке колесо на кадр-два всё же ныряло. Меты пивота:
     rest_pos, lift (ставит collect_wheels).
  3) Из лучей клампа исключены ВСЕ машины (группа cars): кузов соперника
     рядом — не дорога, колесо «вспрыгивало» на него (в тесте ловилось
     как pen 2 м — луч бил в крышу AiCar3).
- Итог: worst pen 0.394 → 0.110 (единичные кадры самой жёсткой посадки),
  на плато ≤ 5 см (глазом не видно); контакты кузова за 90 с: 82 → 71;
  kicks 0. TestWheelSink стал регрессионным: PASS при worst pen < 0.2.
- ГРАБЛИ теста: луч «где полотно» должен исключать все машины, иначе
  ловит кузов соперника, сползшего на трассу с места парковки.
- Все 17 регрессионных тестов PASS (Lap 1292 м, Landing 18.5,
  AICorners 1 инцидент — местами даже лучше базы), WallGhost стабилен
  (far_frames 158, rev=0).

## Фантомные отбросы от ограждений укрощены (2026-08-19)
- Жалоба: «всё так же иногда подскакивает на ровном; очень сильно
  периодически отскакивает от ограждений; еду вдоль ограждения и будто
  на что-то наезжаю — отбрасывает от стены».
- Причина та же, что у подскоков на дороге: стена — тримеш из сотен
  сегментов, кузов-коробка при скольжении цепляет ВНУТРЕННИЕ рёбра
  стыков, решатель даёт разовый импульс до 14-17 м/с к оси (или вверх).
  Старое ведение при v_out < 0 сохраняло направление НЕИЗМЕННЫМ —
  отброс жил много кадров, машину реально уносило от стены.
- Телеметрия tools/TestWallGhost.tscn: 60 с газ + руль в стену, метрики
  «отбросов» (скорость к оси > 2.5), подскоков и — ГЛАВНАЯ — кадров,
  когда машину отнесло от стены > 2 м (far_frames), при среднем
  прижатии avg_dist. Скоростные всплески в тесте видны ДО капа (тест
  читает скорость раньше обработки машины) — судить надо по far_frames.
- Фикс (Car.gd, _wall_slide): в пристенке (контакт или окно доворота
  0.35 с) и БЕЗ руления прочь скорость ОТ стены ограничена 1.2 м/с
  (кривизна выпускает плавно, руль от стены — свободно); клапан
  подскока vy ≤ 1.0 расширен с «только контакт» на всё окно.
  Заодно клапан ровной езды ужесточён 1.0 → 0.6 м/с (жалоба «всё так
  же подпрыгивает»; 0.6 м/с ≈ 2 см — глазом не видно).
- Итог: far_frames 651 → 157 (в 4 раза; остаток в основном легитимные
  переходы между бортами на шиканах), avg_dist 7.15 → 7.56 (плотнее
  к стене при руле в неё). Все 17 регрессионных тестов PASS.

## Пятна травы убраны + фикс «подпрыгивает на ровном» (2026-08-19)
- Жалоба: «у полей пятна одинаково расположенные, большие, некрасиво;
  иногда на ровном месте машина подпрыгивает».
- Трава: (1) при конвертации grass_1 пятна смягчены — текстура
  смешана к своему среднему цвету (blend 0.45 к среднему (76,106,33));
  (2) тайл 10 → 14 м; (3) в _build_ground вершинам задаётся
  низкочастотная вариация яркости (два синуса разных масштабов,
  vertex_color_use_as_albedo) — повторяемость тайла глазом не видна.
- Подпрыгивание: диагностика тестом tools/TestBumps.tscn (одиночная
  машина на ИИ 90 с, соперники заглушены в углу карты; печатает
  всплески vy на плато). Причина — «фантомные» удары днища о
  внутренние рёбра треугольников trimesh-коллизии дороги (в момент
  подскока y машины низкий — днище чиркает по полотну).
- Фикс (Car.gd): клапан vn у земли стал двухступенчатым — 3 м/с
  разрешено только 0.35 с после посадки (аркадный отскок), при
  устоявшейся езде (_ground_time > 0.35) И ровной опоре
  (_ground_normal.y > 0.995) — максимум 1 м/с (~5 см подскока).
  Склоны/трамплины не задеты: там нормаль наклонена и клапан мягкий.
  Новый счётчик _ground_time (время на земле без отрыва).
- Результат: жёсткие подбросы 2-7 м/с на ровном исчезли; остаточные
  ≤ 1 м/с незаметны. Все 17 регрессионных тестов PASS.

## Зелёные поля + трибуны лицом к трассе (2026-08-19)
- Запрос: «трибуны должны быть развернуты к трассе и не залазить на
  неё, поля за трассой сделай зелёные, возьми материалы из ассетов».
- Земля: _build_ground получил планарные UV по миру (тайл 10 м) и
  текстуру травы grass_1 из Cartoon Tracks Pack (сконвертирована PIL
  из psd вместе с grass_2/gravel — лежат в cartoon/textures про
  запас). Низ скайбокса перекрашен в тон травы. ВАЖНО: после
  добавления новых PNG обязательно `godot --headless --import`,
  иначе load() вернёт null и земля будет белой.
- Трибуны: на стенде ScreenshotTribunes.tscn выяснено — у BEDRILL
  Tribune «лицо» +Z, у мультяшных prop_seats_* — -Z (задом). В
  _build_tribunes мультяшным добавлен разворот (флаг flip); новая
  страховка _push_outside: после установки берётся мировой AABB всех
  мешей пропса, и если ближайший угол ближе 1.5 м к ограждению —
  пропс отодвигается наружу (лечит и «залезание» на полотно из-за
  несимметричных пивотов у мультяшных моделей).
- В screenshot_track добавлен кадр tribune75.png (панорама с
  мультяшной трибуной).
- Тесты: Lap/Ramp/Offtrack/Finish/AICorners/WallSlide/MineDrop PASS.

## Голубое небо + разметка на дороге (2026-08-19)
- Вопрос пользователя: «почему у нас красное небо?» — это был фон
  BG_COLOR (0.35, 0.18, 0.12) с комментарием «небо чужой планеты».
  С зелёными деревьями и шарами смотрелось чужеродно.
- Main._setup_environment: вместо заливки — ProceduralSkyMaterial
  (голубой верх, светлый горизонт, коричневый низ в тон рельефу: в
  изометрии сверху за краем земли видна именно нижняя полусфера
  скайбокса, она сливается с пустыней). Ambient стал нейтральным.
- Разметка (TrackDecor._build_road_marks, из ассетов + процедурная):
  1) скобы стартовой решётки prop_gridline под 4 слотами машин
     (позиции повторяют Main._spawn_cars: ряды -2/-7 м, колонны ±2.2);
  2) белые стрелки Arrow_02_free (BEDRILL) за 18 м до вершин крутых
     поворотов, нацелены на вершину («куда ехать»); пики кривизны
     вынесены в общий _turn_peaks (их же используют щиты);
  3) пунктирная осевая: штрих 2.4 м каждые 8 м по всему кругу
     (BoxMesh, наклон по профилю через look_at), возле старта пропуск.
- Стенд tools/ScreenshotMarks.tscn: сравнение стрелок/линий из обоих
  паков на тёмной «дороге» (мультяшные стрелки оказались нечитаемыми
  серыми плашками — взяты BEDRILL).
- Тесты: Ramp/Lap/Finish/Position/AICorners/MineDrop/WallSlide PASS.

## Трасса длиннее + декор из готовых ассетов (2026-08-19)
- Запрос: «бери всё что нужно и сделай красивую трассу подлиннее».
- Трасса: радиус контура 33 → 52 м (+третья гармоника «дыхания»),
  круг 221 → 346 м; точек кривой 40 → 56, SAMPLES 256 → 384,
  земля 190 → 280 м (GROUND_RES 104).
- Ассеты в `assets/models/track_env/`: BEDRILL Track Environment Free
  (арка, баннер, флаги, конусы, шины, знаки, фонари, трибуна — FBX с
  текстурой, работают из коробки) и Cartoon Tracks Pack 1
  (`cartoon/`: стартовые огни, шары, деревья, трибуны, башня, тент,
  реклама, таблички команд). Мультяшные FBX ссылаются на .psd,
  которые Godot не читает: PSD сконвертированы PIL в PNG 1024
  (`cartoon/textures/`), назначаются кодом по имени материала FBX.
- Новый `scripts/TrackDecor.gd` (вызов из TrackBuilder._build_decor):
  финишная арка с клетчатым баннером (вместо самодельной из
  цилиндров — из _build_start_line убрана, шахматная лента осталась),
  ферма стартовых огней с красными LED (консоль нависает над полотном),
  башня, тент, таблички команд у старта; 3 трибуны, 5 шаров в небе,
  мелочь вдоль обочин с шагом 21 м (флаги/конусы/шины/реклама/фонари,
  чередуя стороны), знаки перед 5 самыми крутыми поворотами (поиск
  пиков кривизны, знак на внешней стороне за 12 м), 90 деревьев
  случайной россыпью (seed фикс.). Весь декор БЕЗ коллизий и за
  ограждением — геймплей не задет. Модели BEDRILL несут запечённое
  смещение из их демо-сцены — нормализация по AABB (_recenter).
- Грабли: в списках TREES/BALOONS забыл расширение «.FBX» — load()
  молча падал, деревья и шары не появлялись. Скриншоты обязательны.
- Служебные сцены: `tools/ScreenshotTrack.tscn` (кадры трассы в PNG:
  обзор, старт, середина, игровой ракурс — гонять после правок декора
  и СМОТРЕТЬ картинки), `tools/ScreenshotProps.tscn` (стенд BEDRILL-
  пропсов в ряд).
- Все 17 тестов PASS на новой трассе (Lap: круг 346 м, ИИ 1278 м;
  AICorners: 2 инцидента по 0.3 с — в норме).

## Ограждения выше + мины не висят в воздухе (2026-08-19)
- Запрос: «сделай ограждения повыше; мины не должны оставаться в
  воздухе, если их положили в воздухе».
- TrackBuilder.gd: WALL_HEIGHT 1.7 → 2.6 (прыжок с места ~1.9 м —
  стену больше не перепрыгнуть, только с трамплина). Кромка ведения
  у стены (Car._wall_slide) берёт ту же константу — поднялась сама.
  Обновлён комментарий у jump_impulse в Car.gd.
- Mine.gd: мина, сброшенная в полёте (с трамплина, в прыжке), больше
  не парит — падает с ускорением 9.8 м/с², луч вниз на шаг кадра ищет
  опору (слой 1: дорога/земля; стены — слой 2 — не опора), нашла —
  ложится на поверхность (+0.08 м). Улетевшая ниже y=-60 удаляется.
- Тест `tools/TestMineDrop.tscn` (test_mine_drop.gd): одна мина
  поднята на 4 м — за 2 с должна упасть до полотна (< 0.3 м), вторая
  сброшена как обычно. PASS (упала на 4.32 м, зазор 0.08 м у обеих).
- Регрессия: все 17 тестов PASS (WallSlide/WallTurn/Ramp/Offtrack/
  Lap/AICorners/Flip/Landing/Dive/AirSpin/Respawn/Finish/Position/
  Heading/CarBounce/Explosion + MineDrop).

## Машины «прилипали» друг к другу — разлеплены (2026-08-18)
- Жалоба: «при столкновении машины словно прилипают друг к другу».
  Две причины: трение материала корпуса (0.15) действует и МЕЖДУ
  машинами — корпуса в контакте цеплялись и ехали вместе; а после
  разового рикошета ничто не разводило прижатые кузова.
- Сделано (Car.gd):
  1) physics_material_override.friction 0.15 → 0.05 (сцепление с дорогой
     и так отдельной аркадной силой в _drive, штраф у стены — вручную).
  2) В _bounce_off_cars, пока корпуса СОПРИКАСАЮТСЯ (каждый кадр
     контакта, не только новый), — расталкивание 7 м/с² по горизонтали
     от соперника. Активный таран продавливает (движок 15 м/с²).
- TestCarBounce: фаза 3 — две машины борт к борту внахлёст (1.65 м межд
  центрами, без управления) должны разлепиться до ≥ 2.2 м за 0.75 с
  (факт 2.35; корпуса перестают перекрываться с 1.7). Заодно центральный
  таран стал чище: 5.2° (было 7.5).

## ИИ больше не вылетает на поворотах + руль зависит от скорости (2026-08-18)
- Жалоба: «противники на поворотах сами вылетают за трассу всегда».
  Базовый замер (новый тест TestAICorners, 60 с): 9 вылетов на троих.
- Диагностика (телеметрия в тесте, DEBUG_ZONE): главный механизм — ИИ
  ВЗЛЕТАЕТ с гребня подъёма на плато на 26 м/с (dy до +3.7 м, vy +4.7,
  0 колёс на земле), летит над кромкой ограждения (выше кромки ведение
  у стены отключено by design), трасса поворачивает — а он по прямой,
  падает снаружи (отметки 0.30-0.36). Раньше ИИ вообще не тормозил:
  газ всегда 1.0 (0.45 лишь при развороте > 50° — уже поздно).
- Сделано (Car.gd, _ai_control + _ai_allowed_speed + _axis_dir):
  умное планирование скорости — сэмплы оси трассы на 60 м вперёд
  (шаг 4 м), два предела:
  1) поворот В ПЛАНЕ: v = 0.8 * steer_speed_min / κ (нос крутится с
     конечной скоростью);
  2) ГРЕБЕНЬ (выпуклый перелом профиля): быстрее v²·κ_верт = g машина
     отрывается — предел 1.35*sqrt(g/κ_верт) (подскок можно, дальний
     полёт нельзя);
  запас тормозного пути: у точки на дистанции d можно v² = v_цели²+2ad
  (a = 8 м/с²). Если скорость выше предела +1.5 — тормоз, выше — накат.
- Main.gd _check_recovery: таймер вылета ЗАТУХАЕТ (−2*delta), а не
  сбрасывается в ноль — машина, дребезжащая у порога OFFTRACK_DIST
  (кадр внутри/кадр снаружи), раньше НИКОГДА не набирала таймер и
  каталась за ограждением бесконечно (в тесте: 24 «фронта» за 2 с).
- По ходу (просьба пользователя): скорость поворота коррелирует со
  скоростью машины — turn_rate *= clamp(|v|/steer_full_speed, 0, 1)
  (экспорт, 10 м/с). Стоя развернуться нельзя, полная сила руля с 10 м/с.
- Итог: 1 вылет за 60 с (толкучка на прямой, возврат за 0.6 с), темп ИИ
  948 м/мин (было ~1030 — цена торможения перед гребнями/поворотами).
- Тест `tools/TestAICorners.tscn` (test_ai_corners.gd): seed(1234) для
  воспроизводимости (машины/разброс ИИ случайны), инциденты = фронты
  «вне трассы» с окном слияния 3 с; критерии: ≤ 3 инцидентов, каждый
  эпизод снаружи < 2.5 с (автовозврат работает), худший ИИ > полкруга.
  DEBUG_ZONE := true — телеметрия зоны 0.25-0.40 для отладки.

## После финиша все машины останавливаются (2026-08-14)
- Запрос пользователя: «после финиша все машинки останавливаются».
  До правки останавливался (и то накатом, без тормоза) только игрок
  (controls_enabled = false), а ИИ бесконечно наматывали круги —
  проверено headless-тестом (за 15 c после финиша худший ИИ проезжал
  266 м).
- Car.gd: новый флаг `race_over`; в _physics_process при race_over и
  on_ground — тормозная сила против хода (brake_power), остаток
  горизонтальной скорости < 0.5 м/с обнуляется (иначе на уклоне ползла
  бы вечно), рысканье гасится. В воздухе не тормозим — сначала посадка.
- Main._finish_race(): у ВСЕХ машин controls_enabled = false +
  race_over = true. Застревание (stall) их не респавнит — проверка
  идёт только при controls_enabled.
- Тест `tools/TestFinish.tscn` (test_finish.gd): машина игрока
  переводится на ИИ-управление, гонка финиширует естественно (~47 c);
  через 8 c после финиша никто не должен двигаться. PASS (все встали
  за ~3 c, макс. скорость после остановки 0.00). TestRamp и TestLap —
  PASS (ветка активна только при race_over, физика гонки не задета).

## Позиция в гонке показывалась неверно — исправлена (2026-08-14)
- Жалоба: «позиция игрока в гонке неправильно отображается». Причина:
  прогресс каждой машины копился ОТ ЕЁ МЕСТА НА РЕШЁТКЕ (у всех старт 0),
  а решётка 2×2 — задний ряд стоит на ~5-7 м позади. ИИ из заднего ряда
  весь заезд имел фору прогресса: догнав игрока «колесо в колесо», по
  прогрессу он был впереди, и HUD показывал игроку худшее место.
- Фикс (Main._spawn_cars): стартовый прогресс = фактическая отметка
  машины относительно СТАРТОВОЙ ЛИНИИ (offset близко к концу круга →
  прогресс отрицательный, у игрока ≈ -2, у заднего ряда ≈ -7). Дальше
  всё сравнимо честно; финиш = 4 полных круга именно от линии.
- Тест `tools/TestPosition.tscn` (test_position.gd): ИИ заднего ряда
  телепортируется на 1 м позади игрока — место должно остаться 1
  (до фикса было 2). PASS (прогресс P=-1.7, ИИ2=-3.5).
  TestLap (1101 м/мин) и TestFinish (финиш 47 c, все встали) — PASS.

## Запуск из SiteKeeper (2026-08-14)
- Плитка «▶ Запустить игру» на вкладке Big Head Racing в SiteKeeper.
- Реальный лаунчер — `play.bat` (латиница!): `shell.openExternal` Electron
  молча игнорирует file://-ссылки с кириллицей в имени («Играть.bat» из
  плитки не запускался). «Играть.bat» оставлен и просто вызывает play.bat.

## Трасса сделана плоской (2026-08-20)
- Просьба пользователя: «давай сделаем трассу просто плоской без
  перепадов высот пока».
- TrackBuilder.gd: новый флаг `FLAT_TRACK := true` — _profile_height()
  возвращает 0. HEIGHT_KEYS и вся механика рельефа СОХРАНЕНЫ: вернуть
  перепады = выставить false. Контур в плане, земля, декор, трамплины
  (остались как прыжки на ровном) подстроились сами. Круг 343 м.
- Правки тестов под плоскую трассу и попутные фиксы:
  1) TestCarBounce: порог «гэп разлепления» 2.2 → 2.0 (факт на плоской
     2.18 — на рельефе фазе 3 помогал уклон; перекрытие корпусов
     заканчивается на 1.7, запас остался).
  2) TestWheelSink/TestBumps: заглушенным соперникам ставится
     alive=false — иначе автовозврат Main._check_recovery (вылет за
     трассу) через секунду ВОЗВРАЩАЛ «запаркованных в углу» соперников
     на трассу, и тест-машина их таранила (ловилось как утопание/пинки).
  3) Car.gd, кламп колёс: упреждение считается по НОРМАЛИ опоры
     (сближение точки колеса с плоскостью), а не только по vy — при
     заезде на трамплин поверхность «набегает» под колесо горизонтально
     (v·sin 9° ≈ 4.7 м/с), и колесо на кадр-два ныряло в кромку.
- Все 18 тестов PASS (Lap 1415 м — на плоской ИИ быстрее; AICorners
  0 инцидентов; WheelSink worst pen 0.147 — кромка трамплина, 1-2
  кадра). TestBumps: body_contacts 71 → 17, kicks 0. Скриншоты
  ScreenshotTrack смотрены глазами — трасса плоская, декор на местах.
  ВАЖНО: ScreenshotTrack НЕ работает в --headless (dummy-рендерер,
  вечное ожидание frame_post_draw) — запускать с окном.
- ПОДТВЕРЖДЕНИЕ пользователя (2026-08-20): «вот когда трасса плоская
  всё работает отлично». Итого вся серия жалоб (подскоки, утопание
  колёс, притормаживания, фантомные пинки) была завязана на РЕЛЬЕФ:
  переломы профиля + грубые фасеты тримеша полотна на склонах/ложбинах.
  Когда будем возвращать перепады (FLAT_TRACK=false), сначала: мельче
  сэмплы полотна на переходах профиля (SAMPLES локально/адаптивно) или
  более пологие S-переходы в HEIGHT_KEYS, и прогнать TestWheelSink/
  TestBumps на рельефе — теперь они это ловят числами.

## Система оружия с боксами — как в картинг-аркадах (2026-08-20)
- Запрос пользователя: одно оружие в руках; одно случайное даётся на
  старте; дальше — боксы на трассе (случайный вид); 8 видов оружия;
  уничтожение = мгновенный респавн с миганием и «призраком».
- НОВОЕ УСТРОЙСТВО БОЯ (HP/боезапас УДАЛЕНЫ: hp, ammo, mines,
  take_damage, shoot, drop_mine, refill_ammo, _explode больше нет):
  - `scripts/Weapons.gd` — enum 8 видов + имена: MINE, ROCKET, OIL,
    MAGNET, LASER, FREEZE, AIRSTRIKE, BOOST.
  - Car: `weapon` (одно, -1 = пусто), `use_weapon()` (тратит),
    `destroy()`, `apply_freeze/apply_oil_slip/push_from_blast`,
    `is_ghost()`. Кнопки fire И drop обе используют оружие.
  - СЛОИ КОЛЛИЗИЙ: машины переехали со слоя 1 на СЛОЙ 4
    (layer 0b100, mask 0b111). «Призрак» = layer 0 + mask 0b011
    (дорога/стены остаются, машины навылет). Area3D снарядов/мин/
    боксов ловят машины маской 0b100. Лучи подвески как были (mask 1).
- Виды оружия:
  - Мина (Mine.gd переделана): взрыв при наезде РАСТАЛКИВАЕТ все машины
    в радиусе 6 м (не уничтожает), сила тает с расстоянием.
  - Ракета (Projectile.gd переделан, flag freeze=false): 55 м/с вперёд,
    уничтожает машину при попадании.
  - Масло (OilSlick.gd): пятно сзади, наехавшего заносит (закрутка
    2.2-3.2 рад/с + грип 0.8 на 1.1 с; окна _bump_spin_time и
    _ext_push_time не дают рулю/капам съесть занос).
  - Магнит: все машины разово получают импульс 6 м/с К использовавшему.
  - Лазер (LaserFx.gd — визуал): луч 70 м, полуширина 1.6 м,
    уничтожает ВСЕХ на пути.
  - Заморозка (Projectile freeze=true): попавший синеет (IceShell —
    голубая полускорлупа на кузове) и едет ×0.55 на 3 с; дебаф ЗАРАЗЕН
    при контакте машин (передаётся остаток, _bounce_off_cars).
  - Авиаудар (Airstrike.gd): цель — ЛИДЕР гонки (Main.leader_car());
    4 тени на оси перед ним (10-28 м, случайный сдвиг вбок), через 1 с
    падение — все машины в радиусе 3.2 м от тени уничтожаются.
  - Ускорение: ×1.45 к пределу и тяге на 2.5 с.
- Уничтожение (destroy): вспышка → машина СРАЗУ на оси трассы с нулевой
  скоростью → «призрак» 2 с: мигает 3 раза (только визуал), НЕ
  сталкивается с машинами, но может ехать/разгоняться; потом всё как
  раньше. Дебафы при смерти сбрасываются, оружие сохраняется.
- Боксы (WeaponBox.gd): золотой вращающийся куб, 5 линий × 3 шт по
  кругу (t = 0.12/0.3/0.52/0.7/0.92), респавн 5 с. Подбор заменяет
  текущее оружие. Main._spawn_weapon_boxes().
- ИИ: применяет по типу — ракета/лазер/лёд по врагу в конусе впереди,
  мина/масло по врагу сзади, магнит при враге ближе 12 м, буст на газу,
  авиаудар сразу.
- HUD: вместо HP-бара и боезапаса — «Оружие: <имя>» (жёлтым) или
  «возьми бокс»; подсказка внизу обновлена.
- Тест `tools/TestWeapons.tscn` (test_weapons.gd): 12 сценарных
  проверок (ракета→призрак, лёд+заражение, мина толкает, лазер бьёт
  двоих, буст, магнит, масло легло/заносит, авиаудар создан/отработал,
  бокс даёт оружие). ГРАБЛИ: (1) жертв парковать только с alive=false —
  иначе автовозврат притащит их на трассу под снаряды теста; (2) позиции
  дальше ~15 м ставить ПО КРИВОЙ, а не по прямой от точки оси (прямая
  на 25 м уводит на ~5 м вбок, к стене).
- TestExplosion переписан под destroy/призрак (мигание, снятые слои,
  возврат на трассу). Все 19 тестов PASS; гоночные тесты (Lap 1443,
  AICorners 0 инцидентов) идут с ЖИВЫМ оружием у ИИ — цикл гонки
  устойчив к хаосу уничтожений.

## Помощь развороту у стены + дым/искры + ракеты авиаудара (2026-08-20)
Запрос: (1) после перпендикулярного удара в отбойник машина долго
разворачивается вдоль трассы («пока раза 4 не ударишься — не
развернёшься»); (2) тень от ракет авиаудара поменьше и видимые падающие
ракеты; (3) дым из-под колёс на заносе; (4) искры при столкновении машин.
- Помощь развороту (Car.gd). Причина застревания: сила руля ~ скорости
  (turn_rate *= |v|/steer_full_speed) — у стены машина почти стоит, руль
  бессилен; а нажатый руль отключает штатный доворот вдоль ограждения
  (steering_away в _wall_slide). Два механизма:
  1) автодоворот в _clamp_heading: нос поперёк оси > 35° И скорость
     < 8 м/с И колёса на земле → корпус сам крутится к касательной
     2.4 рад/с (до 35°, дальше руль);
  2) нижний порог силы руля: при |угол к оси| > 35° speed_factor не
     ниже 0.6 — рулить можно и почти стоя. Новый var _track_ang_abs
     (ставит _clamp_heading).
  Тест tools/TestWallRecover.tscn: машина СТОИТ вплотную к стене носом
  под 80°, «игрок» жмёт газ + руль от стены; критерий — угол < 30° за
  ≤ 2 с (факт 0.30; до фикса 0.68 — фикс главное помогает, когда игрок
  без газа в пол или сдаёт назад: раньше руль стоя был мёртв).
- Авиаудар (Airstrike.gd): тень-диск 2.56 → 1.1 м (SHADOW_RADIUS);
  видимые ракеты — серый цилиндр, красный нос-конус остриём вниз,
  4 стабилизатора, оранжевое «пламя» сверху; падают с 34 м квадратично
  по остатку таймера (у земли ~68 м/с), нос касается точки в момент
  взрыва. Начало координат ракеты — остриё.
- Дым на заносе (Car.gd): 2 CPUParticles3D у задних колёс
  (_build_smoke), серые тающие клубы, local_coords=false (остаются
  позади). Включение: на земле И (|боковой снос| > 3 м/с при скорости
  > 5 ИЛИ занос от масла). Новый var _side_speed (пишет _drive).
- Искры (новый scripts/SparksFx.gd, CPUParticles3D one-shot):
  жёлто-оранжевый сноп в точке контакта машин, сила от скорости
  сближения; спавн в _bounce_off_cars при closing > 2, только у одной
  машины из пары (меньший instance id) — иначе дублировалось бы.
  Сам удаляется по сигналу finished. После нового class_name — не
  забывать `--headless --import`.
- Все 20 регрессионных тестов PASS (Lap 1414, AICorners 0 инцидентов,
  Heading ровно 80.0° — помощь развороту лимит не ломает).
- Доработка дыма (2026-08-20, «дым слишком квадратный, на небольших
  заносах не нужен»): клубы были резкими квадратами — у QuadMesh не
  было текстуры. Теперь мягкое круглое пятно (GradientTexture2D
  FILL_RADIAL 64×64, белый центр → прозрачный край) + случайный поворот
  билборда (angle 0-360) + рост клуба по кривой (0.4 → 1.5 за жизнь).
  Порог включения поднят: |боковой снос| > 5 м/с при скорости > 8
  (было 3 и 5) — лёгкое подруливание не дымит; масло как было.

## Стена гасит удар без отброса + ракеты из-за экрана + магнит (2026-08-20)
Запрос: «машина отлетает при ударе об ограждения — стена должна гасить
удар, но не отталкивать; тени от ракет меньше и ракеты летят сверху
из-за экрана; магнит сильнее».
- Стена (Car.gd, _wall_slide). Источник «отлетания»: при НАЖАТОМ руле
  прочь от стены кап скорости «от стены» (1.2 м/с) не действовал вовсе,
  а ветка выпуска восстанавливала скорость из памяти _recent_hspeed и
  выстреливала под 22° от ограждения (sin22°·20 м/с ≈ 7.5 м/с наружу).
  Фиксы: (1) кап скорости от стены теперь ВСЕГДА в пристенке — без руля
  прочь 1.2 м/с, с рулём прочь 3.0 м/с (отойти можно, отлететь нельзя;
  выпуск под 22° жив, но его наружная компонента режется тем же капом
  в этом же кадре); (2) гашение удара усилено: штраф скорости сближения
  при перехвате 25% → 40% (скользящий удар почти не теряет — v_out мал,
  перпендикулярный гасится ощутимо).
- Авиаудар (Airstrike.gd): тень 1.1 → 0.75 м; ракеты летят НАКЛОННО
  «из-за верхнего края экрана»: старт = точка удара + UP·40 +
  26 м по горизонтали вдоль взгляда камеры (изокамера не вращается,
  дальше по взгляду = выше на экране; headless-фолбэк — вектор изокамеры
  (-0.707, 0, -0.707)). Корпус наклонён по траектории:
  quaternion = Quaternion(DOWN, вектор падения).
- Магнит: импульс 6 → 11 м/с.
- Тесты: WallSlide/WallTurn/WallHit/WallRecover/Weapons/Landing/Lap/
  AICorners PASS; WallGhost far_frames 162 (база 157-158), rev=0 —
  отбросы не выросли, ведение не сломано.

## Красивый UI из GUI Pack Cartoon + эффекты из Epic Toon FX (2026-08-20)
Запрос: «ассеты для UI из E:\UnityProjects\cars\Assets\GUIPackCartoon,
спецэффекты из E:\UnityProjects\Fish\Assets\Epic Toon FX, сделай
красивый UI».
- Ассеты скопированы в проект: `assets/ui/` (панели-пилюли, круги,
  кнопка, баннер-лента, стрелки, 8 иконок оружия, шрифт LilitaOne.ttf)
  и `assets/fx/` (smoke_cloud_2x2, sparkle, explosion_3x3). Источники:
  GUIPackCartoon\Demo\Sprites (готовые PNG; Sources — psd/svg) и
  Epic Toon FX\Textures. После добавления — `--headless --import`.
- Weapons.gd: словарь ICONS + static icon(kind) — иконки для HUD
  (мина=бомба с шипами, ракета=ракета, масло=фиол. зелье, магнит,
  лазер=синяя молния, заморозка=голубое зелье, авиаудар=петарды,
  буст=жёлтая молния).
- HUD гонки (Main.gd, _setup_hud переписан, хелперы _make_panel/
  _make_label): скорость и круг/место — глянцевые панели-пилюли
  (btn_rect_gray + self_modulate серо-синий), слот оружия — тёмный
  круг с иконкой и подписью («?» при пустом), предупреждение — красная
  плашка btn_rect_red, отсчёт 3-2-1-GO — огромные цифры LilitaOne с
  «выпрыгиванием» (tween scale 1.6→1, TRANS_BACK), финиш — розовый
  баннер-лента с текстом. _ammo_label/_center_label удалены
  (_weapon_icon/_weapon_name/_count_label/_finish_root).
- Экран выбора (CarSelect.gd): заголовок на розовом баннере, имя машины
  шрифтом LilitaOne на панели (имена латиницей — у LilitaOne НЕТ
  кириллицы, русский — дефолтный шрифт с обводкой), голубые
  стрелки-кнопки листания по бокам подиума, зелёная глянцевая СТАРТ
  (StyleBoxTexture + modulate_color на hover/pressed).
- Эффекты: дым колёс — мультяшные облачка из атласа 2×2 (материал
  particles_anim 2×2 + anim_offset random = случайный кадр каждой
  частице); искры — звёздочка sparkle.png; FlashFx ПЕРЕПИСАН:
  AnimatedSprite3D, 9 кадров explosion_3x3 (AtlasTexture), билборд,
  красится modulate в цвет эффекта, сигнатура spawn прежняя.
- ГРАБЛИ Godot UI (все пойманы скриншотами):
  1) set_anchors_preset СОХРАНЯЕТ текущий размер (подгоняет offsets) —
     «full rect» лейбл остаётся крошечным у левого верха; нужен
     set_anchors_and_offsets_preset.
  2) TextureRect: expand_mode ставить ДО size — при дефолтном
     KEEP_SIZE присвоение size клампится к размеру текстуры (512).
  3) NinePatchRect.modulate красит и детей (текст темнел) — красить
     через self_modulate.
  4) panel_round (Rounded Rectangle 256px) не годится для 9-slice:
     радиус в полтекстуры, панель выходит «ромбом» — панели собраны
     из кнопки-пилюли btn_rect_gray (margin 30).
- Доработка по замечаниям (2026-08-20, «текст в флагах по центру,
  чёрные облака с выпуклостями, оружие на E»):
  1) Панели HUD переведены с 9-slice кнопок пака на ПЛОСКИЕ
     StyleBoxFlat-панели (скруглённый прямоугольник, тонкая рамка):
     у ВСЕХ «прямоугольников» GUI Pack Cartoon края пузатые (замерено
     PIL: гуляют до 12-19 px) — растянутая 9-slice панель выглядит
     «облаком с выпуклостями». Кнопки/баннеры/иконки пака остались.
  2) Тексты на лентах-баннерах отцентрированы (убраны смещения вверх).
  3) Оружие перенесено на E (InputSetup: fire=[KEY_E], было Ctrl/J),
     подсказка внизу обновлена.
  4) Шрифт заменён: пользователь дал «Softie Cyr.ttf» (из
     E:\UnityProjects\Fish\Assets\TextMesh Pro) — МУЛЬТЯШНЫЙ И С
     КИРИЛЛИЦЕЙ (у LilitaOne кириллицы не было) → assets/ui/Softie.ttf,
     применён ко ВСЕМУ UI (HUD, отсчёт, баннеры, кнопка, подсказки),
     параметр cartoon у _make_label убран.
- ГРАБЛИ PowerShell (повторные!): Get-Content -Raw без -Encoding UTF8
  читает файл в ANSI(1251) → после Set-Content кириллица в Main.gd
  превратилась в мохибейк. ВОССТАНОВЛЕНО обратной перекодировкой .NET
  (UTF8-байты → GetString → GetBytes(1251) → байты; BOM-символ убрать).
  Больше НЕ править файлы с кириллицей через PowerShell-Replace —
  только Edit-инструменты.
- Скриншот-стенды (С ОКНОМ, headless не рендерит):
  tools/ScreenshotHud.tscn (кадры: отсчёт, GO, езда, предупреждение —
  подделывается в _process с process_priority=100, иначе Main гасит, —
  и финиш) и tools/ScreenshotSelect.tscn (экран выбора). PNG в
  tools/shots/, смотреть глазами после правок UI.
- Все 20 регрессионных тестов PASS.

## Метки авиаудара ярче + дым к колёсам + новая трасса 442 м (2026-08-20)
Запрос: «тень от ракет посильнее выделяться должна; дым не такой
длинный и из-под задних колёс (сейчас по бокам); трассу подлиннее и
другой формы».
- Авиаудар (Airstrike.gd): SHADOW_RADIUS 0.75 → 0.9, тень плотнее
  (alpha 0.8, unshaded) + ЯРКО-КРАСНОЕ КОЛЬЦО-ПРИЦЕЛ (TorusMesh,
  emission 2.0) ребёнком тени — пульсирует вместе с ней, видно издалека.
- Дым (Car.gd): эмиттеры сдвинуты с краёв корпуса (x ±0.85 — клубы
  торчали по бокам) внутрь колеи за задние колёса (±0.55, z 1.5);
  шлейф короче: lifetime 0.75 → 0.5, разлёт 0.5-1.2 (был 0.8-2.0),
  клубы мельче (quad 0.7, scale 0.55-0.95, рост до 1.2), amount 16.
- Трасса (TrackBuilder.gd): новый контур — базовый радиус 52 → 68,
  ПЕРВАЯ гармоника (10·sin(a+0.7)) даёт асимметрию «яйцом» + шиканы
  8·sin2a/4·sin3a/2·sin6a; точек 56 → 64, SAMPLES 384 → 512, земля
  280 → 340 м (GROUND_RES 126). Круг 343 → 442 м. test_curve: излом
  в плане 2.42° (лимит 6°). Боксы/трамплины/декор привязаны к доле
  круга — подстроились сами.
- В ScreenshotHud добавлен кадр hud_airstrike.png (авиаудар целится
  в ИГРОКА напрямую, не через use_weapon: лидер может быть вне кадра).
- Все 20 тестов PASS на новой трассе (Lap 1474 м/мин, AICorners 0
  инцидентов, WheelSink pen 0.000 — на плоской трассе с новым контуром
  колёса вообще не тонут).

## Настоящая гоночная конфигурация трассы + переменная ширина (2026-08-21)
Запрос: «трассу посложней, покруче повороты, прямые участки и изгибы,
сужения и расширения тоже».
- ПРИНЦИП ИЗМЕНЁН: контур больше не «дышащая окружность» (гармоники
  радиуса давали только плавные дуги — ни прямых, ни шпилек). Теперь
  трасса задаётся ЯВНОЙ последовательностью участков `SEGMENTS`
  («черепахой»): `["S", длина, полуширина]` — прямая,
  `["A", радиус, угол°, полуширина]` — дуга. Круг 442 → 726 м.
  Конфигурация: стартовая прямая ~46 м → быстрый правый R45 →
  S-шикана R28 → ШПИЛЬКА R19 (150°) → длинный левый R40 → длинная
  прямая ~77 м → R32 → крутой правый R26 (105°) → пологий левый R50.
- ЗАМЫКАНИЕ КОЛЬЦА ТОЧНОЕ, без подгонки руками: сумма углов дуг = 360°
  (обязательное условие, иначе излом на стыке), а длины ДВУХ «свободных»
  прямых (в SEGMENTS длина -1.0) считает `_solve_free_lengths()` —
  смещение линейно по этим длинам, поэтому хватает системы 2×2 по трём
  пробным проходам. Конфигурация спроектирована и проверена сначала
  скриптом на Python (замыкание, самопересечение, отрисовка в PNG),
  потом перенесена в GDScript.
- ГРАБЛИ (важные): в первой версии «черепахи» касательная записывалась
  по курсу ДО поворота, а шаг делался по курсу ПОСЛЕ — точка и её
  касательная расходились на полшага, безье «водило», эффективный
  радиус выходил меньше заданного (test_curve: 7.87° при лимите 6°).
  Правильно: касательная в точке — по текущему курсу, а шаг — ПО ХОРДЕ
  под ПОЛОВИНОЙ угла шага (chord = 2R·sin(δ/2)). После фикса излом
  5.55° = ровно кривизна шпильки, дефектов нет.
- ПЕРЕМЕННАЯ ШИРИНА полотна: 6.0 м (шпилька) … 11.0 м (прямые), плавные
  переходы (smoothstep) по ключам из SEGMENTS. `half_width_at_ratio/
  _at_offset/_at_pos()`. TRACK_HALF_WIDTH теперь = МАКСИМУМ (11) и
  используется только там, где нужен консервативный запас (обочина,
  деревья). Протащено во всё, что зависело от ширины:
  полотно и стены (_widths по сэмплам), стартовая решётка,
  Car._wall_slide (грань стены в ТЕКУЩЕЙ точке — иначе в шпильке
  ведение включалось бы внутри стены), Main (порог вылета
  OFFTRACK_MARGIN + ширина в точке; разброс боксов), Airstrike
  (разброс теней), TrackDecor (трибуны/мелочь/знаки жмутся к кромке).
- Трамплины больше не на фиксированных долях круга (они попадали бы на
  дуги нового контура), а в серединах двух самых длинных ПРЯМЫХ —
  `_ramp_ratios()` по собранным в _walk участкам.
- ПОБОЧНЫЙ ФИКС: TrackDecor._outward() считал «наружу» как направление
  от начала координат — верно только для круглой трассы; на контуре с
  шиканой декор уезжал бы ВНУТРЬ кольца. Теперь пробуются обе стороны:
  наружу та, где дальше до полотна (с другой стороны соседний виток).
- test_curve: лимит излома больше не константа 6°, а считается от
  САМОГО КРУТОГО радиуса конфигурации (step/R_min ×1.4) — крутой
  поворот это задумка, а не дефект; настоящий излом даст угол в разы
  больше. Печатает min_radius.
- ГРАБЛИ теста оружия: фазы должны быть ИЗОЛИРОВАНЫ. Жертва мины стоит
  в 25 м по кривой, и на трассе с прямыми это совпало с лучом лазера
  (70 м по прямой) — жертва уходила в «призраки», а призрака не берут
  ни магнит, ни масло (снят слой машин, Area3D его не видит): падали
  две последние проверки. Теперь перед лазером жертва паркуется.
- Земля 340 → 400 м (GROUND_RES 148), SAMPLES 512 → 768: контур
  габаритом 240×169 м, сэмплы должны оставаться ~1 м.
- ПОБОЧНЫЙ ФИКС (ошибка в логе): WeaponBox выключал `monitoring` прямо
  в обработчике body_entered — физика в этот момент внутри своего
  колбэка и блокирует смену («Function blocked during in/out signal»).
  Теперь `set_deferred("monitoring", false)`.
- Тесты, которые пришлось чинить под трассу с прямыми и дугами (все
  правки — про МЕСТО замера, физику не трогали):
  1) TestWallTurn ставил машину на 6% круга — теперь это вход в поворот
     R=45, и на 28 м/с центробежная сила (17 м/с², больше сцепления)
     прижимала её к внешней стене: тест мерил снос, а не «прилипание»
     (левый борт 1.26 м при норме 1.5). Место пуска теперь ВЫЧИСЛЯЕТСЯ:
     начало самой длинной ПРЯМОЙ без трамплина (_pick_start_t), а
     «вплотную к борту» — от фактической кромки, а не фиксированные
     7.8 м. Итог: борта симметричны (2.06 / 2.11 м), контроль 3.42.
  2) TestBumps: порог «пинка» был константой 2.5°/кадр — на шпильке
     машина законно доворачивает ~3°/кадр. Теперь порог считается от
     КРИВИЗНЫ ОСИ в этой точке (_axis_curvature, ×1.6): ловим доворот
     СВЕРХ поворота трассы. Зона трамплина стала несимметричной
     (−0.01…+0.10 круга): приземление после прыжка — не «подброс на
     ровном». Трамплины тест берёт у трассы (_ramp_ratios), а не из
     зашитых [0.44, 0.875]. Итог: kicks=0, подбросов нет.
  3) TestWeapons — см. грабли выше (изоляция фаз).
  4) TestOfftrack / TestWallGhost / TestWallRecover / TestAICorners —
     переведены на фактическую полуширину в точке.
- ГРАБЛИ (повтор известных): `var x := _main._track.метод()` не
  компилируется — `_main` объявлен как Node3D, тип не выводится
  («Cannot infer the type»), и тест молча ВИСНЕТ (скрипт не грузится,
  фаза не наступает). Писать тип явно. Проверять stderr, а не только
  stdout: parse-ошибки видны только там.

## Мина и магнит бьют сильнее (2026-08-21)
**Проблема:** оба оружия отрабатывали вяло. Мина: радиус 6 м, импульс
9 м/с в эпицентре, ЛИНЕЙНЫЙ спад — уже в 3 м от центра оставалось 4.5 м/с,
которые сцепление (grip 14, постоянная времени ~0.7 с) съедало за пару
кадров. Подброса не было ВООБЩЕ: на ровном полотне клапан отскока режет
вертикальную скорость до 0.6 м/с (`allowed` в `_physics_process`), а
взрыв этот клапан не открывал. Закрутки не было тоже. Магнит: 11 м/с
без учёта дистанции.

**Что сделано:**
- `Mine.gd`: BLAST_RADIUS 6 → 10 м, BLAST_SPEED 9 → 18 м/с, спад стал
  КВАДРАТИЧНЫМ (`1 - t²` вместо `1 - t`) — рядом с эпицентром взрыв
  держит почти полную мощь и сходит на нет только к кромке. Добавлены
  BLAST_SPIN 3.2 рад/с и BLAST_LIFT 0.45 (доля подброса), обе тают тем
  же falloff. Вспышка FlashFx 2.0 → 3.2 под новый радиус.
- `Car._use_magnet()`: 22 м/с вблизи → 11 м/с на 40 м и дальше (lerp по
  дистанции: магнит достаёт всю трассу, но рядом сдёргивает жёстко),
  закрутка до 2.4 рад/с (только вблизи), подброс 0.12 — магнит ВОЛОЧИТ
  по земле, а не подкидывает. Вспышка 1.8 → 2.6.
- `Car.push_from_blast(dir, power, spin := 0.0, lift := 0.35)` — два
  новых необязательных параметра, старые вызовы совместимы. Главное:
  на окно толчка снимаются ВСЕ страховки, которые иначе съедали удар:
  1) `_ext_push_time` 0.4 → 0.7 с (кап боковых пинков — толчок честный);
  2) `_jump_time = max(_jump_time, 0.4)` при lift > 0.05 — ИНАЧЕ ПОДБРОСА
     НЕ ВИДНО: клапан отскока от земли режет вертикаль до 0.6 м/с на
     ровном. Заодно открывается клапан вертикали у стены (строка 825) —
     прижатую к отбойнику машину мина тоже подкидывает;
  3) `_bump_spin_time = max(_bump_spin_time, 0.8)` при spin ≠ 0 — иначе
     жёсткий lerp рысканья (yaw_rate 10) гасит закрутку в тот же кадр
     и разворота от взрыва не будет. Закрутка клампится ±4 рад/с.

**Замеры** (временный стенд, машина стоит, снят после):
| случай | пик скорости | смещение за ~1.5 с |
|---|---|---|
| мина в упор | 17.9 м/с (было ~9) | 11.4 м + подброс ~2.3 м + вращение |
| мина в 5 м (полрадиуса) | 13.4 м/с (было ~1.5) | 9.4 м |
| магнит, жертва в 8 м | 19.7 м/с (было 11) | 12.4 м к магу |

**Грабли:** мина взрывается ТОЛЬКО от наезда машины (`body_entered` →
`_try_trigger`), сама по себе — никогда. В замерочном стенде мина,
положенная в 5 м сбоку от жертвы, просто лежала до конца фазы, и
«сила» читалась как 0.2 м — пришлось ставить на неё вторую машину.
Это же ограничение надо помнить при любых будущих тестах взрыва.

**Файлы:** `scripts/Mine.gd`, `scripts/Car.gd`.
**Тесты:** все 20 сцен + test_curve — PASS (пороги TestWeapons «мина
толкает > 2 м/с» и «магнит > 1 м/с» пройдены с большим запасом).

## Магнит ещё сильнее + масло заносит всерьёз (2026-08-21)
**Магнит** (`Car._use_magnet`): 22 → 32 м/с вблизи (это почти max_speed
34), 11 → 18 м/с на дальней дистанции, радиус спада 40 → 45 м, закрутка
2.4 → 3.6 рад/с. Вспышка 2.6 → 3.2. Замер: жертва в 8 м — пик скорости
19.7 → 29.4 м/с, сдвиг к магу 11.3 → 20.8 м, пик рысканья 1.92 → 2.96
рад/с. Тоннелирования сквозь отбойник нет: у Car включён `continuous_cd`.

**Масло** (`Car.apply_oil_slip` + `OilSlick`): раньше занос был вялый —
1.1 с, сцепление 0.8, тяга при этом ПОЛНАЯ (газ вытаскивал из заноса).
Теперь:
- `slip_duration` 1.1 → 2.0 с;
- новый экспорт `slip_grip` 0.3 (было зашито 0.8, обычное сцепление 14);
- новый экспорт `slip_thrust` 0.35 — ТЯГА И ТОРМОЗ режутся на масле
  (колёса буксуют). Это главное по ощущениям: из заноса больше нельзя
  выехать газом, машину просто несёт юзом;
- закрутка 2.2…3.2 → 3.2…4.4 рад/с, кламп ±3.5 → ±4.5;
- радиус пятна 1.7 → 2.4 м (и коллизия, и меш) — мимо уже не проскочишь.

**Замер масла** (влетел на 20 м/с, окно 1.2 с): пик рысканья 2.63 → 3.79
рад/с. Максимальный угол разворота в обоих случаях ~111° — он упирается
в `_clamp_heading` (max_track_angle_deg = 80° к оси трассы, плюс сама ось
поворачивает под скользящей машиной). То есть «сильнее» у масла
проявляется НЕ в большем угле, а в том, как быстро машину туда кидает,
сколько она там держится (1.1 → 2.0 с) и в потере газа.

**Грабли замера:** боковой снос от масла = 0.0 м — и это ВЕРНО, а не
баг стенда: сцепления почти нет, машина крутится на месте вокруг
вертикали, продолжая скользить по прежнему курсу. Мерить масло надо по
углу/рысканью, а не по сносу. И второе: длинное окно замера (2.5 с)
давало одинаковые цифры до и после — на 20 м/с машина за это время
доезжала до отбойника, и стенд мерил УДАР О СТЕНУ, а не масло. Окно
пришлось урезать до 1.2 с и печатать контроль «занос сработал».

**Риск, принятый осознанно:** `apply_oil_slip` ставит
`_ext_push_time = slip_duration`, то есть кап боковых пинков (защита от
фантомных рывков на ровном, см. раздел от 2026-08-19) теперь отключён на
2 с вместо 1.1. Окно длинное, но это ровно тот случай, когда дикое
боковое движение — задумка, а не глюк.

**Файлы:** `scripts/Car.gd`, `scripts/OilSlick.gd`.
**Тесты:** все 20 сцен + test_curve — PASS.

## Занос от масла крутит в разрешённую сторону (2026-08-21)
**Проблема:** сторона закрутки была чисто случайной. У ограждения в
половине случаев занос втыкал нос В СТЕНУ — а там его тут же и съедали
`_clamp_heading` (предел 80° к оси) и ведение у стены: вместо эффектного
вращения выходил тычок в отбойник.

**Решение:** новый `Car._spin_away_from_wall() -> float`. На свободном
полотне возвращает 0.0 (сторона остаётся случайной), у ограждения —
знак рысканья, уводящий нос ОТ стены. `apply_oil_slip` берёт его, если
он ненулевой.
- «Близко к стене» = `dist + 3.0 м >= half_width_at_offset(off) −
  WALL_THICKNESS/2`. Грань берём в ТЕКУЩЕЙ точке трассы, а не константой:
  полотно переменной ширины (см. раздел про гоночную конфигурацию).
- Нормаль «наружу» считается ОТ ОСИ трассы (`pos − axis_pos`), поэтому
  работает для обоих бортов, внутреннего и внешнего, без отдельных веток.
- Знак «в стену» — ровно тот же, что `into_sign` в `_wall_slide`:
  `signf(fwd.signed_angle_to(n, UP))`. Разрешённая сторона — минус он.
  ВАЖНО держать эти две формулы согласованными: если знак разъедется,
  занос будет крутить нос туда, откуда ведение у стены его выталкивает.

**Тест:** в TestWeapons добавлена фаза «масло у стены» — жертва ставится
в 1 м от грани отбойника носом вдоль трассы, занос роняется напрямую
(`apply_oil_slip`, без пятна: проверяется ВЫБОР СТОРОНЫ, а не
срабатывание Area3D). Две проверки: «занос у стены не в стену» (знак
рысканья) и «нос ушёл от стены» (через 0.5 с проекция носа на нормаль к
стене < −0.15). Тест ПРОВЕРЕН НА ЛОЖНОЕ СРАБАТЫВАНИЕ: с инвертированным
знаком в `_spin_away_from_wall` обе проверки падают.

**Файлы:** `scripts/Car.gd`, `tools/test_weapons.gd`.
**Тесты:** все 20 сцен + test_curve — PASS.

## Значки действующих эффектов над машиной + новый значок буста (2026-08-21)
Запрос: «когда на тебя действуют магнитом — показать над машиной магнит,
когда ускорение — значок ускорения, но значок ускорения нужно другой».

**Новый значок ускорения.** Старый `assets/ui/w_boost.png` был
`Bolt - Yellow (Border).png` из GUI Pack Cartoon (совпадает побайтно —
проверено по md5, так что откатить можно из пака) — пузатая жёлтая
клякса, молния в ней не читается. Нарисован свой: ДВОЙНОЙ ШЕВРОН вверх,
256×256, в стиле пака (толстая тёплая обводка #C4551A, глянцевый
градиент #FFE855 → #FF930E, белый блик по левому плечу). Генератор — PIL,
суперсэмплинг ×4.
- ГРАБЛИ рисования: сначала обводка делалась штрихом по контуру
  (`draw.line(..., joint="curve")`) — она съедала половину заливки
  ВНУТРЬ, а на остром носу шеврона `joint="curve"` срезал кончик
  «галочкой». Правильно — рисовать обводку ТОЛЬКО НАРУЖУ: дилатация
  маски формы (union 64 сдвигов по окружности радиуса обводки), под неё
  подкладывается цвет обводки, сверху — заливка по исходной маске.
- После замены PNG обязателен `godot --headless --path . --import`.

**Значок над крышей** (`Car`): `Sprite3D`-билборд, собирается в
`_build_status_icon`, живёт в `_tick_status_icon` (зовётся из `_process`).
- `top_level = true` ОБЯЗАТЕЛЕН: иначе значок наследует поворот кузова и
  при закрутке от масла/кувырке ездит вокруг машины и уходит под землю.
  Позиция ставится вручную каждый кадр.
- Два источника, и они устроены ПО-РАЗНОМУ. Разовый (магнит) —
  `show_effect_icon(kind, duration)`, зовёт применивший
  (`_use_magnet` → жертве, 1.5 с). Длящийся (буст) читается прямо из
  `_boost_time`, СВОЙ таймер ему не заводится: `destroy()` обнуляет буст,
  и отдельный таймер оставил бы значок висеть над уже отброшенной
  машиной. Магнит приоритетнее буста.
- Высота: 2.35 м, а у игрока 3.25 м — над его крышей уже висит зелёная
  стрелка-маркер (Main, 2.4 + конус 0.6). Ширина значка 1.7 м (1.3 в
  кадре читалось мелко).
- Оживление: «выпрыгивание» (scale 1.6 → 1 за 0.16 с), покачивание по
  высоте, затухание alpha в последние 0.3 с.

**ГРАБЛИ стенда (повтор известных, снова наступил):** в
`screenshot_hud.gd` написал `var right := _main._car.global_transform...`
— `_main` объявлен как Node3D, тип `_main._car` НЕ выводится, скрипт не
парсится, и стенд ВИСИТ с пустым окном без единого кадра и без сообщений
в stdout (ошибка ушла в stderr). Пишите тип явно: `var player: Car =
_main._car`. И ещё: первый кадр значков попал на оранжевые тени
авиаудара (фаза 330) — снимок сдвинут на 430/465.

**Файлы:** `scripts/Car.gd`, `assets/ui/w_boost.png` (перерисован),
`tools/screenshot_hud.gd`, `tools/test_weapons.gd`.
**Снимки:** `tools/shots/hud_status_magnet.png`, `hud_status_boost.png`.
**Тесты:** все 20 сцен + test_curve — PASS. В TestWeapons добавлены
проверки «значок магнита у жертвы» и «значок буста у атакующего».

## Вернули перепад высот — одна горка (2026-08-21)
Запрос: «давай вернём в одном месте высоту в виде горки». Трасса была
сделана плоской 2026-08-20 (`FLAT_TRACK := true`), но вся механика
рельефа сохранялась — полотно, ограждения, обочина и декор и так следуют
`HEIGHT_KEYS`.

**Что сделано:** `FLAT_TRACK := false`, а прежний многоплатовый профиль
RnRR заменён на ОДНУ горку:
```
[0.000, 0.0] [0.140, 0.0] [0.178, 4.0] [0.190, 4.0] [0.228, 0.0] [1.000, 0.0]
```
Высота гребня 4 м, подъём и спуск по ~28 м, между ними ~9 м ровного
гребня. Круг из-за рельефа 726 → 727 м.

**Почему именно доля 0.14…0.23.** Горка симметрична относительно
середины прямой 0.156…0.211 (40 м). Места выбирались не на глаз:
- трамплины трасса ставит САМА на серединах двух самых длинных прямых
  (`_ramp_ratios()` → 0.564 и 0.722) — прыжок с трамплина, стоящего на
  склоне, непредсказуем, пересекаться нельзя;
- стартовая прямая 0.000…0.064 занята решёткой и створом;
- гребень должен быть на ПРЯМОЙ (крен полотна не делается — `_sample_frames`
  держит «вправо» горизонтальным, так что на горке есть тангаж, но нет
  поперечного наклона; на дуге это ощущалось бы как проседание).
Подъём начинается ещё на выходе из первого правого, спуск заканчивается
на входе в шикану — это осознанно, так интереснее.

**Тест TestBumps переписан на самопроверку.** В нём лежал массив
`FLAT_ZONES` — «ровные» доли круга, СКОПИРОВАННЫЕ из старых HEIGHT_KEYS.
При любой правке профиля он молча разъезжается с реальностью: зоны
съезжают на склоны, и тест начинает считать законный подъём «подбросом»
(или наоборот — перестаёт смотреть туда, где ровно). Заменён на
`_is_level(t)`: ровно там, где `TrackBuilder._profile_height` не меняется
в окрестности ±0.02 круга. Теперь правка профиля тест не ломает.

**Замеры до/после (тот же seed):**
| | плоская | с горкой |
|---|---|---|
| TestBumps: подбросы / пинки | 0 / 0 | 0 / 0 |
| TestBumps: касания кузовом дороги за 90 с | 29 | **23** |
| TestAICorners: вылеты ИИ за 60 с | 0 | **1** (эпизод 1.0 с) |
| TestRamp: скорость на замере | 25.7 м/с | 18.9 м/с |

- Касаний кузовом стало МЕНЬШЕ, не больше — горка ничего не разбила.
- Один вылет ИИ появился (порог теста — до 3 вылетов и эпизод < 2.5 с,
  автовозврат его подобрал за 1.0 с). Случился он на доле 0.46, ДАЛЕКО
  от горки: это эффект бабочки — рельеф сдвинул тайминги всей пачки, и к
  16-й секунде машины оказались в другой толкучке. Саму горку ИИ проходит
  без замечаний.
- TestRamp мерит скорость на доле 0.12, а это теперь подножие подъёма —
  машина честно тормозит в горку. ПОБОЧНО ВЫЯСНИЛОСЬ: этот тест давно
  устарел — он считает, что трамплин на доле 0.12, а трамплины стоят на
  0.564 и 0.722. Фактически он проверяет «машина едет и не застревает».
  Не переделывал (вне задачи), но знать об этом надо.

**Снимки:** `tools/shots/hill_side.png` (силуэт сбоку),
`hill_drive.png` (вид гонщика — дорога уходит вверх, за гребнем ничего
не видно). Грабли стенда: камеру «за 26 м до подножия» сначала считал
экстраполяцией по касательной — подъём начинается на выходе из дуги, и
камера уехала ЗА ограждение, в кадре был один красный борт. Правильно
брать точку ПО КРИВОЙ: `sample_baked(length * 0.140 - 26.0)`.

**Файлы:** `scripts/TrackBuilder.gd`, `tools/test_bumps.gd`,
`tools/screenshot_track.gd`.
**Тесты:** все 20 сцен + test_curve — PASS.

## Сетевая игра на двоих через VDS + одиночные боксы (2026-08-21)
Запрос: «при запуске двух экземпляров игры играть по сети через мой VDS,
реального игрока показывать маркером, плюс два бота; боксы лежат по
одному, не исчезают, каждый проехавший забирает свой случайный бонус».

### Боксы с оружием
Было: тройки поперёк трассы, бокс исчезал на 5 с после подбора — кто
успел, тот и съел. Стало: ПО ОДНОМУ на отметку посреди полотна, не
исчезает никогда, каждый проехавший получает СВОЙ случайный бонус.
От повторной выдачи защищает личный для машины откат 2.5 с
(`WeaponBox._next_pickup`, ключ — instance_id): `body_entered` бьёт
только на входе, но машина, стоящая на боксе, дёргается на подвеске и
входит-выходит каждые пару кадров — без отката она фармила бы оружие.

### Сеть: выделенный сервер
Пользователь просил «как лучше и быстрее» — выбран ВЫДЕЛЕННЫЙ СЕРВЕР на
VDS (а не «хостит один из игроков»): у хоста был бы нулевой пинг, а у
второго весь — условия неравные. Транспорт ENet/UDP, порт 9977.

- `scripts/Net.gd` (новый autoload): режим OFFLINE/SERVER/CLIENT, подъём
  сервера, подключение, раздача слотов, адрес в `user://net.cfg`.
- Сервер — ТОТ ЖЕ `Main.tscn`, запущенный ключом `-- --server`: без
  камеры, HUD и своей машины. Отдельная сцена не нужна, и это важно:
  адресация RPC в Godot идёт ПО ПУТИ УЗЛА, у сервера и клиента гонка
  обязана лежать в одном и том же `/root/Main`.
- Машин по сети всегда 4: слоты 0 и 1 — живые игроки, 2 и 3 — боты.
  НЕзанятый слот ведёт бот; игрок подключился — машина переходит под его
  ввод, вышел — снова бот. Гонка не встаёт из-за отсутствия второго.
- Ростер моделей: клиент присылает свой выбор в `_rx_hello`, сервер
  раздаёт всем — иначе игрок видел бы у соперника не ту машину.

### Что где считается
Сервер считает ВСЮ физику и 20 раз в секунду шлёт снимок: на машину
10 float (позиция, кватернион, скорость) + 3 байта (оружие, живость с
«призраком», значок эффекта). Кватернион, а не базис: 4 числа вместо 9 и
корректная интерполяция поворота.
- Чужие машины на клиенте — МАРИОНЕТКИ: физика выключена (`freeze`
  kinematic), позиция тянется к снимку, причём цель ЭКСТРАПОЛИРУЕТСЯ по
  скорости из снимка — 20 снимков против 60 кадров иначе дают рывки.
- СВОЯ машина на клиенте считается ЛОКАЛЬНО (`NetRole.OWNED`), иначе руль
  отвечал бы через пинг. Снимок её мягко подтягивает (lerp 0.12), а при
  невязке > 4 м переставляет.
- Ввод: оси — ненадёжным пакетом каждый кадр (потерять кадр газа не
  страшно, ждать переотправки — страшно), прыжок и выстрел — надёжным
  (потерянный выстрел не восстановить).

### Оружие по сети
Стреляет сервер. Клиент выстрел только ВИДИТ: `net_broadcast_weapon` →
`_rx_weapon_fx` → клиент поднимает копию с флагом `inert` (добавлен в
Mine, OilSlick, Projectile, Airstrike) — она летит и рисуется, но никого
не бьёт. Иначе машину било бы дважды и по-разному на каждом экране.
Боксы на клиенте тоже инертны: оружие выдаёт сервер и присылает в снимке.
`Car._player_control` больше не зовёт `use_weapon()`, если роль не LOCAL —
локальный выстрел породил бы вторую ракету и списал бокс дважды.

### Маркеры
Своя машина — зелёная стрелка, живой соперник — ОРАНЖЕВАЯ, боты без
стрелки: так «реальный игрок» виден с первого взгляда.
ПОБОЧНЫЙ ФИКС: высота значка эффектов над крышей считалась по
`is_player`, а по сети маркеры у обоих живых игроков — теперь по факту
маркера (`Car.has_marker`).

### НАСТОЯЩИЙ БАГ, пойманный на стенде
`Main._on_peer_left` брал слот из `Net.slot_of_peer` — но Net
подписывается на `peer_disconnected` РАНЬШЕ (в `start_server`), к моменту
вызова Main слот уже стёрт, обработчик молча выходил, и машина ушедшего
игрока оставалась на его последнем вводе — газ в пол навсегда. Лечение:
Net сам эмитит `player_joined(id, slot)` / `player_left(id, slot)`, слот
едет В СИГНАЛЕ, порядок подписки больше ни на что не влияет.

### Стенд `tools/TestNet.tscn` (сквозной, с настоящим сервером)
Корень сцены зовётся **Main** и несёт тот же `Main.gd` (см. выше про пути
RPC), проверяльщик — ДОЧЕРНИЙ узел: дочерний `_ready` идёт раньше
родительского, там и подключаемся, чтобы `Main._ready` увидел уже
выставленный режим клиента.
Проверяет: выдачу слота, ростер, что своя машина поехала (значит сервер
принял ввод и вернул позицию), что марионетки едут (снимки идут), что
маркер соперника есть и что своя машина не марионетка.
Результаты: одиночный клиент PASS (проехал 180 м); ДВА клиента разом —
оба PASS, слоты 0 и 1, каждый видит другого; после ухода обоих сервер сам
перезапустил трассу.

### Грабли UI (поймано скриншот-стендом)
Панель подключения сперва поставил на анкер 0.75 по горизонтали — и она
уехала ПОД сетку машин (та начинается с ~685 px): в кадре её не было
вовсе. Теперь адрес и «ПО СЕТИ» стоят в левой половине, рядом со «СТАРТ»,
на фиксированных offset'ах от левого края. Проверять такое надо глазами:
`godot --path . res://tools/ScreenshotSelect.tscn -- tools/shots`.

### Развёртывание
`server/README.md` (установка Godot 4.3 headless, rsync проекта,
`ufw allow 9977/udp`, ручная проверка, systemd) и
`server/bighead-server.service`. Самая частая причина «сервер не ответил»
— открыт TCP вместо UDP.

### Что осталось приблизительным (честно)
- «Призрак» после уничтожения у марионеток не мигает: таймер приходит
  прямым присвоением, а `_tick_effects` у них не крутится.
- Клиент, подключившийся В СЕРЕДИНЕ заезда, считает свой прогресс с нуля
  (позиции-то приходят, а накопленный путь — нет), поэтому круг и место в
  его HUD будут врать до конца заезда.
- Взрывы и попадания на клиенте видны по РЕЗУЛЬТАТУ (машину отбросило,
  она стала призраком), а не покадрово: копии оружия инертны.

**Файлы:** новые `scripts/Net.gd`, `server/README.md`,
`server/bighead-server.service`, `tools/test_net.gd`, `tools/TestNet.tscn`;
правлены `scripts/Main.gd`, `Car.gd`, `WeaponBox.gd`, `Mine.gd`,
`OilSlick.gd`, `Projectile.gd`, `Airstrike.gd`, `CarSelect.gd`,
`project.godot`, `README.md`.
**Тесты:** все 20 сцен + test_curve — PASS (оффлайн не задет), плюс
сетевой стенд на одном и на двух клиентах.

## Лобби 5 секунд + адрес VDS по умолчанию (2026-08-21)
Запрос: «вначале 5 сек для подключения игрока по сети, если его нет — по
старинке с ботами». Плюс пользователь дал адрес VDS: 139.100.234.166.

- Сервер (`Main`): первый подключившийся игрок запускает таймер
  `LOBBY_WAIT = 5.0` (`_tick_lobby` в `_server_tick`). Пришёл второй —
  старт сразу; не пришёл — старт с ботом в пустом слоте («по старинке»).
  Пробел по-прежнему стартует немедленно. Клиенту в лобби идёт обратный
  отсчёт: `_rx_lobby(players, secs)` шлётся при каждой смене целой
  секунды, подпись «Ждём второго: N…».
- `Net.host` по умолчанию = 139.100.234.166 — поле на экране выбора сразу
  готово к «ПО СЕТИ»; своё значение по-прежнему запоминается в net.cfg.
- Заодно: таймаут подключения на клиенте (CONNECT_TIMEOUT = 5 с) — ENet
  молчит очень долго, и без него экран «Подключение…» висел бы, пока
  игрок сам не догадается выйти. Не ответил — рвём и пишем «Сервер не
  ответил за 5 с».
- TestNet ПЕРЕВЁРНУТ: раньше он ПРОСИЛ сервер стартовать
  (_rx_start_request), теперь наоборот проверяет, что заезд начался САМ
  (новая проверка «старт без второго игрока», начало на ~6.8 с ≈ 5 с
  лобби + отсчёт 3-2-1). Прогоны: одиночный клиент PASS, два клиента
  разом — оба PASS (при двоих старт мгновенный).

**Файлы:** `scripts/Main.gd`, `scripts/Net.gd`, `scripts/CarSelect.gd`,
`tools/test_net.gd`, `README.md`, `server/README.md`.

## Сетевая игра на двоих + сервер на VDS (2026-08-21)
Запрос: «при запуске двух экземпляров игры можно было играть по сетке, в игре
показывается реальный игрок с маркером и два бота, подключение через мой VDS;
боксы лежат по одному, не исчезают, каждый проехавший берёт свой бонус».
Затем: «вначале 5 сек на подключение, если игрока нет — по старинке с ботами»
и «разворачивай сервер сам».

### Боксы с оружием
Было: тройка боксов поперёк трассы на каждой из 5 отметок, подобравший
забирал бокс, остальным 5 секунд пусто. Стало: **один** бокс по центру
полотна, **не исчезает**, и каждая проехавшая машина получает СВОЙ случайный
бонус. От повторной выдачи — личный откат на машину (`PER_CAR_COOLDOWN` 2.5 с)
в словаре `_next_pickup` по `get_instance_id()`. Откат нужен именно личный, а
не общий: `body_entered` бьёт только на входе, но машина, стоящая на боксе,
дёргается на подвеске и входит-выходит каждые пару кадров — без отката она
фармила бы оружие.

### Модель сети — ВЫДЕЛЕННЫЙ СЕРВЕР
`scripts/Net.gd` (autoload `Net`) + сетевая часть в `Main.gd`. Сервер — тот же
`Main.tscn`, запущенный с ключом: `godot --headless --path . res://scenes/Main.tscn -- --server`.
Транспорт ENet/UDP, порт 9977. Никто из игроков не хостит — пинг у обоих
одинаковый.
- Машин всегда 4: слоты **0 и 1** держатся за живыми игроками, **2 и 3** боты.
  Слот без игрока ведёт бот (`net_role = LOCAL`), игрок подключился — сервер
  переводит машину на присланный ввод (`SERVER_INPUT`), вышел — снова бот.
  Одна и та же машина, никаких спавнов на ходу.
- `Car.NetRole`: LOCAL / SERVER_INPUT / **PUPPET** (клиент физику не считает,
  тянется к снимкам) / **OWNED** (своя машина на клиенте).
- **OWNED — главное решение по отзывчивости.** Своя машина на клиенте
  считается ЛОКАЛЬНО (иначе руль отвечал бы через пинг), а снимок сервера
  подтягивает её мягко (`lerp` 0.12); при расхождении больше 4 м —
  переставляет. Стрелять локально при этом ЗАПРЕЩЕНО (`net_role != LOCAL` →
  выход из `_player_control`): оружие тратит сервер, локальный выстрел породил
  бы вторую ракету и списал бокс дважды.
- Снимок: на машину 10 float (позиция, КВАТЕРНИОН, скорость) + 3 байта
  (оружие+1, живость с призраком, значок эффекта+2), 20 раз в секунду,
  `unreliable_ordered`. Кватернион вместо базиса — 4 числа вместо 9 и
  корректная интерполяция поворота.
- Ввод: оси — `unreliable_ordered` каждый кадр (потерять кадр газа не страшно,
  ждать переотправки — страшно), прыжок/выстрел — `reliable` (потерянный
  выстрел не восстановить).
- Марионетки экстраполируются по скорости из снимка: снимков 20/с, кадров 60 —
  без экстраполяции машины едут рывками.
- **Оружие у клиентов только КАРТИНКА.** Сервер шлёт событие выстрела,
  клиент порождает копию с `inert = true` (Mine/OilSlick/Projectile/Airstrike):
  monitoring выключен, никого не бьёт. Иначе машину било бы дважды и по-разному
  на каждом экране. Боксы на клиенте тоже инертны — оружие приходит в снимке.

### Маркеры
`_build_player_marker(color)`: **своя** машина — зелёная стрелка, **живой
соперник** — оранжевая, боты без стрелки. Поэтому же в `Car` появился
`has_marker`: высоту значка эффекта над крышей теперь выбирает он, а не
`is_player` — по сети маркеры есть у обоих живых игроков, а `is_player` только
у своей машины.

### Лобби: 5 секунд и по старинке
Первый подключившийся запускает отсчёт `LOBBY_WAIT = 5.0` (`_tick_lobby`).
Второй успел — старт сразу, не успел — стартуем с ботом в пустом слоте.
Клиенту каждую секунду уходит `_rx_lobby(игроков, секунд)`. Плюс на клиенте
`CONNECT_TIMEOUT = 5 c`: ENet сам может молчать очень долго, поэтому ждём
ответа сервера вручную и честно пишем «не ответил», а не висим на
«Подключение…».

### Развёрнуто на VDS 139.100.234.166 (Selectel, Ubuntu 24.04, 1 CPU/960 МБ)
Godot 4.3 headless в `/usr/local/bin/godot`, проект в `/opt/bighead`,
служба `bighead-server.service` под пользователем `bighead`, автозапуск.
Расход ~113 МБ RAM и ~22% ядра. Детали и команды — `server/README.md`.
Ключ доступа — `vps_root_key` из проекта `Desktop\shift-journal` (адрес уже
был в `known_hosts`, а `vps_key` от мессенджера не годится: он для юзера
`tunnel`, у которого «This account is currently not available»).

### ГРАБЛИ (все настоящие, поймано на живом сервере)
1. **RPC адресуются ПО ПУТИ УЗЛА.** На сервере гонка в `/root/Main`, значит и у
   клиента обязана быть `/root/Main`. Поэтому тестовый стенд `TestNet.tscn`
   имеет корень с именем **Main** и тем же `Main.gd`, а проверяльщик висит
   ДОЧЕРНИМ узлом (его `_ready` вызывается раньше родительского — там и
   подключаемся, чтобы `Main._ready` увидел уже выставленный режим клиента).
   Загрузить Main как ребёнка своей сцены нельзя — путь разъедется.
2. **Порядок обработчиков `peer_disconnected`.** `Net` подписывается первым и
   к моменту вызова `Main` уже СТИРАЕТ слот из `slot_of_peer` — `Main` не
   находил машину и оставлял её на замершем вводе ушедшего игрока (газ в пол
   навсегда). Лечится своими сигналами `Net.player_joined/player_left(id, slot)`,
   слот едет прямо в сигнале.
3. **Подсевший к идущему заезду навсегда оставался в лобби**: отсчёт он
   пропустил, `controls_enabled` никто не включал. Добавлен `_rx_race_running`.
4. **journald глотал ВСЁ.** Headless-рендер сыпал в stderr тысячи
   `Parameter "m" is null` (dummy-рендер на каждый меш), journald включал rate
   limit и выбрасывал в том числе `[net]`-строки — лог сервера был бесполезен,
   и первый неудачный прогон на VDS я диагностировал вслепую. Лечение:
   **вся косметика на сервере отключена** — декор трассы и створ (у них ноль
   коллизий), свет/небо, модели машин, кубы боксов, дым, скорлупа льда, значок
   эффекта, а меши полотна/земли/стен/трамплинов не цепляются к телам
   (`TrackBuilder._add_visual`, коллизия — отдельный узел). Осталось 4 строки
   на перезапуск трассы: предупреждение возникает уже на `mesh.mesh = ...`,
   до всякой проверки. Дальше чистить — переписывать геометрию, от которой
   зависят все физические тесты; не стал. Побочно: память 148 → 113 МБ.
5. Минимальная Ubuntu: нет `unzip`, и Godot падает без `libfontconfig.so.1`
   даже с `--headless`. Полный список библиотек — в `server/README.md`.
6. В systemd-юните обязателен `Environment=HOME=/home/bighead`: без HOME Godot
   не может писать в `~/.local/share/godot` и падает при старте.
7. **Отключая косметику, я сам сломал `test_curve`.** `TrackBuilder` стал
   обращаться к autoload `Net`, а `tools/test_curve.gd` запускается через
   `--script` — в этом режиме autoload'ы НЕ ЗАГРУЖАЮТСЯ. Компиляция скрипта
   падала, стенд молча ВИС (ошибка только в stderr). Лечение: в TrackBuilder
   своя статическая проверка `_headless_server()` по командной строке, без
   всякого autoload. Правило на будущее: **код, который трогают
   `--script`-стенды, не должен зависеть от autoload'ов.**

### Тест
`tools/TestNet.tscn` — сквозной: подключается к работающему серверу, НЕ просит
старт (проверяет как раз 5-секундное ожидание), даёт газ и сверяет, что слот и
ростер выданы, заезд начался сам, своя машина проехала, снимки ботов идут,
маркер соперника есть, своя машина не марионетка. Адрес — аргументом после
`--` (по умолчанию 127.0.0.1).

Прогнано: локально (1 клиент и 2 одновременно) и **на живом VDS** (1 и 2
клиента). Все PASS. В логе сервера видно, как оба заняли слоты 0 и 1 и заезд
стартовал сразу, а после выхода обоих трасса перезапустилась.
Оффлайн-регрессия: все 20 сцен + test_curve — PASS.

### Осталось на потом
- Экрана «результаты сетевого заезда» нет: финиш показывает своё место, дальше
  Enter → гараж.
- Круги клиент считает сам по присланным позициям (совпадает, потому что
  позиции те же), но подсевший посреди заезда начнёт счёт с нуля.
- «Призрак» после уничтожения у марионеток не мигает: `_ghost_time` ставится
  присвоением из снимка, а собственный таймер у них не крутится.

## Сетевые фиксы: пропавший пол и дёрганое движение (2026-08-24)
Жалоба: «при игре по сети нет пола (нижней поверхности), движение машины
очень прерывистое, дёрганное».

### Пол
Причина — МОЯ ЖЕ ОШИБКА при отключении косметики на сервере (21.08):
автозамена `body.add_child(mesh)` → `_add_visual(body, mesh)` зацепила
строку ВНУТРИ САМОГО помощника `_add_visual` — он стал вызывать сам себя
вместо `add_child`. Рекурсия до лимита стека глоталась молча, и меши
полотна/земли/стен/трамплинов не добавлялись ВООБЩЕ НИ У КОГО (клиент,
одиночная игра — у всех чёрная пустота под колёсами; физика работала —
коллизии отдельными узлами). Урок: после массовой автозамены перечитать
diff самого хелпера. Диагноз поставлен стендом `tools/ShotNet.tscn`
(сетевой клиент с окном, снимки + печать числа детей у Ground/Road:
было по 1, стало по 2).

### Дёрганое движение — три настоящих причины
1. **Подтяжка СВОЕЙ машины.** `net_correct` делал `lerp(0.12)` от
   расхождения с сервером КАЖДЫЙ снимок — рывки до половины кадрового
   шага. Теперь: мёртвая зона 0.5 м (меньше — не трогаем вовсе; локальная
   и серверная физика при одном коде на разных кадрах расходятся
   постоянно), шаг подтяжки не больше 8 см за снимок (NET_FIX_STEP,
   против ~40 см обычного шага — глазу не видно), скорость в мягком
   режиме не трогаем совсем. Замер: макс. сдвиг за снимок ровно 0.080 м.
   ГРАБЛИ по пути: попытка размазать подтяжку по кадрам двиганьем
   global_position КАЖДЫЙ кадр физики уронила скорость машины втрое —
   телепорт динамического тела каждый кадр ломает решатель. Не делать.
2. **Экстраполяция марионеток при неровном приходе пакетов.** Возраст
   снимка не ограничивался: запоздавший пакет уводил цель далеко вперёд,
   следующий возвращал назад. На локалхосте невидимо, на двух клиентах —
   4 рывка назад за 419 кадров. Теперь `MAX_EXTRAP` + запрет шага НАЗАД
   против присланной скорости (лучше замереть на кадр, чем откатиться).
   Итог: 0 рывков назад во всех прогонах, включая живой VDS.
3. **Частота снимков** 30 → 60 Гц (= частоте физики). Замер: 5.0% → 1.2%
   расхождения на локалхосте. Трафик ~10 КБ/с на клиента — ничто.

### MAX_EXTRAP подобран замерами НА ЖИВОМ VDS (двумя клиентами)
0.05 с → 42% | **0.12 с → 25%** | 0.22 с → 39%. На локалхосте 2-5% при
любом значении — по нему подобрать НЕЛЬЗЯ, пакеты идут ровно.

### Что НЕ помогло (чтобы не переделывать в третий раз)
- «Едем от текущего места к последнему снимку за фактический интервал» —
  ~50%: машина доезжает за кадр-другой и ЖДЁТ следующего пакета.
- Учебный буфер снимков с отставанием (interpolation delay 0.12-0.15 с,
  рисуем между двумя полученными снимками) — 60% на интернете, 21% на
  локалхосте. Хуже простой экстраполяции ВЕЗДЕ. Вернул экстраполяцию.

### Метрика плавности (tools/test_net.gd) — сама дважды врала
1. Мерил в `_process` — headless крутит его в разы чаще физики, почти все
   шаги нулевые. Мерить надо в `_physics_process`, где марионетка и
   двигается.
2. Брал скорость из `car.linear_velocity` — у ЗАМОРОЖЕННОГО
   кинематического тела Godot пересчитывает её сам по сдвигу трансформа
   и выдаёт вдвое больше реальной (62 м/с при максимуме 34). Полчаса
   чинил интерполяцию, которая была не виновата. Брать `_snap_vel`.
Итоговая метрика: средняя относительная разница «путь за кадр» против
«присланная скорость × dt». Эталоны: одиночная игра 0.3%, локалхост
2-5%, живой VDS 21-31%. Порог теста 35% — учитывает реальный канал и
ловит все неудачные варианты (50-60%).

### Ещё два сетевых фикса по пути
- **hello с повтором.** Клиент, подключившийся, пока сервер перезапускал
  сцену после чужого финиша (8 с), слал `_rx_hello` в несуществующий
  `/root/Main` — RPC молча пропадал, клиент навсегда оставался без слота
  (все машины марионетки). Теперь `_say_hello` повторяет запрос раз в
  секунду до 12 раз, пока не выдан слот. Поймано вживую стендом ShotNet.
- **Оранжевая метка — только над ЖИВЫМ игроком.** Раньше вешалась на
  вторую машину безусловно, вися над ботом. Теперь `_rx_welcome` везёт
  маску занятых слотов, `_rx_slot_taken` показывает/прячет метку при
  подключении/выходе. ГРАБЛИ: рассылку `_rx_slot_taken` сначала поставил
  в КОНЕЦ `_on_peer_joined` — когда второй игрок запускал заезд своим
  подключением, функция выходила раньше отправки (у первого метка не
  загоралась). Поймано двухклиентским прогоном: проверка «метка скрыта»
  обязана падать при двух клиентах — и не падала у первого.

**Файлы:** `scripts/Car.gd`, `scripts/Main.gd`, `scripts/TrackBuilder.gd`,
`tools/test_net.gd`, `tools/shot_net.gd` + `tools/ShotNet.tscn` (новый
стенд: сетевой клиент с окном и снимками экрана).
**Тесты:** все 20 оффлайн-сцен PASS; TestNet PASS на локалхосте (1 и 2
клиента) и на живом VDS (1 и 2 клиента). Код на VDS обновлён.

## Рывки по сети добиты: своя машина стала КЛИЕНТ-АВТОРИТЕТНОЙ (2026-08-24)
Жалоба (повторная, после фиксов подтяжки): «машина по-прежнему двигается
рывками, играть невозможно». Причина АРХИТЕКТУРНАЯ, и точечными фиксами
(мёртвая зона, шаг 8 см, 60 Гц) она не лечилась: сервер считал свою копию
машины игрока по вводу, пришедшему с опозданием на пинг, — его состояние
ВСЕГДА отстаёт и расходится с локальным на каждом повороте. Подтяжка к
нему тянула машину назад каждый снимок, а невязка больше 5 м (обычное
дело на повороте при реальном пинге) давала жёсткий телепорт.

### Что сделано
Своя машина клиента теперь полностью клиент-авторитетна:
- Клиент считает её физику сам и 60 раз/с шлёт серверу состояние
  (`_rx_pstate`: позиция, кватернион, скорость — те же 10 float).
  Подтяжка/телепорты своей машины УДАЛЕНЫ ВООВСЕ (`net_correct`,
  NET_DEADZONE/NET_SNAP/NET_FIX_STEP — выпилены). Сеть свою машину не
  трогает совсем — рывкам взяться неоткуда.
- Сервер держит машину живого игрока МАРИОНЕТКОЙ (тот же
  `_follow_snapshot`, что у марионеток клиента) и ретранслирует её
  второму игроку. Роль SERVER_INPUT и весь ввод осей (`_rx_input`,
  net_th/st/hb/jump) удалены; `_rx_press` шлёт только выстрел.
- ГРАБЛИ: в снимке для марионетки сервер обязан слать скорость ИЗ
  ПРИСЛАННОГО СНИМКА (`_snap_vel`), а не `linear_velocity` — у
  замороженного тела Godot пересчитывает её по сдвигу трансформа и
  завышает вдвое (те же грабли, что в метрике test_net 24.08).
- Эффекты оружия по машине игрока сервер ПЕРЕСЫЛАЕТ владельцу
  (`Car._forward_fx` → `Main.net_forward_fx` → RPC `_rx_fx`):
  DESTROY / BLAST(толчок мины и магнита) / FREEZE / OIL / BOOST — физику
  (толчок, закрутку, телепорт «призрака», тягу буста) применяет клиент,
  иначе игрок удара бы не почувствовал. BOOST ради этого вынесен из
  use_weapon в `apply_boost()`. Сервер параллельно ведёт свои таймеры
  (призрак/заморозка) для снимков — марионетки теперь тикают
  `_tick_effects` (побочно починилось «призрак у марионеток не мигает»).
- Выстрел игрока по-прежнему применяет ТОЛЬКО сервер (net_fire у
  марионетки обрабатывается в ветке PUPPET `_physics_process`).
- Возврат на трассу (R и автовозврат) для своей машины делает КЛИЕНТ:
  `_check_recovery` пропускает марионеток на сервере, а на клиенте правит
  только свою машину.
- В `_rx_state` для OWNED не трогаем ни позицию, ни alive/призрака —
  от сервера своей машине нужны только оружие из бокса и значок эффекта.

### Стенды и тесты
- test_net/shot_net газуют через `Input.action_press("accelerate")`
  (раньше слали `_rx_input`, его больше нет); метрика «подтяжки» удалена.
- TestNet: одиночный PASS (марионетка 4.3%, рывков назад 0), два клиента
  разом — оба прошли (FAIL «метка скрыта» у первого закономерен: второй
  игрок подключился и метка загорелась). Все 20 оффлайн-сцен +
  test_curve — PASS.

### Осталось честно приблизительным
- Столкновение игрок-игрок каждый видит по-своему (каждый толкает
  марионетку соперника у себя); заморозка «заразой» от машины игрока
  к боту по сети не передаётся (у марионетки не крутится
  _bounce_off_cars). На игру на двоих не влияет.

**Файлы:** `scripts/Car.gd`, `scripts/Main.gd`, `tools/test_net.gd`,
`tools/shot_net.gd`, `README.md`, `server/README.md`.

### Сервер на VDS обновлён (та же сессия)
Протокол RPC изменился (`_rx_input` удалён, `_rx_press` без аргументов,
новые `_rx_pstate`/`_rx_fx`) — старый сервер с новым клиентом
несовместим, поэтому код на VDS залит сразу (scripts/scenes/project.godot
→ /opt/bighead, restart bighead-server). Проверка НА ЖИВОМ VDS: TestNet
одним клиентом — PASS (марионетка 15.9% против прежних 21-31%, рывков
назад 0, своя машина проехала 206 м); двумя клиентами разом — у обоих
все проверки ok, кроме закономерной «метка скрыта» (второй игрок
подключился — метка обязана загореться, тест одиночный по устройству).

## Дёрганый соперник, дикий импульс при ударе, «пропала трасса» (2026-08-24)
Жалобы после первой игры вдвоём: «противник движется дёргано; врезается в
момент рывка — я получаю огромный импульс ускорения; у игрока по сети
пропадала трасса». Три причины, все настоящие:

1. **Двойное сглаживание при ретрансляции.** Сервер слал второму клиенту
   позицию СВОЕЙ марионетки (которая уже сглаживает и экстраполирует
   присланный владельцем поток), а клиент сглаживал поверх — рывки.
   Теперь `_pack_state` для машин живых игроков ретранслирует СЫРОЕ
   последнее состояние владельца (`_snap_pos/_snap_rot/_snap_vel`).
   Замер на живом VDS двумя клиентами: соперник-игрок 12.8-15.3%,
   рывков назад 0 (норма ботов 21-31%). В test_net добавлен отдельный
   замер машины соперника-игрока (осмыслен только при двух клиентах).
2. **Огромный импульс — два источника, оба закрыты.**
   а) `_bounce_off_cars` брал `other.linear_velocity` — у замороженной
   марионетки Godot пересчитывает её по сдвигу трансформа и на рывке
   канала выдаёт десятки м/с; рикошет 0.4×сближение швырял машину.
   Теперь скорость соперника — `contact_velocity()` (у марионетки — из
   снимка), а сближение ограничено 20 м/с (толчок ≤ 8 м/с).
   б) Депенетрация решателя: марионетка ТЕЛЕПОРТИРУЕТСЯ к снимкам и,
   шагнув в наш кузов, решатель выдавливает нас диким разовым импульсом.
   Кап: при касании марионетки всё, что физика добавила к скорости сверх
   конца нашего прошлого кадра (`_post_vel`), ограничено 5 м/с. Честный
   рикошет не задет (наш импульс идёт позже в том же кадре), толчок
   взрыва защищён окном `_blast_time` (ставит push_from_blast), а после
   телепорта память чистит reset_speed_memory — иначе кап «возвращал» бы
   только что обнулённую скорость.
3. **«Пропала трасса»** — камера уезжала вслед за машиной, отброшенной
   этим импульсом в пустоту (а при NaN в трансформе — навсегда: lerp с
   NaN отравляет камеру). Страховки: `_check_recovery` мгновенно (без
   таймера) возвращает машину при нечисловой позиции или |pos| > 600 м
   (NaN сперва обнуляется — respawn_transform от NaN вернул бы NaN);
   `_client_tick` не шлёт битое состояние; `_rx_pstate` выбрасывает
   битые пакеты (иначе NaN отравил бы снимки ВСЕМ); IsoCamera
   пропускает кадр при нечисловой цели.

ГРАБЛИ деплоя: после появления НОВОГО class_name (Minimap из
параллельной сессии) сервер на VDS упал в Parse Error «Could not find
type Minimap» — кэш глобальных классов строит только `--import`, одного
копирования файлов мало. Лечение: `systemctl stop` → `sudo -u bighead
HOME=/home/bighead godot --headless --path /opt/bighead --import` →
`start`. Правило: **новый class_name → --import и на VDS тоже.**

Прогнано: 20 оффлайн + curve + TestMinimap PASS; TestNet локально (1 и
2 клиента) и на живом VDS (2 клиента) — PASS (FAIL «метка скрыта» при
двух клиентах закономерен). Код на VDS обновлён.

**Файлы:** `scripts/Car.gd`, `scripts/Main.gd`, `scripts/IsoCamera.gd`,
`tools/test_net.gd`. (В коммит попал и WIP мини-карты параллельной
сессии: `scripts/Minimap.gd`, правки `Main.gd`/`TrackBuilder.gd`,
`tools/TestMinimap.tscn` — Main.gd общий, разделить нельзя; её секцию
в журнал допишет она.)

## Мини-карта: видно, где кто едет (2026-08-24)
Просьба: «сделай мини карту, чтобы было видно где кто находится».

### Что сделано
- `scripts/Minimap.gd` (новый) — Control, который сам рисует карту:
  полотно трассы одним вызовом `canvas_item_add_triangle_array` (квады
  между сэмплами оси), красные линии ограждений, белая чёрточка старта
  и точки машин. Своя машина — зелёный ТРЕУГОЛЬНИК носом по ходу,
  живой соперник — оранжевая точка, боты — серо-голубые (те же цвета,
  что у стрелок над машинами, чтобы не заводить второй язык меток).
  Выбывшая машина гаснет до alpha 0.35.
- `TrackBuilder.plan_samples()` — публичная выдача оси трассы в плане
  (X,Z) и полуширины полотна; раньше `_pts`/`_widths` были внутренними.
- `Main._setup_hud()` — панель 228×158 в правом верхнем углу, привязана
  к правому краю (anchor 1.0), чтобы не уезжать при другом разрешении.
  По сети `_rx_welcome`/`_rx_slot_taken` красят точки: свой слот
  зелёный, второй ЖИВОЙ игрок оранжевый (пока слот ведёт бот — серый).

### Две грабли
- **Сжатие карты = SIN наклона камеры, а не косинус.** Взял сгоряча
  cos(32°)=0.85 — направления на карте разошлись с экранными на 12°.
  Горизонтальный сдвиг на экране изокамеры поднимается ровно на
  sin(наклона): 0.53. С ним расхождение 0.0°. Ловится тестом.
- **Свою машину рисовать ПОСЛЕДНЕЙ.** На старте все стоят кучей, и точка
  бота накрывала зелёную стрелку — себя на карте было не найти.
- Перпендикуляр полотна берётся в МИРОВОМ плане до поворота: сжатие по
  вертикали неравномерно тянет углы, и «перпендикуляр» повёрнутой оси
  перестал бы быть перпендикуляром дороги.

### Тест
`tools/TestMinimap.tscn` + `tools/test_minimap.gd`: все точки внутри
панели, противоположные стороны круга далеко друг от друга, соседние —
рядом, у каждой машины своя точка, и главное — угол сдвига вдоль трассы
на КАРТЕ сходится с проекцией камеры (порог 3°, факт 0.0°).
PASS; все 20 оффлайн-сцен прогнаны заново — PASS.
Служебно: `tools/crop_shot.gd` — вырезать и увеличить кусок снимка
(смотреть карту на скриншотах HUD).

**Файлы:** `scripts/Minimap.gd` (новый), `scripts/Main.gd`,
`scripts/TrackBuilder.gd`, `tools/test_minimap.gd`,
`tools/TestMinimap.tscn`, `tools/crop_shot.gd`, `README.md`.

## Регрессионные тесты (все PASS)
`godot --headless --path . res://tools/TestX.tscn`, X = Ramp | Respawn |
Offtrack | Flip | Lap | Explosion | Heading | WallSlide | AirSpin |
Landing | Dive | Finish | Position | WallTurn | CarBounce | AICorners |
MineDrop | WheelSink | Weapons | WallRecover | Minimap.
Плюс `--script tools/test_curve.gd`.
СЕТЕВОЙ стенд отдельно — ему нужен ЖИВОЙ сервер в фоне:
```
godot --headless --path . res://scenes/Main.tscn -- --server   # в другом окне
godot --headless --path . res://tools/TestNet.tscn
```
Два клиента разом (проверка обоих слотов) — запустить TestNet дважды.
Гонять после правок физики/трассы.

## Дальше
- Деньги за место в заезде + магазин апгрейдов (двигатель/броня/шины/
  боезапас) между гонками — сердце прогрессии RnRR.
- Подбираемые бонусы на трассе (деньги, ремонт, ускорители-стрелки).
- Несколько трасс (пресеты точек в TrackBuilder), выбор дивизиона.
- Звук: двигатель (pitch от оборотов), выстрел, взрыв, музыка.
- Урон от столкновений машин, имена соперников над машинами.
- Дальше по роадмапу: ИИ-соперники (путь по той же Curve3D + резинка),
  круги/чекпоинты/позиции, оружие (пушка вперёд + мины назад, как в RnRR),
  деньги/апгрейды между заездами, несколько трасс, сплитскрин, музыка.

## Открытые вопросы
- Годовая версия Godot: писал под 4.3 (features в project.godot), 4.4/4.5
  тоже подойдут — при открытии Godot может предложить конвертацию, это ок.
- `Curve3D.closed = true` появился в Godot 4.3 — на более старых версиях
  заменить на дублирование первой точки в конец.
- Арт: пока BoxMesh-заглушки; потом низкополи-модели (Kenney.nl racing kit
  бесплатный — хороший старт) или свои.
